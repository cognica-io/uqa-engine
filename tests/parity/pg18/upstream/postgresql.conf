# The optional upstream prepared-transaction specs require prepared transactions.
max_prepared_transactions = 20
max_connections = 100
max_worker_processes = 16
max_parallel_workers = 8
shared_buffers = '128MB'
fsync = on
