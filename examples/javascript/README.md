# Shared JavaScript scenarios

These modules contain the binding-neutral SQL, fixtures, and assertions shared by the Node.js and Browser WASM examples. They are implementation support for [`../node`](../node) and [`../browser`](../browser), not a separate UQA Engine binding.

Engine creation, persistence, callback lifecycle, and cleanup remain in the platform-specific entry points.
