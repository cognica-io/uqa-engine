{
  "postgresql": "PostgreSQL 18.4 (Debian 18.4-1.pgdg13+1) on aarch64-unknown-linux-gnu, compiled by gcc (Debian 14.2.0-19) 14.2.0, 64-bit",
  "image": "sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636",
  "setup": [
    "CREATE TABLE grouping_alias_probe(id integer, n integer)",
    "INSERT INTO grouping_alias_probe VALUES (1,10),(2,20),(3,10)"
  ],
  "cases": [
    {"sql": "SELECT min(id) AS first_id, count(*) AS n FROM grouping_alias_probe GROUP BY n ORDER BY first_id", "rows": [[1, 2], [2, 1]]},
    {"sql": "SELECT min(id) AS first_id, 0 AS n FROM grouping_alias_probe GROUP BY n ORDER BY first_id", "rows": [[1, 0], [2, 0]]},
    {"sql": "SELECT n + 1 AS shifted, count(*) AS tally FROM grouping_alias_probe GROUP BY shifted ORDER BY shifted", "rows": [[11, 2], [21, 1]]},
    {"sql": "SELECT n + 1 AS shifted, count(*) AS tally FROM grouping_alias_probe GROUP BY 1 ORDER BY shifted", "rows": [[11, 2], [21, 1]]},
    {"sql": "SELECT min(id) AS first_id, count(*) AS n FROM grouping_alias_probe GROUP BY GROUPING SETS ((n),()) ORDER BY first_id,n", "rows": [[1, 2], [1, 3], [2, 1]]},
    {"sql": "SELECT n + 1 AS shifted, count(*) AS tally FROM grouping_alias_probe GROUP BY DISTINCT GROUPING SETS ((shifted),(n + 1)) ORDER BY shifted", "rows": [[11, 2], [21, 1]]},
    {"sql": "SELECT abs(n) FROM grouping_alias_probe GROUP BY abs ORDER BY abs", "rows": [[10], [20]]},
    {"sql": "SELECT count(*) AS tally FROM (SELECT n AS x, n AS x FROM grouping_alias_probe GROUP BY x) s", "rows": [[2]]},
    {"sql": "SELECT 1 AS x, 1.0 AS x GROUP BY x", "sqlstate": "42702"},
    {"sql": "SELECT id AS x, n AS x FROM grouping_alias_probe GROUP BY x", "sqlstate": "42702"},
    {"sql": "SELECT min(a.id) AS first_id, count(*) AS n FROM grouping_alias_probe a JOIN grouping_alias_probe b ON a.id = b.id GROUP BY n", "sqlstate": "42702"},
    {"sql": "SELECT count(*) AS tally FROM grouping_alias_probe GROUP BY tally", "sqlstate": "42803"},
    {"sql": "SELECT row_number() OVER () AS rank FROM grouping_alias_probe GROUP BY rank", "sqlstate": "42P20"},
    {"sql": "SELECT n + 1 AS shifted FROM grouping_alias_probe GROUP BY shifted + 1", "sqlstate": "42703"}
  ]
}
