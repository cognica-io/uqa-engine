# Nori indexing and retention corrections

The active correction covers #141 and #142. PostgreSQL assignment-target work (#148) is paused separately. This work must fix the storage owners and restore the existing allocation/output gates without another merge exception, timing retries, or committed machine reports.

## Ownership and completion requirements

- `uqa-storage::mvcc::serializable::liveness` owns local participant and receipt lease lifetime. Releasing the final handle must return the empty registry allocation without a subsequent transaction or explicit reclamation. Failed admission must release all reservations without taking SSI/provider admission during destruction.
- Core owns failure-atomic persistent map insertion. MVCC single-record updates must reuse unique private paths, copy shared snapshot/savepoint paths and reserve all required nodes before changing a root. Local lease payloads share their admitted participant allocation.
- `uqa-storage::inverted_index` owns in-memory corpus representation, mutation staging and retained-reader accounting. Remove redundant retained structures while preserving ordered postings, graph identity, source metadata, atomic batches, snapshots, cancellation and exact quota admission. Existing native allocation ceilings remain unchanged.
- The Nori benchmark targets and Python verifiers must support allocation/output checks without timing samples. CI must run analysis, indexing, phrase and both persistent-provider checks independently so one failure does not hide subsequent results.
- Acceptance requires existing output fingerprints, commit/rollback and reopen results, final-reader cleanup and the existing allocation ceilings. Runtime evidence uses Linux Docker. Generated diagnostics remain under ignored output directories or CI artifacts.

## Progress

- #142: reproduced the exact 48-byte leak with a minimal owner test. Local lease destruction now reclaims its registry entry and unused capacity, and participant construction precedes the list lock so failed publication can release safely. All 12 liveness tests and the redb commit/rollback, final-reader and reopen regression pass in Linux Docker. All four redb allocation-only workloads now complete with zero retained private bytes and unchanged live/reopened outputs. The existing redb ceilings cover Linux x86_64 and macOS aarch64; allocation acceptance on the reviewed Linux x86_64 target remains pending CI, without adding a new ceiling for the local Linux aarch64 host.
- #141: replaced reverse-term AVL sets with admitted ordered vectors, removed duplicate cached position projections, combined document metadata/reverse terms and per-field counters, and transferred staged posting nodes through Core's allocation-preserving append. All five Linux Docker indexing workloads pass every existing 64-bit allocation ceiling and complete graph/metadata output contract. The previously failing populated-batch retained allocation counts are now 912/927 and 932/947 (observed/limit); all 17 original failures are resolved locally. Core's full 253 unit tests and all 55 Storage inverted-index tests pass, including mutation atomicity, node accounting, snapshots, cancellation and reconstructed morphology.
- Analysis and phrase allocation/output gates pass locally. Verifier tests cover explicit allocation-only protocols, unchanged output/ceiling enforcement and rejection of timing observations or baselines. Native CI independently runs all five gates; selected WASM CI independently runs analysis, indexing, SQLite and phrase. No timing samples, generated reports or changed limit files are included.
- Native CI at `2a033832` passes analysis, indexing, phrase and SQLite. All four redb workloads pass zero private retention and live/reopened output checks, exposing 12 previously masked allocation violations. Their unchanged ceilings remain the acceptance target. The follow-up removes repeated copying of uniquely owned private-record tree nodes, the extra local lease payload allocation, per-field analysis scratch budgets and duplicate reverse/posting term keys. Local Linux aarch64 now falls within every existing Linux x86_64 counter ceiling in a diagnostic comparison, including peak counts 11,463 / 3,864 / 3,935 / 3,935 and total counts 1,942,760 / 156,163 / 181,775 / 173,821. This does not add or accept an unreviewed target; final Linux x86_64 CI acceptance remains required.
- WASM CI at `2a033832` passes analysis, indexing, SQLite and phrase allocation/output gates, along with both analyzer checks in Chrome and IndexedDB. Neither issue is closed yet. Generated local evidence is outside the repository; the final PR records CI artifacts and source identity.

The latest staging correction passes all 505 Storage unit tests, all 244 Storage integration tests and all 47 redb contract tests in Linux Docker. Core's 256 unit tests and redb's 54 unit tests passed after the persistent-map and lease changes. Strict Clippy with both morphology features and ownership/dependency checks pass. No allocation ceiling or expected output changed.
