# PostgreSQL 18 compatibility plan

Status: Active living plan

Update rule: Update this plan, its synchronized status ledger, the manual, and `tests/parity/pg18/manifest.json` in the same compatibility PR whenever supported behavior or a remaining gate changes.

## Goal

The long-term goal is complete PostgreSQL 18 compatibility at every externally observable boundary: SQL parsing and execution, data types and casts, catalogs, errors, transactions and concurrency, frontend/backend protocol 3.0 and 3.2, and behavior seen by standard PostgreSQL clients. UQA Engine may retain its own internal storage and execution architecture, but an implementation difference is not a reason to expose different PostgreSQL behavior.

This plan distinguishes a PostgreSQL 18 baseline from complete compatibility. The baseline is reached when PostgreSQL 18 grammar, metadata, fixtures, and protocol primitives are authoritative and every unimplemented PostgreSQL 18 shape fails explicitly. Complete compatibility is reached only when the PostgreSQL 18 regression, isolation, protocol, catalog, and client matrices pass without semantic exemptions.

## Non-negotiable rules

- Parser acceptance is not execution support. Every newly accepted AST field must be implemented or rejected before any state change.
- Workarounds are prohibited. A compatibility defect must be fixed at the owning parser, type, catalog, planner, executor, storage, or protocol boundary; source rewriting, silent normalization, test-only behavior, and local dependency overrides are not shippable implementations.
- Silent approximation is a compatibility bug. This includes dropping `RETURNING` aliases, enforcing a `NOT ENFORCED` constraint, selecting a text overload for `bytea`, or omitting catalog state.
- PostgreSQL 18 is the differential oracle. Checked-in fixtures record the exact PostgreSQL server version, architecture, query text, columns, ordered rows, NULLs, value types, errors, and SQLSTATE where applicable.
- Historical PostgreSQL 17 measurements remain historical evidence. Active fixture names, paths, defaults, tests, and compatibility prose move to `pg18`; old benchmark and changelog facts are not relabeled.
- Protocol 3.0 remains supported while protocol 3.2 is added. Version negotiation and unsupported `_pq_.` options must follow PostgreSQL rather than relying on the current libpq default.
- A compatibility claim is no broader than its passing evidence. The manual must identify the current milestone until the complete-compatibility gates pass.
- Add every newly confirmed compatibility gap to the manifest and this plan immediately with an incomplete status; promote it to `verified` only after focused tests and the PostgreSQL 18 differential oracle pass.

## Current implementation status and open PostgreSQL 18 bugs

The historical starting point used `pg_query` 6.1.1 with PostgreSQL 17 grammar, reported `server_version` as `17.0-uqa`, stored the active TPC-H-derived oracle in `expected/pg17.json`, and accepted only frontend/backend protocol 3.0 primitives. Active assets now use `pg18`, session metadata reports `18.0-uqa`, and the checked-in 22-query oracle records PostgreSQL 18.4 server and platform provenance.

The PostgreSQL 18 parser migration replaces the four DML `returning_list` fields with complete `returning_clause` handling. PostgreSQL 18's PL/pgSQL JSON producer also needs to serialize `retvarno` for datum-backed `RETURN` and `RETURN NEXT`; UQA Engine consumes those slots directly and does not rewrite routine source. The reproducible parser chain is imported as the `uqa-pg-query` workspace crate from `jaepil/pg_query.rs@516b3a03fed42e606ce01bc8b5a864a1698c210d` and `jaepil/libpg_query@898cd71c96375d6d4219916996701571dbe2b239`; the latter contains the PostgreSQL 18 parser, corrected PL/pgSQL datum serialization, structured `%TYPE` and `%ROWTYPE` identifier metadata, trigger-promise and type-cache fixes, and a process-wide pthread exit key with a `PTHREAD_KEYS_MAX + 1` regression test so one integration-test executable can safely create parser threads throughout its lifetime.

The following compact ledger is the readable projection of the machine-readable compatibility manifest. Schema version 2 owns milestone names and exit gates, assigns every evidence item to exactly one milestone, derives milestone status from those item states, and synchronizes this ledger and the manual snapshot; pull-request checks reject missing, duplicate, orphaned, or textually drifting accounting.

<!-- pg18-manifest-status:start -->

| Milestone | Name | Status | Exit gate |
| --- | --- | --- | --- |
| `M0` | PG18 baseline | `in_progress` | PG18 parser pinned; all AST deltas audited; unsupported shapes fail explicitly; active names and fixtures use pg18; 22/22 TPC-H-derived results match PostgreSQL 18 |
| `M1` | Discovered semantic fixes | `complete` | Bounded DML row-image, constraint-metadata, identified-function, and independently verified semantic slices pass their PostgreSQL 18.4 evidence |
| `M2` | Protocol 3.2 | `complete` | Byte-exact codec tests and live PostgreSQL 18 libpq 3.0/3.2/latest negotiation and cancellation tests pass |
| `M3` | PG18 DDL and types | `in_progress` | Generated columns, range/multirange, temporal constraints, catalogs, dump/restore, and reopen tests pass |
| `M4` | Core regression parity | `in_progress` | PostgreSQL 18 core regression and isolation suites pass with every remaining failure recorded and reduced to zero |
| `M5` | Client parity | `in_progress` | The supported driver, migration, introspection, dump/restore, COPY, and pooling matrix passes |
| `M6` | Complete compatibility | `not_started` | M0 through M5 and every manifest item are verified, the final zero-exemption audit passes, and the manual removes the implemented-subset qualification |

| Evidence item | Milestone | Status |
| --- | --- | --- |
| `baseline.pg18-differential-probes` | `M0` | `verified` |
| `baseline.tpch-derived-queries` | `M0` | `verified` |
| `parser.pg18-chain` | `M0` | `partial` |
| `query.join-using-natural` | `M3` | `partial` |
| `query.parenthesized-join-alias` | `M1` | `verified` |
| `query.fetch-with-ties` | `M1` | `verified` |
| `query.pattern-escape` | `M1` | `verified` |
| `query.group-by-distinct` | `M1` | `verified` |
| `query.table-function-with-ordinality` | `M1` | `verified` |
| `query.rows-from-and-multi-array-unnest` | `M1` | `verified` |
| `query.named-window` | `M1` | `verified` |
| `query.row-locking-complete-matrix` | `M4` | `partial` |
| `query.cte-search-cycle-and-materialization` | `M4` | `verified` |
| `dml.returning-row-images` | `M1` | `verified` |
| `dml.returning-row-images-extended` | `M4` | `verified` |
| `dml.merge-not-matched-by-source` | `M4` | `verified` |
| `ddl.constraint-metadata` | `M1` | `verified` |
| `ddl.constraint-lifecycle` | `M3` | `partial` |
| `ddl.relation-forms-and-options` | `M3` | `partial` |
| `ddl.ctas-column-names` | `M1` | `verified` |
| `ddl.ctas-with-no-data` | `M1` | `verified` |
| `ddl.select-into` | `M1` | `verified` |
| `ddl.view-column-aliases` | `M1` | `verified` |
| `functions.identified-pg18-additions` | `M1` | `verified` |
| `functions.full-pg18-matrix` | `M4` | `partial` |
| `functions.fixed-builtin-overload-resolution` | `M1` | `verified` |
| `functions.to-reg-object-lookups` | `M1` | `verified` |
| `routines.scalar-domain-overload-resolution` | `M1` | `verified` |
| `routines.procedure-call-overload-resolution` | `M1` | `verified` |
| `routines.table-setof-overload-resolution` | `M1` | `verified` |
| `routines.stored-view-function-drop-restrict` | `M1` | `verified` |
| `routines.polymorphic-variadic-pseudotype-overloads` | `M4` | `verified` |
| `catalog.user-routine-pseudotype-variadic-identity` | `M3` | `verified` |
| `routines.alter-volatility-null-input-lifecycle` | `M3` | `verified` |
| `routines.function-procedure-drop-cascade` | `M3` | `verified` |
| `routines.function-procedure-drop-cascade-extended` | `M3` | `partial` |
| `relations.schema-usage` | `M4` | `verified` |
| `relations.ordinary-table-privileges` | `M4` | `verified` |
| `routines.extension-language-privilege-security` | `M4` | `partial` |
| `functions.json-null-stripping` | `M1` | `verified` |
| `functions.array-transforms` | `M1` | `verified` |
| `functions.integer-base-conversion` | `M1` | `verified` |
| `functions.random-range` | `M1` | `verified` |
| `functions.reverse-overloads` | `M1` | `verified` |
| `functions.md5-overloads` | `M1` | `verified` |
| `functions.crc-checksums` | `M1` | `verified` |
| `functions.gamma-functions` | `M1` | `verified` |
| `functions.string-binary-lengths` | `M1` | `verified` |
| `functions.uuid-extraction` | `M1` | `verified` |
| `execution.static-row-schema-and-spill-v1` | `M3` | `partial` |
| `types.declared-identity-casts-and-catalog` | `M3` | `partial` |
| `ddl.alter-type-and-migration` | `M3` | `partial` |
| `catalog.pg-database-locale` | `M3` | `partial` |
| `plpgsql.datum-slots-and-bound-cursors` | `M4` | `partial` |
| `sequences.nontransactional-values-and-catalog-state` | `M4` | `partial` |
| `transactions.snapshot-catalog-visibility` | `M1` | `verified` |
| `protocol.frontend-backend-3.2` | `M2` | `verified` |
| `protocol.frontend-backend-3.2-extended` | `M5` | `partial` |
| `ddl.generated-columns` | `M3` | `partial` |
| `ddl.temporal-constraints` | `M3` | `partial` |
| `ddl.triggers` | `M4` | `partial` |
| `ddl.rewrite-rules` | `M4` | `partial` |
| `graph.age-default-label-drop` | `M4` | `verified` |
| `graph.age-cypher-complete-matrix` | `M4` | `partial` |
| `regression.core-and-isolation` | `M4` | `not_audited` |
| `clients.driver-and-operations-matrix` | `M5` | `partial` |
| `compatibility.complete-zero-exemption-audit` | `M6` | `not_audited` |

<!-- pg18-manifest-status:end -->

| Area | Current status | Remaining gate |
| --- | --- | --- |
| Active PG18 baseline | Active paths, scripts, tests, defaults, and fixtures use `pg18`; the parser chain is imported as `uqa-pg-query` from the recorded revisions; 22/22 TPC-H-derived results match PostgreSQL 18.4 | Complete the AST coverage inventory |
| Qualified joins | `JOIN ... USING`, `USING (...) AS alias`, and `NATURAL JOIN` preserve structural AST metadata, bind against both physical row types, resolve the implemented equality/common-type matrix before execution, coerce differently declared keys, implement merged-column ordering and outer-join value selection, preserve input qualification and duplicate non-key output slots, and report PostgreSQL column SQLSTATEs | Complete collations, domains, user-defined operators, and the full PostgreSQL equality/coercion matrix |
| Verified SELECT slices | Parenthesized JOIN aliases, `FETCH ... WITH TIES`, pattern `ESCAPE`, `GROUP BY DISTINCT` and `ALL`, table-function `WITH ORDINALITY`, multi-function `ROWS FROM`, FROM-position multi-array `unnest`, named `WINDOW` definitions, recursive CTE controls, and CTE materialization policy have PostgreSQL 18.4 result, metadata, and SQLSTATE coverage | Continue one independently reviewed and manifested parity slice at a time |
| Recursive CTE controls and materialization | `SEARCH` depth/breadth values, `CYCLE` marks and paths, generated-column scope, path-aware `UNION` distinctness, working-table-wide recursive steps, validation ordering, durable plans, and default/`MATERIALIZED`/`NOT MATERIALIZED` execution policy match the checked PostgreSQL 18.4 cases | Continue the wider upstream recursive-query and optimizer regression matrix under newly discovered independent items |
| Row locking | `FOR UPDATE`, `FOR NO KEY UPDATE`, `FOR SHARE`, `FOR KEY SHARE`, `OF`, `NOWAIT`, and `SKIP LOCKED` retain row identity through supported scans, joins, subqueries, views, CTE placement, mutations, savepoints, and persistent providers; shape-before-target and clause-order validation, explicit/implicit CTE handling, base `pg_catalog`, and Apache AGE catalog-relation cases match the checked oracle | Complete the upstream row-lock, isolation, process-boundary, remaining virtual-view/unsupported-relation, and deadlock matrix |
| Derived relation creation | CTAS positional column names, CTAS `WITH NO DATA`, ordinary, temporary, and unlogged CTAS and `SELECT INTO`, CREATE VIEW positional column names, temporary views, materialized-view snapshots and refresh, relation reloptions, regular-view and materialized-view creating-role ownership with owner-authorized ALTER, replacement, refresh, and DROP, target-role-validated ownership transfer, role dependencies, durable relation and column ACLs, rooted grant-option chains, regular-view definer and security-invoker authorization, delegated materialized-view MAINTAIN with owner-context refresh, catalog and information-schema projection, privilege inquiry, transactionality, durable reopen behavior, automatically updatable single-source projection views, nested rewriting, creation-time star-expanded public-row-type name boundaries, per-column updatability, nested automatic-view rule ordering, conditional-INSTEAD rejection, automatic and direct or nested `INSTEAD OF` trigger-backed view `MERGE` target paths, and direct validation plus inner-to-outer enforcement of `LOCAL` or `CASCADED` check options are implemented and tested | Complete typed tables, storage/access-method/tablespace options, crash recovery, concurrent materialized-view refresh and indexes, remaining updatability shapes involving CTE-backed definitions or other unverified query forms, and complete security-barrier optimizer behavior |
| Index namespace identity | Standalone indexes have schema-qualified typed catalog identities in their table schema, share the schema-local relation namespace, preserve quoted components, allow equal local names across schemas, use PostgreSQL collision suffixes, expose distinct stable `regclass` OIDs, retain temporary indexes only in their owning session, and bind `DROP INDEX` once through schema `USAGE` and effective `search_path`; the PostgreSQL 18.4 oracle and storage migration, reopen, collision, catalog, and SQLSTATE tests cover the boundary | Complete expression indexes, remaining access methods, complete index ownership and ACL behavior, dump/restore, and the upstream index regression schedule |
| Relation hierarchies | Ordinary inheritance and list/range/hash partitioning preserve physical row identity, typed hash-key expressions, parent and `ONLY` scans, cross-partition wait-chain DML, DML and COPY routing, durable bounds and column and inherited-constraint provenance, recursive column/CHECK/`NOT NULL` ALTER behavior, safe legacy dangling-edge repair, `tableoid`, hierarchy-aware planner statistics, `pg_inherits`, `pg_partitioned_table`, `pg_class.relpartbound`, `pg_get_expr`, `pg_get_partkeydef`, partitioned index catalog identity, and atomic durable `INHERIT`, `NO INHERIT`, `ATTACH PARTITION`, and `DETACH PARTITION` lifecycle changes | Complete the remaining upstream inheritance and partition regression schedules, privilege behavior, dump/restore, interruptible concurrent-detach pending/finalize and multi-session lock-wait behavior, and unsupported index-method interactions |
| `RETURNING WITH (OLD/NEW ...)` | Old and new images and custom aliases are preserved through SQL AST, planning, and DML execution across ordinary and partitioned INSERT, UPDATE, DELETE, ON CONFLICT, and every MERGE mutation action. BEFORE-trigger mutation and suppression, immutable original OLD images, non-retroactive AFTER-trigger writes, stored generated values, same-leaf and cross-leaf updates, source and destination physical `tableoid`, source columns, `merge_action()`, `DO NOTHING`, source-before-target `*` layout, and structurally identified OLD, NEW, and merge-action carriers match the PostgreSQL 18.4 live oracle | Continue broader trigger and partition lifecycle work under their owning manifest items without reopening this verified row-image boundary |
| Virtual row metadata | Compatibility names `_score` and `_doc_id` remain available; one unambiguous local-table source additionally exposes binding-only `_meta.score` and `_meta.doc_id` aliases backed by structural physical slots, so identically named user columns remain independently addressable and wildcard output is unchanged | Extend the namespace only when another engine-owned row attribute has a stable public contract |
| Constraint lifecycle | Named CHECK, foreign-key, and `NOT NULL` constraints implement `NOT VALID`, failure-atomic CREATE/ALTER publication and validation, referenced-primary-key inference, PostgreSQL-directional cross-type equality and referential actions, supported ALTER state changes, actual initially-deferred foreign-key checks before outer-commit temporary actions, per-transaction `SET CONSTRAINTS` with persistent `ALL` state, exact name/search-path resolution, operation-sensitive durable constraint-object, row-identity, and firing-relation-bound events, retroactive immediate validation across nested frames, routine, callback, PostgreSQL simple-query transaction control, and savepoint-aware modes, queued-event survival across deferrability changes, pending-event ALTER, DROP, and TRUNCATE protection with referenced-parent checks and rename continuity, current- and cross-session constraint replacement, table-rename continuity, and allocated temporary-namespace lifetime, durable catalog flags and reopen, multi-action ALTER atomicity, and dependency-aware drop | Implement inheritance and partition propagation, the complete dependency graph, `pg_dump`/`pg_restore`, and the upstream constraint regression matrix |
| Bounded identified PG18 functions and casts | The original bounded array, bytea, Unicode, UUID-generation, checksum, JSON, numeric, interval, Roman-numeral, aggregate, and regular-expression inventory is implemented and differentially verified, including its identified `pg_proc` metadata; separately tracked slices retain their own exact evidence | This bounded inventory is complete; newly discovered gaps are tracked independently and do not reopen it |
| Full PG18 function, operator, cast, and catalog matrix | Dedicated verified evidence items cover the implemented slices | Inventory every PostgreSQL 18 signature and catalog identity and reduce every unverified entry to zero |
| Implemented fixed-signature overload resolution | The documented fixed-signature built-in subset uses one registry and candidate-selection path for ordinary execution and generated-expression typing and binding, including visible SQL user functions, explicit `pg_catalog` qualification, search-path order, exact and implicit matches, preferred and unknown categories, domain base types, named and default slots, declared return types, and durable stored bindings where volatility permits; exact `pg_proc` evidence now includes unit `random()`, `to_hex(integer\|bigint)`, `gen_random_uuid()`, `casefold(text)`, `uuidv4()`, and both `uuidv7` signatures | Complete the PostgreSQL built-in, operator, cast, extension, and `pg_proc` matrix |
| PostgreSQL catalog lookup functions | `to_regproc(text)`, `to_regprocedure(text)`, `to_regclass(text)`, `to_regnamespace(text)`, `to_regrole(text)`, and `to_regtype(text)` resolve represented catalog objects with exact `reg*` aliases, dedicated identifier-string and type-name parsing, numeric and zero OID forms, qualification and `search_path` or global-role visibility, user-routine overload handling, missing and ambiguous soft failures, NULL propagation, cross-database, overqualification, and malformed-type SQLSTATEs, stable generated-expression rejection, direct hard `regnamespace` casts to catalog OID carriers, durable scalar and array `regrole` OID storage across rollback, reopen, and role removal, stored scalar-constant dependency rejection with exact input-error precedence, and exact `pg_type`, `pg_proc`, and `information_schema.routines` metadata | Extend catalog object and type coverage only through independently verified catalog and type items while completing the full function and catalog matrix |
| Scalar SQL routine overload resolution | Ordinary scalar SQL functions retain direct-cast and scalar-subquery declared types through candidate selection, structurally match named and default slots before effective-signature search-path shadowing, preserve typed identities through catalog reopen, prefer exact implemented information-schema domain signatures over their base types, including nested casts that restore the domain at the call boundary, and distinguish unqualified polymorphic function-like syntax from quoted, qualified, and regular search-path-selected routines | Complete extension-language, privilege/security, and enum polymorphism |
| Procedure, `CALL`, and scalar PL/pgSQL overload resolution | Scalar PL/pgSQL functions retain direct-cast and scalar-subquery declarations, while procedures and `CALL` retain direct-cast and concrete PL/pgSQL datum declarations through candidate selection, including typed NULL variables and exact implemented information-schema domains; `CALL` rejects subqueries and distinguishes missing required OUT/TABLE placeholders (`42883`) from a structurally matching wrong-kind function (`42809`) | Complete extension-language, privilege/security, and enum polymorphism |
| Table-returning and `SETOF` overload resolution | Table-returning and `SETOF` functions retain direct-cast and scalar-subquery declared types, exact implemented information-schema domains, named and default slots before effective-signature search-path shadowing, stable scalar-versus-set projection bindings, user-versus-built-in search-path selection, exact DML and correlated LATERAL source bindings, and durable table-routine and stored-view identities across catalog reopen | Complete extension-language, privilege/security, and enum polymorphism |
| Polymorphic, VARIADIC, and pseudo-type routines | Simple and compatible substitutions cover implemented scalar, array, built-in range, and paired multirange carriers; positional expansion, explicit array pass-through, defaults, named notation, fixed-versus-expanded ranking, declaration validation, concrete SQL/PL/pgSQL/TABLE/SETOF/CALL result types, nested/generated/view bindings, exact SQLSTATEs, and reopen behavior match the checked-in PostgreSQL 18.4 with Apache AGE oracles | Add enum carriers and polymorphism under the type item; complete extension-language and privilege/security interactions under their separate routine item |
| User-routine `pg_proc` identity | Functions and procedures expose input and all-argument identity, modes, names, defaults, variadic and return OIDs, set state, owner, security, strictness, leakproofness, parallel-safety, configuration, support OID, EXECUTE ACL, and implemented pseudo-type OIDs | Complete remaining built-in and extension-language rows plus the object-wide ownership, ACL, membership, and grantor matrix |
| Routine attribute lifecycle | Exact or uniquely visible functions support volatility, null-input, security, leakproof, parallel, support, and local configuration changes; all routine kinds support membership-aware ownership transfer; PostgreSQL 18 role membership, `pg_auth_members`, all six name/OID `pg_has_role` overloads, all six current-user or explicit-role name/OID `has_schema_privilege` overloads, schema `CREATE` enforcement across supported durable relation and routine creation, indexes, and indexed key constraints, routine schema `USAGE` enforcement across calls and routine DDL with exact stored-binding semantics, transitive SET and INHERIT behavior, CREATEROLE delegation limits, SECURITY INVOKER role persistence, SECURITY DEFINER restrictions, body preservation, catalog exposure, privilege checks, atomicity, and reopen are covered | Passwords, per-role settings, remaining object namespace paths, default privileges, extension languages, routine-ACL grantor chains, and full support-function and planner consequences |
| Relation namespace `USAGE` | Dynamic table, view, materialized-view, and foreign-table lookup filters the effective search path and rejects inaccessible qualified schemas before object or column lookup; relation mutations, TRUNCATE, supported ALTER and DROP paths, stored-view creation, hard and soft `regclass` input, inherited grants, prepared-plan revocation, invoker and definer contexts, and exact stored identities in views, materialized views, SQL-standard query bodies, and declared cursors follow PostgreSQL 18.4 | Complete remaining object namespace paths, foreign-table ACLs, remaining relation-owner enforcement, default privileges, and the upstream privilege regression matrix |
| Ordinary-table ownership and table and column ACLs | The creating role, nullable table ACL, and nullable or explicitly empty per-column ACLs are persisted atomically with each ordinary table; inherited owner authority protects ALTER, owner or containing-schema owner authority protects DROP, OWNER TO validates role existence, SET authority, and target-schema CREATE with superuser bypass, owned serial and identity sequences and owner-issued ACL paths transfer atomically, owner, grantor, and grantee dependencies block DROP ROLE, and transactions, savepoints, temporary tables, cross-engine refresh, reopen, and stable relation identity follow PostgreSQL 18.4. Table-wide `SELECT`, `INSERT`, `UPDATE`, `DELETE`, `TRUNCATE`, `REFERENCES`, `TRIGGER`, and `MAINTAIN` implement `PUBLIC`, rooted grant-option paths, `RESTRICT` and `CASCADE`, implicit owner rights, explicit sequence targets, all-in-schema sequence exclusion, query and DML enforcement, target-read-sensitive `MERGE` and DML, `COPY`, hierarchy parent checks, `pg_class.relacl`, information-schema visibility, and all six `has_table_privilege` overloads. Column `SELECT`, `INSERT`, `UPDATE`, and `REFERENCES` add independent grant-option paths and dependent revoke, exact direct, joined, correlated, source, target, system-column, `COPY`, `MERGE`, and foreign-key authorization, implicit-width and default-value insertion, column rename and drop, owner-transfer grantor rewriting, role dependencies, `pg_attribute.attacl`, `information_schema.columns`, `column_privileges`, and `role_column_grants`, all twelve `has_column_privilege` overloads including sequence and attnum behavior, and a checked-in PostgreSQL 18.4 oracle | Complete foreign-table ACLs, foreign-table ownership, remaining relation-owner checks, default privileges, row-level security, and the upstream privilege regression matrix |
| Range-function groups and multi-array `unnest` | Explicit `ROWS FROM` preserves independently bound members, anonymous-record member descriptors and type checks, concatenated column order, longest-member zip with NULL padding, outer aliases, group ordinality, implicit LATERAL behavior, and durable stored-view bindings; unqualified multi-array `unnest` in FROM expands to canonical unary `pg_catalog.unnest` members while qualified calls retain ordinary overload resolution | Continue the remaining PostgreSQL table-function signatures and upstream range-function regression matrix |
| Stored routine and view dependencies with transitive CASCADE | Stored scalar, table-function, function-group, CTE, subquery, join, set-operation, and SQL-standard routine query plans retain exact user-function identities; RESTRICT reports `2BP01`; explicit multi-target drops satisfy internal dependencies; and CASCADE removes generated columns, direct and transitive stored views, and transitive dependent functions or procedures with durable reopen behavior and primary notices | Extend the same exact graph to SQL-standard command bodies, parameter defaults, and every remaining PostgreSQL dependency-object kind, deletion ordering, and diagnostic detail |
| Apache AGE label DROP lifecycle | `drop_label` follows AGE 1.8.0 `DROP TABLE ... RESTRICT` for empty and nonempty default and user labels, including protected direct table drops, selectable inherited label relations, inherited-label and stored-view dependencies, durable view binding through graph rename and cascading graph drop, durable catalog and sequence removal, incident edge-row preservation with dangling endpoint ids, transactions, rename and reopen, restrictive graph drop after all labels are gone, cascading recovery, and safe `42P01` failures for broken-default accesses that crash the upstream backend | Continue `graph.age-cypher-complete-matrix` without reopening this verified slice |
| Explicitly tracked syntax and lifecycle gaps | Relation forms and options plus remaining routine dependency kinds fail explicitly without partial mutation | Implement the exact PostgreSQL 18.4 behavior and promote each independent manifest item only after live differential evidence |
| PG18 database locale catalog | `pg_database` exposes PostgreSQL 18's fixed engine-database identity, bootstrap owner, builtin provider and Unicode locale fields, durable default and explicit `CONNECT`, `CREATE`, and `TEMPORARY` ACLs, and exact `datdba` and `datacl`; all six name/OID `has_database_privilege` overloads, rooted grant chains, dependent revoke, role dependencies, transactions, cross-engine refresh, reopen, NULL, error, and catalog behavior are differentially tested; `CREATE SCHEMA` and every implemented temporary relation or index creation form enforce the corresponding database privilege with inherited grants, immediate revocation, transactional restoration, and PostgreSQL error precedence | Complete collation and locale-provider behavior, database creation, alteration, removal, owner transfer, and `CONNECT` enforcement at the embedding connection boundary |
| PL/pgSQL assertions, datum slots, loops, transaction control, and bound cursors | Procedural conditions use assignment-style Boolean coercion; `ASSERT` implements success, false and NULL failure, lazy text messages, `P0004`, named `assert_failure` handling with `OTHERS` exclusion, unchanged `FOUND` and `ROW_COUNT`, and the default-on `plpgsql.check_asserts` Boolean setting with `pg_settings` and routine-local configuration. Array `FOREACH` and static-query, dynamic-query, and bound-cursor `FOR` loops preserve PostgreSQL expression evaluation, target, `FOUND`, prefetch, pinning, cleanup, and error behavior. `COMMIT` and `ROLLBACK`, including `AND CHAIN` and `AND NO CHAIN`, run in standalone top-level procedure and anonymous-block contexts and uninterrupted direct `CALL` or `DO` nesting, retain local datum values, start fresh transaction segments, preserve chained characteristics, reject atomic contexts and dynamic execution with exact SQLSTATEs, keep completed segments across later failures, materialize query-loop portals as holdable, reject command-driven loop boundaries, close explicit cursors, and survive catalog reopen. `retvarno`, the `-1` cursor sentinel, bound cursor arguments, named arguments, bound and unbound static or dynamic query and command `OPEN`, directional single-row `FETCH`, `MOVE`, `FOUND`, `ROW_COUNT`, and `CLOSE` are structural AST and interpreter state backed by the pinned parser revisions; row-returning commands cover `SHOW`, `EXPLAIN`, output-producing `CALL`, and `INSERT`, `UPDATE`, `DELETE`, or `MERGE` with `RETURNING`, execute completely on first portal use including `MOVE 0`, reject commands without result rows before mutation, preserve PostgreSQL 18 command scroll defaults, explicit-scroll DML row images, scrollable `CALL` output, and the explicit-scroll `MERGE` rejection, and remain side-effect-free when closed unread; query cursors evaluate target expressions for OFFSET-discarded rows, stop at LIMIT, pull one row at a time, re-evaluate volatile scan, `VALUES`, filter, projection, sort, and limit pipelines during backwards movement and revisits, preserve PostgreSQL `FETCH 0` and `MOVE 0` execution timing, traverse native and nested `UNION ALL` branches in the requested direction, and materialize supported non-backwards plan or complete-`Append` boundaries with stable values; query-dependent scrollability uses lazy session portals, and `refcursor` parameters and returns name portals that survive routine exit until close or outer transaction end; top-level SQL `DECLARE`, `FETCH`, `MOVE`, and `CLOSE` add holdability, transaction cleanup, and savepoint lifecycle; qualified named types and `%TYPE` references resolve against actual table metadata and ordinary assignment/return coercion propagates SQL cast errors | Complete remaining PL/pgSQL diagnostics families and cursor behavior for PostgreSQL physical node families not implemented by UQA |
| Nontransactional sequence values and catalog state | `nextval` and both `setval` forms preserve PostgreSQL 18 called-state, next-allocation, session `currval`, strict NULL, default-bound, relation-kind, privilege, and read-only behavior across failed statements, PL/pgSQL exception subtransactions, transaction rollback, savepoint rollback, and reopen for in-memory and persistent providers; positive `CACHE` sizes reserve bounded blocks durably, consume and abandon them per session, invalidate them on definition changes and caller `setval`, and preserve PostgreSQL transaction ownership; direct sequence scans expose the physical `last_value`, `log_cnt`, and `is_called` tuple with `SELECT` enforcement, shared-namespace shadowing, durable cache-log state, PostgreSQL reset behavior, and matching `pg_class.relnatts` and `pg_attribute` metadata for the three positive-numbered columns; `pg_sequence_parameters`, `pg_get_sequence_data`, `pg_sequence_last_value`, and `pg_sequences.last_value` preserve their distinct privilege and called-state rules plus exact built-in catalog identities; `RENAME TO` and `SET SCHEMA`, including historical `ALTER TABLE` syntax for sequence targets, preserve stable OIDs, current- and sibling-session cache blocks and values, implemented stored dependencies, transaction and savepoint rollback, durable reopen, ownership metadata, and exact lifecycle errors; `SET LOGGED` and `SET UNLOGGED`, including historical syntax, preserve exact changed versus no-op cache behavior across sessions, transaction and savepoint rollback, durable persistence, temporary errors, and `pg_class.relpersistence`; `OWNED BY table.column` and `OWNED BY NONE` preserve automatic or internal dependencies through rename, owner drop, `TRUNCATE`, transaction rollback, savepoints, and reopen; creating-role ownership, membership-aware owner administration, SET-authorized direct and historical `OWNER TO`, dependent-role protection, `pg_class.relowner`, and `pg_sequences.sequenceowner` preserve stable identity and transactional, temporary, rename, and reopen lifecycle; `USAGE`, `SELECT`, and `UPDATE` ACLs cover explicit, historical `ON TABLE`, and all-in-schema targets, `PUBLIC`, independent rooted grantor paths, grant options, dependency-aware revoke, owner self-revocation and transfer, role dependencies, all six name/OID `has_sequence_privilege` overloads, `pg_class.relacl`, transactions, savepoints, temporary lifecycle, cross-engine refresh, and reopen; durable schema ownership and `USAGE` and `CREATE` ACLs preserve membership-aware grant paths, dependent revoke, role dependencies, transactions, cross-engine refresh, reopen, `pg_namespace`, `information_schema.schemata`, and PostgreSQL sequence creation, name-resolution, access, owner-transfer, schema-move, catalog-visibility, and error-precedence boundaries; `pg_sequences` exposes the configured cache size and durable reservation endpoint, while `pg_get_serial_sequence` exposes the column-owner link | Complete unlogged crash recovery, remaining dependency object kinds, immediate cross-session visibility and atomic allocation under every concurrent writer state, and the remaining upstream sequence regression matrix |
| Sequence information-schema catalog | Implicit serial and identity backing sequences retain their PostgreSQL integer type across reopen; `information_schema.sequences` projects exact declared type, numeric precision, start, bounds, increment, and cycle state, excludes internal identity dependencies, and filters rows by inherited ownership or any sequence privilege against the statement catalog snapshot; both sequence catalog views hide other sessions' temporary namespaces | Continue the remaining upstream sequence regression matrix under `sequences.nontransactional-values-and-catalog-state` |
| Protocol 3.2 | Byte-exact tests cover minor negotiation, ordered `_pq_.` reporting, message tag `v`, variable cancellation keys, legacy 3.0 validation, FunctionCall, context-aware cleartext/MD5/GSS/SSPI/SASL authentication messages, notifications, COPY format validation, exact text/binary format-vector resolution, layered 256-byte cancellation keys, malformed live peers, and the PostgreSQL 18 reserved-3.1 edge; PostgreSQL 18.4 `psql`/libpq plus pinned psycopg, pgx, and node-postgres tests cover startup negotiation, authentication ordering, SSL rejection and retry, prepared reuse, binary values, COPY, transaction recovery, and pooling | Add credentialed GSS/SSPI/OAuth environments, TLS channel binding, replication and pipeline modes, and the complete malformed-peer corpus; credential verification remains the embedding server's responsibility |
| Static row types and spill format | Scans, projections, filters, joins, CTEs, aggregates, windows, DML `RETURNING`, cursors, foreign tables, and empty virtual relations carry declared `ColumnType` metadata without reconstructing it from runtime values; materialization remains at the final consumer boundary; executor-only attributes use structural identities, wildcard visibility and score provenance are structural, spill format version 1 stores those layouts with logical `(alias, column)` identities and declared schemas without a compatibility reader, and version 0.1.6 persisted compiler dispatch markers migrate at catalog restore | Extend exact static typing to every remaining expression/operator and persistent relation kind |
| Declared types, casts, and catalogs | Integer widths, OID/XID, floating widths, character variants, UUID, temporal types, arrays, domains, `refcursor`, foreign schemas, migrations, and PostgreSQL's six built-in range/multirange families retain exact identities; source-sensitive OID/XID/bytea casts preserve declared width and PostgreSQL SQLSTATEs; legacy vectors and scalar or array forms of all four supported `reg*` aliases retain declared identity and PostgreSQL text/COPY output, including catalog names, qualification, `0` as `-`, numeric unresolved OIDs, built-in type aliases, NULL preservation, and exact supported virtual-catalog `regclass` OIDs; scalar `regprocedure` resolves and renders exact function or procedure signatures for implemented user routines and built-ins, while `pg_type` exposes `regprocedure` and `_regprocedure` and `pg_proc` exposes the four scalar I/O rows; ranges canonicalize discrete bounds, multiranges normalize members, and supported functions, operators, and simple/compatible polymorphic routines retain subtype identity; `pg_type`, `pg_range`, and `pg_proc` expose the verified built-in, pseudo-type, domain, and information-schema relationships across storage, generated expressions, foreign tables, schema rewrites, and reopen | Complete all remaining built-in and extension type I/O routines in `pg_proc`, composite/domain constraints, collations, enums, user-defined ranges and their polymorphism, range ordering and arithmetic, typmods, and binary formats |
| `ALTER COLUMN TYPE` and migration | `USING` remains structural AST and is evaluated against every old row inside the atomic ALTER transaction; source-sensitive implicit casts retain the old declared type; failed rewrites roll back schema and data; migration preserves supported scalar widths and rejects unknown source types instead of converting them to text; range-to-paired-multirange rewrites preserve `WITHOUT OVERLAPS`, incompatible `PERIOD` relationships fail with `42804`, and generated dependencies plus durable reopen state are validated before publication | Complete the PostgreSQL assignment-cast matrix, every remaining dependency kind, domain checks, collation changes, and every `ALTER TYPE` regression case |
| Virtual and stored generated columns | Core definition, durable reopen, dependency rewrites, selective virtual evaluation, exactly-once stored evaluation, DML row images, DDL-time static typing for the implemented expression surface, exact stored SQL routine overload binding and dependencies, supported constraints and indexes, catalogs, ALTER operations, and failure atomicity are implemented and covered by the consolidated engine integration executable | Complete the PostgreSQL built-in function and operator overload matrix, privileges, inheritance and partition propagation, dump/restore, and the upstream regression cases |
| Row, statement, and constraint triggers | Durable PL/pgSQL BEFORE and AFTER triggers cover INSERT, UPDATE, DELETE, TRUNCATE, UPDATE OF, create-time boolean WHEN validation, row-change-time AFTER WHEN selection with deferred execution, recursive referential-action row and statement lifecycles, zero-row statements, ordered explicit, descendant, and cascading TRUNCATE targets, stored and virtual generated images, alphabetical ordering, ON CONFLICT, MERGE, executable replacement, partition clones, transactional and temporary lifecycle and dependencies, `pg_trigger`, and compact and pretty `pg_get_triggerdef`. Table and view INSERT SELECT, UPDATE, and DELETE source and target scans retain the statement-start snapshot across BEFORE statement-trigger writes. Partition-moving UPDATE and UPDATE FROM execute PostgreSQL's source UPDATE/DELETE and destination INSERT row-trigger lifecycle, suppression, destination mutation and constraint failure, RETURNING, physical-delete referential behavior, and root update transition rows; partition-moving MERGE retains the physical DELETE/INSERT row triggers and empty UPDATE transition sets. PostgreSQL 18 `session_replication_role` selects Origin/Replica/Always/Disabled trigger modes, enforces its enum and superuser contract, and suppresses foreign-key triggers in replica mode. PostgreSQL 18 transition relations add typed OLD TABLE and NEW TABLE row sets for AFTER row and statement triggers across multi-row and zero-row INSERT, UPDATE, and DELETE, INSERT SELECT, ON CONFLICT, UPDATE FROM, MERGE, partition and inheritance descendants, cumulative direct multi-foreign-key actions, statement-global ordering across multi-row conflict-update cascade trees, recursive chain and branching cascade waves including trailing empty sets for AFTER ROW triggers, canonical catalog deparsing, nested-routine scope isolation, and persistence guards. PostgreSQL 18 constraint triggers add AFTER ROW execution, optional FROM dependencies, immediate and deferred modes, captured row images, retroactive SET CONSTRAINTS firing, savepoint and commit lifecycle, queued-event cancellation, independent trigger and constraint names and OIDs, trigger-owned `pg_constraint` rows, and partition clone identities. PostgreSQL 18 `INSTEAD OF` view triggers add exact definition validation, alphabetical row chaining and suppression, statement timing, original DELETE OLD propagation, final row-image RETURNING, INSERT SELECT, UPDATE FROM, DELETE USING, catalog exposure, rename and drop lifecycle, enable-mode rejection, and reopen behavior. Direct and nested automatic-to-trigger view MERGE paths cover all mutation actions and DO NOTHING, action-path consistency, statement-trigger ordering, trigger-returned row images and suppression, repeated candidates, replication-mode suppression, user-rule rejection, final check options, hidden target rows, statement-start snapshots, and failure atomicity. Automatically updatable nested single-source projection views rewrite INSERT VALUES, INSERT SELECT, ON CONFLICT, UPDATE, UPDATE FROM, DELETE, DELETE USING, and MERGE into base DML, retain defaults, leading implicit inputs, creation-time star-expanded public-row-type boundaries, scalar, EXISTS, and IN subqueries in projections and predicates with correlated and unqualified references, nested CTE expression bodies, local-alias collision avoidance, statement snapshots, qualifier-correct current, OLD, and NEW computed row images, check options, MERGE, rewrite-rule images, INSERT RETURNING, and correlated scalar-subquery binding across the complete containing DML namespace, including target and `excluded` rows, `FROM` or `USING` sources, and explicit `OLD` and `NEW` `RETURNING` aliases, ordinary `old` and `new` source aliases plus source-only hidden-name resolution including unaliased derived sources, target-before-source UPDATE FROM and DELETE USING RETURNING stars plus source-before-target MERGE RETURNING stars, computed projections and physical partition tableoid row images, base triggers, MERGE rejection of user rewrite rules, nested-layer `ALSO` and `INSTEAD` rule suppression and action ordering including nonautomatic rule-backed inner-view boundaries with qualification-before-action lazy row projection, provider-level RETURNING subqueries, pre-routing NULL tableoid, base-rule defaults and identities, final-action INSERT rule command counts, suppressed UPDATE and DELETE zero command counts, actual RETURNING-provider selection, direct rule-plus-trigger ordering, conditional-INSTEAD rejection, source, input, assignment, condition, and view-materialization laziness, omitted computed-column NULL rule images, CASE short-circuiting, event-specific rule action cardinality, view-rule ON CONFLICT, uniqueness-before-check-option errors, duplicate view mappings, rule-backed information-schema flags, computed-column action inputs, original view-trigger suppression, and suppressed INSERT identity and default behavior, replication-suppressed trigger routing, target-list SRF rejection, CREATE and ALTER check-option rejection, inner-to-outer post-BEFORE check options including UPDATE FROM and MERGE, system-column read-only status, no-writable-column DELETE capability, materialized-view relation errors, and information-schema flags; a 609-case PostgreSQL 18.4 with AGE stateful oracle reopens UQA between every case | Remaining work covers direct partition-clone lifecycle, privileges, exact node trees, dump/restore, and the complete upstream trigger schedule |
| Rewrite rules | Durable table rules rewrite INSERT VALUES, INSERT SELECT, UPDATE, and DELETE; actions that reference OLD or NEW execute set-oriented over matching event rows, while row-independent UPDATE and DELETE actions follow PostgreSQL qualification cardinality for no-predicate, false-predicate, empty-target, UPDATE FROM, and DELETE USING commands. Rules preserve nullable integer row images, structurally identify OLD/NEW action attributes without fabricated SQL names, retain collision-free and correlated LATERAL action sources, enforce PostgreSQL CTE, set-operation member, conditional set-operation action, unconditional ON INSERT set-operation one-row success and multi-row `0A000`, and ON CONFLICT event-reference scope errors and unqualified INSERT and DELETE conditions, execute alphabetical ALSO and conditional or unconditional INSTEAD actions, suppress with DO NOTHING, fire action statement triggers once, detect canonical recursion, reject active-rule ON CONFLICT and MERGE, retain enable state through replacement, rewrite rename/drop and token-safe column dependencies, protect reserved and view `_RETURN` rules, reject materialized-view rules, and expose `pg_rewrite`, `pg_rules`, rule flags, and both `pg_get_ruledef` forms. Unconditional INSTEAD providers implement positional DML RETURNING with exact event-row type and type-modifier validation, one-provider restrictions, lazy projection evaluation, provider current/OLD/NEW images and aliases, UPDATE FROM source projections for UPDATE-provider actions, and DELETE USING source projections for DELETE-provider actions; Origin, Replica, Always, and Disabled rule modes follow `session_replication_role`; a 194-case PostgreSQL 18.4 with AGE oracle reopens UQA between every case | Implement rule condition subqueries, OLD/NEW record expansion, NOTIFY actions, complete dependencies, inheritance and partition behavior, privileges, exact node trees and deparsing, dump/restore, and the complete upstream rule schedule |
| `md5(text\|bytea)` overloads | Exact text/bytea overload resolution occurs before runtime carrier erasure, text hashes its UTF-8 bytes, bytea hashes its raw payload, unsupported signatures report SQLSTATE `42883`, stored generated expressions retain the selected built-in or user binding, and `pg_proc` exposes the strict, immutable, parallel-safe, leakproof OIDs 2311 and 2321 | Continue the independently manifested PostgreSQL 18 function, operator, type, and catalog matrix |
| `crc32(bytea)` and `crc32c(bytea)` checksums | Bytea-only overload resolution occurs before runtime carrier erasure, unknown inputs acquire bytea context, both algorithms return unsigned 32-bit values as `bigint`, unsupported and ambiguous signatures report SQLSTATEs `42883` and `42725`, stored generated expressions retain the selected built-in or user binding across reopen, and `pg_proc` exposes the strict, immutable, parallel-safe, leakproof OIDs 6364 and 6365 | Continue the independently manifested PostgreSQL 18 function, operator, type, and catalog matrix |
| `gamma(double precision)` and `lgamma(double precision)` | Native execution follows PostgreSQL's host C math-library contract, numeric inputs bind through the exact float8 signature, poles and range failures preserve SQLSTATE `22003`, unsupported and ambiguous signatures preserve SQLSTATEs `42883` and `42725`, user overloads obey implicit and explicit `pg_catalog` search order, stored generated expressions retain the selected binding across reopen, and `pg_proc` exposes exact OIDs 6383 and 6384 | Continue the independently manifested PostgreSQL 18 function, operator, type, and catalog matrix |
| One-argument string and binary length overloads | The twelve PostgreSQL 18.4 `length`, `char_length`, `character_length`, `octet_length`, and `bit_length` overloads for text, character, and bytea preserve Unicode character counts, blank-padding rules, UTF-8 and raw byte counts, strict NULL, integer results, preferred text context, scalar-subquery and user-overload resolution, durable generated-expression bindings, and exact `pg_proc` rows including the two `bit_length` SQL bodies | Continue the separately manifested encoding-aware, bit-string, geometric, path, and text-search length overload families |
| Temporal constraints | `PRIMARY KEY` and `UNIQUE ... WITHOUT OVERLAPS` plus `PERIOD` foreign keys enforce nonempty nonoverlapping parent periods, aggregate coverage, mutation safety, ALTER validation, `pg_constraint.conperiod`, and reopen behavior for matching built-in range/multirange types against a checked-in 49-case PostgreSQL 18.4+AGE oracle | Complete physical GiST/exclusion-index planning, referential actions beyond `NO ACTION`, deferrable enforcement, dump/restore, and the upstream temporal regression matrix |

The verified view-trigger `MERGE` slice includes direct and nested automatic-to-trigger targets, action-path consistency, all mutation actions and `DO NOTHING`, statement-trigger ordering, trigger-returned row images and suppression, repeated candidates, replication-mode suppression, user-rule rejection, final check options, hidden target rows, failure atomicity, and statement-start snapshots.

## Workstreams

### 1. PostgreSQL 18 parser and AST safety

Use the reviewed PostgreSQL 18 parser chain imported as `uqa-pg-query` from `jaepil/pg_query.rs@516b3a03fed42e606ce01bc8b5a864a1698c210d` and `jaepil/libpg_query@898cd71c96375d6d4219916996701571dbe2b239`. The wrapper exposes PostgreSQL's raw parser modes directly, so PL/pgSQL expressions and one-, two-, and three-part assignments are parsed structurally without rewriting input text, and PL/pgSQL `%TYPE` and `%ROWTYPE` declarations retain each normalized identifier component separately from their display spelling. The C library creates its pthread destructor key exactly once per process and associates each thread's parser memory context with that shared key, preventing key exhaustion without splitting test executables or pre-initializing unrelated libraries. Any future parser update must be reviewed, tested in both repositories, pushed first, and then adopted through `scripts/sync-uqa-pg-query.py` with a new recorded revision, checksum list, and regenerated `Cargo.lock`; native, Python, Node.js, Browser WASM, and supported-platform builds remain required because the dependency contains C code and generated protobuf types.

All four DML paths now consume the complete `ReturningClause` instead of the removed `returning_list` fields. Remaining parser-baseline work is to audit every changed protobuf message and field between PostgreSQL 17 and 18, especially constraint enforcement, generated-column kind, temporal key flags, MERGE variants, COPY options, and utility statements, with compiler tests proving each unsupported field fails before catalog or storage mutation.

Add an AST coverage inventory that maps every PostgreSQL 18 top-level statement and every semantically relevant option to implemented, explicitly rejected, or not-yet-audited status. The `not-yet-audited` count must be zero for the baseline milestone, and the explicitly rejected count must be zero for complete compatibility.

### 2. Active compatibility baseline and fixture rename

The active compatibility assets and live references were renamed atomically:

| Old | New |
| --- | --- |
| `scripts/run-tpch-pg17.py` | `scripts/run-tpch-pg18.py` |
| `benchmarks/tpch/expected/pg17.json` | `benchmarks/tpch/expected/pg18.json` |
| `tests/parity/pg17/` | `tests/parity/pg18/` |
| `crates/uqa-engine/tests/pg17_semantics.rs` | `crates/uqa-engine/tests/pg18_semantics.rs` |
| `target/benchmark-runs/tpch-pg17.json` | `target/benchmark-runs/tpch-pg18.json` |
| `uqa-tpch-pg17` and `uqa-pg17-age` defaults | PostgreSQL 18-specific defaults |

Fixture loaders, test modules and functions, assertion text, manifest provenance, current README and manual sections, container images, output labels, and `server_version = 18.0-uqa` now use the PostgreSQL 18 baseline. Version-neutral environment variable names such as `UQA_TPCH_PG_CONTAINER` remain stable, while historical `HISTORY.md` entries and dated PostgreSQL 17 performance snapshots remain historical rather than being relabeled.

Regenerate active expected data only after the script confirms a live PostgreSQL 18 server and every UQA result matches it. Commit the exact PostgreSQL 18 patch version in the fixture and manifest.

### 3. PG18 DML row images

`RETURNING` row-image aliases are represented in the SQL AST and plan instead of being flattened into ordinary projections. INSERT exposes a NULL old image and the inserted new image, DELETE exposes the deleted old image and a NULL new image, UPDATE retains both versions, and every ordinary-table MERGE action selects its corresponding images while retaining source columns and `merge_action()` behavior; `DO NOTHING` emits no row, and unqualified MERGE `*` emits source columns before target columns.

Planner- and executor-only attributes are identified by `InternalRelationId` and `InternalColumnRef`; wildcard visibility and retrieval-score provenance are structural as well. This mirrors PostgreSQL 18's `resjunk`, `resno`, `Var`, and tuple-slot model instead of synthesizing SQL names. Existing version 0.1.6 view, generated-expression, and SQL-standard function catalogs with legacy compiler dispatch names are upgraded at restore, while bound user routines with the same names remain user routines.

Default `old` and `new` qualification and `WITH (OLD AS ..., NEW AS ...)` renaming are implemented, including conflicts with table aliases and user columns. MERGE uses full-join candidate generation, fixes each candidate kind once, evaluates clauses in written order, enforces kind-specific source and target visibility, permits one source to change multiple targets, and reports `21000` before a second mutation of one target. Live PostgreSQL 18.4 differential coverage now includes ordinary and partitioned INSERT, UPDATE, DELETE, ON CONFLICT, and every MERGE mutation action with BEFORE-trigger mutation and suppression, immutable original OLD images, non-retroactive AFTER-trigger writes, stored generated values, same-leaf and cross-leaf updates, and source and destination physical `tableoid` identities.

### 4. PG18 constraints and generated columns

Extend catalog definitions so constraints preserve name, type, enforcement, validation, inheritance, temporal flags, referenced columns, and expression. Runtime validation must consult those flags rather than inferring behavior from the presence of a compiled expression.

Named `NOT NULL` is a first-class constraint with `pg_constraint.contype = 'n'` and consistent `pg_attribute.attnotnull`. Named CHECK, foreign-key, and `NOT NULL` constraints now preserve validation, enforcement, deferrability, and inheritance state; enforce new rows under `NOT VALID`; publish CREATE/ALTER state atomically; infer omitted referenced columns from the primary key; validate and execute directional numeric and temporal cross-type foreign keys with PostgreSQL equality semantics; execute supported ALTER forms; defer initially-deferred foreign keys until validation before outer-commit temporary actions with savepoint-aware, operation-sensitive events bound to durable constraint-object identities, target-row rewrites, and exact firing relations; retain queued checks across later deferrability changes; reject event-sensitive ALTER, DROP, and TRUNCATE operations on relations or referenced-parent triggers with pending events while retaining rename continuity; implement per-transaction `SET CONSTRAINTS` with persistent `ALL` state, PostgreSQL name/search-path and allocated temporary-namespace resolution, retroactive immediate validation across nested frames, routine, reentrant callback, PostgreSQL simple-query transaction control, and savepoint rollback behavior, current- and cross-session constraint replacement, and table-rename continuity; remove bounded dependencies; roll back comma-separated ALTER actions together; and survive storage reopen. Remaining work is inheritance and partition propagation, the complete dependency graph, `pg_dump`/`pg_restore`, and the upstream constraint regression matrix.

Virtual and stored generated columns now persist the generation expression and kind, reject direct writes except PostgreSQL-supported `DEFAULT` forms, evaluate virtual columns only when a projection or enforced constraint requires them without physical storage, maintain stored columns exactly once at the prepared-write boundary, reject generated-to-generated references, statically type the implemented expression surface before catalog mutation, bind and persist the exact stored SQL routine overload used for evaluation and dependency checks, rewrite relation and column dependencies, expose `attgenerated`, `pg_attrdef`, and information-schema metadata, and preserve definitions across storage reopen and schema changes. Remaining work is the complete PostgreSQL built-in function and operator overload matrix, privileges, inheritance and partition propagation, dump/restore, and the complete upstream regression matrix.

The six built-in range and multirange families now preserve declared identity, canonicalize text values, execute the implemented constructor, bound, emptiness, overlap, containment, adjacency, and merge surface, and expose PostgreSQL 18 `pg_type`, `pg_range`, and selected `pg_proc` metadata. `WITHOUT OVERLAPS` primary/unique keys and `PERIOD` foreign keys enforce nonempty periods, overlap rejection, aggregate coverage, mutation safety, ALTER validation, temporal catalog flags, and reopen behavior. Remaining work is binary I/O, complete ordering and arithmetic, user-defined range types, GiST/exclusion-index planning, temporal referential actions beyond `NO ACTION`, deferrable enforcement, dump/restore, and the full upstream regression matrix.

Scalar `regprocedure` now resolves an exact input signature to the PostgreSQL-shaped routine OID across qualified names, `search_path`, user functions, procedures, and implemented built-ins, renders the visible signature from that OID with `0` as `-`, reports `42883` for a missing overload, and exposes the exact `regprocedure` and `_regprocedure` type rows plus the four scalar I/O routine rows. Array value input and output and the remaining `reg*` alias family stay bounded by `types.declared-identity-casts-and-catalog` rather than being implied by this slice.

### 5. PG18 functions, casts, collations, and output

The bounded original PostgreSQL 18 function-and-cast inventory is implemented and differentially verified: `array_sort`, `array_reverse`, `reverse(bytea)`, identified integer/`bytea` casts, Unicode 16 full case mapping, `casefold`, `json_strip_nulls` and `jsonb_strip_nulls` array-null options, `uuidv4`, `uuidv7`, `crc32`, `crc32c`, `gamma`, `lgamma`, interval week and negative-quarter extraction, Roman-numeral parsing, array/composite aggregates, and named regular-expression arguments. This closes only `functions.identified-pg18-additions`; `functions.full-pg18-matrix` remains incomplete, while the implemented-carrier routine overload slice is closed by `routines.polymorphic-variadic-pseudotype-overloads`, missing enum carriers and polymorphism remain under `types.declared-identity-casts-and-catalog`, the implemented role/security slice and its remaining server-wide consequences stay under `routines.extension-language-privilege-security`, and the implemented session-portal cursor slice plus its remaining directions and dynamic forms stay under `plpgsql.datum-slots-and-bound-cursors`. The `array_sort(anyarray [, boolean [, boolean]])` and `array_reverse(anyarray)` discovery is independently verified with first-dimension multidimensional behavior, preserved bounds, concrete base-array return types and array-domain flattening, declaration-order-independent named arguments, unknown literal and bare-parameter Boolean context, explicit non-Boolean rejection, concrete user-overload ranking and ambiguity, polymorphic and undefined-function errors, scalar-subquery and generated-column typing, comparator failures for `json` and nested composites, and exact OIDs 6381 and 6388 through 6390. The `reverse(text)` and PostgreSQL 18 `reverse(bytea)` overload pair is independently verified with Unicode and raw-byte results, preferred `text` resolution for unknown literals, NULL, and untyped parameters, character-family implicit casts, scalar-subquery typing, exact undefined-function errors for invalid types, names, and arities, concrete user-overload ranking with explicit and implicit `pg_catalog` search order, stored generated-expression binding, and exact OIDs 3062 and 6382. The `md5(text|bytea)` slice shares that exact overload-resolution boundary while always returning `text`; it hashes raw `bytea` payloads, rejects unrelated types and invalid arities as undefined functions, persists generated-expression bindings, and exposes PostgreSQL OIDs 2311 and 2321 with their leakproof metadata. The subsequent `uuid_extract_version(uuid)` and `uuid_extract_timestamp(uuid)` discovery is implemented as its own verified parity slice with declared UUID overload resolution, PostgreSQL 18 version 1 and 7 timestamp conversion, exact return types and errors, immutable generated-column support, and `pg_proc` metadata. The `to_bin(integer|bigint)` and `to_oct(integer|bigint)` slice likewise preserves the declared 32-bit or 64-bit width through execution, scalar-subquery output binding, and generated-expression validation, emits PostgreSQL's two's-complement text for negative values, and exposes OIDs 6330 through 6333 with exact `pg_proc` metadata. The `random(integer,integer)`, `random(bigint,bigint)`, and `random(numeric,numeric)` slice uses PostgreSQL's shared xoroshiro128** stream and exact inclusive sampling algorithms, preserves overloads and arbitrary-precision numeric scale, keeps consumed draws and reseeding nontransactional across statement, transaction, and savepoint rollback, rejects invalid bounds and generated-column use with PostgreSQL SQLSTATEs, and exposes OIDs 6339 through 6341 with their strict, volatile, parallel-restricted `pg_proc` metadata; future discoveries must remain independently accounted instead of being hidden inside the broad inventory.

The documented fixed-signature subset now uses one registry and resolver for ordinary execution and generated-expression typing and binding. The shared path covers the listed case-folding, reverse, hash, checksum, one-argument string and binary length, gamma, JSON null-stripping, integer-base conversion, catalog lookup, random, and UUID generation and extraction signatures together with visible SQL user-function candidates, qualification, search-path order, exact and implicit matches, preferred and unknown categories, domain base types, named and default slots, declared return types, and durable stored bindings where volatility permits. Consolidated catalog tests verify the PostgreSQL 18 `pg_proc` identities for `nextval(regclass)` (1574), `currval(regclass)` (1575), both `setval` signatures (1576 and 1765), unit `random()` (1598), `to_hex(integer|bigint)` (2089 and 2090), `gen_random_uuid()` (3432), `to_regprocedure(text)` (3479), `to_regtype(text)` (3493), `to_regproc(text)` (3494), `to_regclass(text)` (3495), `to_regnamespace(text)` (4086), `to_regrole(text)` (4093), `casefold(text)` (6412), `uuidv4()` (6428), and `uuidv7()` / `uuidv7(interval)` (6429 and 6430). Polymorphic routine matching builds on this candidate selection while retaining specialized substitutions and variadic call bindings, and neither verified slice claims PostgreSQL's complete built-in, operator, cast, extension, or `pg_proc` matrix.

The six PostgreSQL catalog lookup functions are independently verified for represented relation, routine, namespace, role, and type rows. The implementation follows PostgreSQL's `SplitIdentifierString` behavior for reserved words, punctuation, whitespace, quotes, case folding, and 63-byte clipping, uses the dedicated PostgreSQL type-name parser for aliases, typmods, and arrays, accepts decimal, leading-zero octal, and zero OID forms without catalog existence checks, applies qualification and `search_path` where applicable, distinguishes exact `regprocedure` signatures from ambiguous `regproc` names, resolves roles through the global durable role catalog, follows NULL and missing-object soft failures, preserves the direct `regnamespace` hard-input contract and catalog OID identity and the direct scalar and array `regrole` hard-input contract and OID storage across rollback, reopen, and role removal, rejects non-NULL scalar `regrole` string constants retained by stored DDL with `0A000` after exact hard-input, CHECK, partition-key, and routine declaration and body error precedence while allowing runtime conversions, numeric and NULL constants, arrays, and SQL source-string bodies, rejects cross-database relation, routine, and type names with `0A000`, rejects overqualified names and malformed type syntax with `42601`, exposes exact stable, strict, parallel-safe, non-leakproof lookup rows plus `regrole` type I/O and `reg*` information-schema metadata, and remains excluded from immutable generated expressions. Focused engine tests and the 182-case PostgreSQL 18.4 role-state transcript plus the 275-case transaction-state transcript cover this boundary.

Ordinary scalar SQL function overload resolution is independently verified for direct and scalar-subquery declared integer widths, named and default argument filtering before effective-signature search-path shadowing, typed identity across catalog reopen, exact information-schema domain selection over its base type, and nested casts that restore the domain at the call boundary. Compiler and generated-column evidence also distinguishes unqualified `COALESCE`, `GREATEST`, `LEAST`, and `NULLIF` syntax from quoted or schema-qualified calls and preserves search-path selection for ordinary names such as `upper` and `concat`. Focused evidence is isolated in `sql_routine_identity::scalar_overloads`, compiler syntax-binding tests, and generated fixed-binding tests; procedure, `CALL`, PL/pgSQL datum, table-routine, and `SETOF` evidence is tracked in the adjacent ledger items.

Procedure, `CALL`, and scalar PL/pgSQL overload resolution is independently verified for PostgreSQL's implicit preference of `double precision` over `numeric` for integer input, scalar-function widths from direct casts and scalar subqueries, procedure widths from direct casts, named and default arguments, concrete PL/pgSQL datum declarations and typed NULL variables, and exact information-schema domain selection for `%TYPE` variables. `CALL` rejects subqueries, and its wrong-kind evidence covers functions with OUT and TABLE parameters without exercising their table-returning execution: omission of the required output placeholder is undefined procedure (`42883`), while a structurally matching call that supplies the placeholder is wrong object type (`42809`).

Table-returning and `SETOF` function overload resolution is independently verified for direct and scalar-subquery declared integer widths, unknown-category selection and ambiguity, exact information-schema domain selection, named and default arguments before effective-signature search-path shadowing, and typed table-routine identity across catalog reopen. Exact table-function source bindings reach UPDATE, DELETE, MERGE, and correlated LATERAL execution, and stored views serialize each selected identity across reopen. `DROP FUNCTION` RESTRICT checks those exact stored-view identities before mutation, reports dependent-objects-still-exist (`2BP01`) for the referenced overload, leaves unrelated overloads droppable, and preflights multi-target drops atomically; scalar expressions and table-function groups nested through CTEs, subqueries, joins, set operations, and scalar subqueries participate in the same dependency scan, and `CREATE OR REPLACE VIEW` replaces the prior dependency set. Select-list projection keeps scalar and set-returning overloads distinct and preserves the selected signature through execution, while visible user `SETOF` and ordinary single-argument `unnest` overloads and their built-in namesakes follow search-path order without inheriting built-in row-shape handling. Focused evidence is isolated in `sql_routine_identity::table_routine_*`, `sql_routine_identity::scalar_view_function_dependencies_are_exact_replaceable_and_drop_atomic`, `sql_routine_identity::view_function_dependency_scan_covers_function_groups_and_nested_query_shapes`, `sql_routine_identity::multi_argument_from_unnest_uses_postgresql_syntax_before_user_overloads`, `sql_table_functions::table_function_scalar_subqueries_preserve_declared_argument_types`, `sql_plpgsql::set_returning::set_projection_*`, and `pg18_semantics::md5_overloads::pg18_set_returning_user_overload_uses_the_combined_stable_binding`.

The implemented-carrier polymorphic and variadic slice is verified within a checked-in 200-case stateful oracle captured from PostgreSQL 18.4 with Apache AGE and replayed through separate UQA CLI processes. The matcher resolves simple `anyelement`, `anyarray`, and `anynonarray` substitutions and common-type `anycompatible`, `anycompatiblearray`, and `anycompatiblenonarray` substitutions; models positional variadic packing, explicit array pass-through, defaults, named explicit calls, fixed-versus-expanded ranking, and declared-array identity; and retains concrete call bindings through SQL and PL/pgSQL scalar functions, TABLE, SETOF, RETURN NEXT, CALL, nested overloads, generated columns, stored views, and reopen. Declaration validation and resolution preserve PostgreSQL's `42804`, `42883`, `42725`, and `42P13` boundaries. Built-in range and multirange substitutions are independently verified by the type-and-temporal oracle; enum substitutions remain owned by the type item rather than being hidden in this verified routine slice.

User SQL and PL/pgSQL routine catalog rows are independently verified for input-only `proargtypes` identity, `proallargtypes`, `proargmodes`, `proargnames`, `pronargs`, `pronargdefaults`, `proargdefaults`, variadic element OIDs, return OIDs, set-returning state, and implemented pseudo-type OIDs. Function and procedure kind is not part of the shared schema/name/input-signature identity, pure OUT and TABLE positions are excluded, INOUT remains an input, and VARIADIC retains its declared array identity.

The routine security lifecycle now persists logical roles, supported role attributes, and PostgreSQL 18 membership edges with per-grantor ADMIN, INHERIT, and SET options; `GRANT` and `REVOKE ROLE`, CREATE ROLE membership clauses, legacy ALTER GROUP, cycle and dependency handling, transactional reopen, CREATEROLE attribute-delegation limits, transitive role assumption and privilege inheritance, `pg_auth_members`, all six current-user or explicit-user name/OID `pg_has_role` overloads, and scalar and array `regrole` storage and stored-expression behavior are covered by a checked-in 182-case PostgreSQL 18.4 stateful oracle. Routine owners and EXECUTE ACLs follow inherited role privileges; every replacement and explicit DROP target is ownership-checked before mutation; ownership transfer additionally requires a SET-enabled path to the target; and unauthorized operations remain atomic. Non-owner routine ACL delegation selects a direct or inherited rooted grant-option source, preserves independent grantee/grantor paths in `pg_proc.proacl`, validates `GRANTED BY`, warns without mutation when authority is absent, rejects dependent `RESTRICT`, recursively applies `CASCADE` while retaining alternate rooted paths, and rewrites owner-issued paths on ownership transfer. `SECURITY DEFINER` rejects direct and nested `SET ROLE`, while a successful `SECURITY INVOKER` role change persists and an error restores the prior identity. Routine-local configuration is restored after calls, strict-null execution still checks privileges, `CREATE OR REPLACE` preserves owner and ACL, and dependent owners, ACL grantors or grantees, and membership grantors prevent role removal. Exact, explicit-empty, omitted, `%TYPE`, search-path, kind, privilege, body-preservation, catalog, atomicity, and reopen paths cover volatility, null-input behavior, security, leakproof, parallel, recognized support-function, configuration, and ownership changes. All six current-user or explicit-role name/OID `has_schema_privilege` overloads cover `USAGE`, `CREATE`, `WITH GRANT OPTION`, comma-any checks, membership-aware ownership and ACLs, system and current temporary namespaces, strict NULLs, missing objects, error precedence, and exact `pg_proc` metadata. Schema `CREATE` enforcement covers supported durable table, CTAS, `SELECT INTO`, view, materialized-view, sequence, foreign-table, function, procedure, index, and indexed key-constraint creation with search-path `USAGE`, inherited grants, revocation, transactions, savepoints, inferred temporary views, and PostgreSQL source-analysis, definition-error, and collision precedence. Routine calls and ALTER, DROP, GRANT, and REVOKE target lookup enforce schema `USAGE` with qualified-error precedence, inaccessible search-path filtering, inherited grants, immediate prepared-plan invalidation, invoker and definer identity, and namespace-check-free exact bindings stored in views, generated expressions, and SQL-standard bodies. Dynamic ordinary relation lookup enforces the same namespace boundary for qualified and effective-search-path query, mutation, TRUNCATE, supported ALTER and DROP, stored-view definition, and `regclass` paths, while views, materialized views, SQL-standard query bodies, and declared cursors execute their authorized definition-time identities without re-resolving the schema name. Ordinary tables now persist ownership and table-wide and column ACL state atomically, enforce inherited owner authority and all eight PostgreSQL 18 table-wide privileges, transfer owned sequences and owner grant paths under PostgreSQL target-role rules, expose `relowner`, `relacl`, and `attacl`, block dependent role removal, and preserve transactional, temporary, cross-engine, reopen, hierarchy, query, DML, inquiry, and information-schema behavior. Passwords, per-role settings, remaining object namespace paths, foreign-table ACLs, remaining relation-owner enforcement, default privileges, row-level-security interactions, extension languages, and complete optimizer and security consequences remain under `routines.extension-language-privilege-security`.

Regular and materialized views persist their creating role and ACLs in typed catalog columns rather than deriving authorization state during lookup. Inherited owner authority protects regular-view replacement and ALTER; direct DROP also permits the containing-schema owner; OWNER TO applies target existence, SET, and target-schema CREATE checks with superuser bypass while rewriting owner-rooted ACL paths; and owner, grantor, and grantee dependencies protect DROP ROLE. Relation and column ACLs cover all table-shaped privileges, PUBLIC, independent rooted grant-option paths, dependent RESTRICT and CASCADE, implicit owner rights, ALL TABLES IN SCHEMA, replacement preservation, pg_class.relacl, pg_attribute.attacl, information-schema visibility, all name/OID has_table_privilege and has_column_privilege forms, transactions, savepoints, temporary lifecycle, cross-engine refresh, SQLite and key-value migration, invalid-chain rejection, and reopen. Each regular-view boundary checks the caller-facing privilege and evaluates underlying relations using that view's owner unless security_invoker retains the invoking privilege subject, including nested automatic DML, MERGE, and INSTEAD OF trigger paths while SQL-visible current_user remains the caller. Materialized snapshots enforce their own SELECT independently of sources; owner or delegated MAINTAIN refreshes the stored query under the owner's privileges and current_user, and unpopulated reads and direct DML preserve PostgreSQL privilege-before-state-or-kind error precedence. Direct view-drop authorization precedes dependency reporting, while DROP TABLE ... CASCADE removes the complete dependent-view closure without requiring ownership of each dependent view. The checked-in PostgreSQL 18.4 ownership and privilege oracles fix these semantics; complete security-barrier optimizer behavior remains under the relation security gates.

The fixed `uqa` database now persists its bootstrap owner and PostgreSQL 18 default or explicit `CONNECT`, `CREATE`, and `TEMPORARY` ACLs. Grants and revokes preserve `TEMP`, `PUBLIC`, inherited ownership, independent rooted grant-option paths, `GRANTED BY`, dependent `RESTRICT` and `CASCADE`, role dependencies, transactions, savepoints, cross-engine refresh, durable reopen, and exact `pg_database.datdba` and `datacl` output. `CREATE SCHEMA` and every implemented temporary table, sequence, view, CTAS, `SELECT INTO`, index, and indexed key-constraint form enforce the corresponding database privilege with inherited grants, immediate revocation after temporary-namespace allocation, transaction and savepoint restoration, and PostgreSQL source-analysis and definition-error precedence. All six current-user or explicit-role name/OID `has_database_privilege` overloads cover comma-any and grant-option checks, strict NULLs, missing names and OIDs, error precedence, and exact `pg_proc` metadata against checked-in PostgreSQL 18.4 oracles. Database creation, alteration, removal, owner transfer, and `CONNECT` enforcement at the embedding connection boundary remain under `catalog.pg-database-locale`.

The routine CASCADE graph removes one or several exact explicit targets, generated columns, direct and transitive stored views, and transitive dependent functions or procedures whose SQL-standard query bodies bind the target at creation. RESTRICT ignores dependencies internal to the same explicit multi-target drop but rejects surviving dependents with `2BP01`; SQL string and PL/pgSQL bodies retain PostgreSQL's dynamic dependency behavior; primary cascade notices, failure atomicity, and durable binding rebuild on reopen are covered. `routines.function-procedure-drop-cascade-extended` remains partial until SQL-standard command bodies, parameter defaults, every remaining dependency-object kind, deletion ordering, and complete diagnostics are differentially verified.

The independently verified range-function slice represents explicit `ROWS FROM` as one structural group whose members retain individual function identities, arguments, declared columns, and stored bindings while the outer alias, positional aliases, and `WITH ORDINALITY` apply to the concatenated output. Execution zips member streams to the longest cardinality, NULL-pads exhausted members, preserves declaration-order columns, appends one one-based `bigint` ordinality column, resets ordinality for each correlated invocation, and follows implicit and explicit LATERAL semantics including left-join null extension. Typed member column definitions are required for anonymous `record` and `SETOF record` routines, preserve SQL and PL/pgSQL composite fields, validate source-to-target type compatibility and field counts, apply compatible coercions and type modifiers, and distinguish non-record results from known OUT-shaped records with PostgreSQL's `42601` errors. PostgreSQL's FROM-position transform for unqualified multi-argument `unnest` expands each array argument into a canonical unary `pg_catalog.unnest` member before ordinary user-overload selection; explicit `ROWS FROM(unnest(a, b))` uses the same transform, while unqualified singleton members resolve normally, schema-qualified multi-argument calls remain ordinary functions, `pg_catalog.unnest(a, b)` is undefined (`42883`), and select-list calls do not receive the FROM-only transform. Focused evidence is isolated in the compiler and planner range-function tests, `sql_table_functions::rows_from_*`, `sql_routine_identity::multi_argument_from_unnest_uses_postgresql_syntax_before_user_overloads`, and the PostgreSQL 18.4 with Apache AGE live differential matrix.

The `crc32(bytea)` and `crc32c(bytea)` checksum slice is independently verified with exact raw-byte results, unsigned 32-bit values represented as `bigint`, bytea inference for unknown literals and untyped parameters, strict NULL propagation, scalar-subquery typing, undefined-function and cross-schema ambiguity errors, concrete user-overload ranking with explicit and implicit `pg_catalog` order, durable generated-expression bindings, and exact OIDs 6364 and 6365 including leakproof metadata.

The `gamma(double precision)` and `lgamma(double precision)` slice is independently verified against PostgreSQL 18.4 on the same native C math library, including exact stable results and platform-owned last-bit behavior, implicit numeric conversion to float8, strict NULL, infinity and NaN handling, pole, overflow, and underflow SQLSTATEs, invalid-input and undefined-function errors, scalar-subquery typing, combined built-in and user-overload ranking with implicit and explicit `pg_catalog` order, durable generated-expression bindings, and exact OIDs 6383 and 6384 with their full `pg_proc` and information-schema metadata.

The `json_strip_nulls(json [, boolean])` and `jsonb_strip_nulls(jsonb [, boolean])` slice is independently verified against PostgreSQL 18.4 with recursive object-null removal, the default retention and optional removal of array nulls, strict NULL, declaration-order-independent `target` and `strip_in_arrays` names, unknown and domain typing, scalar subqueries, exact undefined and ambiguous function errors, effective-signature shadowing by user overloads under implicit and explicit `pg_catalog` order, immutable generated-expression bindings across reopen, exact OIDs 3261 and 3262, textual `json` order, duplicate-key, decoded-string, and numeric-lexeme preservation, and canonical `jsonb` output.

For each function, add exact overload resolution, return type metadata, strictness and volatility, NULL propagation, boundary and error behavior, text and binary result encoding, and `pg_proc` visibility. Differential cases must include Unicode, empty arrays, multidimensional arrays, NaN and infinity, malformed JSON, invalid formats, and type ambiguity.

Preserve declared source and result types through the physical plan instead of inferring them from shared runtime carriers. OID, XID, bytea, narrowing integer, character, domain, array, and assignment casts must resolve from the declared source type, emit PostgreSQL 18 SQLSTATEs, and retain exact metadata through scans, joins, CTEs, spills, DML, cursors, and wire row descriptions. `ALTER COLUMN TYPE USING` is an executable expression over the old row, not ignorable syntax, and migration must reject unknown type identities.

Audit PostgreSQL 18 migration changes that overlap UQA Engine, including timezone-abbreviation precedence, CSV `COPY` end markers, inheritance-aware `VACUUM` and `ANALYZE`, full-text collation behavior, and EXPLAIN output. Server-internal AIO and optimizer implementation changes do not need identical internals, but their externally visible plans, errors, and results remain differential targets.

### 6. Frontend/backend protocol 3.2

Add protocol constants and a negotiation API that accepts major version 3 startup packets, selects the highest supported minor version not greater than the request or the embedding server's explicit maximum, reports every unsupported `_pq_.` option, and encodes `NegotiateProtocolVersion` with message tag `v`. A request for an unsupported major version remains a protocol error. The embedding server's implementation maximum must be 3.0 or 3.2; PostgreSQL 18 nevertheless accepts a frontend 3.1 request unchanged and echoes selected version 3.1 when it must report an unsupported `_pq_.` option, while 3.2 enables variable-length keys. PostgreSQL 18 libpq never requests 3.1 and rejects a 3.2-to-3.1 downgrade.

Represent cancellation secrets as validated opaque bytes. Protocol 3.0 requires exactly four bytes. Under PostgreSQL 18 protocol 3.2, `BackendKeyData` emits 4 through 256 bytes and the server's `CancelRequest` parser accepts 1 through 256 bytes. Preserve zero bytes, enforce the distinct message boundaries, and provide an explicit four-byte constructor for existing integrations.

Byte-exact unit tests cover PostgreSQL 18 message formats, downgrade responses, unsupported `_pq_.` options, malformed lengths, SSL/GSS pre-startup requests, and cancellation-key boundaries. Ignored live interoperability tests use PostgreSQL 18.4 `psql` as a thin libpq driver and a server assembled directly from `uqa-pg-wire` codecs; they verify `max_protocol_version=3.0`, `3.2`, and `latest`, an explicit 3.2-to-3.0 server downgrade, authentication sequencing, SSL rejection and startup retry, legacy cancellation, and a 256-byte protocol 3.2 cancellation key.

The extended protocol slice now resolves Bind and FunctionCall format vectors through one zero, one, or per-value implementation, preserves binary parameters and result selection, decodes the context-dependent frontend `p` message under an explicit cleartext, MD5, Kerberos, GSS, SSPI, or SASL state, composes and removes layered middleware cancellation-key prefixes up to 256 bytes, and rejects malformed fragmented live peers through the bounded decoder. Complete the remaining protocol evidence with credentialed Kerberos, SSPI, and OAuth environments, TLS channel binding, replication and pipeline modes, and the complete malformed-peer corpus; credential verification and secret storage remain the embedding server's responsibility rather than being approximated in the codec.

### 7. Complete SQL, catalog, and transaction compatibility

Drive remaining work from the PostgreSQL 18 official regression schedules rather than an ad hoc feature list. Import queries and expected behavior in license-compatible differential harnesses, categorize failures by parser, binder, type system, planner, executor, catalog, transaction, protocol, or administration, and maintain a burn-down manifest with owners and evidence.

Close the existing embedded-runtime gaps: PostgreSQL integer widths and overflow, numeric precision and formatting, collations, domains, enums, user composites and user-defined ranges, unlogged crash recovery, advanced materialized-view behavior, inheritance and partitioning, complete row-lock and isolation regression coverage, the remaining advanced trigger and rewrite-rule matrices, sequences, COPY, complete system catalogs, roles and ACLs, MVCC snapshots and deadlock behavior, prepared statements and portals, large objects, extensions, replication-facing protocol, WAL, and administration surfaces.

The relation-hierarchy slice now covers ordinary `INHERIT` and `NO INHERIT`, partition `ATTACH` and `DETACH`, durable ordered edges, exact row-type and bound validation, typed hash-key expressions, inherited identity and constraint transitions with DETACH provenance, recursive column/CHECK/`NOT NULL` ALTER behavior, default-partition exclusion, explicit-transaction rollback, rename and reopen including legacy dangling-edge repair, hierarchy-aware COPY input and output, cross-partition wait-chain DELETE and MERGE, physical row identity, and the implemented catalog surface. Remaining hierarchy work is the wider upstream regression schedule, privilege and dump/restore behavior, interruptible concurrent-detach pending/finalize and multi-session lock waits, and unsupported index-method interactions.

Use PostgreSQL 18 SQLSTATE, primary error text where clients depend on it, transaction-abort behavior, command tags, row descriptions, OIDs, typmods, binary formats, and catalog visibility as part of the contract. Performance optimizations may differ but must not alter these observations.

Keep row schemas as static plan metadata from the first scan through the last consumer. Materialize only where a final API or blocking physical operator requires owned rows; spilling does not justify materializing identity into ad hoc field names. Spill format version 1 records the declared schema and logical `(alias, column)` identity directly, and no legacy compatibility reader is maintained.

### 8. Client and operational compatibility

The checked-in Docker matrix pins psycopg 3.3.4 with psycopg-pool 3.3.1, pgx 5.10.0, node-postgres 8.23.0 with pg-copy-streams 7.0.0, and PostgreSQL 18.4 psql/libpq. It runs prepared-statement reuse, each driver's supported binary Bind/result choices, COPY in and out, transaction failure through `25P02` and rollback recovery, and single-connection pool reuse first against PostgreSQL 18.4 and then against the same scripted server assembled from `uqa-pg-wire`; expected driver versions and operations are checked in instead of inferred from a successful exit.

Complete the client gate with JDBC, Npgsql, SQLAlchemy, common migration tools, full schema introspection, `pg_dump`, `pg_restore`, and a real UQA SQL server that can exercise connection reset, restart and crash recovery, backup/restore, upgrade, cancellation, notices, administration, and long-running concurrency beyond the network-independent codec boundary.

Add long-running concurrency, restart, crash-recovery, backup/restore, and upgrade tests. A full PostgreSQL compatibility claim requires operationally observable behavior, not only an in-memory SQL test suite.

## Milestones and exit gates

Milestone names, exit gates, evidence ownership, and derived states are defined once in manifest schema version 2 and rendered in the synchronized ledger above. A milestone is `complete` only when every owned item is `verified`, `not_started` only when every owned item is `not_audited`, and otherwise `in_progress`; M6 additionally requires M0 through M5 and every manifest item to be complete.

## Required verification on every compatibility change

Run `cargo fmt --all --check`, focused owning-crate tests, the relevant Docker PostgreSQL 18.4 with Apache AGE oracle matrix, and manifest validation while iterating. Do not dispatch the full cross-platform suite for intermediate commits.

```sh
cargo fmt --all --check
python3 tests/parity/pg18/run_diff.py --validate-manifest
```

After implementation and review changes converge, push the final pull-request head and run `bash scripts/run-premerge-ci.sh` exactly once. The dispatcher selects Rust, JavaScript/WebAssembly, and Python suites from the final diff; any later push invalidates that result and requires one replacement run for the new head.

Live wire verification requires `UQA_PG18_DOCKER_HOST` to name the test server host as reachable from the client container; this is explicit because the correct address differs between Docker Desktop, native Linux Docker, and remote container runtimes. It uses a PostgreSQL 18 client container named `pg-parity` by default, and `UQA_PG18_WIRE_CONTAINER` selects another container.

Run repository policy scripts, binding builds and examples, and supported-platform CI whenever the parser dependency, public AST, catalog serialization, value representation, or wire types change.

## Completion accounting

Maintain the machine-readable PostgreSQL 18 compatibility manifest at `tests/parity/pg18/manifest.json` alongside the differential harness. Schema version 2 records each milestone title and exit gate, assigns every item to exactly one owning milestone, and records each item's PostgreSQL reference, UQA Engine test, supported boundary, status, and open issue. Every compatibility PR must update its manifest evidence, this plan's narrative, the generated ledger above, and the authoritative manual together. `run_diff.py --validate-manifest` rejects invalid ownership, derived-status contradictions, plan or manual drift, and any complete-compatibility claim before M6 is derived complete. Milestone completion requires positive evidence for every owned row; absence of a failing test is not evidence.

The final complete-compatibility audit must inspect the current parser revision, manifest, test results, live server provenance, protocol traces, client matrix, catalog diffs, persistent reopen behavior, and manual. The project must not declare complete PostgreSQL 18 compatibility while any item is missing, explicitly rejected, silently approximated, or supported only by an indirect test.
