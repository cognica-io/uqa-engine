# Nori analyzer implementation plan

Status: Active implementation

Update rule: Reconcile this plan in every Nori implementation PR that changes a task, public contract, dictionary input, verification result, or remaining gate. Completion requires the full [Nori design](../design/nori-analyzer.md), including consumers of morphological token graphs, rather than tokenizer compilation alone.

## Objective and fixed decisions

Implement the Lucene 10.5.1 Nori analyzer in native Rust for UQA Engine and its supported bindings. Preserve exact reference token attributes and dictionary interpretation, integrate positional graphs and source offsets into retrieval and highlighting, and make analyzer revisions durable and transactional. Keep the existing `standard_cjk` n-gram analyzer distinct.

The compatibility reference is Lucene commit `64ce863a2bea79c69c19c4d56268c26710ff0ff9` and the dictionary/JVM hashes in the [reference manifest](../../tests/parity/nori/manifest.json). Every JVM operation runs in Docker, including Java compilation, fixture generation, and dictionary export. Production execution is Rust. A changed reference requires a reviewed input/output diff and a new semantic fingerprint.

Work starts from UQA commit `e913303cd9ffd6662706053cc39caaaeb8f38b23` on `feature/nori-analyzer-design`, tracked by [PR #106](https://github.com/cognica-io/uqa-engine/pull/106). The existing 31 Docker reference cases establish examples, not complete UQA parity. Keep source changes and their verification in logical commits and update the PR description around the implemented scope.

## Work and dependencies

| Work item | Owner | Dependencies | Required result | Status |
| --- | --- | --- | --- | --- |
| Reference examples | `tests/parity/nori` | Pinned Docker and jars | Reproducible 31-case baseline with full token/end attributes and input hashes | Verified baseline |
| Source coordinates and character-edit maps | `uqa-analysis` | Existing character-filter behavior | Checked UTF-8/UTF-16 spans, original end state, and composable replacement/deletion/insertion maps | Verified; 144 analysis tests including 10 new integration cases and the source-map doctest |
| Rich token stream contracts | `uqa-analysis` | Source coordinates | Explicit token increments, lengths, end state, and metadata-preserving stage interfaces | Verified generic contract, lossless term representation, and native Korean morphology bridge |
| Generic stage migration | `uqa-analysis` | Rich contracts | All existing tokenizers and filters preserve metadata; `analyze` remains the ordered term projection; explicit metadata policy for every expansion/removal | Verified semantic migration and common/Korean filter allocation ownership; public native, compiled, and uncompiled analyzer methods propagate a runtime allowance and callback through every configured stage, including regex range/capture searches and reserved NFA workspace; current analysis suites pass with and without Nori, including strict package Clippy |
| Complete model export | Docker reference tools | Pinned resources | Every lexicon entry, entry order, context ID, cost, reading, morpheme, unknown class, matrix cell, and Unicode value exported and compared | Verified neutral export on Docker arm64 and amd64; repeated hashes and read-only exhaustive verification; CSV regeneration reproduces all nine jar resources byte for byte on both Docker platforms |
| Portable dictionary bundle | `uqa-analysis`, `uqa-nori-data` | Complete model export | Deterministic versioned packer, strict loader, content identity, shared immutable resources, corruption tests, and package provenance | Verified codec, full-model reconstruction, shared resource identity, and native/WASM decode/cache limits; corrected binding archives and refreshed Rust/source archives pass the [recorded package checks](../../benchmarks/nori/DELIVERY.md), with final preflight and remote-head acceptance still open |
| Korean morphology runtime | `uqa-analysis::nori` | Rich contracts and bundle | User dictionary compilation, UTF-16 lattice, exact candidate/tie/backtrace behavior, all compound modes and unknown/punctuation options | Standalone native runtime verified against 38 user and 238 tokenizer cases; lossless common-token bridge and feature-enabled immutable `nori` registry entry verified; Engine catalog registration rejects every built-in name |
| Korean filters and normalization | `uqa-analysis` | Morphology runtime | POS stops, reading forms, simple Unicode lowercase, separate normalization entry point, optional exact-decimal Korean numbers | Default native analyzer, POS/readings/lowercase filters, and normalization verified against 423 cases; optional exact numbers and composition verified against 823 cases; native-to-common bridge and shared Korean filter execution verified; generic Nori pipeline composition and retained numeric/filter/normalization allocation ownership verified |
| Compilation and resources | `uqa-analysis`, Engine composition | Generic stages and bundle | Immutable compiled handles, explicit resource resolver, canonical descriptors and bounded shared caches | Native resource resolution and bounded caches plus existing generic pipeline snapshots verified; canonical generic descriptors, runtime profiles, bounded compilation cache, and immutable restoration verified; Nori pipeline composition verified; shared provider revision retention, atomic Engine/provider rebuilds, and durable descriptor restoration verified |
| Positional occurrence storage | Core/storage owners and SQLite provider | Rich contracts | Multiplicity, graph edges, offsets, explicit length policy, versioned codecs, atomic rebuilds, and provider conformance | Common occurrence values, lossless keys, immutable staging, codecs, and independent score frequencies/lengths verified; Memory, Key/Value, and native SQLite graph storage, exact-key queries, revision/source-end metadata, atomic rebuilds, scoring, initial-open source migration, and durable Nori SQL lifecycles verified |
| Lossless query execution | Storage, scoring, operators, planner, fusion, and execution | Occurrence storage and compilation | Exact term support, all ranking paths, persisted bounds, calibration, all-field statistics, and attention features | Verified in native owners and Memory/SQLite/redb SQL lifecycles; Engine retains resource and transaction adapters |
| Graph phrases and source highlighting | SQL, execution, operators, analysis | Occurrence storage and compilation | Whole-phrase analysis, graph path matching with holes, safe corrected source highlighting, and optimizer preservation | Typed phrase lowering, memoized native matching, emitted-unit calibration, whole-source highlighting, and explicit-analyzer SQL execution implemented under retained revisions; native highlighter and SQL runtime accounting implemented; phrase query analysis, key projection, native matching, candidate scoring and result ownership share one allowance; provider cursor/cluster accounting and SQL cancellation recovery are covered across Memory, SQLite, and redb, while the owner graph matcher has deterministic differential coverage |
| Catalog revisions | Native lifecycle owners and Engine state | Compilation and occurrence storage | Durable independent index/search descriptors, owner validation, name-collision preflight, atomic rebind, rollback/reopen/epoch tests | Exact independent descriptors, owner validation, source migration, rename, rollback/savepoint, session/reopen restoration, and built-in-name collision preflight verified |
| Public surfaces and bindings | SQL, facade, CLI, Python, Node.js, WASM | All runtime integration items | `nori` configuration, diagnostics, explicit-analyzer highlighting, real binding execution, feature-disabled failures, and bundled resources | SQL diagnostics, explicit-analyzer highlighting, runtime controls, and feature-enabled binding delivery are verified; a shared 47-step persistent Nori contract now exercises Rust/SQLite/redb, Python, Node.js, and WASM, with nine feature-disabled steps through actual Rust/Python/Node.js/WASM artifacts; six fresh Chrome sessions verify IndexedDB restoration and host memory; corrected full package matrices and independent archive checks pass, with final preflight and remote-head acceptance still open |
| Physical provider indexing measurements | `uqa-storage-sqlite`, `uqa-storage-redb` | Occurrence storage and pinned Nori revision | Native/WASM commit and rollback measurements, closed-file reopen checks, full graph parity, and measured resource limits | Verified SQLite/redb on macOS aarch64 and Linux x86_64 plus WASM SQLite through four workloads, twelve complete reports, and an allocation-only reference; target-specific allocation ceilings preserve each earlier limit, and all nine live/reopened results per workload match Memory and its reviewed hashes; SQL/session overhead and physical-provider phrase cost remain separate measurements; browser host memory is recorded independently |
| Native graph phrase measurements | `uqa-operators`, existing analysis/storage/scoring APIs | Retained analyzer revisions and occurrence storage | Complete query graphs, exact document support, scored result agreement, shared query allowance, native/WASM allocation and timing evidence | 36 workloads over six corpora and three modes measured in both analysis scopes; four repeated native/WASM reports agree on every output and allocation counter, with owner-side validation, reviewed limits, and native/WASM CI gates |
| Public SQL and session measurements | `uqa` facade benchmark over existing owners | Analysis, retained provider revisions, SQL execution and session APIs | Complete transaction/reopen results, provider/session score agreement, native/WASM timings and reviewed allocation ceilings | Two clean original macOS repeats validate all 102 workloads; fixing ordinary SQL-created catalog inputs then makes all six allocation counters equal across two complete four-transaction experiments in both SQLite and redb, with unchanged results and reopen verification. The pinned SQLite input also passes actual WASM package checks. Full native/WASM SQL/session calibration over these inputs remains required |
| Cooperative cancellation measurements | `uqa-analysis` existing benchmark | Fixed corpora, public controlled tokenizer/analyzer APIs, memory allowances | Native/WASM return latency, exact cancellation, preserved unrelated reservations, complete recovery, and reviewed regression limits | The existing benchmark now offers 108 beginning/middle/final-poll measurements across all six corpora, three modes, and tokenizer/analyzer scopes. Tooling validates every cancellation, reservation, recovered graph, and timing sample; actual native/WASM runs and calibrated limits remain required |
| Complete parity and release gates | Owning tests, CI, package tools | All preceding items | Exhaustive export checks, expanded differential fixtures, fuzz/regression coverage, measured benchmarks, licenses and publishable artifacts | Reference regeneration and feature-enabled/disabled suites verified; the corrected full Python and JavaScript matrices and all binding archives pass; SQL/session calibration, final artifact identity refresh, and final remote-head CI remain pending |

Implement the rich contracts and existing-stage migration first so Korean token emission has one real destination. Model export can advance independently of storage work. Introduce Nori registration only after the native tokenizer and its required filters consume the validated model. Do not make unsupported phases or providers return flattened terms under a Nori capability label.

## Contract checklist

- [x] Preserve existing term-only analyzer outputs, JSON tags and serialized aliases, fallible configuration behavior, and uncompiled synonym-file reload semantics.
- [x] Carry UTF-8 and UTF-16 source coordinates, original-source edit maps, lossless term units, graph increments and lengths, optional Korean morphology, keyword state, and final skipped positions.
- [x] Reproduce every exported dictionary value and the reference's stable homograph ordering, user entry rules, Java classification, space penalties, integer/tie decisions, and bounded backtrace behavior in the standalone native tokenizer.
- [x] Support `none`, `discard`, and `mixed`, optional unknown unigrams and punctuation retention, POS filtering, Hanja readings, simple lowercase, normalization, and Korean number composition in the standalone native runtime; generic registry, Engine SQL integration, and feature-enabled binding delivery are verified.
- [x] Preserve same-position multiplicity and complete graph edges in every provider; declare normalization length policy and rebuild incompatible positional formats from source.
- [x] Match actual quoted phrases and highlight original spans, including compound alternatives, removed-position holes, inflections, user nouns, Hanja, and character-filter replacements. Owner highlighter tests, retained-revision SQL phrase/highlight tests across providers, and the shared persistent binding fixture cover these cases; the verified native and binding runs are recorded below, and final remote-head CI remains a delivery gate.
- [x] Persist exact analyzer/resource revisions and independent index/search bindings, and prove atomic write/rebuild/rollback/reopen behavior across sessions. Generic durable bindings and Nori graph lifecycles are verified in Memory, Key/Value/redb, and native SQLite.
- [x] Execute equivalent scenarios through Rust, Python, Node.js, and browser WASM; verify missing-feature and missing-resource errors. The shared 47-step persistent SQL contract and nine feature-disabled steps cover user rules, complete diagnostics, graph queries, unavailable resources, failure atomicity, retained revisions, and reopen. Actual Python, Node.js, and WASM builds pass both configurations; fresh Chrome sessions verify IndexedDB restoration, complete fixed-corpus diagnostic identity, and host memory observations.
- [ ] Ship dictionaries and notices without auxiliary build-time or runtime dictionary downloads; validate all package archives and resource identities.
- [ ] Publish benchmark baselines and enforce recorded regression gates without claiming unmeasured throughput or memory results.

## Verification strategy

Each implementation commit records the commands actually run and what they prove. Pure analysis changes run the complete `uqa-analysis` test target and library tests, strict Clippy for the affected package, and formatting. Storage or retrieval changes add the owning provider/operator/SQL tests and actual Engine integration scenarios. Public manual SQL changes run the existing manual compilation/execution harness. A green reference run alone never proves that Rust matches Lucene.

```sh
python3 tests/parity/nori/run_reference.py
cargo test -p uqa-analysis --locked
cargo clippy -p uqa-analysis --all-targets --locked -- -D warnings
bash scripts/check-rustfmt.sh
python3 scripts/check-integration-test-harnesses.py
python3 scripts/check-workspace-dependencies.py
```

Add test modules under each crate's existing single integration target and preserve the original test inventory. A new dictionary data crate may have one integration target. Use minimized cross-boundary regressions for metadata corruption and failure atomicity rather than tests that merely repeat implementation details.

The expanded Docker oracle compares complete tokenizer, filter, analyzer, and stream-end output, not just term sets. Validate the entire exported model separately. Differential inputs cover long/ambiguous paths, all Unicode boundaries used by the model, user-dictionary corner cases, stacked tokens, and exact decimal behavior. Native and WASM runs must agree with the same fixtures. A changed expected result is permitted only when its pinned reference changes intentionally.

Before feature completion, run the change-aware pre-merge suites against the final remote PR head, actual binding examples, package-license checks, and package build/dry-run checks. Record the exact commands and artifacts. Benchmark dictionary decode and sharing, memory, native/WASM analysis, indexing, scoring, graph phrases, and cancellation with fixed corpora and host metadata.

## Progress ledger

Never mark a work item verified based on intent, parser acceptance, a narrow test from another owner, or a successful compilation alone. Keep incomplete requirements visible here until their authoritative evidence is recorded.

| Checkpoint | Evidence | Remaining work |
| --- | --- | --- |
| Design and executable examples | Commits `78a82b3d` and `e913303c`; Docker reference contains 31 cases; PR Checks run `34667416290` passed for that design head | No UQA Nori implementation at that checkpoint |
| Original-source mapping | [`source.rs`](../../crates/uqa-analysis/src/source.rs), character-filter integration, and [`source_offsets.rs`](../../crates/uqa-analysis/tests/analysis/source_offsets.rs); `cargo test -p uqa-analysis --locked --quiet` passed 40 unit, 102 integration, and 2 doc tests; strict package Clippy passed | Rich token and analyzer pipeline propagation, morphology, and every downstream item remain active or pending |
| Generic token graph pipeline | [`token.rs`](../../crates/uqa-analysis/src/token.rs), migrated analyzer/tokenizer/filter execution, and [`rich_tokens.rs`](../../crates/uqa-analysis/tests/analysis/rich_tokens.rs); 44 unit, 113 integration, and 3 doc tests passed; complete `uqa-storage`, `uqa-storage-sqlite`, and `uqa-operators` tests passed; strict package Clippy and repository format/header/line/harness/dependency checks passed; the 31-case Docker arm64 reference was reproduced with cached offline inputs | Korean morphology and model export, graph-aware consumers, durable revisions, bindings, and the remaining release gates |
| Engine consumers of migrated analysis | Existing `uqa-engine` integration filters `analyzer`, `highlight`, and `fts` passed 22, 9, and 37 tests respectively; `queries::manual_sql_examples::manual_sql_examples_compile_or_execute` passed | Existing term-based behavior is verified; positional storage and morphological search remain pending |
| Complete neutral model export | [`NoriModel.java`](../../tests/parity/nori/NoriModel.java), [format and commands](../../tests/parity/nori/MODEL.md), and [reviewed manifest](../../tests/parity/nori/model_manifest.json); 774,582 surfaces, 816,283 system entries, 14 unknown entries, 10,292,646 matrix cells, 65,536 character definitions, and 1,114,112 Unicode values exported and compared in Docker; independent arm64 and amd64 exports reproduced all five hashes; read-only verification passed and direct JVM verification rejected corruption in each of the five files; all 133 repository tooling tests passed, including six new model-export regressions | Rust packer/loader and analysis parity, original CSV regeneration, distributable resources, and remaining consumers |
| Portable runtime codec | [Format and commands](../design/nori-bundle-format.md); 9,829,534-byte bundle with artifact SHA-256 `0d920523991ed60909972d85df630c65747ff5d49e1138e4998ca39f0079538d`; arm64 and amd64 neutral inputs packed identically, both passing all five reconstructed hashes; standalone verification passed; `cargo test -p uqa-analysis --features nori-tools --locked --offline --quiet` passed 63 unit, 113 integration, and 3 doc tests; loader-only feature passed 60/113/3 and feature-disabled passed 44/113/3; strict Clippy passed for tools and loader-only builds | Bundled data distribution, shared resource interning, native morphology, actual WASM execution, CSV regeneration, and all downstream work remain pending |
| Bundled dictionary distribution | [`uqa-nori-data`](../../crates/uqa-nori-data/README.md) includes the exact bundle, model/resource manifests, and upstream Lucene, MeCab, and Unicode notices; the analysis integration harness reconstructs all five neutral hashes from embedded bytes; data-crate identity test passed; all 23 Cargo archives passed offline packaging and license checks; the 9,682,047-byte data archive passed native Cargo verification and an extracted-library WASM build; 135 tooling tests, strict package Clippy, and repository policy checks passed | Actual WASM decoding/analysis, binding-specific delivery, default resource composition and interning, CSV regeneration, and all morphology/retrieval/lifecycle work remain pending |
| Optional-feature CI coverage | Linux/macOS Rust test jobs explicitly execute `uqa-analysis/nori-tools` and the embedded model; both Clippy jobs explicitly lint the optional codec/tool paths alongside the default workspace; the equivalent focused commands passed locally | Remote full Rust suites remain a pre-merge gate; ordinary PR Checks are policy/format checks |
| Standalone user rules and Korean tokenizer | Native immutable user compilation and rolling UTF-16 Viterbi match 38 user-dictionary and 238 tokenizer snapshots; both expanded oracles reproduced on Docker arm64 and amd64, including a repeat through the shared provenance-checking runner; every token/end attribute contributes to the long-stream hashes; forced future-path ties/rebasing and EOS-cost selection have isolated regressions; limits and cancellation return no partial successful output and permit tokenizer reuse; tools-feature tests passed 65 unit and 119 integration tests, plus 4 executed doc tests; runtime-only passed 62/118/4, and feature-disabled passed 44/113/3; strict tools-feature and runtime-only Clippy passed; 140 tooling tests and repository format/header/line/harness/dependency/hygiene checks passed; all 23 publishable Cargo archives passed offline packaging and license checks, including complete notices and modification attribution with the ported analysis code; the manual SQL compile/execute harness passed | Raw surrogate units and intra-pair offsets must survive the generic token bridge, persisted term identity, and safe source highlighting; Korean filters and normalization, compiled-resource interning, occurrence storage, lifecycle, binding execution, benchmarks, CSV regeneration, and full pre-merge gates remain open |
| Default Korean analysis and normalization | The standalone immutable `KoreanAnalyzer` and ordered `KoreanFilter` stages match 423 Docker snapshots on arm64 and amd64; all eight original analyzer examples also match; the complete scalar traversal plus every unpaired surrogate gives the same 2,164,736-unit simple-lowercase hash; native regressions preserve present-empty readings, left-POS selection, stacked edges and trailing holes, and reject overflow, bounds, cancellation, and unknown configuration properties; complete tools-feature tests passed 65 unit, 123 integration, and 4 doc tests, runtime-only passed 62/122/4, and feature-disabled passed 44/113/3; strict Clippy passed for tools and runtime-only features; 140 tooling tests, repository policy checks, all 23 offline package/archive-license checks, and the manual SQL compile/execute harness passed | Optional exact-decimal Korean numbers must preserve lookahead and stream-end attribute state as well as keyword/stack behavior; generic rich-token integration, graph storage and retrieval, durable revisions, bindings, and remaining release gates are still open |
| Exact Korean number composition | The optional `KoreanFilter::Number` and direct numeric-prefix helpers match 823 Docker cases on arm64 and amd64, including arbitrary-precision decimal arithmetic, lookahead metadata, keyword/stack rules, aborted prefixes, and nested filter exhaustion; the four original number examples also match. Native cancellation and limit checks distinguish malformed-input fallback from resource failure and guard repeated-addition work. Full tools-feature tests passed 65 unit, 127 integration, and 5 executed doc tests; runtime-only passed 62/126/5 and feature-disabled passed 44/113/3. Strict Clippy passed for tools and runtime-only configurations. The manual SQL compile/execute harness, 140 tooling tests, repository format/header/harness/dependency/hygiene checks, and all 23 offline package/archive-license checks passed | Generic lossless token/source integration, compiled resources, graph storage and retrieval, durable revisions, public registration, actual bindings, and remaining release gates are still open |
| Exact UTF-16 source projection | Character-edit segments retain both coordinate systems and compose UTF-16 ranges before calculating a final scalar-covering UTF-8 span. Four regressions cover every unit range, replacements/deletions/insertions, agreement with strict projection at all scalar boundaries, and the actual surrogate-splitting Nori user rule after HTML removal. Full tools-feature tests passed 65 unit, 131 integration, and 6 executed doc tests; feature-disabled passed 44/116/4; strict tools-feature Clippy and the manual SQL compile/execute harness passed | Raw term/morphology integration into the generic token representation and persisted term keys remain open; safe source projection alone does not complete graph retrieval or highlighting |
| Lossless common-token integration | `TokenTerm` preserves canonical strings and explicit unpaired UTF-16 units with checked string projection. Nori output converts into `AnalyzedText` with exact source spans, morphology, graph/keyword state, and opaque exhaustion attributes; generic filters preserve those values. The bridge passes all 803 successful text cases across the pinned tokenizer/analyzer/number corpora. Every one of the 65,536 UTF-16 units round-trips through JSON without identity collisions, and supplementary-pair construction shares string identity. Full tools-feature tests passed 67 unit, 136 integration, and 7 executed doc tests; feature-disabled passed 45/118/5; strict tools-feature Clippy and the manual SQL compile/execute harness passed. Complete storage/SQLite-provider/operator suites passed 618 tests, 140 tooling tests passed, repository policy checks passed, and all 23 Cargo archives packaged offline with verified licenses | Generic Korean filter execution and immutable compilation/resources, persisted term keys and graph occurrences, retrieval/highlighting, durable revisions, public registration, actual bindings, and remaining release gates stay open |
| Shared Korean filter execution | Native and common tokens now use the same POS/reading/simple-lowercase/number kernel without fabricating missing morphology. The common kernel matches 1,023 complete stream results, including another 138 generic-stream cases reproduced on Docker arm64 and amd64; all 823 prior number outputs remain unchanged. Shared source provenance survives input lifetimes, composes raw filtered spans before correction, and restores precise gram spans when rewrites match the original source. Six public regressions cover source boundaries, optional metadata, generic stops, limits, cancellation, and reuse. Full tools-feature tests passed 68 unit, 142 integration, and 8 executed doc tests; feature-disabled passed 45/118/5; strict Clippy passed for both configurations. The manual SQL compile/execute harness, all 618 storage/SQLite-provider/operator tests, 140 tooling tests, repository policy checks, and all 23 offline package/archive-license checks passed | Immutable compilation and resource interning, persisted term keys and graph occurrences, retrieval/highlighting, durable revisions, public registration, actual bindings, and remaining release gates stay open |
| Immutable dictionary and user resources | `NoriResources` provides explicit alias/artifact resolution, verifies requested and declared hashes, shares immutable dictionary and exact user-rule snapshots, and bounds LRU retention by entry counts and encoded/source bytes. Concurrent and reentrant resolution, corruption/missing-resource failures, byte/count eviction, uncached oversized entries, exact empty/comment source identity, failed-rule recovery, and installed tokenizer stability have regressions. Default resolution uses the static bundled bytes with no copy or I/O; normal Cargo dependency trees include `uqa-nori-data` only with `nori`. Full tools-feature tests passed 77 unit, 144 integration, and 9 executed doc tests; runtime-only passed 74/143/9 and feature-disabled passed 45/118/5. Strict Clippy passed for all three configurations. The manual SQL compile/execute harness, 140 tooling tests, repository policy checks, and all 23 offline package/archive-license checks passed; packaged resource modules and the optional data dependency were inspected | Canonical analyzer descriptors, generic pipeline compilation and its bounded cache, complete persisted resources/bindings, graph storage and retrieval, actual bindings, measured heap/performance defaults, and remaining release gates stay open |
| Prepared generic pipeline execution | `Analyzer::compile()` owns prepared regexes, ordered character mappings, stop sets, and resolved synonym maps in an `Arc<CompiledAnalyzer>`. Compiled and uncompiled APIs share the same token/source kernels; uncompiled stages retain file reloads and execution order. Five integration regressions verify configuration ownership, file edits/deletion/replacement, validation order/recovery, complete stage parameters and empty/source/graph results, and concurrent handle reuse. Full tools-feature tests passed 77 unit, 149 integration, and 10 executed doc tests; feature-disabled passed 45/123/6; strict Clippy passed for both configurations. All 618 storage/SQLite-provider/operator tests, the manual SQL compile/execute harness, 140 tooling tests, repository policy checks, and all 23 offline package/archive-license checks passed | Canonical resolved descriptors and fingerprints, explicit Nori resource composition and generic configuration, bounded compiled-analyzer interning, positional storage and graph retrieval, durable revisions, actual bindings, and remaining release gates stay open |
| Canonical generic descriptors and compilation ownership | `AnalyzerDescriptor` freezes explicit defaults, stop sets, inline synonym maps, runtime Unicode/regex profiles, compatibility revisions, and declared length policy under a canonical fingerprint. `AnalyzerResources` validates restoration, interns compiled revisions under count/descriptor-byte LRU bounds, and preserves live handles after eviction. Synonym parsing bounds expanded JSON before map growth while preserving ordered legacy semantics. Regressions verify a fixed portable identity, file-independent restoration, strict schema/revision/profile checks, duplicate properties, size limits, failed publication, concurrent sharing, and eviction; every existing prepared-stage parameter case also restores and compares complete token/source output. Full tools-feature tests passed 81 unit, 161 integration, and 11 executed doc tests; runtime-only passed 78/160/11 and feature-disabled passed 49/135/7. Strict Clippy passed for all three configurations. All 618 storage/SQLite-provider/operator tests, the manual SQL compile/execute harness, 140 tooling tests, repository policy checks, and all 23 offline package/archive-license checks passed | Nori resource composition/configuration, persisted lossless term keys and occurrence graphs, phrase/highlight consumers, durable catalog revisions, actual bindings, and remaining release gates stay open |
| Korean stages in compiled common pipelines | Strict Korean tokenizer/filter JSON composes with existing stages and resolves exact artifact/profile identities, optional user-rule snapshots, and default overlap-discounted length. Compiled normalization retains the tokenizer profile independently of analysis stages. Compiled and restored results match all 803 successful text reference streams and 14 normalization cases; another 90 cases preserve existing generic tokenizer contracts with Korean filters. Regressions cover source/surrogate graphs, missing resources, exact source identity, reentrant callbacks, disabled dictionary retention, concurrent sharing, and eviction. Full tools-feature tests passed 81 unit, 172 integration, and 12 executed doc tests; runtime-only passed 78/171/12 and feature-disabled passed 49/136/7. Strict Clippy passed for all three configurations and affected storage providers. All 619 storage/SQLite-provider/operator tests passed with Nori enabled, including rejection of Korean assignments and writes before mutation in the current linear stores. The manual SQL compile/execute harness, 140 tooling tests, repository policies, and all 23 offline package/archive-license checks passed; packaged composition modules and optional data dependency were inspected | Lossless persisted term keys and occurrence graphs, phrase/highlight consumers, durable descriptor bindings, public registration and actual bindings, pipeline-wide cancellation/accounting, measured defaults, and remaining release gates stay open |
| Common occurrence codecs and scoring metadata | `TokenOccurrence`, canonical binary `TokenTermKey`, and immutable field staging preserve duplicate edges, graph lengths, unpaired terms, exact source spans, stream-end state, and declared length policy. Version 2 clustered/reverse-vocabulary codecs retain independent frequency and normalization length; legacy position data explicitly requires a source rebuild. Ten storage regressions cover all UTF-16 units, compiled Korean/generic streams, corruption, fixed bytes, and mixed-version score cursors across blocks. Default frequency accessors, all four WAND paths, calibration, exhaustive Engine scoring, and persisted SQLite bounds use actual score metadata; graph fixtures match direct BM25 calculations. Core passed 154 tests; default storage/scoring passed 401; Nori-enabled storage/scoring/SQLite/operator suites passed 851. Engine integration passed 2,604 with 2 existing ignored cases, including the manual SQL harness, and the new Engine score/bound regression passed separately. Affected strict Clippy configurations, 140 tooling tests, repository policies, and all 23 offline Cargo package/license checks passed; all ten new source modules match their packaged bytes | Provider graph writes and lossless key integration, atomic source migrations and descriptor bindings, graph phrases/highlighting, public registration and real bindings, cancellation/accounting, measured defaults, and remaining release gates stay open |
| Compiled provider bindings and atomic revision rebuilds | Memory, Key/Value, and SQLite now use shared immutable index/search handles, freeze a deferred default on first successful resolution, and validate explicit assignments before publication. Index-only changes retain the previous search revision. Query planning, matching, scoring, calibration, and detached portals use exact retained handles. Engine field rebinding stages replacement sources and publishes postings with the candidate binding; failed rebuilds retain old handles without rereading their files. Nine provider regressions cover file edits/removal, independent sides, invalid assignments, deferred retry, cache-disabled concurrency, real SQLite/KV storage failures, and atomic Memory batches. Linear providers reject Korean and overlap-discounted revisions through every new installation path. Engine SQL verifies UPDATE/search after file removal and actual posting-failure rollback of a persistent COPY before and after reopen. Nori-enabled storage/scoring/SQLite/operator suites passed 860 tests; Engine passed 203 unit and 2,604 integration tests with 2 existing ignored release probes, including the manual SQL compilation/execution harness. Default and Nori-enabled strict Clippy, repository policies, and all 23 offline Cargo package/license checks passed; all 21 changed source files match their staged archive bytes | Exact catalog descriptor/independent-phase persistence and epoch restoration, actual graph provider storage and source migration, phrase/highlight consumers, public Nori registration and real bindings, cancellation/accounting, measured defaults, and release gates remain open; Memory batch insertion currently retains a provisional copy whose throughput and heap cost require the planned benchmarks |
| Memory occurrence storage and revision ownership | Memory indexes now write canonical term keys, complete occurrence lists, cached unique-position projections, and original stream-end metadata with the exact analyzer fingerprint and length policy. Empty/all-stopped fields retain their metadata. Populated index revisions require an atomic source rebuild; search-only changes, failed point/batch/rebuild operations, and snapshots preserve the old graph. Engine GIN creation uses atomic revision rebuilds and removal clears the indexed field before releasing its binding. Six provider regressions cover duplicates, holes, both length policies, original UTF-8/UTF-16 spans, raw surrogate keys, compound edges, replacement/removal, repeated batch IDs, maximum document IDs, and failure atomicity. Core statistics preserve all 65,536 UTF-16 units without scalar aliases; real Memory postings match exact BM25 expectations in all four WAND paths. Nori-enabled core passed 86 unit and 68 integration tests; storage passed 162 unit, 23 integration, and 1 doc test; scoring passed 90/132; operators passed 33/56; SQLite passed 225 unit and all 146 integration cases (145 in its full suite plus the new exact-key rejection regression separately). Engine passed 203 unit and 2,604 integration tests with 2 existing ignored probes, including the manual SQL compilation/execution harness; 22 analyzer cases passed again after the final ownership adjustment. Six default-feature Memory cases, affected strict Nori Clippy, repository policies, and all 23 offline package/license checks passed. All nine changed source files match the staged tree and packaged bytes | Key/Value and SQLite graph writes and atomic retained-source migrations, durable descriptor/independent-phase ownership and epochs, complete graph phrases and raw query terms, source highlighting, SQL registration and actual bindings, cancellation/accounting, measured memory/throughput defaults, and final pre-merge gates remain open; Memory bulk insertion still uses a full provisional copy requiring benchmark measurement |
| Durable analyzer descriptors and lifecycle | Catalogs retain canonical named revisions and strict independent field-binding envelopes, with atomic pair installation, GIN ownership checks, and legacy source migrations. Registered files can disappear and named definitions can change without changing installed revisions. Column renames install the retained pair before rewriting sources. Removing the final explicit GIN owner restores the table default if a plain GIN remains. Eight Engine regressions cover both persistent providers, Memory ownership/rename behavior, asymmetric reopen, typed defaults and explicit registration with unused missing defaults, savepoints, sibling sessions, and actual migration/assignment write failures. Six storage/provider regressions verify strict envelopes, atomic pairs, and descriptor/label coherence. Nori-enabled Engine passed 203 unit and 2,612 integration tests with 2 existing ignored probes, including the manual SQL harness; execution passed 351; storage passed 162 unit, 25 integration, and 1 doc test; SQLite passed 225 unit and 150 integration tests after its final catalog adjustment. The eight final Engine cases passed with and without Nori; default storage envelope tests passed. Strict Clippy passed for both feature configurations, repository policies passed, and all 23 offline Cargo archives passed license checks; all 32 changed Rust source files match the Git index and packaged bytes | Persistent graph writes and format migrations, graph phrase/highlight execution, reserved-name preflight and public Nori diagnostics, actual binding artifacts, cancellation/accounting, measured memory/throughput defaults, and full pre-merge gates remain open |
| Key/Value occurrence persistence and original-source migration | Fixed revision/end metadata codecs and a separate binary graph namespace publish complete occurrences, reverse terms, lengths, field counts, and original offsets in one batch. Engine initial open rebuilds both legacy positional formats under exact restored bindings; descriptor failure rolls back the preceding per-document conversion. Nori-enabled Engine passed 203 unit and 2,615 integration tests with 2 existing ignored probes; execution passed 351; storage passed 165 unit, 31 integration, and 1 executed doc test; SQLite passed 225 unit and 150 integration tests after updating its remaining linear-provider guards. Final storage and all 15 redb Engine scenarios passed with and without Nori, and all 8 durable analyzer scenarios passed without Nori. Strict all-target Clippy passed for both configurations, and repository format/header/line/harness/dependency/capability/benchmark/hygiene/source-license policies passed. Actual Nori redb UPDATE/ROLLBACK/rename/reopen, raw surrogate column/table lifecycle, full document-ID boundaries, tokenless fields, metadata corruption, and failed storage publication have regressions | SQLite graph storage and source migrations, complete graph phrase/highlight execution and lossless query terms, reserved-name preflight and public Nori diagnostics, actual binding artifacts, cancellation/accounting, measured defaults/CSV regeneration, and final remote pre-merge/package build/dry-run gates remain open |
| SQLite initial restore transaction | Initial provider sessions defer catalog preparation until Engine owns the restore transaction. Ordered schema steps and legacy FTS shape/source repairs use nested savepoints, so later analyzer, migration, and actual rebuild-write failures restore the original schema and all durable rows. Standalone catalog open also rolls back earlier steps after a later migration failure. Nori-enabled Engine passed 203 unit and 2,617 integration tests with 2 existing ignored probes, including the manual SQL harness; SQLite passed 226 unit and 150 integration tests. Strict all-target Clippy passed with and without Nori; repository policies passed | Native SQLite occurrence storage, graph query/highlight consumers, public Nori registration and bindings, cancellation/accounting, measured defaults, and the final release gates remain open |
| Native SQLite occurrence persistence | Schema 48 stores canonical binary terms, version 2 clusters, source metadata, field revisions/counts, and an explicit source-rebuild obligation in native SQLite tables. Point/batch replacement and complete rebuild publish atomically; old rows retire only with reconstruction. Actual Nori UPDATE/ROLLBACK/rename/reopen, raw surrogate lifecycles, duplicate/gapped/tokenless streams, score and metadata corruption, batch/rebuild write failures, and reconstruction with an already-current binding catalog have regressions. Tracked cache refresh now restores committed bindings before reopening changed physical indexes. Nori-enabled Engine passed 203 unit and 2,618 integration tests with 2 existing ignored probes; all 18 final FTS lifecycle cases, including original-source reconstruction and retired-schema column rename/drop, passed with and without Nori. SQLite passed 225 unit and 155 integration tests with and without Nori. Strict all-target Clippy and repository policies passed | Complete graph phrase/highlight consumers and lossless query terms, public Nori registration/reserved-name preflight/diagnostics, actual binding artifacts, cancellation/accounting, measured defaults/CSV regeneration, and final remote pre-merge/package build/dry-run gates remain open |
| Lossless query identities and native scoring ownership | Canonical term keys now survive exhaustive scoring, all four WAND paths, persisted SQLite bounds, document-derived calibration samples, typed multi-field search, all-field planning, and attention features. Maximum document IDs remain live candidates. `uqa-scoring` owns scoring/statistics/block-bound/ranking/sampling algorithms and `uqa-planner` owns all-field aggregation; Engine only adapts retained resources and boundaries. Native scoring passed 92 unit and 132 integration tests, planner 99 unit and 26 integration tests, SQLite 225 unit and 157 integration tests, and Nori-enabled Engine 202 unit and 2,631 integration tests with 2 existing ignored probes. The SQLite occurrence-scoring test moved from Engine to the provider harness. Strict all-target Clippy passed for all eight affected crates in both feature configurations. The full eight-crate Nori run passed 4,314 tests with 2 existing ignored probes. Archive and remote checks are recorded against clean commit IDs in the PR. |
| Native graph phrase execution | Quoted SQL retains the whole phrase through optimizer rewrites. Native operators intersect first/terminal posting support and traverse memoized graph states; scoring follows positional acceptance and retains emitted units. Borrowed memory cursors avoid copying complete term support. Nori-enabled owner suites passed 4,771 tests across Engine, execution, operators, planner, scoring, SQL, storage, and SQLite, with 2 existing ignored probes. Engine passed 202 unit and 2,638 integration tests, including the manual SQL harness. Actual Nori compound paths, raw UTF-16 terms, and punctuation-bearing user entries passed SQL checks in Memory, SQLite, and redb, including durable reopen. Native tests cover holes, repeated terms, maximum document IDs, alternative edge lengths, cancellation, memory failure, and exponentially many paths. | End-to-end analyzer/provider allocation accounting, source highlighting, broader differential graph cases, public bindings, measured defaults, and final release gates remain open. A subsequent compressed-provider monitor regression and correction are recorded below. |
| Compressed transaction monitor safety | The SQLite owner now treats every pinned compressed transaction as unsafe for its independent change-version monitor. A deterministic busy-handler regression pauses a pending writer, proves that the monitor is blocked while the existing pinned reader remains usable, and fails on the original safety flag. The corrected guard passes that regression and an additional concurrent session-open/write diagnostic. After removing temporary diagnostics, Nori-enabled Engine passed 202 unit and 2,638 integration tests with 2 existing ignored probes; SQLite passed 226 unit and 157 integration tests. Without Nori, SQLite passed 226 unit and 157 integration tests and Engine passed 202 unit tests. Strict all-target Clippy passed for storage, SQLite, and Engine in both configurations; dependency, ownership, harness, format, header, line, hygiene, and source-license checks passed. | The remaining Nori implementation and final release gates stay open. |
| Original-source highlighting and named SQL revisions | The analysis crate matches complete source/query analysis by lossless terms, merges overlapping original spans only for rendering, and keeps a selected match whole in short fragments. SQL validates a seventh analyzer-name argument; execution retains one compiled revision and Engine contributes eight resource-adapter lines. Existing SQL calls and NULL analyzer names retain English word scanning. Native regressions cover character replacement, entities, grams, raw surrogate identity/reference offsets, Hanja readings, inflections, compounds, punctuation-bearing user entries, and frozen synonyms. Memory/SQLite/redb SQL checks cover named revisions, prepared calls, errors and NULL evaluation order, savepoints, session epochs, and durable reopen. Nori-enabled analysis, SQL, execution, and Engine suites passed 4,018 tests with 2 existing ignored probes: analysis 78 unit/180 integration/12 doc tests, SQL 505/45, execution 354, Engine 202/2,642 including the manual SQL harness. Without Nori, the three native owners passed 1,102 tests, Engine highlighting passed 13, and the manual SQL harness passed with the new example executed. Strict all-target Clippy passed for all four owners in both configurations; format, dependency, ownership, harness, header, line, benchmark coverage, hygiene, and source-license checks passed. | Complete analyzer/provider/rendering cancellation and allocation accounting, expanded differential graph cases, built-in registration and reserved-name checks, SQL diagnostics, actual binding/resource delivery, measured defaults, and final remote pre-merge/package build/dry-run gates remain open. |
| Native tokenizer byte reservations | Core owns a shared atomic byte allowance, unique allocation leases, and fallible vectors/deques; analysis owns reservations for UTF-16 input, rolling lattice capacity, pending/output buffers, readings, and morphemes. Growth reserves both buffers before allocation, pruning retains capacity charges, and budgeted results hold output reservations after return. Six core regressions verify concurrent owners, growth failure, retained capacity, ownership transfer, overflow, and zero-sized elements. Four native regressions verify exact returned capacities including readings/raw-surrogate morphemes, allocation failure, shared owners, cancellation across execution, and borrowed UTF-16 input. Complete core plus analysis suites passed 439 tests with `uqa-analysis/nori-tools` (core 92 unit/68 integration/1 doc, analysis 81/185/12) and 359 without Nori (core 92/68/1, analysis 49/142/7). After equivalent lint corrections, all 161 core tests and seven tokenizer tests passed; strict all-target core/analysis Clippy passed in both configurations. Existing pinned Docker fixture outputs remain unchanged. Nori-enabled Engine dependency compilation passed; this is not a fresh Engine runtime suite. Dependency, ownership, integration-harness, format, header, line, benchmark, hygiene, and source-license checks passed. No Engine source changes or dependency-policy exceptions. | Common compiled analysis, generic and Korean filters, source maps/projections, provider cursors, and rendering still need caller budget/cancellation propagation. SQL work_mem is not yet connected to these native tokenizer entry points. Expanded graph differentials, public registration/diagnostics, actual bindings, measured defaults/CSV regeneration, and final remote pre-merge/package build/dry-run gates remain open. |
| Character-stage source allocation ownership | Core adds reserved UTF-8 strings and immutable shared payload leases. Character stages stream replacements directly into reserved text and edit-map buffers, prepare replacement fragments once, reserve capture offsets before search, and build checked scalar indexes. Shared source views and Korean token contexts retain map/index reservations; later edits preserve older views. The unchanged default path reuses an unlimited allowance across chained edits. Three core regressions cover Unicode growth failure and shared-result lifetime/failure. Ten analysis regressions cover source limits, cancellation, clone lifetime, different allocation owners, capture slots, identity expansions, source-span semantics, and retained Korean contexts; one table compares 1,344 pattern/input/replacement combinations against the pinned regex library. Complete core/analysis suites passed 453 tests with `uqa-analysis/nori-tools` (core 95 unit/68 integration/1 doc, analysis 81/195/13) and 372 without Nori (core 95/68/1, analysis 49/151/8), including the new executed Rust example. Strict all-target Clippy passed in both configurations, and all eight Nori-enabled Engine SQL highlighting integration tests passed. Existing Docker-derived Nori fixture results remain unchanged; no JVM was invoked. Dependency, ownership, integration-harness, format, header, line, benchmark, hygiene, and source-license checks passed without Engine source changes or dependency exceptions. | Caller propagation through compiled analysis, generic/Nori token stages, new retained source copies, provider cursors, and rendering remains open. Literal/regex searches still run inside the library between cancellation polls; their internal interruption and private capture-storage assumptions must be resolved for the complete runtime resource gate. Public registration/diagnostics, actual bindings, broader graph differentials, measured defaults/CSV regeneration, and final remote pre-merge/package build/dry-run gates remain open. |
| Common tokenizer allocation ownership | Core can split an existing unique lease without releasing or reacquiring bytes. Analysis emits word ranges incrementally, reserves gram boundaries and generic token buffers, transfers native morphology leases, and counts both encodings during lossless UTF-16-to-UTF-8 conversion. Retained source copies and projection payloads keep shared reservations. Budgeted calls prepare new coordinate caches privately, so failures leave borrowed inputs unchanged. One core regression covers splitting a full allowance and preserving unrelated owners; eleven analysis regressions cover exact native attribute reservations, hidden terminal state, scalar spans and order, surrogate identity, shared source lifetime, encoding overlap, cancellation, allocation failures, and borrowed-input rollback. Complete core/analysis suites passed 466 tests with `uqa-analysis/nori-tools` (core 96 unit/68 integration/1 doc, analysis 82/205/14) and 382 without Nori (core 96/68/1, analysis 49/159/9). The new typed example executed in both configurations, and strict all-target Clippy passed for both owners in both configurations. Seventeen Nori-enabled Engine integration tests passed: seven phrase tests, eight SQL highlighting tests, and two occurrence/rebuild tests, including Memory/SQLite/redb and durable reopen. Existing Docker-derived Nori corpora remain unchanged; no JVM was invoked. Dependency, ownership, harness, format, header, line, benchmark, hygiene, and source-license checks passed without Engine source changes or policy exceptions. | Caller propagation through compiled analysis, generic/Nori token filters, provider cursors, SQL, and rendering remains open. Library substring/regex interruption, regex workspace accounting, and private capture-storage assumptions still need resolution. Public registration/diagnostics, actual bindings, expanded graph differentials, measured defaults/CSV regeneration, and final remote pre-merge/package build/dry-run gates remain open. |
| Porter and ASCII folding allocation ownership | Porter stemming now reserves character/consonant scratch and scalar or lossless output, checks cancellation during loading, prefix scans, suffix stages and encoding, and computes repeated-y consonant state without recursive prefix walks. A 100,000-character regression runs on a 64 KiB stack, and 70,491 UTF-16 prefix/suffix combinations match complete outputs captured from the preceding implementation. ASCII folding uses the existing normalization dependency’s public single-scalar decomposition callback and writes directly into reserved UTF-8/raw-unit term buffers, eliminating normalized strings and scalar-segment intermediates. All 1,112,064 Unicode scalars match the previous per-character NFKD/fallback contract; raw-unit, allocation-failure, independent-owner and cancellation tests also pass. Complete analysis suites passed 309 tests with nori-tools (89 unit/205 integration/15 doc) and 225 without Nori (56/159/10), including the new executed Rust example. Strict all-target Clippy passed in both configurations. Thirty-four Nori-enabled Engine integration tests passed: 17 SQL full-text cases, seven phrase cases, eight highlighting cases, and two occurrence/rebuild cases. Existing Docker-derived Nori corpora remain unchanged; no JVM was invoked. Dependencies, ownership, harness registration, format, headers, line ceilings, benchmark coverage, hygiene and source licenses passed with no Engine source changes or policy exceptions. | Lowercase and token/morphology cloning/removal/expansion, generic and Korean filter caller propagation, compiled analysis, provider cursors, SQL and rendering still need complete allocation/cancellation control. Library substring/regex interruption, workspace accounting and capture-storage assumptions remain open, together with public registration/diagnostics, actual bindings, expanded graph differentials, measured defaults/CSV regeneration and final full premerge/package build/dry-run gates. |
| Contextual lowercase allocation ownership | Full lowercase now emits directly into reserved scalar/raw term buffers. Two borrowed forward iterators preserve original-input final-sigma context, skip case-ignorable runs without repeated rescanning, and poll cancellation; isolated UTF-16 units delimit context and remain exact. Immutable Cased/Case_Ignorable tables use the existing regex-syntax public HIR API, with no new dependency or runtime regex workspace. All 3,336,192 scalar/context cases match Rust string lowercasing, and all 65,536 UTF-16 units are checked in two raw contexts. Limit sweeps and cancellation at every callback release partial output without disturbing an independent owner. Three captured Unicode 16 descriptor identities remain unchanged and restore successfully; different context tables or versions contribute explicit semantic hashes. Complete analysis suites passed 316 tests with nori-tools (95 unit/206 integration/15 doc) and 232 without Nori (62/160/10); strict all-target Clippy passed in both configurations. Thirty-four Nori-enabled Engine integration cases passed for SQL full-text, phrases, highlighting and occurrence/rebuild behavior. Existing Docker-derived Nori corpora pass unchanged; no JVM was invoked. Dependency, ownership, harness, format, header, line, benchmark, hygiene and source-license policies passed with no Engine source changes or policy exceptions. | Token/morphology cloning, removal and expansion, generic/Korean filter and compiled caller propagation, provider cursors, SQL and rendering still need complete allocation/cancellation control. Library substring/regex interruption, workspace accounting and capture-storage assumptions remain open, together with expanded graph differentials, public registration/diagnostics, actual bindings, measured defaults/CSV regeneration and final full premerge/package build/dry-run gates. |
| Token copying and gram-coordinate ownership | TokenTerm, AnalysisToken and AnalyzedText now expose reserved copy entry points. Analysis-owned morphology helpers reserve readings, morpheme vectors and raw surfaces; complete copies retain hidden terminal state, final gaps and independent token buffers while sharing existing source projection/map leases. Synonym expansion copies the replacement directly, and gram expansion counts and reserves a complete encoding-boundary table before using precomputed UTF-16 positions. A tight-quota regression failed with incremental table growth and passes with one complete allocation. Ten owner unit tests verify 9,331 scalar/raw input sequences and all their complete-character substrings, exact owned capacities, unchanged source and morphology, independent owners, source lifetime, allocation failures and cancellation. One new integration test copies real Nori output with Hanja readings, inflections and raw user segmentation, drops the original and verifies retained data and complete release. The public copy example executes as a Rust doctest in both configurations. Complete analysis passed 328 tests with nori-tools (105 unit/207 integration/16 doc) and 243 without Nori (72/160/11); strict all-target Clippy passed in both configurations. Thirty-four Nori-enabled Engine full-text, phrase, highlighting and occurrence/rebuild regressions passed. Existing Docker-derived fixture corpora remain unchanged; no JVM was invoked. Dependencies, ownership, harness registration, headers, line limits, benchmark coverage, format, hygiene and source licenses passed with all implementation changes in analysis and no policy exceptions. | Retained ownership through generic/Korean filter input, output, term replacement, removal and expansion still needs completion, followed by compiled/provider/SQL/rendering caller propagation. Library substring/regex interruption, workspace and capture-storage accounting remain open, together with expanded graph differentials, public registration/diagnostics, actual binding artifacts, measured defaults/CSV regeneration and final full premerge/package build/registry dry-run gates. |
| Common filter batch allocation ownership | Common filters now retain input/output vectors, replaced terms, morphology, and hidden terminal reservations. Removal compacts existing capacity and releases discarded attributes only after destruction; an empty vector is freed before boxing the trailing token. Expansion reserves its full output vector, transfers unchanged tokens, and retains independently copied alternatives/grams. Prepared stop/synonym key comparisons and length scans poll cancellation. Fourteen new unit tests include 2,048 complete batch comparisons against the preceding allocating/substring reference, every allowance and callback failure, buffer identity, full-allowance transfers, zero-increment terminal state, checked position overflow, and a native Nori chain with readings, inflections, raw user segmentation, and retained source leases. Complete analysis suites passed 342 tests with nori-tools (119 unit/207 integration/16 doc) and 256 without Nori (85/160/11); strict all-target Clippy passed in both configurations. Thirty-four Nori-enabled Engine full-text, phrase, highlighting, and occurrence/rebuild regressions passed. Dependency, ownership, harness, format, header/line, benchmark, hygiene, and source-license policies passed with implementation/tests in analysis and no policy exceptions. Existing Docker-derived corpora remain unchanged; no JVM was invoked. | The controlled common-stage entry point remains internal. Korean filter batches and compiled/provider/SQL/rendering caller propagation, library search interruption and workspace/capture accounting, expanded graph differentials, public registration/diagnostics, actual binding artifacts, measured defaults/CSV regeneration, and final full premerge/package build/registry dry-run gates remain open. |
| Korean filter and complete analyzer allocation ownership | Core adds front insertion, back removal, and borrowed iteration to its reserved deque. Analysis shares native/common batch ownership and POS compaction, reuses native reading/lowercase capacity, retains numeric lookahead and terminal snapshots, and reserves exact-decimal coefficients, formatting, fallback copies, and scalar/raw normalization. Borrowed term iteration and cancellable source comparison avoid unreserved temporary terms. Public token-filter, native analyzer, compiled analyzer, and uncompiled analyzer methods propagate one runtime allowance and callback; preparation retains separate ownership and uncompiled synonym files still reload. One core, six analysis unit, and thirteen integration regressions cover exact capacities, independent owners, tight allowances, every callback cancellation, source/cache lifetime, raw user segmentation, complete graphs, normalization, and fixed/reloaded resources. All 334 direct numeric-normalization snapshots are checked with retained reservations; existing pinned native/common and compiled/restored corpora remain unchanged. Complete core/analysis suites passed 530 tests with nori-tools (core 97 unit/68 integration/1 doc; analysis 125/220/19) and 428 without Nori (core 97/68/1; analysis 85/164/13), including three new executed Rust examples. Strict all-target Clippy passed for both owners in both configurations. Thirty-four Nori-enabled Engine full-text, phrase, highlighting, and occurrence/rebuild regressions passed. Dependency, ownership, harness, format, header/line, benchmark, hygiene, and source-license checks passed with implementation/tests in core and analysis and no Engine or dependency-policy changes. No JVM was invoked. | Provider cursors, SQL work_mem, and highlight rendering still need caller budget/cancellation integration. Library search interruption and workspace/capture accounting, expanded graph differentials, built-in registration/reserved-name validation and SQL diagnostics, actual binding artifacts/resource delivery, measured defaults/CSV regeneration, and final full premerge/package-build/registry dry-run gates remain open. |
| Source highlighting and SQL runtime allocation ownership | Analysis now moves query-term allocations while releasing unused token morphology, hidden terminal state and source projections. Cancellable in-place ordering preserves scalar/raw identity; reserved spans, clusters and borrowed windows render directly into one complete output allocation without a full-source character table or per-fragment copies. The native legacy word scanner preserves the pinned Unicode class, contextual lowercase and per-call uncompiled resource reloads. Budgeted typed entry points retain the output lease. SQL execution shares live work_mem and cancellation through borrowed query splitting, analysis, matching and rendering after NULL short circuits; failures retain SQLSTATE 53200/57014. Engine adds only its existing runtime view to the scalar adapter. Rendering differentials cover 12,800 source/window/density combinations, and native word membership matches regex for every Unicode scalar. New owner tests cover every callback cancellation, allocation limits, unrelated live leases, moved raw/scalar buffers, long words/tags and legacy raw-error priority. Complete analysis/execution suites passed 735 tests with nori-tools (analysis 137 unit/220 integration/20 doctests, execution 358 unit) and 632 without Nori (analysis 96/164/14, execution 358); strict all-target Clippy passed both configurations and the Nori-enabled Engine targets. The highlighter Rust example executes in both configurations. Current Nori-enabled Engine checks passed 37 tests: highlighting 10, manual SQL compile/execute 1, FTS 17, phrases 7 and durable occurrences 2, including prepared/live settings, independent sessions, cancellation, revisions and reopen. Dependency, capability, harness, formatting, source-header/line, benchmark, hygiene and license checks passed without policy changes; all JVM operations remain Docker-only and this checkpoint invoked none. | Provider phrase-analysis and cursor allocation ownership; library search interruption/workspace/capture accounting; expanded graph differentials; built-in registration and reserved-name preflight; SQL diagnostics; actual binding/resource delivery; measured defaults and CSV regeneration; final full remote premerge and package-build/dry-run gates. |
| Native phrase allocation ownership | Core shares cancellable in-place ordering and borrowed views of guarded values. Storage retains one allowance through complete query analysis, canonical scalar/raw key encoding and ordered graph projection. Candidate scoring owns reserved IDFs and scalar statistics and sums native contributions directly in emitted order; BM25 and Bayesian results match the previous scorer bit for bit. Operators retain graph, occurrence-result, memoized-state, field-result and final-posting reservations; execution includes complete analysis in live session work_mem and cancellation, including empty analyzed phrases. Thirteen new core/storage/scoring/operator/SQL regressions cover allocation boundaries, every available callback failure, raw identities, source spans, previous-result lifetime, independent reservations, exact score bits, prepared statements and session settings. Complete core/analysis/storage/scoring/operators/execution suites passed 1,434 tests with nori-tools and 1,331 without Nori; targeted Nori-enabled Engine checks passed 38 tests (phrases 8, FTS 17, highlighting 10, durable occurrences 2 and manual SQL compile/execute 1). Strict all-target Clippy passed for both owner configurations and the Nori-enabled Engine targets. Dependency, Engine capability, harness, source-header/line, formatting, benchmark, hygiene and license checks passed. Operators were rechecked in both configurations after Clippy cleanup. All JVM operations remain Docker-only; this work invoked none. | Producer-owned provider cursor payloads, cluster reads and occurrence-decoder scratch; library search interruption/workspace/capture ownership; expanded graph differentials; built-in registration and reserved-name preflight; SQL diagnostics; actual binding/resource delivery; measured defaults and CSV regeneration; final full remote premerge and package-build/registry dry-run gates. |
| Provider occurrence read allocation ownership | Common storage now owns `StorageReadControl`, typed memory/cancellation failures, borrowed score/position directories, a selected-document occurrence decoder, and a cursor that retains one encoded cluster and one reusable score block. Canonical controlled keys use the same encoder as ordinary persistence. Memory reads retain cursor/result reservations; Key/Value callbacks borrow memory/redb payloads, while SQLite reserves bound values and encoded payloads before materialization. SQLite probes and reads share the caller’s transaction or one temporary read snapshot, and prefix-existence checks avoid fetching unrelated large values. Phrase operators now consume these producer-owned cursors, occurrences, and field scalars directly. Tests preserve the pinned old decoder’s corruption acceptance, every-byte common allocation boundaries, cancellation/reuse, maximum document IDs, constant cursor memory across 2 versus 128 clusters, binary prefix ordering, typed provider/transaction errors, concurrent BLOB growth, and pinned SQLite snapshot visibility. The redb legacy migration regression now verifies the required source rebuild and occurrence persistence after reopen. Complete storage/SQLite/redb/operator suites passed 718 tests in each default and Nori-tools configuration; strict all-target Clippy passed both. Nori-enabled Engine phrase integration passed 8 tests. Dependency, Engine capability, harness, source headers/line limits, formatting, benchmark, hygiene, and license checks passed; no Engine implementation, dependency edge, or feature policy changed, and no JVM ran. | Library substring/regex interruption and workspace/capture ownership; expanded graph differentials; built-in Nori registration and reserved-name preflight; SQL diagnostics; actual binding/resource delivery; measured defaults and CSV regeneration; final full remote premerge and package-build/registry dry-run gates. |
| Native Nori registry and SQL diagnostics | `uqa-analysis` now exposes the immutable feature-enabled `nori` built-in and one shared built-in-name API; Engine rejects built-in catalog names on registration and performs the same collision preflight while restoring persisted revisions. SQL `list_analyzers` follows the feature set, and `analyze_text(name, input)` returns one JSONB object with complete token terms, UTF-8/UTF-16 offsets, graph positions, final stream state, optional Korean morphology, and the resolved descriptor fingerprint. Default and Nori-enabled Engine integration tests cover aliases, metadata, wrong arguments, feature-disabled failure, in-memory collisions, and persisted collision rejection; feature forwarding was added to the Rust facade and all binding packages, whose Python, Node.js, and WASM artifacts now execute the Nori diagnostic with the bundled resource. No JVM ran. | Measured defaults and CSV regeneration, and final full pre-merge/package-build/registry dry-run gates remain open. |
| Phrase graph differential coverage | `uqa-operators` now compares the memoized graph matcher with an independent path evaluator over 2,000 deterministic small query/document graphs, including holes, edge lengths, alternatives, duplicate terms, and absent postings. The owner suite passes 46 unit and 57 integration tests with strict all-target Clippy and formatting; the existing Engine integration target also verifies SQL cancellation recovery across Memory, SQLite, and redb, with no dependency edge or Engine implementation change and no JVM run. | Arbitrary/configured regex search interruption and regex workspace/capture accounting remain open, followed by measured defaults/CSV regeneration and final full pre-merge/package-build/registry dry-run gates. |
| Cooperative literal mapping scans | `uqa-analysis` now scans literal character-filter mappings at owned UTF-8 boundaries with bounded byte comparisons and callback checks, preserving Rust's non-overlapping and empty-pattern behavior without a library search call. Two unit regressions prove long unmatched inputs poll throughout the scan and cancellation releases the unpublished edit while leaving the input and allowance unchanged. No Engine source or dependency edge changed, and no JVM ran. | Regex library search interruption, workspace ownership, and private capture-storage assumptions remain open alongside provider phrase accounting, measured defaults/CSV regeneration, and final full pre-merge/package-build/registry dry-run gates. |
| Cooperative built-in analysis scans | `uqa-analysis` now owns cancellable UTF-8 traversal for the built-in Standard and Letter word tokenizers and the HTML `<[^>]+>` character filter, while configured expressions outside the pinned DFA retain their library-search boundary. The owner tests exhaustively compare all strings over four symbols through length five for HTML replacement and cover long unmatched scans plus cancellation rollback for both built-in tokenization and HTML filtering; default analysis passes 103 unit, 164 integration, and 14 doc tests, and Nori-enabled analysis passes 142 unit, 219 integration, and 20 doc tests, with strict Clippy, formatting, dependency policy, and harness checks. No Engine source or dependency edge changed, and no JVM ran. | Unsupported regex search interruption, regex workspace/capture accounting, provider end-to-end phrase resource accounting, measured defaults/CSV regeneration, and final full pre-merge/package-build/registry dry-run gates remain open. |
| Cooperative configured regex scans | `uqa-analysis` now prepares pinned `regex-automata` sparse forward and reverse DFAs for Pattern tokenizers and PatternReplace stages, including expressions with capture groups. Analysis polls between every bounded transition, reconstructs exact UTF-8 match spans with leftmost-first and greedy behavior, resolves reserved capture slots only after a range is found, falls back to the validated `regex` expression for unsupported DFA features, and preserves empty-match advancement at Unicode scalar boundaries. Differential coverage includes anchors, alternation, greediness, Unicode classes, capture reordering, empty matches, long unmatched inputs, cancellation rollback, and the existing source-map corpus; default analysis passes 116 unit, 164 integration, and 14 doc tests after the owner regressions, while Nori-enabled analysis remains covered by the same feature matrix. No Engine source or dependency edge changed, and no JVM ran. | Interruption during capture-slot resolution and unsupported configured regex searches, complete regex workspace/capture-storage accounting, measured defaults/CSV regeneration, and final full pre-merge/package-build/registry dry-run gates remain open. |
| Package archive and registry preflight | At clean head `ba2c239ebf2b98105ef8aaf4b2df00026fd7cea2`, `scripts/package-publishable-crates.sh` built and license-checked all 23 publishable Cargo archives, including the Nori data resource crate; `scripts/publish-crates.sh` compiled and completed the dry-run upload contract for the four registry-independent bootstrap crates without publishing. The package manifests include the direct pinned `regex-automata` dependency through `uqa-analysis`, and no archive or source tree was left modified. No JVM ran. | Package builds for every release binding and final registry dry-run evidence remain open together with measured defaults/CSV regeneration and capture-bearing regex workspace/interruption accounting. |
| Change-aware remote pre-merge suites | Exact PR HEAD `ba2c239ebf2b98105ef8aaf4b2df00026fd7cea2` passed Pre-merge CI run `34734797650` (Linux/macOS workspace tests, FDW, Clippy, Rust 1.90, docs, deny, and PostgreSQL upstream oracle), JavaScript Bindings run `34734799293` (Node HTTP 16/24, native Nori addon, WASM bundle and examples), and Python Wheels run `34734800896` (native Nori Python smoke, CLI, stable ABI and Python tests). PR Checks run `34734230661` also passed its exact-head formatting check. No JVM ran. | The Python dispatch is the smoke workflow rather than the release full matrix; package builds for every release binding, final registry dry-run evidence, measured defaults/CSV regeneration, and capture-bearing or unsupported regex workspace/interruption accounting remain open. |
| Nori phrase provider resource gate | The existing Engine integration target now exercises a Nori analyzer over 4,097 repeated Korean occurrences in Memory, SQLite, and redb. The phrase succeeds with `work_mem = 16MB`, fails with SQLSTATE `53200` at `64kB` while reading the provider-owned occurrence payload, and succeeds again after restoring the session setting; pre-cancelled phrase execution returns SQLSTATE `57014` for every provider and the same session recovers after reset; the feature-disabled target preserves its explicit missing-feature path. No Engine implementation or dependency edge changed, and no JVM ran. | Arbitrary/configured regex search interruption and regex workspace/capture accounting, measured defaults/CSV regeneration, and final full pre-merge/package-build/registry dry-run gates remain open. |
| Configured regex capture and workspace ownership | Analysis now uses the pinned dependency’s public Thompson states and Unicode look assertions for cancellable capture resolution and patterns outside bounded DFA capabilities. State frontiers, capture rows, result slots, and the explicit traversal stack reserve before allocation and release with the searcher; capture storage has no private dependency-layout assumptions. DFA preparation delegates syntax capabilities to the dependency and preserves UTF-8 boundaries for empty ASCII assertions. Differential coverage compares ranges and every capture at all restart offsets, nested repetitions, Unicode/ASCII boundaries, replacement expansion, and retained source coordinates. Every-byte allowance sweeps, every callback cancellation, long scans, and reuse preserve independent owners and publish no partial output. Default analysis passes 125 unit/164 integration/14 doc tests; nori-tools passes 167/220/20, including the unchanged Docker-derived corpora. Strict Clippy, formatting, dependency, Engine capability, harness, source-header/line, benchmark, hygiene, and source-license checks pass. No dependency edge, feature policy, or Engine implementation changes; no JVM ran. | Measured defaults/benchmark baselines and CSV regeneration; complete equivalent binding scenarios and release binding package builds; final release acceptance. Exact-head remote and package preflight results are recorded in the PR. |
| CSV source regeneration and continuous reference checks | A Docker-only tool rebuilds the pinned 49,775,061-byte MeCab archive through the public Lucene DictionaryBuilder with UTF-8 and normalization disabled. The reviewed provenance covers 40 CSV files and three definitions totaling 202,683,519 bytes, builder/JVM identity, and all nine generated resources. Two arm64 executions and one amd64 execution reproduce every jar-resource byte and hash. The new CI job requires this check, exhaustive neutral model export, and all six reference drivers before the merge gate can pass. Local execution of those CI commands on amd64 verified 774,582 surfaces, 816,283 words, 10,292,646 matrix cells, 1,114,112 Unicode values, and 1,691 reference cases. All 149 tooling tests and dependency, Engine capability, harness, formatting, source-header/line, benchmark, hygiene, and license checks passed. The complete tooling suite also passes on the PR checker’s Python 3.9 runtime; explicit field-count validation rejects truncated or extended JVM identity responses without requiring newer zip APIs. Corrupted source/resource bytes, invalid archive inputs, existing output, Docker failure, runtime mismatch, and attempted expected-manifest rewrites cannot publish partial output. No Rust runtime dependency or packaged dictionary changed; every JVM operation ran in Docker. | Measured native/WASM defaults and benchmark baselines; complete equivalent binding scenarios and release binding package builds; final release acceptance. Exact-head remote results are recorded in the PR. |
| Standalone native and WASM allocation/performance evidence | The analysis owner now provides one benchmark target with a pinned development-only allocation counter and six original fixed corpora in all three decompound modes. Repeated native and WASM runs reproduce all 39 allocation measurements and all 36 serialized token graphs; 64 cached handles add no model allocation. The 9,829,534-byte bundle peaks at 88,384,710 requested heap bytes on native and 86,255,402 on WASM. Four hashed reports record complete timing samples, toolchains, CPU, flags, and runtime source identities. Reviewed 32-bit/64-bit allocation ceilings allow no increase, and a separately requested same-environment timing comparison uses a measured 1.16 ratio ceiling. Native pre-merge and WASM binding jobs enforce resource/output checks. All 163 tooling tests pass on Python 3.12 and 3.9; strict Nori-tools Clippy, repository ownership/dependency/license/benchmark checks, and package inventory pass. The runtime graph remains 108 edges and no Engine implementation changes. No JVM ran. | Indexing and phrase-query measurements; browser host memory; complete equivalent persistent binding scenarios and release binding packages; final release acceptance. The standalone analysis evidence does not close those separate gates. |
| Memory batch ownership and native/WASM indexing measurements | Storage now stages only affected documents with their global field-counter context, reuses ordered point replacements, and publishes after complete validation. Untouched occurrence/position buffers retain their allocations. Tests cover repeated IDs, deletions, field moves, late document/field overflow, corrupt reverse/posting state, and generated equivalence with point updates. The storage suite passes 186 unit, 31 integration, and one doc test in both default and Nori-enabled configurations, with strict all-target Clippy in both. A feature-guarded storage benchmark uses six fixed corpora and full serialized graph/metadata digests. Its original implementation source exactly matches benchmark commit 8acf747b. For 16 additions to 2,048 native documents, maximum additional allocator requests fall from 65,311,546 to 2,102,359 bytes; WASM requests 1,317,263 bytes. Four complete native/WASM reports reproduce every allocation count and graph hash; reviewed ceilings allow no increase, and explicit same-environment timing comparisons use a measured 1.13 limit. Native and WASM CI enforce indexing gates alongside analysis. The shared runner also passes actual complete analysis gates on both targets. All 175 tooling tests pass on Python 3.12 and 3.9, and ownership/dependency, harness, benchmark, source, formatting, hygiene, and license checks pass. Runtime edges remain 108, Engine is unchanged, and no JVM ran. | Persistent-provider and SQL/transaction indexing measurements, phrase-query measurements, browser host memory, complete persistent binding scenarios, release binding packages, and final release acceptance. Exact-head remote and package results are recorded in the PR. |
| Physical provider resource evidence and CI selection | Provider-owned SQLite/redb benchmark targets use only development instrumentation and the dependency feature `uqa-analysis/nori`. A shared fixture protocol measures explicit index transactions after copying closed seed files; every warmup, timed sample, and allocation sample verifies live and reopened graphs against Memory. Seven complete reports and an allocation-only reference cover native SQLite/redb and WASM SQLite, with all graph/metadata/statistic hashes matching the earlier Memory baselines. SQLite allocation counters repeat exactly; redb cumulative allocation totals differ by three bytes in commit workloads while other counters repeat. Per-provider/pointer-width ceilings use maximum observed values without padding, and measured repeat variation gives timing ratios of 1.16 for SQLite and 1.21 for redb. Actual SQLite version/journal/synchronous/page settings, C compiler overrides, and all shared benchmark sources participate in provenance. Native and WASM CI jobs run the relevant provider gates; changes to runners, corpus, or reviewed benchmark inputs select both jobs, and dictionary data changes select all runtime bindings. Strict all-target Clippy passes with and without Nori; all 190 tooling tests pass on Python 3.12 and 3.9, and ownership, dependency, format, benchmark inventory, source/license, public hygiene, and CLI gates pass. No Engine implementation or runtime dependency changes and no JVM execution. | SQL/session indexing and transaction overhead, phrase-query measurements, browser host memory, complete equivalent persistent binding scenarios, release binding packages, and final release acceptance remain open. |
| Target-specific physical-provider allocation gates | Repeated clean-commit Linux CI runs exposed that equal pointer widths do not imply equal redb allocation layouts across operating systems. SQLite counters match macOS exactly; redb retains 688–872 more requested bytes on Linux, with only three-byte repeat variation in cumulative allocation. Complete graphs, metadata, statistics, runtime source hashes, and every live/reopened result agree. Physical-provider limits now identify target OS, architecture, and pointer width, reject unmeasured targets, and retain all previous macOS/WASM ceilings. Four raw Linux reports preserve original collection-time gate results and exact source run/attempt/artifact identities. Per-target ceilings are observed maxima without padding; Linux timing repeats pass the unchanged SQLite 1.16 and redb 1.21 policies in both directions. All 192 tooling tests pass on Python 3.12 and 3.9; ownership, dependency, format, benchmark inventory, hygiene, and license checks verify this calibration correction; no runtime or Engine implementation changes and no JVM execution. | SQL/session indexing and transaction overhead, phrase-query measurements, browser host memory, complete equivalent persistent binding scenarios, release binding packages, and final release acceptance remain open. |
| Native phrase allocation and timing gates | An operator-owned benchmark measures pre-analyzed graph matching and complete phrase analysis/matching through public budgeted APIs, using 2,048 repeated corpus documents plus six exact source controls. All 36 conditions verify every document and score across a warmup, seven timing samples, and an allocation sample. Four native/WASM reports reproduce all six allocation counters and agree exactly on query identities, graph sizes, all matching document identities, and BM25 scores. Requested additional heap peaks at 187,416 native bytes and 186,484 WASM bytes. Reviewed ceilings permit no allocation increase; explicit timing comparisons use a measured 1.23 ratio ceiling. Native/WASM CI gates enforce the output and resource contracts. All 202 tooling tests pass on Python 3.12 and 3.9; strict all-target Clippy passes with and without Nori, together with ownership, dependency, harness, benchmark, formatting, source, hygiene, and license checks. Instrumentation uses only development dependencies and the existing analysis feature; no runtime or Engine algorithm change and no JVM execution. | SQL/session indexing and transaction cost, physical-provider phrase I/O, browser host memory, complete equivalent persistent binding scenarios, release binding packages, and final release acceptance remain open. |
| Equivalent persistent Nori binding contracts | One shared fixture supplies 47 SQL steps to the existing Rust Engine integration harness and the actual Python, Node.js, and WASM packages. Rust executes SQLite and redb with Nori and nine additional feature-disabled steps; its runtime dependency tree excludes the dictionary when Nori is disabled. Complete token/morphology/source diagnostics and analyzer fingerprints agree across bindings. The fixture verifies supplied user rules, source mapping, Hanja readings, connected phrase paths and particle gaps, original-source highlighting, unavailable resources, invalid segmentation, built-in protection, failure atomicity, savepoints, rollback, retained revisions after catalog-name replacement, new writes, and two close/reopen boundaries. Rebuilt Python passes all 14 tests, Node.js passes all 30 native/HTTP tests, and WASM passes all 11 package tests. The final Rust contract passes in both feature configurations and strict integration-target Clippy passes; all 20 dispatch tests pass on Python 3.12 and 3.9. Shared inputs select every affected CI suite. Ownership, dependency, harness, format, source, benchmark, hygiene, and license checks pass with no runtime dependency or Engine implementation changes. No JVM ran. | Custom binding builds without Nori, browser IndexedDB persistence/host memory, SQL/session and physical-provider phrase costs, release binding package builds, and final acceptance remain open. |
| Complete redb commit-order calibration | A clean Linux CI observation exposed a missing three-byte cumulative-allocation outcome. The pinned upstream implementation updates table roots in randomized order and copies differently sized serialized table definitions; a provider-owned public-API probe isolates this with 64 reopened samples in each of four conditions. New tables and identical-type controls are stable; existing provider-type tables produce only two cumulative-byte totals three apart, with every other counter unchanged. Calibration now requires both outcomes for every commit workload on macOS and Linux. The preserved failed CI report supplies the missing Linux maximum without padding or changes to another ceiling, and native CI repeats the probe. All 204 tooling tests pass on Python 3.12 and 3.9, strict provider Clippy passes, and ownership, dependency, format, harness, benchmark inventory, source, hygiene, and license checks pass. No runtime code, dependency edge, feature setting, or Engine ownership changes. | SQL/session and physical-provider phrase costs, custom binding builds without Nori, browser IndexedDB persistence/host memory, release binding package builds, and final acceptance remain open. |
| Independent release binding builds | Manual Python Wheels and JavaScript Bindings dispatches can request the complete release matrix with `full_matrix=true`. Release and manual verification share the same Node/WASM build jobs, and the native Windows ARM64 job now executes its addon tests and examples. Both manual workflows retain their normal checks and read-only permissions; no release or registry publication is invoked. Actionlint and YAML parsing validate the workflow definitions, and the moved jobs preserve the existing build and archive checks. | The full remote package matrix still must finish successfully; custom binding feature-disabled builds, browser persistence/memory, SQL/session/provider phrase measurements, and final acceptance remain open. |
| Real-browser and feature-disabled binding acceptance | Actual Nori-disabled Python, Node.js, and WASM artifacts pass all 14, 17, and 11 package tests; normal artifacts still pass their Nori-enabled suites. Runtime dependency trees omit the dictionary only when disabled, and CommonJS/ESM checks load the same selected addon. The WASM build script now honors disabled defaults, separate outputs, and Cargo target directories. Three enabled and three disabled Chrome sessions execute all shared fixture steps across 15 new page instances and nine IndexedDB reloads. The enabled browser also reproduces 54 complete native SQL corpus diagnostics. Hash-pinned reports retain 165 post-GC memory observations: the largest enabled and disabled samples are 143,655,635 and 29,526,920 bytes. These are browser page estimates, not RSS or continuous allocation peaks. CI builds and executes both feature configurations and checks native/browser output identity. All 213 tooling tests pass on Python 3.12 and 3.9, including rejection of incomplete reload, diagnostic, and memory evidence. The manual SQL harness, repository ownership/dependency and license policies, Actionlint, and actual Python/Node/WASM examples pass; the five Browser WASM examples also executed through Chrome. Negative controls reject both an enabled addon in disabled test mode and an invalid mode value. | SQL/session and physical-provider phrase measurements, successful full remote release binding package builds, and final acceptance remain open. |
| Binding archive attribution and resource checks | Direct inspection of all 15 artifacts from the successful full package matrix found the exact embedded dictionary but missing upstream notices in binary binding packages. Python, Node, and WASM now ship the same seven canonical attribution and source-resource/model files; Python also records them in wheel license metadata. The release checker rejects missing, changed, or conflicting notices and requires the complete pinned bundle in release runtimes. The old Node archive is rejected by the expanded check. Git checkout-filter tests also verify the canonical modification attribution and every binding copy under Windows CRLF conversion; the canonical Markdown attribution is explicitly pinned to LF. Locally rebuilt Node root/platform packages, WASM, Python wheel, and Python source distribution pass the corrected archive checks; the wheel passes all 14 binding tests and reproduces the 18 native diagnostic hashes. | The corrected full remote archive matrix and final release acceptance remain open; SQL/session calibration continues separately. |
| Rust CLI feature forwarding | The Rust `usql` crate now defaults to its explicit `nori` feature and forwards that feature through the existing Engine interface. The workspace CLI dependency disables defaults so Python controls its own feature; Python forwards `nori` explicitly to both Engine and CLI. Standalone CLI tests pass all 30 scenarios in each configuration, strict Clippy passes both, and normal dependency trees exclude the dictionary for both disabled CLI and Python builds. A newly built disabled Python wheel passes all 14 binding tests and archive checks. Native CI also exercises the standalone disabled CLI. | Full current-head CI and SQL/session calibration remain required. |
| Complete source-distribution benchmark inputs | The Python source archive previously included benchmark Rust files without their fixed Nori corpus, shared persistent-provider module, or TPC-H support module. Its include rules now preserve those dependencies, and the archive checker follows literal Rust source/data references and compares every included input with the repository. A rebuilt archive passes the resource and source-input checks, and its extracted Cargo workspace validates with the locked dependency graph. Regressions reject missing, changed, and duplicate supporting inputs. | The corrected full remote package matrix and SQL/session calibration remain open. |
| Public SQL/session benchmark and complete-result candidates | The facade benchmark uses only development instrumentation and existing provider/session APIs, with no Engine algorithm or runtime dependency change. Each transaction verifies every source row and six scored phrases after all nine live executions and persistent reopen operations; query workloads retain every scored document across six corpora, three modes, all supported providers, and independent persistent sessions. The runner rejects incomplete results, changed inputs, inconsistent analyzer identities, missing reopen evidence, invalid samples, and incomparable timing environments. All 224 repository tooling tests, strict all-target facade Clippy with and without Nori, formatting, Actionlint, dependency/ownership, harness, benchmark inventory, and legal-source checks pass. Reusable CI collects two complete native and WASM candidates and preserves their provenance without claiming an allocation gate. | The first macOS native candidate completes all 102 workloads. Clean repeated native/WASM measurements, reviewed limits, and final delivery acceptance remain pending. |
| Generated Unicode tokenizer robustness | The existing analysis integration target now exercises 64 deterministic valid-Unicode inputs across all 12 tokenizer option combinations. Mixed scripts, decomposed Jamo, combining marks, control/noncharacters, supplementary scalars, accepted surrogate-splitting user rules, and long lattice inputs verify complete source coordinates, lossless generic conversion, positive position lengths, reservation lifetime, preserved independent reservations, cancellation at observed callback boundaries, and reuse after interruption. The full Nori-tools analysis suite passes 167 unit, 221 integration, and 20 doc tests with strict all-target Clippy. These invariant checks complement the unchanged Docker semantic oracle. No runtime source, dependency, or test executable is added. | Repeated SQL/session calibration and final delivery acceptance remain open. |
| Current Rust package preflight and independent CI artifact review | Clean commit `f5ce8843` produces all 23 Cargo archives with matching VCS identities, excluded benchmark sources, and valid legal/resource contents. The registry-independent dry-run script compiles and verifies `uqa-pg-query`, `uqa-nori-data`, `uqa-core`, and `uqa-pg-wire`, explicitly suppressing all four uploads. The exact `826f23e0` Rust CI passes; its actual Python source archive and WASM package pass independent dictionary/notice checks, and the source archive also retains every referenced benchmark input. The installed CI WASM archive passes all 11 package tests. | The corrected complete binding matrix, SQL calibration, and final remote-head acceptance remain open. |
| Windows fixture byte identity | The corrected Windows ARM64 wheel builds and passes license/ABI checks, 13 Python tests with one platform-specific skip, and all five examples, but its final full-corpus identity comparison exposed Git CRLF conversion of the unpinned corpus. A reproduction using Git checkout filters and the installed native binding preserves all 18 diagnostic records while changing only the corpus hash from `18a50905d26e3615d9db2a8b9565cbcdefcbcfdd1036a88292624154fc228e6b` to `da08c31dad1ee4edfd1ae5dd591bde3faf283c8e24ddbea584ebc3f75de11fa1`. Nori fixture/evidence JSON and JSONL bytes are now pinned to LF, with checkout-filter regression coverage. The same reproduction then passes the unchanged complete contract. The exporter can print exact verification differences, and CI retains diagnostic evidence on failure; attribute changes select all runtime verification workflows. | The corrected Python matrix must be rerun; repeated SQL calibration and final remote-head acceptance remain open. |
| Corrected complete binding archive acceptance | The `39df0bd8` Python matrix (`34758420618`) passes all six platform wheels, source distribution, enabled/disabled smoke, and minimum supported Python. All seven actual archives independently pass exact Nori resource, canonical-notice, and applicable source-input checks; all six wheel diagnostic artifacts and smoke match the unchanged complete browser contract. The downloaded macOS arm64 wheel passes all 14 Python package tests and native diagnostics. The `826f23e0` JavaScript matrix (`34755946673`) passes, and independent inspection verifies all eight npm archives, including root/platform loaders, versions, platform metadata, notices, and embedded dictionaries; the installed CI Node package passes all 17 tests. | Final-head artifact identities and required CI remain delivery gates. |
| Controlled SQL catalog fixture experiments | Two clean native reports validate every one of the 102 complete SQL workloads. Allocation differences include transaction metadata and redb provider reads; eight ordinary SQL-created catalogs have equal observed non-identity metadata but differing serialized identity lengths. The facade benchmark now captures closed empty SQLite/redb databases through normal SQL, verifies their columns, primary-key and not-null errors, rollback, and empty rows on separate reopened copies, and offers a transaction experiment over fresh or captured inputs using the existing complete nine-sample transaction/reopen verification. Reported file hashes, benchmark sources, flags, CPU, and compiler identify the experiment; partial experiments cannot claim a full regression pass. Strict feature-enabled benchmark Clippy, 24 tooling tests, capture execution, and dependency/ownership/harness/benchmark checks pass. No runtime implementation or dependency changes, and no JVM ran. | Execute repeated controlled experiments, resolve the remaining allocation variation, then record reviewed SQL limits and final native/WASM gates. |
| Pinned SQL catalog inputs | Two clean complete transaction experiments for each of SQLite and redb at `9c213b2c` reproduce all six allocation counters in every workload and preserve all live/reopened results. The four raw reports and their hashes are retained under `benchmarks/nori/sql-fixture-evidence`. The ordinary SQL-created empty catalogs now supply the default benchmark inputs; their original identities remain intact, and normal SQL still constructs every analyzer, index, and source document. Schema-version-2 reports and reviewed-limit validation bind the actual input hashes, with corruption checks before build. The same SQLite input passes constraints, rollback, Nori GIN/search, and reopened row/score checks in the actual CI WASM package under Node. All 228 repository tooling tests and strict feature-enabled benchmark Clippy pass, together with dependency/ownership, harness, benchmark inventory, formatting, and legal-source checks. | Execute complete native/WASM SQL/session repeats, record reviewed limits, refresh affected source archives, and verify the final remote head. |
| Package provenance and current CI reports | [The delivery inventory](../../benchmarks/nori/delivery-evidence.json) records all 23 clean `e09cf627` Rust archives, their normalized manifest/feature/dependency checks, and the refreshed Python source archive with exact benchmark/catalog inputs. Packaged Rust sources/resources/manifests match `f5ce8843`; every internal lockfile checksum change matches its actual dependency archive. The existing default publication preflight at `f5ce8843` compiled four bootstrap crates and suppressed all uploads. Current `e09cf627` Python and JavaScript/WASM checks pass; independent review reproduces four WASM owner gates, six Chrome sessions with 15 page loads/nine reopens/54 full diagnostic comparisons/165 memory observations, and the complete Python diagnostic contract. [Verification scopes and commands](../../benchmarks/nori/DELIVERY.md) separate packaging, source inspection, compiled preflight, and actual binding execution. | Complete pinned SQL/session calibration, run the fresh final preflight, and verify required CI on the final remote head. |
| Cooperative cancellation benchmark coverage | The plan explicitly requires cancellation measurements. Existing owner tests verify cancellation behavior, but previous benchmark reports only exercise successful operations and polling overhead. The existing analysis benchmark now has a separate cancellation mode using public budgeted tokenizer/analyzer APIs, with no runtime dependency or implementation changes. Its 108 workloads distinguish total cancelled-operation time from callback-decision-to-return time, preserve an unrelated reservation, and compare complete recovery output with the existing analysis contract. A native/WASM workflow also rechecks the ordinary analysis gate before collecting two cancellation reports per target. All 238 repository tooling tests and ownership/dependency, harness, source-header, hygiene, and workflow checks pass; Rust compilation and actual measurement are not yet claimed. | Compile and execute both targets, collect the local native pair without overlapping SQL timing, review actual allocation/latency evidence, and enable cancellation regression gates before final acceptance. |
| Cancellation benchmark compilation and WASM execution | At `76df99ec`, the independent cancellation workflow (`34765432556`) passes the existing ordinary WASM analysis gate and both complete cancellation collections, validating every one of 108 workloads per report. Native Clippy identified two helper parameters passed by value without consumption; those helpers now borrow their timing samples and runner. Strict feature-enabled analysis-benchmark Clippy passes locally after the correction. | Collect the native pair and rerun native/WASM CI on the corrected common benchmark source, inspect all raw reports, then record and enforce reviewed cancellation limits alongside the remaining SQL calibration. |
