# Kuromoji analyzer implementation plan

Status: Active implementation in [PR #108](https://github.com/cognica-io/uqa-engine/pull/108), based on UQA 0.3.0. Nori and Kuromoji use the dictionary primitives, section framing, surface ranges, string codecs, Unicode intervals, provenance primitives and offline reconstruction in `uqa-analysis::morphology`. The common `cjk_width` character filter is implemented and verified against pinned Lucene text and offsets. The Japanese source rebuild, complete streamed model, native bundle/loader and immutable data crate are implemented and verified. Japanese user dictionaries are implemented and compared with the pinned reference. Typed Japanese resource owners, bounded caches and the combined analyzer resource builder are implemented. Standalone NORMAL/SEARCH/EXTENDED tokenization matches 209 pinned reference cases, including both discard flags, compound graphs, user rules and all six morphology attributes. The common rich-token/source bridge is implemented with tagged Japanese attributes, preserved Korean serialization, shared allocation transfer and corrected original spans. Native N-best cost and example selection now match 98 additional Docker cases with bounded alternatives, stable graph fixups and immutable preparation. The standalone Japanese analyzer and its base-form, POS, stopword, Katakana-stem and simple-lowercase filters are implemented, with independent width-plus-lowercase normalization. Hiragana/Katakana small-kana filters, kana/romaji reading conversion and exact Japanese number composition/normalization are implemented. Numbers share decimal, prefix and lookahead owners with Nori and match 220 additional Docker cases. Iteration marks, completion, durable common-pipeline integration and binding delivery remain incomplete. No performance claim is made. The tagged 0.3.0 release is independent of this PR.

Update rule: Update this plan with every logical implementation unit that changes ownership, interfaces, reference inputs, completed work, remaining work, or acceptance evidence. Update the PR description and push each verified commit. Record final remote CI outcomes in the PR so recording a successful run does not change the commit it checked. A completed helper or passing constructor is not completion of its larger work item.

## Objective and constraints

Implement the complete [Kuromoji design](../design/kuromoji-analyzer.md): Lucene 10.5.1 Japanese tokenization, all documented analysis components, separate normalization and completion, immutable revisions, existing retrieval consumers, and actual Rust/Python/Node.js/WASM delivery. Preserve generic and Nori results, public Korean attributes, dictionary identities, canonical descriptors, and provider behavior while sharing proven mechanisms.

The reference is Lucene commit `64ce863a2bea79c69c19c4d56268c26710ff0ff9`. The design records the inspected official artifacts and exact defaults. Every JVM invocation, including Java compilation, resource regeneration, model export, and differential verification, must run in Docker. Production execution and packaging must not download dictionaries or require a JVM.

Before each implementation or move, inspect the affected Cargo manifests and enabled features, the [dependency policy](../../scripts/workspace-dependency-policy.json), the [manual ownership map](../manual/internals/04-analyzer-pipeline.md), and existing implementations/tests. Keep analysis algorithms in `uqa-analysis`; the new data crate owns immutable bytes and notices. Engine only supplies existing state, session, transaction, and retained-resource adapters. A private shared module must not import a language module, and Kuromoji must not import Nori.

## Execution order and acceptance ledger

The rows identify reviewable work units and their dependencies. Independent reference preparation can run while source checks or the separate release are pending. Runtime work follows its required representations and resources; do not register an incomplete Japanese analyzer to make an integration test pass.

| Work unit | Owner and dependencies | Completion evidence | Current state |
| --- | --- | --- | --- |
| Shared dictionary primitives | `uqa-analysis::morphology`; existing Nori codec and lexicon | Nori uses one shared reader/writer, lexical-rank builder/lookup, and cost matrix; original errors, full model hashes, fixtures, and controls pass | Implemented and locally verified in `a28199b6`; details below |
| CJK width character filtering | `uqa-analysis::char_filter`; existing source edits and memory owner | Restricted fullwidth ASCII/halfwidth kana conversion, voiced-mark composition, exact corrected UTF-16/UTF-8 spans, chained edits, cancellation and retained budgets agree with the pinned character-filter oracle | Implemented and locally verified: all 1,112,064 scalars, 1,672 voiced-mark combinations, 19 fixed examples, source composition and control tests; Docker regeneration is included in reference CI |
| Pinned Japanese reference and model export | `tests/parity/kuromoji`; pinned Docker image, jars, source resources | Complete input inventory and hashes; source-generation recipe; streamed export and read-only comparison of every surface, ordered word, attribute, matrix cell, unknown class, Unicode property, stop set, and completion mapping | Implemented: all 29 patched IPADIC inputs reproduce nine exact jar resources; complete six-file model export and separate read-only Docker verification pass; Nori retains its original model identity through shared Python orchestration |
| Remaining shared morphology mechanisms | `uqa-analysis::morphology`; correspondence between both language implementations | Shared Unicode profiles, frame codec, resource/cache ownership, bounded lattice, and exact-decimal machinery where both consumers need them; Nori byte/descriptor/control compatibility remains unchanged | Dictionary I/O, frame validation, surface ranges, strings, Unicode intervals, provenance primitives and offline reconstruction are extracted and used by Nori; user preparation limits and prefix traversal serve both languages; shared immutable resource/cache mechanics serve both typed language owners; Both languages use shared UTF-16 input preparation, rolling lattice storage and forward traversal with language-owned matching/emission; Nori limits/errors and path ordering remain unchanged; shared token/source conversion serves both languages; generic filter term/span operations, stream allocation, polling and Java simple lowercase are extracted from Nori into shared owners; Japanese default filters now consume those shared owners; exact decimals, numeric-prefix traversal and lookahead composition serve Nori and Kuromoji through explicit language policies; completion stream policies and durable normalization-profile work remain |
| Japanese bundle, loader, and resources | `uqa-analysis::kuromoji`, `uqa-kuromoji-data`; complete model and common codecs | Separate schema and identity; exhaustive reconstructed model equality; malformed/truncated/oversized/hash-invalid inputs rejected before publication; bounded resource caches and complete licenses | Bundle schema, native packer, validated immutable loader, exhaustive six-stream verifier and data crate are implemented; actual data archive compiles and passes resource/license checks. Typed resource owners, exact hash resolution, bounded model/user caches and the combined analyzer resource builder are implemented |
| Japanese user dictionary | `uqa-analysis::kuromoji`; model and shared lexicon | Exact CSV quoting, comments, duplicates, segmentation/readings/POS, word order/IDs, invalid input and preparation-limit behavior compared with Lucene | Implemented: 62 Docker cases match compilation, ordered prefixes, longest overlapping lookup, segmentation/readings/POS and attribute-access errors; source/entry/surface bounds and retained models are tested |
| Japanese tokenizer and rich token bridge | `uqa-analysis::kuromoji`, shared lattice and token/source owners; model and user rules | NORMAL/SEARCH/EXTENDED, both discard flags, known/unknown/user precedence, penalties, forced backtrace, resegmentation, ordered graph output, all six Japanese morphology attributes and terminal state match reference | Standalone native tokenizer implemented: all 209 Docker cases match, with exact graph/end state and six attributes, bounded resegmentation, retained memory and cancellation; common rich-token/source conversion, generic filters and Korean attribute-absence behavior are verified |
| N-best analysis | `uqa-analysis::kuromoji`; validated tokenizer and bounded candidate representation | Cost and example-derived settings, combined maximum, invalid/empty examples, path deduplication, fixups, deterministic ordering, cancellation and bounded output agree with Lucene | Implemented: 98 Docker cases match signed cost selection, example-derived maximums, grammar/errors and full ordered output; native controls and common graphs pass; Nori and non-positive-cost Japanese calls allocate no N-best arrays. Durable configuration remains in the compilation work unit |
| Default Japanese analyzer | `uqa-analysis`; tokenizer, width stage, rich attributes and pinned profiles | Width, SEARCH with both discard flags, base form, POS stops, exact Japanese stopwords, Katakana stemmer and Java simple lowercase match the complete analyzer oracle | Implemented at the standalone owner: 127 Docker cases cover default/custom chains, individual native/common filters, normalization, malformed user attributes and stream ends; runtime/preparation memory and cancellation tests pass. Common catalog compilation remains separate |
| Optional filters and completion | `uqa-analysis`; rich stream, shared decimal/ownership mechanisms and exact reference resources | Reading/kana/romaji, Japanese numbers, iteration marks, small-kana expansion, completion INDEX/QUERY, keyword behavior, graph/source/end attributes and absent morphology rules match separate component oracles | Small-kana expansion and kana/romaji readings are implemented with complete UTF-16 mapping, context, Ainu pair, keyword, source and control verification. Japanese numbers and scalar/raw UTF-16 normalizers now match 220 independent Docker cases with native/common controls; iteration marks and completion remain; reading romaji and completion romanization use distinct algorithms |
| Normalization and durable compilation | `uqa-analysis`; components, typed resources and profiles | Explicit ordinary Japanese width-plus-simple-lowercase and completion width-only plans; canonical configuration/defaults/resource hashes; snapshot restoration; legacy generic/Nori descriptor bytes and fingerprints unchanged | Standalone ordinary Japanese width-plus-simple-lowercase normalization is implemented and independently verified; durable profile/configuration compilation and completion normalization remain. Document the Rust configuration source migration when it lands |
| Provider and retrieval integration | Existing storage/provider, operators, scoring and analysis owners; compiled Japanese revisions | Complete occurrences, overlap length policy, exact terms, scores and bounds, phrase paths and original-source highlighting agree across Memory/SQLite/redb and relevant search paths | Not implemented; reuse occurrence v2/schema 48 unless an actual representational deficiency is demonstrated |
| SQL lifecycle and runtime controls | `uqa-sql`, `uqa-execution`, existing Engine adapters; compiled revisions and provider support | Diagnostics, normalization, registration/rebinding, independent index/search revisions, prepared queries, savepoints, rollback, sibling sessions, reopen/backup restore and recovery preserve state; cancellation `57014`, allocation failure `53200` | Not implemented |
| Feature isolation and bindings | Cargo owners, facade/CLI/Python/Node.js/WASM; runtime/public contracts | Neither/Nori-only/Kuromoji-only/both compile and execute; normal dependency trees exclude disabled data; same durable SQL scenarios execute through actual bindings and browser IndexedDB reopen | Independent analysis features and exact data-leaf dependency policy are implemented; four actual normal-dependency trees select only enabled dictionaries, and Kuromoji-only owner tests pass. Upper-layer feature forwarding and binding behavior remain |
| Package and final PR acceptance | Existing CI, legal and publication tools; completed runtime/integration | Final-head required CI, owner checks, manual examples, source/wheel/npm/WASM inventory, embedded dictionary bytes, notices, package order and documented upgrade behavior verified | Not implemented; PR remains draft during implementation |

## Logical commits and document updates

Commit mechanical Nori extraction separately from Japanese semantic changes. A runtime unit includes its owning tests, required feature/dependency updates, and current contract documentation. A dictionary unit includes its provenance and package notices. Avoid committing temporary experiments or cleanup-only chains that obscure the final implementation. Push each completed logical unit and update this ledger when its acceptance status changes.

Keep this ledger current instead of appending repetitive chronological reports. The design defines the intended interfaces and invariants; this plan records what is implemented, its dependencies, and outstanding gates; the manual describes verified public behavior. When an implementation decision changes, update the affected design and plan together. Add public manual examples only with their existing compilation/execution checks.

## Compatibility and completion checklist

- [x] Move checked binary I/O, minimized UTF-16 lexicon construction/lookup/enumeration, and connection costs into their analysis owner without reverse dependencies or Cargo graph changes.
- [x] Preserve Nori public dictionary error variants and offsets; pass existing full-model, token/filter, descriptor, cancellation, memory and source-projection tests after the extraction.
- [x] Implement dictionary-independent CJK width conversion with full Lucene text/offset identities, composed source spans, profile-aware descriptors, cancellation and retained memory accounting.
- [ ] Prove Nori packed-byte reproduction and legacy descriptor identity through all remaining shared-resource, token and normalization refactors; keep `UQANORI\0`, its seven sections, version 1 and hash domains unchanged.
- [x] Pin and exhaustively verify the complete Japanese model and resources using Docker; no Rust parity claim is made from source inspection or oracle execution alone.
- [x] Package the native Japanese dictionary, reconstruct all six pinned model streams through the production loader, reject malformed and bounded inputs, and verify the actual data crate archive and attribution inventory.
- [ ] Match full ordered tokenizer/filter/analyzer output, lossless UTF-16, six Japanese attributes, source/graph coordinates, keyword behavior, terminal state and error outcomes, including N-best and completion.
- [ ] Carry one runtime allowance and cancellation callback through input, lattice, alternatives, filters, diagnostics and highlighting; preserve unrelated leases and committed state on every failure.
- [ ] Preserve independent persistent revisions, provider graph/score behavior, original-source highlighting, transaction/savepoint/reopen and real backup restoration.
- [ ] Execute the shared public contract through actual Rust, Python, Node.js and browser WASM artifacts with all four language feature configurations covered at the appropriate owners.
- [ ] Verify final PR-head CI, complete packaged resource/notices inventory, and public/upgrade documentation before marking this work complete.

## Verification and evidence retention

For shared analysis changes, run the owning analysis tests and required Nori reference/model regressions, strict package Clippy, relevant feature configurations, formatting and ownership/dependency checks. Add tests to the owning crate's existing test executable; do not create extra top-level integration targets. Extend to provider, SQL, binding or packaging suites when those boundaries change. Successful compilation is not runtime or parity evidence. Do not repeatedly rerun a passing suite unless a subsequent change or unresolved failure requires it.

Use fixed and seeded differential cases covering compound/inflection ambiguity, unknown group/invoke classes, forced-backtrace boundaries, surrogate handling, halfwidth kana and voiced marks, iteration marks, user CSV corner cases, stopped positions, numeric lookahead, N-best graph geometry, and completion modes. Compare complete outputs and errors against the pinned reference and retain minimized regressions. Test standalone components separately from default analyzer chains and normalization. Never generate expected fixtures from the candidate Rust implementation.

Routine CI gates deterministic semantics, model identities, counts, allocation limits, cleanup, and bounded cancellation work. Uncontrolled workstation/shared-runner timing is not a completion gate. No timing collection is scheduled by this plan. A later timing study requires a stated question, qualified host/noise bound, fixed input, maximum runs, stopping condition and compact result schema before execution; stop if the environment fails qualification.

Track only compact manifests, deterministic bounded fixtures, hashes and conclusions. Export the full model to temporary binary files and compare it as a stream. Keep raw measurement reports outside Git; remove temporary exports after the verified packed artifact and compact evidence exist. Do not accumulate multi-million-line JSON or repeat measurement collection while implementation and release work wait.

## Current verified evidence

Shared dictionary I/O, lexical-rank construction and lookup, section framing, canonical surface/string codecs, costs, Unicode intervals, provenance checks, neutral-model reading and streamed reconstruction belong to `uqa-analysis::morphology`. Language owners retain their bundle schemas, identity domains, attributes, character policies and public errors. Both typed resource owners use the same bounded cache, exact artifact-hash validation and immutable model/user publication; resolvers run outside the cache lock. The combined analyzer resource builder preserves the existing Nori constructor. Failed preparation, alias replacement, reentrancy, concurrent misses, disabled retention and existing live handles are covered by owning tests.

Nori packed bytes remain exactly 9,829,534 bytes, with SHA-256 `0d920523991ed60909972d85df630c65747ff5d49e1138e4998ca39f0079538d` and semantic identity `ee85ee5796c706ea7288e44e64c7f58ba7bd22cea5683c1f4196c29a2435d74a`. Repacking and complete reconstruction preserve every original model value and fixture hash. The common Viterbi owner now serves both languages: ordered position traversal, single-node frontier commits, strict first-cheapest forced backtraces, pruning/rebasing and EOS selection. Nori retains its matching, spaces, unknown grouping, per-forward overlap bound, polling and emission. No N-best arrays are allocated for either single-path consumer.

The [Japanese reference tools](../../tests/parity/kuromoji/README.md) reproduce all nine dictionary resources from 29 pinned patched IPADIC inputs and verify the complete six-stream model independently in Docker. Coverage includes 325,872 surfaces, 392,127 system words, 41 unknown words across 12 classes, 1,731,856 matrix cells, all UTF-16 character definitions and Java code-point properties, 109 stopwords, 27 stop tags and 329 ordered completion mappings. Source regeneration works beneath ordinary and inherited Git contexts without changing the enclosing repository. The native bundle contains 6,654,306 bytes: artifact SHA-256 `bd3dd53f609006e72d0aa6e94ec6067400ac2be328ca0c39263ab36c3197aa97`, semantic identity `dd2691ffee7f5a0d2c29c1a6e66dee249116767208a8cd8e783a6aa479100c8f`. The loader reconstructs every exported byte and rejects malformed, truncated, oversized and hash-invalid inputs before publishing a model.

The independent Japanese data crate is `no_std` with no runtime dependencies. Its actual archive packages and compiles offline with complete Lucene, IPADIC and JDK attribution, including the unchanged terminal IPADIC notice bytes. The legal/README synchronizer now includes the Japanese data and analysis APIs in its canonical templates and is idempotent. User dictionaries match 62 Docker compilation/lookup/attribute cases, including CSV quirks, duplicate rejection, empty fields, overlapping longest phrases and raw UTF-16 segments. Both languages share preparation bounds and prefix traversal; Japanese POS is stored once per phrase. The existing 38-case Nori user oracle is unchanged.

The common CJK width filter matches all 1,112,064 Unicode scalars, 1,672 voiced-mark combinations and 19 fixed examples against pinned Lucene. Its source-map composition, retained memory and cancellation tests pass. The committed oracle retains two exhaustive hashes and the bounded examples; this component does not implicitly run inside the standalone Japanese tokenizer.

`JapaneseTokenizer` implements NORMAL, SEARCH and EXTENDED with both discard flags, user/system precedence, exact unknown grouping, Japanese penalties, alternate segmentation, compound graph emission and extended unknown unigrams. It borrows the immutable model and selected user rules while each invocation owns shared traversal and Japanese resegmentation state. All 209 tokenizer reference cases match complete output hashes or expected errors: every term unit, position increment/length, offset, keyword value, six independent attributes and stream-end value. The fixture set is 393,212 bytes; snapshots expand at most 12 tokens, and longer streams retain full-output hashes and counts. Four owning tests additionally verify exact retained capacities, shared-budget isolation, byte/count/arc/work failures, bounded cancellation, malformed user attributes after partial processing, wrong-model rules and reuse after failure. Common token conversion retains the complete graph and all six attributes, including absent/empty distinctions and raw split-surrogate terms.

N-best adds an independently bounded alternative lattice under `kuromoji/tokenizer/nbest`, with ordered left/right costs, stable span deduplication and graph-length fixups. Pending records defer dictionary attributes until actual emission; example estimation observes graph costs without reading attributes. All 98 Docker cases agree, including signed cost boundaries, 86 complete streams and 12 configuration/emission errors, example grammar and first-occurrence selection, forced fragments, users, extended terms and supplementary/raw units. Their inputs, outputs and manifest total 173,807 bytes. Four owner tests verify the oracle and common graph conversion, exact retained allocations, count/work/byte limits, cancellation and immutable reuse; non-positive costs retain single-path allocation behavior. The example allowance covers all probes in one preparation call. Nori uses no-op traversal hooks and acquires no alternative arrays.

The native token bridge in `token/native.rs` owns source correction, graph validation and reservation transfer for both languages. A private tagged morphology value preserves the existing `korean_morphology` key and accessor while exposing `JapaneseMorphology` through a separate optional key and accessor. Eight new owner tests cover exact serialization and moved attribute buffers, the pinned compound stream, chained HTML/width source edits, generic synonym/lowercase/gram/stop filters, raw token units, independent copies, invalid source/graph inputs, allocation/cancellation rollback and Korean filters over Japanese attributes. Mapped Japanese calls initialize private coordinate caches and preserve borrowed input on failure. Source maps and coordinates keep their original shared owners after view destruction.

`morphology::filter` now owns the existing generic filter-token contract, reserved streams, source-span coverage, polling and pinned simple-lowercase loop. `token/filter.rs` supplies the common-token adapter. Nori retains POS/reading/number rules, profile selection, diagnostics and native mutation adapters; its complete 456-test analysis suite and strict Clippy pass after this mechanical extraction. Japanese base-form, POS, stopword, Katakana-stem and simple-lowercase filters now use the shared owner. Composition-only methods remain in a separate contract now consumed by both language number filters; the Japanese feature enables the shared machinery through its number policy.

`JapaneseAnalyzer` applies CJK width, the selected tokenizer mode with both default discard flags, base form, exact Japanese POS and word stops, Katakana stemming and pinned simple lowercase. Its custom chains retain compiled lookup buffers separately from runtime output; normalization always applies only width and simple lowercase. The default and optional filter corpus now matches 195 Docker observations: 175 complete streams, 14 normalized strings and six errors. Their inputs, outputs and provenance total 314,278 bytes; snapshots expand at most 12 tokens and 8,192 serialized UTF-16 units, otherwise retaining the full hash and token count. The same kernel runs on native and common tokens, preserving holes, graph lengths, optional attributes, keywords, original offsets and opaque terminal state. Invalid user-field access is retained privately until a filter actually requests that attribute or a token becomes an observable result; a preceding stop can remove the token without raising an unused POS error. Eleven owner tests cover parity, exact native leases, prepared lookups, source lifetime, separate normalization, count/byte limits, cancellation, immutable recovery and rejection of invalid Japanese terminal attributes promoted by later Korean number composition.

`JapaneseFilter::HiraganaUppercase` and `KatakanaUppercase` share reserved term mutation with the other Japanese filters. They preserve all non-term attributes and original source spans, including when `ㇷ゚` contracts to `プ`, and apply to keyword tokens and tokens without Japanese morphology. The 23 added Docker cases include all 65,536 UTF-16 units for each filter, every pinned mapping, contraction boundaries, unpaired units, stacked/trailing tokens and custom analyzer chains. Existing 127 complete result hashes remain unchanged. Additional controls verify corrected HTML source lifetime, separate normalization, contraction-aware output limits, partial-write cancellation and retained-budget recovery.

`JapaneseFilter::ReadingForm` keeps kana and modified-Hepburn romaji modes distinct from completion. It checks the reading field only when accessed, preserves empty readings, derives a Katakana fallback only for absent readings, and retains raw units and all non-term attributes. Romanization uses the pinned 86 single, 107 pair and 26 triple replacements plus exact gemination/nasal/long-mark rules; it counts a bounded borrowed traversal before reserving one output buffer and runs the same controlled traversal to emit. The 45 added Docker cases include every UTF-16 fallback unit, all 9,216 Katakana pairs with four suffix contexts, spacing/combining marks, known/unknown/user tokens, N-best graphs and lazy reading errors; all prior 150 observations remain identical. Owner tests cover native lease accounting, mapped source lifetime, normalization independence, exact output limits, cancellation at every callback in counting/emission/conversion, byte failures and immutable reuse.

`morphology::decimal` owns the existing nonnegative coefficient deque, powers of ten, scale alignment, addition, trimming and UTF-16 formatting. A private context contract supplies the language-owned budget, cancellation and typed size/overflow errors. No common module imports a language module. Shared numeric modules are enabled when either language is selected. The mechanical decimal extraction passed the complete 470-test analysis/data/documentation suite and all feature Clippy configurations before Japanese number integration.

The pinned Japanese and Korean number filters have byte-identical token-composition methods; their normalization/parser code also matches after removing comments and normalizing language-symbol method names. `morphology::number` therefore owns the existing prefix traversal, malformed-decimal distinction, reserved lookahead, abort/replay behavior and terminal propagation. `nori/number/policy.rs` supplies its existing digit/exponent tables, decimal/separator predicates, large-power split, morphology accounting, typed bounds and normalization. This mechanical extraction preserves native/common output, memory leases, polling and Nori diagnostics; all 470 existing tests and feature Clippy checks pass. The Japanese policy and public number filter now consume the same owner without copying either algorithm.

`JapaneseFilter::Number` and the scalar/raw UTF-16 normalizers now preserve the pinned Japanese numeric-prefix grammar, malformed-input fallback, exact arithmetic, stacked-token abort/replay, keyword continuation, lookahead metadata and terminal state. The separate 220-case Docker corpus contains 134 prefix examples, 81 complete streams, four errors and one matrix with every UTF-16 unit alone and between digits (131,072 normalized inputs); input/output/provenance total 180,667 bytes. Large strings use compact repeated inputs and complete output hashes/counts, including a 131,075-digit integer and a fraction beyond 17,000 places. Existing 195 filter observations remain byte-identical after extending their shared Java transport. Seven owner tests cover this oracle, exact native/common/scalar/raw reservations, every cancellation callback, numeric/input/token/attribute limits, original HTML/width source ownership, normalization independence and invalid terminal-attribute promotion. The reference CI includes independent amd64 replay of the new numeric corpus.

The full analysis/data/documentation suite passes 479 tests: 226 library tests, 221 integration tests, two data tests and 30 documentation tests. The complete Kuromoji-only suite passes 373 tests: 185 library tests, 164 integration tests and 24 documentation tests. Strict Clippy passes with both tool features and across all four runtime all-target configurations. Independent feature CI now runs the complete Kuromoji-only owner and common integration suites, including shared source reservation lifetime, and normal dependency trees select only requested dictionaries. Ownership policy permits 109 runtime edges across 33 crates; Engine retains 103 adapters and 1,244 Engine-free leaves, and integration coverage remains 218 sources in 22 targets. Required final-head remote CI, including amd64 Docker references, is recorded in the PR; earlier successful heads do not certify subsequent code. No timing collection is part of this evidence.
