# Kuromoji analyzer implementation plan

Status: Active implementation in [PR #108](https://github.com/cognica-io/uqa-engine/pull/108), based on UQA 0.3.0. Nori and Kuromoji use the dictionary primitives, section framing, surface ranges, string codecs, Unicode intervals, provenance primitives and offline reconstruction in `uqa-analysis::morphology`. The common `cjk_width` character filter is implemented and verified against pinned Lucene text and offsets. The Japanese source rebuild, complete streamed model, native bundle/loader and immutable data crate are implemented and verified. Japanese user dictionaries are implemented and compared with the pinned reference. Resource caches, tokenization, analyzer integration, and binding delivery are incomplete. This plan does not certify Japanese tokenizer/analyzer parity or performance. The tagged 0.3.0 release is independent of this PR.

Update rule: Update this plan with every logical implementation unit that changes ownership, interfaces, reference inputs, completed work, remaining work, or acceptance evidence. Update the PR description and push each verified commit. Record final remote CI outcomes in the PR so recording a successful run does not change the commit it checked. A completed helper or passing constructor is not completion of its larger work item.

## Objective and constraints

Implement the complete [Kuromoji design](../design/kuromoji-analyzer.md): Lucene 10.5.1 Japanese tokenization, all documented analysis components, separate normalization and completion, immutable revisions, existing retrieval consumers, and actual Rust/Python/Node.js/WASM delivery. Preserve generic and Nori results, public Korean attributes, dictionary identities, canonical descriptors, and provider behavior while sharing proven mechanisms.

The reference is Lucene commit `64ce863a2bea79c69c19c4d56268c26710ff0ff9`. The design records the inspected official artifacts and exact defaults. Every JVM invocation, including Java compilation, resource regeneration, model export, and differential verification, must run in Docker. Production execution and packaging must not download dictionaries or require a JVM.

Before each implementation or move, inspect the affected Cargo manifests and enabled features, the [dependency policy](../../scripts/workspace-dependency-policy.json), the [manual ownership map](../manual/internals/04-analyzer-pipeline.md), and existing implementations/tests. Keep analysis algorithms in `uqa-analysis`; the new data crate owns immutable bytes and notices. Engine only supplies existing state, session, transaction, and retained-resource adapters. A private shared module must not import a language module, and Kuromoji must not import Nori.

## Execution order and acceptance ledger

The rows identify reviewable work units and their dependencies. Independent reference preparation can run while source checks or the separate release are pending. Runtime work follows its required representations and resources; do not register an incomplete Japanese analyzer to make an integration test pass.

| Work unit | Owner and dependencies | Completion evidence | Current state |
| --- | --- | --- | --- |
| Shared dictionary primitives | `uqa-analysis::morphology`; existing Nori codec and lexicon | Nori uses one shared reader/writer, lexical-rank builder/lookup, and cost matrix; original errors, full model hashes, fixtures, and controls pass | Implemented and locally verified in `a28199b6`; details below |
| CJK width character filtering | `uqa-analysis::char_filter`; existing source edits and memory owner | Restricted fullwidth ASCII/halfwidth kana conversion, voiced-mark composition, exact corrected UTF-16/UTF-8 spans, chained edits, cancellation and retained budgets agree with the pinned character-filter oracle | Implemented and locally verified: all 1,112,064 scalars, 1,672 voiced-mark combinations, 19 fixed examples, source composition and control tests; Docker regeneration is included in reference CI |
| Pinned Japanese reference and model export | `tests/parity/kuromoji`; pinned Docker image, jars, source resources | Complete input inventory and hashes; source-generation recipe; streamed export and read-only comparison of every surface, ordered word, attribute, matrix cell, unknown class, Unicode property, stop set, and completion mapping | Implemented: all 29 patched IPADIC inputs reproduce nine exact jar resources; complete six-file model export and separate read-only Docker verification pass; Nori retains its original model identity through shared Python orchestration |
| Remaining shared morphology mechanisms | `uqa-analysis::morphology`; correspondence between both language implementations | Shared Unicode profiles, frame codec, resource/cache ownership, bounded lattice, and exact-decimal machinery where both consumers need them; Nori byte/descriptor/control compatibility remains unchanged | Dictionary I/O, frame validation, surface ranges, strings, Unicode intervals, provenance primitives and offline reconstruction are extracted and used by Nori; user preparation limits and prefix traversal serve both languages; shared immutable resource/cache mechanics now serve Nori, with the Japanese typed wrapper next; bounded lattice and decimal integration remain |
| Japanese bundle, loader, and resources | `uqa-analysis::kuromoji`, `uqa-kuromoji-data`; complete model and common codecs | Separate schema and identity; exhaustive reconstructed model equality; malformed/truncated/oversized/hash-invalid inputs rejected before publication; bounded resource caches and complete licenses | Bundle schema, native packer, validated immutable loader, exhaustive six-stream verifier and data crate are implemented; actual data archive compiles and passes resource/license checks. Typed resource owners, resolvers and bounded caches are the next implementation unit |
| Japanese user dictionary | `uqa-analysis::kuromoji`; model and shared lexicon | Exact CSV quoting, comments, duplicates, segmentation/readings/POS, word order/IDs, invalid input and preparation-limit behavior compared with Lucene | Implemented: 62 Docker cases match compilation, ordered prefixes, longest overlapping lookup, segmentation/readings/POS and attribute-access errors; source/entry/surface bounds and retained models are tested |
| Japanese tokenizer and rich token bridge | `uqa-analysis::kuromoji`, shared lattice and token/source owners; model and user rules | NORMAL/SEARCH/EXTENDED, both discard flags, known/unknown/user precedence, penalties, forced backtrace, resegmentation, ordered graph output, all six Japanese morphology attributes and terminal state match reference | Not implemented |
| N-best analysis | `uqa-analysis::kuromoji`; validated tokenizer and bounded candidate representation | Cost and example-derived settings, combined maximum, invalid/empty examples, path deduplication, fixups, deterministic ordering, cancellation and bounded output agree with Lucene | Not implemented; Nori and Japanese single-path calls must not allocate N-best state |
| Default Japanese analyzer | `uqa-analysis`; tokenizer, width stage, rich attributes and pinned profiles | Width, SEARCH with both discard flags, base form, POS stops, exact Japanese stopwords, Katakana stemmer and Java simple lowercase match the complete analyzer oracle | Not implemented |
| Optional filters and completion | `uqa-analysis`; rich stream, shared decimal/ownership mechanisms and exact reference resources | Reading/kana/romaji, Japanese numbers, iteration marks, small-kana expansion, completion INDEX/QUERY, keyword behavior, graph/source/end attributes and absent morphology rules match separate component oracles | Not implemented; reading romaji and completion romanization remain distinct algorithms |
| Normalization and durable compilation | `uqa-analysis`; components, typed resources and profiles | Explicit ordinary Japanese width-plus-simple-lowercase and completion width-only plans; canonical configuration/defaults/resource hashes; snapshot restoration; legacy generic/Nori descriptor bytes and fingerprints unchanged | Not implemented; document the planned Rust configuration source migration when it lands |
| Provider and retrieval integration | Existing storage/provider, operators, scoring and analysis owners; compiled Japanese revisions | Complete occurrences, overlap length policy, exact terms, scores and bounds, phrase paths and original-source highlighting agree across Memory/SQLite/redb and relevant search paths | Not implemented; reuse occurrence v2/schema 48 unless an actual representational deficiency is demonstrated |
| SQL lifecycle and runtime controls | `uqa-sql`, `uqa-execution`, existing Engine adapters; compiled revisions and provider support | Diagnostics, normalization, registration/rebinding, independent index/search revisions, prepared queries, savepoints, rollback, sibling sessions, reopen/backup restore and recovery preserve state; cancellation `57014`, allocation failure `53200` | Not implemented |
| Feature isolation and bindings | Cargo owners, facade/CLI/Python/Node.js/WASM; runtime/public contracts | Neither/Nori-only/Kuromoji-only/both compile and execute; normal dependency trees exclude disabled data; same durable SQL scenarios execute through actual bindings and browser IndexedDB reopen | Independent analysis features and exact data-leaf dependency policy are implemented; four actual normal-dependency trees select only enabled dictionaries, and Kuromoji-only owner tests pass. Upper-layer feature forwarding and binding behavior remain |
| Package and final PR acceptance | Existing CI, legal and publication tools; completed runtime/integration | Final-head required CI, owner checks, manual examples, source/wheel/npm/WASM inventory, embedded dictionary bytes, notices, package order and documented upgrade behavior verified | Not implemented; PR remains draft during implementation |

## Logical commits and document updates

Commit mechanical Nori extraction separately from Japanese semantic changes. A runtime unit includes its owning tests, required feature/dependency updates, and current contract documentation. A dictionary unit includes its provenance and package notices. Avoid committing temporary experiments or cleanup-only chains that obscure the final implementation. Push each completed logical unit and update this ledger when its acceptance status changes.

Keep this ledger current instead of appending repetitive chronological reports. The design defines the intended interfaces and invariants; this plan records what is implemented, its dependencies, and outstanding gates; the manual describes verified public behavior. When an implementation decision changes, update the affected design and plan together. Add public manual examples only with their existing compilation/execution checks.

## Compatibility and completion checklist

- [x] Move checked binary I/O, minimized UTF-16 lexicon construction/lookup/enumeration, and connection costs into their analysis owner without reverse dependencies or Cargo graph changes.
- [x] Preserve Nori public dictionary error variants and offsets; pass existing full-model, token/filter, descriptor, cancellation, memory and source-projection tests after the extraction.
- [x] Implement dictionary-independent CJK width conversion with full Lucene text/offset identities, composed source spans, profile-aware descriptors, cancellation and retained memory accounting.
- [ ] Prove Nori packed-byte reproduction and legacy descriptor identity through all remaining shared-resource, token and normalization refactors; keep `UQANORI\0`, its seven sections, version 1 and hash domains unchanged.
- [x] Pin and exhaustively verify the complete Japanese model and resources using Docker; no Rust parity claim is made from source inspection or oracle execution alone.
- [x] Package the native Japanese dictionary, reconstruct all six pinned model streams through the production loader, reject malformed and bounded inputs, and verify the actual data crate archive and attribution inventory.
- [ ] Match full ordered tokenizer/filter/analyzer output, lossless UTF-16, six Japanese attributes, source/graph coordinates, keyword behavior, terminal state and error outcomes, including N-best and completion.
- [ ] Carry one runtime allowance and cancellation callback through input, lattice, alternatives, filters, diagnostics and highlighting; preserve unrelated leases and committed state on every failure.
- [ ] Preserve independent persistent revisions, provider graph/score behavior, original-source highlighting, transaction/savepoint/reopen and real backup restoration.
- [ ] Execute the shared public contract through actual Rust, Python, Node.js and browser WASM artifacts with all four language feature configurations covered at the appropriate owners.
- [ ] Verify final PR-head CI, complete packaged resource/notices inventory, and public/upgrade documentation before marking this work complete.

## Verification and evidence retention

For shared analysis changes, run the owning analysis tests and required Nori reference/model regressions, strict package Clippy, relevant feature configurations, formatting and ownership/dependency checks. Add tests to the owning crate's existing test executable; do not create extra top-level integration targets. Extend to provider, SQL, binding or packaging suites when those boundaries change. Successful compilation is not runtime or parity evidence. Do not repeatedly rerun a passing suite unless a subsequent change or unresolved failure requires it.

Use fixed and seeded differential cases covering compound/inflection ambiguity, unknown group/invoke classes, forced-backtrace boundaries, surrogate handling, halfwidth kana and voiced marks, iteration marks, user CSV corner cases, stopped positions, numeric lookahead, N-best graph geometry, and completion modes. Compare complete outputs and errors against the pinned reference and retain minimized regressions. Test standalone components separately from default analyzer chains and normalization. Never generate expected fixtures from the candidate Rust implementation.

Routine CI gates deterministic semantics, model identities, counts, allocation limits, cleanup, and bounded cancellation work. Uncontrolled workstation/shared-runner timing is not a completion gate. No timing collection is scheduled by this plan. A later timing study requires a stated question, qualified host/noise bound, fixed input, maximum runs, stopping condition and compact result schema before execution; stop if the environment fails qualification.

Track only compact manifests, deterministic bounded fixtures, hashes and conclusions. Export the full model to temporary binary files and compare it as a stream. Keep raw measurement reports outside Git; remove temporary exports after the verified packed artifact and compact evidence exist. Do not accumulate multi-million-line JSON or repeat measurement collection while implementation and release work wait.

## Current verified evidence

Commit `a28199b6` extracts dictionary primitives into `crates/uqa-analysis/src/morphology/` and migrates all Nori consumers and owning tests. It adds no crate or Cargo feature/dependency edge. The algorithms and encoded field order are unchanged; Nori translates shared structural errors into its existing public error variants. Its bundle frame/schema, Korean character tables and morphology interpretation remain language-owned.

The complete analysis/data run passed 169 library tests, 221 analysis integration tests, one data archive test and 20 documentation tests. This includes reconstruction against every pinned neutral-model hash, embedded bundle hash and dictionary identity, existing generic/Nori descriptors, native/common output, source mapping, memory and cancellation coverage. After extracting the neutral character-table reader to satisfy the existing function-size lint, the affected Nori tests passed again: 34 library tests and 51 integration tests. This is functional evidence, not a throughput measurement.

```sh
cargo test -p uqa-analysis -p uqa-nori-data --features uqa-analysis/nori-tools --locked
cargo test -p uqa-analysis --features nori-tools --locked nori
cargo clippy -p uqa-analysis --features nori-tools --all-targets --locked -- -D warnings
cargo clippy -p uqa-analysis --no-default-features --all-targets --locked -- -D warnings
python3 scripts/check-workspace-dependencies.py
python3 scripts/check-workspace-dependencies.py --staged
python3 scripts/check-engine-capabilities.py
python3 scripts/check-integration-test-harnesses.py
python3 scripts/check-rust-file-lines.py
git diff --cached --check
```

All listed checks passed for that implementation unit; `cargo fmt --all` was applied. The dependency policy reports 108 runtime edges across 32 crates; Engine ownership reports 103 adapters and 1,244 Engine-free leaves; integration coverage remains 217 sources in 21 targets. Remote CI for implementation commits is tracked in [PR #108](https://github.com/cognica-io/uqa-engine/pull/108). Earlier documentation-only CI does not certify runtime changes.

The CJK width unit adds seven owning unit tests and one executed API example. Its complete analysis/data run passed 176 library tests, 221 integration tests, one data test and 21 documentation tests. Strict analysis Clippy passes with `nori-tools` and with neither language feature. The [Docker reference](../../tests/parity/cjk_width/README.md) was generated from the pinned jars and then independently reverified offline on `linux/arm64`; Rust matches every emitted unit and corrected UTF-16 boundary. The retained JSON contains only two exhaustive hashes and 19 fixed examples. Shared Docker-helper changes pass the existing 106 Nori script unit tests; the CI change passes 23 pre-merge policy tests. Ownership, dependency, integration-harness, file-size and hygiene checks pass. Final remote CI, including the manual SQL harness and amd64 Docker verification, remains an acceptance gate recorded in the PR.

The [Japanese reference tools](../../tests/parity/kuromoji/README.md) reproduce all nine Lucene dictionary resources from 29 IPADIC source files with the exact upstream EUC-JP patch and builder recipe. The complete model covers 325,872 surfaces, 392,127 system words, 41 unknown words across 12 classes, 1,731,856 matrix cells, all UTF-16 character definitions and Java code-point properties, 109 stopwords, 27 stop tags and 329 ordered completion mappings. Export checks every field against Lucene, and a second Docker invocation verifies the existing files read-only. The three committed JSON manifests total less than 27 KiB; the full model is temporary binary data.

The shared Python extraction and model runner preserve the original Nori Java source and every Nori manifest/hash. Read-only Docker verification of the existing Nori model passes after this refactor: 774,582 surfaces, 816,283 system words, 14 unknown entries and 10,292,646 costs. The existing required reference CI job now includes exact Japanese source regeneration, export and read-only verification on amd64. The 48 relevant Python source/model/reference and pre-merge policy tests pass, along with dependency, Engine ownership, integration topology and file-size checks. Tokenizer, filter and analyzer output oracles remain part of their own upcoming implementation units.

The mechanical extraction moves bounded section framing and decoding, canonical surface ranges and UTF-8 string pools, Unicode interval construction/validation, structural provenance primitives, bounded neutral input reads and streamed reconstruction hashing into `uqa-analysis::morphology`. Nori retains its schema constants, semantic hash domain, typed identity, Korean words/characters, public errors and reference manifests. No Cargo feature or dependency edge is added by this extraction. Japanese loader and packer work is kept in its following implementation commit, so this mechanical change can be reviewed independently.

This extraction passes the complete existing 419 analysis/data/unit/integration/documentation tests and strict Clippy with `nori-tools` and with no language feature. Repacking the complete neutral Nori model produces exactly the existing 9,829,534-byte bundle, verified by byte comparison: artifact SHA-256 `0d920523991ed60909972d85df630c65747ff5d49e1138e4998ca39f0079538d`, semantic identity `ee85ee5796c706ea7288e44e64c7f58ba7bd22cea5683c1f4196c29a2435d74a`. Dependency, Engine ownership, integration topology, file-size and hygiene checks pass. This is deterministic compatibility verification; no timing collection is performed.

The native Japanese [bundle specification](../design/kuromoji-bundle-format.md) records the implemented eight-section `UQAKURO\0` version 1 schema and its separate identity domain. `KuromojiDictionary::from_bytes` publishes one immutable validated model, with exact surface/word queries, six independent attributes, unknown classes, costs, Unicode properties, stop sets and ordered completion mappings. It shares codecs with Nori and imports no Korean module. The offline packer reloads its output through that loader and reconstructs every byte of the six pinned neutral streams into their verified hashes. The bundle contains 6,654,306 bytes, with artifact SHA-256 `bd3dd53f609006e72d0aa6e94ec6067400ac2be328ca0c39263ab36c3197aa97` and semantic identity `dd2691ffee7f5a0d2c29c1a6e66dee249116767208a8cd8e783a6aa479100c8f`.

The dictionary unit passes the full 429-test analysis/data/documentation run, including complete Japanese and Nori model reconstruction. After tightening the offline aggregate string limit, all nine Japanese owner tests pass with Kuromoji alone, including its new cross-resource limit regression. Malformed coverage includes every truncated frame, trailing data, wrong schema/hash, rehashed invalid word/character/Unicode values, missing completion alternatives, UTF-16 ordering, inconsistent provenance counts and all loader limits without invalidating retained models. Strict Clippy passes for the combined configuration, Kuromoji alone and neither language. Repacking both complete models through the final shared code reproduces the exact embedded Japanese and Korean bytes. The actual `uqa-kuromoji-data` archive packages and compiles offline; its complete source and archive resource/license checks pass. The original IPADIC notice bytes are preserved.

The new `no_std` data leaf has no runtime dependencies. The policy permits its single incoming edge from analysis and exact transitive closures: 109 runtime edges across 33 crates. Four actual Cargo normal-dependency trees select neither, Nori, Kuromoji or both data crates as requested; the owning crate remains the only implementation location. Dependency, Engine ownership (103 adapters and 1,244 Engine-free leaves), integration topology (218 sources in 22 targets), file-size and hygiene checks pass, together with 15 repository-policy and 23 pre-merge CI tests. Required CI now executes both embedded models and bundle tools, independent Japanese tests, feature isolation, strict Clippy and the declared MSRV. Remote results are recorded against their exact commit in the PR; prior successful runs do not certify this newer unit.

The Japanese user dictionary adds 62 fixed Docker cases covering complete compilation outcomes, ordered prefixes, overlapping longest matches, UTF-16 segmentation, reading/POS and absent or invalid morphology access. The native owner matches those outputs, validates selected-model contexts, bounds original source/entries/surfaces, retains source/model identity and keeps failed preparation separate from existing dictionaries. Shared user limits preserve the public Nori fields/defaults, and both languages use the common prefix traversal. POS is stored once per phrase instead of once per segment. No Cargo edge changes.

The full suite passes 188 library tests, 221 integration tests, two data archive tests and 23 documentation tests (434 total); strict combined-feature Clippy passes. The existing 38-case Nori user oracle replays unchanged through the extracted language-independent fixture runner. Its source/output hashes remain unchanged; five fixture/provenance tests and 23 pre-merge policy tests pass. Source attribution and license inventory, dependency/Engine ownership, integration topology, file-size and hygiene checks pass. Runtime-only builds exposed an encoder-only Nori import left by the earlier extraction; that import now stays at its use site, and the feature-isolation checker strictly compiles all four runtime configurations as well as checking their normal dependency trees. All four pass. Remote CI is still required for the new pushed head, including the added amd64 Japanese user oracle.

Japanese source regeneration treats extracted inputs independently of enclosing Git metadata and inherited repository paths. The same complete rebuild now passes beneath the checkout's `target/` directory, reproducing all 29 patched source identities and nine exact jar resources. A regression test covers ordinary and inherited Git contexts while proving the enclosing file/index remain untouched; all eight Japanese source/model policy tests pass. Source, patch, builder and model manifests are unchanged. This closes the reference-job failure observed on the earlier native-dictionary CI run without weakening its exact target or hash validation.

The resource extraction moves byte ownership, retention limits/statistics, requested/declared hash checks, resolver lock boundaries, serialized immutable publication and model-scoped user-source interning into `morphology::resources`. Nori retains its public request/hash/resolver/model/handle types and exact errors. Each owner keeps independent limits and typed cache state; names still resolve on every call, exact hashes reuse retained content, callbacks execute outside the lock, and failed work publishes no entry. The existing bounded cache continues to own eviction. Japanese typed resources are intentionally added in the following semantic unit.

All 434 existing analysis/data/documentation tests pass after this mechanical extraction, including simultaneous misses, reentrant resolvers, alias changes, strict limits, corruption/recovery, retained handles, empty user snapshots and legacy descriptor behavior. Strict combined-tool Clippy passes. The feature checker now includes every target in each of the four runtime configurations, covering test-only compilation as well as the production library; all pass. Two offline helper gates were narrowed to their real tool/test consumers after CI exposed unused functions in Nori-only tests. No public behavior, dictionary bytes, descriptor identity or Cargo dependency edge changes.
