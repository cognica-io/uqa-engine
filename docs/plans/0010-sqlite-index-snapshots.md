# Fixed unbound SQLite inverted-index snapshots

Status: Active; implementation and local acceptance on merged main `e8e6e3c0` are complete. Final current-base CI, review and merge remain.

Issue #124 affects the direct, unbound `SQLiteInvertedIndex` API. Its physical snapshot retained analyzer bindings and a clone of the live managed connection, so later replacement or deletion changed an earlier reader. Native MVCC snapshots already retain their selected committed/private record owner. The correction replaces the physical capture while preserving that native path.

## Ownership and implementation

`uqa-storage-sqlite` owns the physical SQLite read boundary and occurrence-row projection. Capture current rows, legacy format presence and field-specific skip/block-max tables inside one existing `read_snapshot` boundary, including writes private to the capturing connection. Reuse the native occurrence row codec for common key/value projections. `uqa-storage::KeyValueRead::retain` already copies selected bytes under a `StorageReadControl`; `KeyValueInvertedIndex::snapshot_from_read` already retains analyzer revisions and supplies immutable posting/statistics readers. Reuse these interfaces instead of reanalyzing unavailable source text, copying unrelated database tables or adding snapshot algorithms to Engine.

The existing SQLite crate dependency direction permits this adapter. Native SQLCipher and Emscripten SQLite use the current `blob`, `cache` and `functions` features; no backup/serialization feature or additional crate is needed. Copied key/value buffers, descriptors, analyzer metadata and retained readers keep the invoking allowance until the final nested reader drops. SQLite page/cache allocations retain their existing provider ownership.

## Acceptance

First preserve the original replacement/deletion failure as a unit test in SQLite's existing harness. Then verify complete occurrence graphs, document and field statistics, metadata, compiled analyzer identity, skip/block-max values, private-write capture and rollback, source closure, file reopen, empty and legacy-format captures, cancellation, quota rejection and complete reservation release. A failed capture must leave earlier readers and source records unchanged. Nested captures must retain the original data and allowance rather than reopening the live connection. Verify malformed rows through the existing typed error path.

Run focused SQLite tests in Linux Docker and the repository ownership, dependency, formatting and harness checks. Keep runtime logs outside Git, update the storage manual and HISTORY, and commit/push the correction as a separate issue branch. The original replacement/deletion regression failed with retained document frequency 0 instead of 2 on the parent source. The correction passes all 741 SQLite unit tests before the final auxiliary ownership refinement; final focused acceptance passes all 40 inverted-index unit tests and 23 integration cases, including the auxiliary ownership refinement and legacy rows without a format marker. Strict all-target SQLite Clippy, formatting, dependency policy (109 runtime edges across 33 crates), Engine ownership (104 adapters), the single integration harness rule (218 sources in 22 targets), Rust headers and the 1,000-line ceiling all pass. The initial implementation passed local and supported-platform acceptance; the current integration acceptance is recorded below. Final current-base CI, review and merge remain. Logs remain in ignored or temporary output, and no timing acceptance is claimed.

Supported-platform CI passes on pre-rebase product commit `e4c84e4c`: [Rust](https://github.com/cognica-io/uqa-engine/actions/runs/36031544957) and [JavaScript/WASM](https://github.com/cognica-io/uqa-engine/actions/runs/36031549639), including all sixteen Linux/macOS Rust test shards and all unchanged native/WASM Nori allocation/output gates. Those runs certify the earlier base. The rebased implementation preserves the same inverted-index product files and owning manifest, confirmed by an empty path-scoped diff against `125728bf`; current-base acceptance is separate.

## Current integration acceptance

PR #154 is merged and its branch is removed. This branch is rebased directly on main `e8e6e3c0`; only the plan-list conflict required resolution, preserving the completed notification and compressed-container records. An empty diff against `f93e495b` for the inverted-index implementation, its manifest and workspace manifests confirms that the snapshot product code is unchanged.

Current-base Linux Docker acceptance passes forty inverted-index unit tests and seventy-eight integration tests: twenty-three analyzer cases, one occurrence-scoring case, twenty-seven native/physical occurrence cases and twenty-seven skip/block-max cases. They include memory, plain/encrypted files, every native file mode, close/reopen, source mutation and rollback, original allowance retention, cancellation and failed-capture cleanup. Strict all-target SQLite Clippy and repository dependency, Engine ownership and single-harness checks pass. Compact evidence is `/private/tmp/uqa-snapshot-final-main-check.log`; raw reports remain outside Git. No timing or allocation benchmark was rerun.

The earlier platform CI certifies the earlier source; final platform acceptance and review for the current main integration are tracked in the issue PR. Only this correction is being opened for review; #130 remains separate until this PR is merged and its branch cleaned up.
