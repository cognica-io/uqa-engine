# Concurrent storage transaction implementation plan

Status: Implementation in progress. Common storage owns versioned records, private command views/savepoints, evaluated shared-index merges and retained commit outcomes. Default native SQLite, SQLite Key/Value and redb now select logical Engine transactions without lifetime provider writer ownership. Native initial sessions publish legacy semantic restoration, catalog migration and baseline conversion together. The basic PostgreSQL independent-writer schedules run through actual SQL; general definition/constraint coordination, serializable histories, durable publication recovery, reclamation/spill and complete binding/platform acceptance remain open. Baseline: UQA 0.3.6, commit `24e464f7709cd5d942df8d0aeef5d522716b1bfb`, inspected on 2026-09-16. The [design](../design/concurrent-storage-transactions.md) defines the complete target and the [storage manual](../manual/internals/03-storage.md) defines implemented behavior.

The storage transaction contract now reports `StorageTransactionModel` together with session affinity. Catalog/backend validation rejects mismatched models or database incarnations and requires a matching reported affinity for versioned sessions; custom providers default to serialized ownership. Catalog sequence provenance identifies private definition/value records and follows savepoint undo, allowing execution to choose autonomous values without bypassing private definitions. The model/provenance owner changes pass 111 common MVCC session tests, the 336-case SQLite unit suite and strict common-storage/SQLite/redb Clippy. Default Engine enablement and its wider regression results are recorded in the Engine command integration section below.

The sequence-session integration uses storage provenance to keep private definitions bound while committed values use an independent session, even after unrelated private writes. Execution selects fresh sequence rows without advancing ordinary data visibility. A peer definition change during an explicit transaction previously caused repeated `DefinitionChanged` results; named value resolution now refreshes the sequence definition before rechecking bounds. Existing callback rollback/savepoint scenarios preserve autonomous sequence values. The later sequence alteration and value-lock units below add relation coordination; dependent-object lifecycle and full Engine concurrency acceptance remain required. Verification in the integration worktree passes all 16 Engine sequence adapter tests and 15 execution sequence tests, including the explicit-transaction definition race on bound native SQLite, SQLite Key/Value and redb. Strict affected-owner all-target Clippy and dependency/ownership checks pass.

Named scoring-parameter updates now use execution-owned logical lock identities with transaction and savepoint scope. Engine adapters acquire the parameter lock before reading or replacing its value and refresh only the storage target, so execution workers do not reenter their parent command's statement gate. Verification in the integration worktree passes the owning lock-identity test and all five direct read/modify/write integration cases, including independent parameters across native SQLite, SQLite Key/Value and redb. Strict affected-owner all-target Clippy and ownership/dependency checks pass. Removal of the lifetime provider writer gate and the independent-writer schedules are recorded in the Engine command integration section below.

Column statistics now remain private until COMMIT and revert on transaction, savepoint and failed-statement rollback, including read-only ANALYZE. PostgreSQL 18.4 Docker verification distinguishes transactional pg_statistic histograms from separately updated pg_class.reltuples. Common storage now owns the maintenance state and merges evaluated counter changes during command refresh and publication; canonical writes, explicit requirements, deletion and object replacement retain their conflicts. Engine resets a parent's pending count only when the analyzing child commits, and automatic analysis records one generation change. Verification in the integration worktree passes 11 common-owner maintenance cases, one native SQLite case covering both commit orders and closed reopen in all four file modes, four Engine unit cases across native SQLite/SQLite Key/Value/redb, all 27 statistics integration cases, nine automatic-worker cases and the prior cache-invalidation regression (53 cases total). The owner cases include repeated refresh, savepoint undo, retained readers, quota failure, receipt recovery and combined graph/occurrence/IVF/HNSW changes. Strict storage/SQLite/Engine all-target Clippy and formatting, dependency, ownership, harness, header, line-cap and hygiene checks pass. These Engine results included the concurrent-transaction activation subsequently committed and verified in the Engine command integration section below. Competing-analysis relation coordination is now covered by the ANALYZE section; PostgreSQL relation-estimate semantics remain required. The earlier full-run cache-invalidation failure also passes its focused regression after the counter correction; the full concurrency acceptance remains open.

Read-only ANALYZE now retains physical write admission after savepoint release, savepoint rollback and statement-error recovery. SQL access characteristics still follow savepoint scope and reject ordinary persistent DML. Native SQLite retains its physical writer state after savepoint undo, so forgetting already granted maintenance permission incorrectly rejected the subsequent COMMIT. Verification on a copy of the PR branch containing only this fix passes all 206 Engine unit cases, including two new cases spanning native SQLite, SQLite Key/Value and redb, and all 10 PostgreSQL isolation/transaction-characteristic integration cases. Strict Engine all-target Clippy and repository ownership/dependency checks pass; this verification was performed independently of the later concurrent Engine activation.

Execution now implements all eight relation-lock conflict sets with transaction/savepoint acquisitions, atomic native holder publication and complete cross-process wait descriptors. Mixed modes retain their combined conflicts, upgrades exclude only the requesting session's own claims, and deadlock traversal includes every conflicting holder. Focused verification passes 33 lock cases with one parent-invoked process helper, including all 64 mode pairs locally and across real child processes, both upgrade and multi-holder cycles, cancellation, admission gaps, savepoint release and crash cleanup. Record format 10 atomically upgrades formats 1–9 and excludes older writers; redb now revalidates format and database identity before every record access and transaction outcome operation. SQLite's 13 format/permit cases and redb's complete 12-unit/41-integration surface pass. The actual format-9 binary from `b480c8475f167dbd5b0337ae0901f5e0b2d57aad` is rejected after migration in all nine provider/file configurations, including retained SQLite connections and snapshots; original fixture records, identity, sequences and committed/pending receipts remain unchanged. The reusable verifier is `scripts/verify-record-format-upgrade.py`; its databases and compact state files are temporary. Strict execution/SQLite/redb all-target Clippy and repository ownership/dependency checks pass. Complete owner verification passes execution 380, SQLite 341 unit and 330 integration, and redb 12 unit and 41 integration cases; both ignored process helpers run through their parent tests. SQL consumers of the relation modes remain separate from these lock-manager checks; default Engine command integration is described below.

Native SQLite, SQLite Key/Value and redb occurrence merging is implemented in [`occurrence resolver`](../../crates/uqa-storage/src/mvcc/occurrence/resolve.rs), with owner tests for shared clusters/totals, document and structural conflicts, late caches, savepoints, retained snapshots, admission retries and receipt recovery. The [storage contract](../manual/internals/03-storage.md) describes the behavior and [upgrade notes](../manual/reference/10-upgrading.md#unreleased-rust-keyvalue-compound-operations) describe the record-format and wrapper requirements. Native SQLite and common Key/Value IVF/HNSW merging now retains ordered evaluated document inputs, prepares matching generations against a fresh snapshot, and fences document/structural conflicts. Both native SQLite and Key/Value HNSW now use the same vector journal and conflict resolver.

The common Key/Value sequence consumer audit now routes definition/name changes and reservation/set-value operations through one evaluated mutation, with fixed-view reads and original write/schema preconditions. A deterministic schedule reproduced duplicate `1..3` reservations in the previous split read/write path. The implementation removes the catalog-local sequence mutex and uses the common provider boundary. Value updates now check the caller's validated definition generation; a deterministic Engine adapter schedule reproduced an old `setval(..., 75)` succeeding after a peer reduced `MAXVALUE` to 50. Execution now resolves changed definitions again and retries only known rejected autonomous value mutations, with session-affinity/idle checks and cancellation. Follow-up verification passes 250 sequence-selected cases across Engine, execution, SQL, common storage, SQLite and redb, including the three-provider definition race and owner tests for cancellation, rejected retries and indeterminate outcomes. The manual SQL check, strict six-owner all-target Clippy, Emscripten SQLite library compilation and repository policies pass; Emscripten is compile-only evidence. Verification covers interleaved value/definition/name changes, selected independent sessions, rollback/savepoints and reopen; the later Engine and sequence-lock units below add autonomous SQL value and definition coordination, while full concurrency acceptance remains required.

Sequence review also distinguishes ownership metadata from allocation-generation replacement. `OWNED BY` and implicit owner attachment preserve the allocation generation; the issuing session abandons its unused cache while peer caches and later nontransactional reservations survive ownership rollback. SQL owns the explicit value-option classification, execution owns generation selection and publication, and Engine retains the existing state adapter. The reference schedule ran on PostgreSQL 18.4 Docker image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`: with `CACHE 3`, A receives 1, B receives 4, A executes `BEGIN; ALTER SEQUENCE ... OWNED BY NONE` and receives 7, then rolls back; A next receives 8 and B next receives 5. A subsequent `INCREMENT BY 10` replaces the generation and B next receives 19. UQA verification passes 95 Engine SQL sequence cases, including the previously failing memory and three-provider ownership schedules, plus 15 Engine sequence-state adapter cases, 507 SQL and 360 execution unit cases, the manual SQL check and strict six-owner all-target Clippy. Raw reference output remains outside the repository.

Common command snapshot refresh is now implemented in [`session/transaction/refresh.rs`](../../crates/uqa-storage/src/mvcc/session/transaction/refresh.rs), with shared effect preparation in [`resolution.rs`](../../crates/uqa-storage/src/mvcc/resolution.rs). It rebases private occurrence totals/clusters, IVF/HNSW generations, graph cache effects and markers before advancing the committed view, preserves canonical requirements and retains effect kinds for subsequent commands. Savepoints retain the matching committed base and private owner; held readers preserve their original boundary. Final command verification passes eight common-owner schedules covering successive refreshes, shared indexes, duplicate savepoints, retained readers, definition/write conflicts, quota failure, caller cancellation, sealed attempts and backend conformance. Common storage also passes its documentation example. Complete provider unit/integration verification passes redb 11 unit and 41 integration cases and SQLite 333 unit and 328 integration cases; the ignored SQLite child entry point is exercised by parent tests. The provider schedules cover all four SQLite file modes, repeated occurrence/vector refresh, rollback, retained readers and reopen. Strict three-owner all-target Clippy, analyzer feature isolation, Emscripten SQLite library compilation and repository ownership/dependency policies pass. Emscripten is compile-only evidence. The verified storage/backend API now supplies the Engine command integration described below; complete SQL concurrency acceptance remains open.

The Engine transaction-error adapter now distinguishes rejected MVCC conflicts, cancellation, memory exhaustion and embedded SQL diagnostics, while authoritative uncertain or terminal commit evidence takes precedence. A real independent record replacement reproduced a rejected SQL COMMIT returning `XX000` instead of `40001`; the regression also checks the winning row, transaction cleanup, callback count and a later successful commit. Additional provider schedules cover resource/constraint failures and an uncertain receipt whose underlying cause is a conflict. Verification passes 11 Engine transaction-outcome integration cases across four SQLite record file modes and redb, including existing lost-reply, callback, deferred-trigger, notification and cleanup regressions. Strict Engine all-target Clippy, formatting and repository ownership/dependency checks pass. Transaction-model selection and private-writer command integration remain open.

## Objective and completion boundary

Implement overlapping logical write transactions for default relational SQLite, SQLite Key/Value and redb using shared storage MVCC and brief serialized physical commits. Transaction B must write and commit independent rows while transaction A retains uncommitted changes; A's later commit or rollback must preserve B. Include shared indexes, catalog/graph state, PostgreSQL 18 transaction semantics, existing persistent open modes, migration, bounded resources and recovery. Engine retains transaction/session adapters; storage and execution algorithms remain with their owning crates.

Design-document completion and runtime completion are separate. This documentation task is complete when both documents agree with the inspected source, define decisions and acceptance gates, pass document/repository checks and are committed and submitted for review. Runtime completion requires every transaction-related implementation unit and acceptance row below, with evidence attached to the implementation revision and the separate-issue boundary described below. A passing two-row test, one provider, a subset of isolation levels or a green reference-only PostgreSQL run cannot substitute for that full boundary.

## Scope boundary and separate issues

PR #112 completes logical concurrent writes and their transaction guarantees for native SQLite, SQLite Key/Value and redb. Work must address a concrete concurrent-write, snapshot, conflict, publication, recovery, migration or resource-lifetime requirement in the acceptance matrix. General SQL expression semantics, catalog output completeness and new protocol features do not become prerequisites merely because their bugs were discovered during this work.

JSON extraction rendering, output labels and operand typing are tracked in [#117](https://github.com/cognica-io/uqa-engine/issues/117); NULLIF equality selection and coercion in [#118](https://github.com/cognica-io/uqa-engine/issues/118); general catalog expression encoding and constraint introspection in [#119](https://github.com/cognica-io/uqa-engine/issues/119). These remain bugs to fix separately, not accepted compatibility differences or PR #112 merge gates. The uncommitted JSON rendering implementation has been preserved separately and removed from this PR's working changes. Earlier progress entries record completed work and evidence; references there to pending generic expression/projection work belong to these issues.

Catalog identities, destination/address reservations, retained snapshots and atomic independent tuple publication remain in scope when required for correct concurrent transactions. The existing main integration also requires rejecting earlier development writers that cannot decode its retained expression identity; that storage compatibility boundary is independent of the deferred rendering and typing work.

## Confirmed starting points

- [`PersistentStorageSession`](../../crates/uqa-storage/src/backend.rs) already pairs catalog/backend handles, and provider factories create independent sessions. Extend this boundary instead of introducing a parallel Engine persistence API.
- In the baseline, SQLite `BEGIN IMMEDIATE`, redb's retained `WriteState`, Engine deferred promotion and Engine eager transaction entry all participate in serialization. The built-in providers now retain logical private writes, and Engine selects that model explicitly; serialized custom providers retain their original writer gate. The reader/writer capability does not imply multiple writers.
- [`uqa-execution::row_locks`](../../crates/uqa-execution/src/row_locks/mod.rs) owns row/relation waits, deadlocks, cross-process coordination and row-change rechecks. Engine's `row_locks.rs` is only a reexport.
- Common storage already owns K/V catalogs, rows, occurrences, B-tree, IVF and HNSW behavior. Physical clusters/counters are shared by different rows, so opaque buffered K/V replacement is insufficient.
- Existing ownership/dependency policy forbids common storage and its tests from depending on providers, and forbids execution from depending on either concrete provider. No policy weakening or new Engine runtime dependency on redb is planned.
- The manifest audit resolved rusqlite 0.39.0 and redb 4.1.0; native SQLCipher, compressed rollback-journal and Emscripten plain SQLite must retain their separate physical contracts. No new Cargo feature currently supplies this behavior.

## Engine command integration

Paired catalog/backend handles now validate `StorageTransactionModel` and database incarnation as well as session affinity. Versioned frames keep private writes across command refresh and savepoint undo; serialized custom providers retain their existing writer admission. Default native factories preserve an already bound session’s options. An unbound initial session restores catalog semantics and converts the native baseline in one physical transaction, then attaches the logical session to existing handles. Failed restoration or conversion leaves the prior schema and rows intact. Three new SQLite owner tests verify rollback, conversion failure/write-permit cleanup and a peer conversion between session creation and transaction entry; all five existing analyzer migration cases pass, including the two failed-open atomicity regressions.

READ COMMITTED refresh preserves private changes, while fixed-isolation data snapshots remain separately retained. Refresh after a tuple/relation wait keeps the original command baseline and source snapshots; commit prepares maintenance counters against the current publication view. Execution selects autonomous committed-sequence values using storage-owned private-definition provenance, including after unrelated writes; private creation/restart remains transactional. The new SQL schedules cover commit, rollback and savepoint endings with and without a preceding read, independent Engine instances and SQLite file modes. redb instances share its supported physical database owner.

Native conversion normalizes empty retired full-text tables after source reconstruction, refuses unreconstructed source rows and leaves unrelated guard corruption detectable. The SQLite owner checks pass 27 focused cases plus a final injected failure after layout normalization; existing full-text restoration and failure-atomicity scenarios pass all 18 cases.

The complete Engine integration run at `868c325f452320dca63279f8dcab3d418ce24d03` passes 2,695 cases, fails `pg18_vacuum_full_reclaims_persistent_file_space` and leaves two opt-in release-profile diagnostic probes ignored. The earlier statistics/cache publication regression now passes in this complete run. The retention and lossless current-record units below now provide the snapshot horizon and close that VACUUM FULL regression; the historical complete run remains earlier evidence. The result does not establish completion of the design's acceptance matrix.

Complete SQL definition/constraint coordination, nested command coverage, remaining SQL consumers of the eight relation modes, serializable histories, durable publication recovery, admission/reclamation/spill and actual binding/platform acceptance remain required. The runtime completion checklist stays open.

## ANALYZE relation coordination

`uqa-execution::maintenance::analyze` owns name binding, retained object identity, lock selection, post-wait rechecks and target iteration. Engine supplies catalog/session adapters and separates opening a maintenance scope from physical writer admission. Named ANALYZE uses a temporary AccessShare binding acquisition and then ShareUpdateExclusive on the selected object; descendant sampling keeps AccessShare. Transaction completion and savepoint undo retain or release the corresponding marks. Renames follow the selected object, disappearance after binding emits a warning, and a replacement encountered during initial binding is resolved again. Automatic analysis retains ShareUpdateExclusive before sampling through publication, and rechecks the sampled identity and maintenance generation. Row writers can still commit independently.

Current verification passes all 212 Engine unit cases, including nine automatic-statistics cases and four new three-provider lock schedules, plus all 27 persistent SQL statistics cases. Execution passes five ANALYZE ownership cases and five relation-lock cases, including preservation of preexisting modes when temporary binding unwinds or is cancelled. Strict Engine/execution/SQLite all-target Clippy and ownership/dependency/harness checks pass. PostgreSQL 18.4 Docker (`postgres:18.4`, image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`) independently confirms six schedules: ShareUpdateExclusive waits released by COMMIT, ROLLBACK and ROLLBACK TO; AccessShare binding waits followed by DROP (`42P01`) or replacement (new row estimate 2); and cancellation (`57014`). The compact runtime observations remain outside the repository. This verifies the ANALYZE integration surface; it does not establish full concurrency, reclamation or binding/platform acceptance.

## Explicit SQL relation locks

`uqa-sql` now lowers every PostgreSQL table-lock mode and owns mode-specific privileges and stored-query target traversal. `uqa-execution::statement::table_locks` owns ordered acquisition, provisional binding, post-wait identity/privilege checks, inheritance and stored-view recursion. Engine supplies catalog and transaction adapters. `NOWAIT` checks local and native foreign holders without registering a logical wait or reporting a deadlock; rejected upgrades preserve earlier acquisitions. Stable bindings promote their temporary acquisition into the transaction/savepoint mark. Root names are rebound after replacement; inherited targets follow their original object identity across rename and skip a dropped object without adopting a reused name. Cancellation is never swallowed by a concurrent disappearance.

Engine scenarios cover native SQLite, SQLite Key/Value and redb; all eight modes in read-only transactions; NOWAIT failure and transaction abort; commit/rollback/savepoint release; owner/invoker view privileges and ONLY inheritance; root drop/replacement; child rename; cancellation; and first repeatable-read snapshot timing. PostgreSQL 18.4 Docker independently confirms the 64 mode conflicts, 72 privilege cases, transaction-context/view/inheritance cases and child-rename schedule. The manual includes an executable LOCK example. Execution-owner tests separately check binding replacement/disappearance, child identity and cancellation cleanup.

Final combined verification for the explicit-lock and encoding changes passes 221 Engine, 395 execution, 510 SQL and 227 common-storage unit tests (1,353 total). The execution suite also invokes its ignored process helper through parent tests. The manual SQL harness compiles the manual fences and executes the new LOCK example with the other executable examples. Strict SQL/storage/execution/planner/Engine all-target Clippy, formatting, dependency/ownership policy, harness registration, Rust headers, file-size limits and diff hygiene pass.

Virtual relation names, kinds and regclass OIDs now share the SQL-owned registry and execution snapshot namespace described below. Built-in identity, bootstrap ACLs and system-view source locking are implemented below. Durable catalog ACL changes and complete pg_class/attribute/type projection remain required; SQL and PL/pgSQL cursor metadata is implemented in the cursor section below. Other definition consumers, including direct/cascading removal and dependency-rewrite paths, still require a complete relation-mode and post-wait definition audit. These remain implementation defects in the active SQL coordination work; the general definition/constraint, RC/RR/SSI, timeout and platform acceptance rows remain open.

## Virtual catalog namespace and locking

SQL owns one registry for the implemented virtual relation names, kinds and OIDs. Common namespace candidates preserve implicit pg_catalog precedence, explicitly positioned pg_catalog/pg_temp and information_schema/AGE search-path visibility. Execution's immutable catalog snapshot stops every reader at the first matching physical or virtual relation. Query rows/schema, stored view and cursor binding, deparsed names and regclass input/output use this boundary. Session catalog mutation checks also resolve the actual selected relation, so a preceding user table named pg_prepared_statements remains writable. Stored-view column introspection uses the SQL virtual row type. Engine's exact kind adapter now delegates to the common catalog reader, including creation collision checks; it owns no new namespace algorithm.

PostgreSQL 18.4 Docker confirms 22 namespace, retained-binding, quoted-name, mutation and creation-collision groups. The ignored 3,463-byte namespace reference has SHA-256 `5c613f530f13120753b8e3d1b99b3504d371c5f53f743ef302e1e23aff14dab3`.

SQL now supplies built-in object identities, bootstrap ACLs and ordered system-view references, reusing the existing virtual projection registry and adding metadata for nine referenced catalog relations. Execution resolves explicit catalog LOCK through the common provisional-binding/authority checks and recursively applies the selected mode as each view owner. Query locking retains AccessShare on the root and transitive system sources, including through stored user views and cursors. Name resolution, creation collisions and regclass share these additional identities. Engine adds only the built-in identity adapter. Row projection availability is separate from relation metadata; the nine additional projectors, complete system pg_class/attribute/type rows, remaining catalog writes' lock modes and physical catalog-index locking are still required work. SQL and PL/pgSQL `pg_cursors` metadata is implemented in the cursor section below.

The system-relation unit passes 32 owning cases (18 Engine, seven execution, seven SQL), followed by all five system-catalog Engine cases after the final identity-adapter change. The three persistent providers cover 46 explicit catalog roots, 37 catalog SELECT roots, stored views/cursors, shadowing, 12 observed root/transitive waits and savepoint release. The Engine bootstrap ACL matrix covers 368 target/mode combinations. The broader catalog/cursor/row-lock/view/prepared/CTE/manual integration selection passes all 1,339 cases, including compilation of manual SQL fences and execution of the executable examples. PostgreSQL 18.4 Docker independently confirms the 46 lock sets, 368 ACL combinations and four observed wait/release schedules; the ignored 619-byte result has SHA-256 `54af6aecd7f836e45a84a705215b8593bf5548e3911978130c62df2abe2f1c62`. The 10,754-byte catalog identity/ACL/ordered-source reference has SHA-256 `0029584a1de0637fe22b1929d6e2ccfac178227b5eaf4a2a0711e60fa963cd43`. Strict SQL/execution/Engine all-target Clippy, formatting, staged dependency/ownership, harness, header, file-size and hygiene checks pass. Generated reference outputs remain outside the repository. No dependencies, Cargo features, provider formats or allocation limits change.

System-catalog privilege inquiry and persisted GRANT/REVOKE now share the same state as SELECT, prepared-query and explicit LOCK authorization. SQL owns bootstrap defaults, public attribute identities and effective system-table masks; execution owns per-relation/per-attribute ACL persistence, candidate validation and tuple coordination; Engine lends registry guards and retains transaction snapshots. Catalog metadata prefix reads and private-key provenance belong to storage. Relation and attribute ACL tuples persist separately so unrelated targets do not conflict. PostgreSQL 18.4 Docker confirms independent relation/attribute commits, `XX000` after a conflicting catalog tuple commit, successful continuation after rollback or savepoint undo, table-vs-column UPDATE inquiry masks and dependent column revocation; its ignored 1,757-byte reference has SHA-256 `b99845e29dd12ad5562539efa8233fd89ff6dd7ee5e082c6dd5cc256fa2ad1c6`. The related owner tests pass 93 cases (Engine 19, execution 20, SQL 35, native SQLite 18 and common storage one). All 1,427 selected Engine integration cases pass, including compiled manual SQL fences and the executed catalog-ACL example. Strict SQL/execution/storage/SQLite/Engine all-target Clippy and repository policy checks pass; the three role-dependency cases also pass after extracting the system dependency check. The final fixed-snapshot adapter checks pass both provider-matrix cases after the state adapter is simplified. These results cover catalog ACL persistence and ACL tuple concurrency; the full acceptance scope below remains open. Cross-object role/drop coordination and the remaining definition consumers, complete catalog projections and write/index locks, SSI, reclamation, recovery, platforms, bindings and final controlled acceptance remain required work. Role creation/deletion and ACL dependency publication need shared role-identity coordination next: the current per-session role guards do not replace PostgreSQL's [dependency-side object lock](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/catalog/pg_shdepend.c) and [DROP ROLE object lock](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/user.c). Implement and compare grant-versus-drop commit, rollback, savepoint and identity-reuse schedules before considering that dependency consumer complete.

Role identity preparation removes the name-derived OID alias: a new role receives a nonreserved 32-bit OID, attribute changes and undo retain the stored value, recreation has a new identity, and all implemented owner and membership OID projections use the captured role registry. Existing metadata retains its OIDs; restoration rejects invalid ranges, duplicates and a changed bootstrap identity. No dependency, Cargo feature or provider format changes. PostgreSQL 18.4 Docker independently confirms role create/undo/recreate identity and 14 observed GRANT-versus-DROP schedules across an ordinary table and `pg_class`: committed grants make a waiting DROP report `2BP01`, committed drops make a waiting GRANT report `42704`, rollback and savepoint undo release the waiter, and same-name recreation does not substitute for the dropped identity. The ignored 1,609-byte reference has SHA-256 `4c9276eb361fc2d6e6a45f0123c603f4624f6543d7306bd7d37d01db885c9a84`. The owning role selection passes 24 cases (Engine four, execution six, SQL 14); after bootstrap validation and fixture cleanup, all 12 focused SQL role cases pass again. All 1,195 selected Engine integration cases pass, including manual SQL compilation and executable examples. Strict SQL/execution/Engine all-target Clippy, formatting, dependency, Engine ownership, harness, header, line-limit and hygiene checks pass. This identity change does not yet implement cross-object dependency locks.

Direct Rust API catalog refresh reproduced a separate private-state bug: after an external commit, `list_named_analyzers()` could replace a fixed-data transaction’s role and membership registries with committed values, losing private creates, attribute changes, drops and grants. The three new regression cases fail against the previous refresh implementation; ordinary SQL statement-boundary refresh already preserves those records through the provider command view. Execution now owns the unchanged role metadata I/O and merges only metadata records identified as private by storage; Engine supplies immutable snapshots and installs the merged state together with private system ACLs. Role and membership provenance are checked separately so an untouched record still receives external commits. The fix passes all 18 related Engine cases and three execution metadata cases. The final owned-value restoration representation passes all seven Engine role cases and three execution metadata cases, plus 225 selected Engine integration cases including the manual SQL harness. The three new Engine cases cover 36 provider/isolation/direction combinations, including separate role versus membership provenance and savepoint/full undo. Strict SQL/execution/Engine all-target Clippy and repository policy checks pass. The aggregate records remain a separate independent-publication requirement, and this correction does not add shared role dependency locks.

Role definition publication now writes only changed role records and their OID uniqueness records. CREATE ROLE skips membership publication when it adds no edges, so unrelated role creation and attribute changes no longer collide through either aggregate. DROP retains its existing membership publication even when no visible edges are removed: dropping that write before shared dependency coordination would let a concurrent membership commit escape the checked snapshot. Independent DROP-versus-DROP and membership/drop waits remain required work. Initial restoration validates roles and memberships before atomically replacing the legacy role map with a version marker and independent records; secondary sessions require completed migration. Old role readers fail to decode the marker. Exact metadata deletion retains transaction-private provenance, savepoint undo and publication preconditions in common Key/Value and native SQLite storage, including the existing graph definition fences and path invalidation. Execution owns record layout, migration, delta publication and private overlay; Engine passes before/after snapshots and initial-open mode. Final focused validation passes eleven Engine role cases and fifteen execution role cases, including preservation of DROP membership publication. The complete execution unit suite passes 415 cases; common storage passes all 227 unit and 197 integration cases, and native SQLite passes all 346 unit and 330 integration cases. Two subprocess entry points are ignored by direct harness discovery and are exercised by their owning parent tests. All 1,195 selected Engine integration cases pass on the final implementation, including compiled manual SQL fences and executed examples. Strict execution/storage/SQLite/Engine all-target Clippy, formatting, dependency, ownership, harness, header, line-limit and hygiene checks pass. No benchmark or Nori allocation gate is rerun. The new Engine tests cover 27 provider/isolation/completion combinations and nine private-attribute/external-deletion refresh combinations, forced concurrent OID collision rejection and initial-migration rollback after a later restoration failure across native SQLite, SQLite Key/Value and redb. PostgreSQL 18.4 Docker independently confirms all nine isolation/completion schedules, with the second writer committing before the first finishes; the ignored 863-byte reference has SHA-256 `f8132912d925459f6c614490e93a05b31db069989db7e8fcd98b056d7fc1b22e`. Per-membership publication, stable role incarnation reservation and shared role dependency/tuple coordination remain required; this unit does not establish those semantics or the remaining full acceptance scope.

Role creation at `35539a160e6546acbed6d98b588a2e313f83567a` reserves a typed shared-catalog name and public OID before writer admission, releasing registry guards before waits and refreshing before deciding whether to reject or retry. A competing same-name creator receives `23505` after the winner commits, while full or savepoint rollback permits creation; an already visible name still receives `42710`. A persisted nonzero 128-bit role incarnation distinguishes a replacement independently of public OID reuse. Execution owns allocation, reservations, binding and migration; SQL constructs and validates values, and Engine only adapts transaction marks, cancellation and refresh. Role-record format 2 converts both the old aggregate and development record format 1 in initial restoration's rollback boundary. Common provider record format 11 excludes already-open predecessor writers that cannot preserve the new protocol. Actual binaries from `4280722f5c28bfb94c1eea3f4baf51a4542e316a` verify all nine native SQLite, SQLite Key/Value and redb file configurations, including retained SQLite predecessor connections, unchanged database identity, records, sequence and receipts. PostgreSQL 18.4 Docker confirms all nine same-name creation schedules; the ignored 469-byte reference has SHA-256 `68071c48e81d0b32a982f66ed9dcb78b1741e8a5699bb909cbb2cf1cd6bc671b`. Engine validation covers 27 provider/isolation/completion creation schedules and three-provider identity/migration rollback and reopen. Execution tests force an OID collision discovered after refresh, reject a same-name/same-OID replacement incarnation and prove typed object locking with an actual subprocess. That checkpoint supplies creation reservations and incarnation binding; shared-object dependency consumers, membership publication and the rest of the full acceptance scope were still open. Focused validation passes 47 cases across Engine, execution and SQL. Complete unit suites pass 421 execution, 528 SQL, 346 SQLite and 12 redb cases; the existing ignored execution subprocess helper is exercised by its parent tests. All 1,195 selected Engine integration cases and strict five-crate all-target Clippy pass. Formatting, dependency, ownership, harness, header, line-limit and hygiene checks pass. No Nori or timing measurement is repeated.

Table-shaped ACL publication at `88d4441fe0fc78925555474cc6995b60724ae820` consumes typed shared-object addresses for added grantee and grantor dependencies on ordinary, column, view, materialized-view, foreign and system relation ACLs. SQL compares each relation or column ACL independently before execution combines the lock set, so an existing table reference cannot hide a new column reference. PUBLIC, owner-covered and pinned dependencies, unchanged ACLs and removed references introduce no new role waits. Execution binds the original role incarnations before waiting, releases registry guards, retains AccessShare locks and recomputes candidates after catalog refresh and writer preparation. DROP binds each requested incarnation before its AccessExclusive wait and refresh; a missing retained target raises PostgreSQL `XX000` during definition removal, including with IF EXISTS, while a waiting dependency publisher raises `42704`. DROP also enforces CREATEROLE before missing-name handling and disallows non-superuser administrators from deleting superusers. Private rows, role records, rollback and savepoint undo survive the waits. Engine supplies only the existing lock, refresh and registry adapters. Common record format 12 excludes incompatible writers without changing role-record format 2 or native mapping format 8. Actual binaries from `35539a160e6546acbed6d98b588a2e313f83567a` verify all nine file configurations, preserving identities, data and receipts while rejecting retained predecessor writers. PostgreSQL 18.4 Docker verifies 147 ACL/deletion schedules, 24 competing-deletion schedules and both authority rules; the ignored 10,941-byte reference has SHA-256 `f22087021b5f9a621353e4c484da262931f1cb1296c3c998d1853d8c2a98db74`. UQA tests cover 441 ACL/deletion and 72 competing-deletion schedules across all three providers and isolation modes, plus no-op/revoke lock behavior, original-object diagnostics and reopen checks. Focused validation passes 70 cases across Engine, execution and SQL. Final validation passes all 20 selected Engine unit cases, 423 execution and 530 SQL unit cases, 346 SQLite and 12 redb unit cases, and all 1,195 selected Engine integration cases, including the manual SQL harness. The existing ignored execution subprocess helper is exercised by its parent tests. Registry instrumentation verifies guard release before dependency waits and refresh, retained guards through final persistence, and release before notices. Strict five-crate all-target Clippy, formatting, dependency, Engine ownership, harness, header, line-limit and hygiene checks pass. No Nori allocation or timing measurement is repeated.

Database, schema, sequence, function and procedure ACL publication now shares the same role-dependent mutation preparation as table-shaped ACLs. The SQL owner projects grantee and grantor references from each ACL representation and computes added dependencies; execution releases object registry guards, memberships and role definitions before waits and writer preparation, then retains the final guards through persistence and publication. A newly discovered dependency after writer preparation causes another lock/refresh/preparation pass without discarding the originally bound incarnations. Unchanged and removed role references introduce no new dependency locks. Execution tests force that additional preparation pass, verify guard lifetimes and reject a replacement role before writer admission while releasing the provisional lock. Engine adds only the four context adapter bindings. PostgreSQL 18.4 Docker verifies 126 added dependency schedules and 12 no-op/revoke lock cases across database, schema, sequence and both routine kinds, including historical sequence grants through TABLE syntax; the ignored 9,323-byte reference has SHA-256 `f400147353192fd08d9f5ac696f8d03bbad7cf3b612f57ef6fca3e0196069acf`. UQA extends the existing matrix to 819 ACL/deletion schedules and 78 no-op/revoke checks across three providers and isolation modes; routine outcomes use actual EXECUTE permission through function and procedure calls. Common record format 13 excludes prior writers that omit these dependency locks. Actual binaries from `88d4441fe0fc78925555474cc6995b60724ae820` verify all nine file configurations, preserving identity, data and receipts and rejecting incompatible predecessors. Final validation passes all 20 selected Engine unit cases, 425 execution and 531 SQL unit cases, 346 SQLite and 12 redb unit cases, and all 1,195 selected Engine integration cases, including the manual SQL harness. The existing ignored execution subprocess helper is exercised by its parent tests. Strict five-crate all-target Clippy, formatting, dependency, Engine ownership, harness, header, line-limit and hygiene checks pass. No Nori allocation or timing measurement is repeated.

Routine privilege inquiry now implements all six PostgreSQL `has_function_privilege` overloads in SQL, with the execution catalog adapter supplying captured routine definitions, built-in identities and exact-signature lookup. NULL, role/name/OID resolution, namespace USAGE, PUBLIC and missing-object behavior, grant options and diagnostic precedence follow PostgreSQL. Exact signature parsing retains its SQLSTATEs and accepts the current database qualifier. Built-in `abs` now publishes the six catalog signatures already supported by SQL; the inquiry and regprocedure conversion share those OIDs. Owner EXECUTE is an explicit, revocable ACL privilege, while ownership retains implicit grant options; actual function/procedure calls and `pg_proc.proacl` use that same state. Versioned routine metadata preserves legacy implicit owner rights through initial-open conversion, rejects unknown versions, refuses conversion during secondary restoration and rolls back conversion with a later restoration failure. Common record format 14 excludes older writers; role-record format 2 and native mapping format 8 remain unchanged. Actual binaries from `d46a9cd79f89f0478baf1095847c950153eb7b74` verify all nine file configurations and reject incompatible retained predecessors while preserving records, identities and receipts. PostgreSQL 18.4 Docker supplies six overload identities and 43 permission/input observations (8,083 bytes, SHA-256 `0e2b793e2dac8d3acb0d3227365fe5485c1c4c9055dcc84131dab0a7692822ce`) plus ten exact-input cases (1,802 bytes, SHA-256 `995e34fd503586dbbe21d27a0bc94b6582ad6d93189b3e765243d9af37c5e281`). References are ignored temporary artifacts. [PostgreSQL ACL inquiry](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/utils/adt/acl.c), [regprocedure input](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/utils/adt/regproc.c) and [built-in catalog definitions](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/include/catalog/pg_proc.dat) provide the corresponding source reference. Engine adds only tests for this change. Validation passes all 24 selected Engine unit cases, 426 execution and 536 SQL unit cases, 346 SQLite and 12 redb unit cases, and all 1,315 selected Engine integration cases, including the executed manual inquiry example. Focused compiler tests also pass after the argument-parser extraction. The existing ignored execution subprocess helper is exercised by its parent tests. Strict five-crate all-target Clippy, formatting, dependency, Engine ownership, harness, header, line-limit and hygiene checks pass. No Nori allocation or timing measurement is repeated.

Owner transfer now uses the shared execution preparation loop for tables, views, materialized views, foreign tables, sequences, schemas and routines. SQL owns permission checks against captured role/membership values. New owners retain their original OID and incarnation across role-lock and writer waits; discarded candidates release all registry guards before waiting and final candidates retain authorization through persistence. Relation targets are locked before binding the requested role, while routine ALTER binds the role before its target lock, matching PostgreSQL lookup order. Actual changes retain the role AccessShare dependency until transaction completion. Unchanged owners introduce no role dependency or writer admission; relation targets still require ownership, while schema and routine no-ops skip ownership, SET and CREATE checks. Routine transfer now checks the new owner's namespace CREATE, and an unchanged owned sequence succeeds without detachment. Table transfer refreshes the role catalog after its retained target lock, then persists owned sequences before publishing table security. Stored namespace security validates an existing namespace rather than a new declaration, preserving system-schema owner overrides through refresh, rollback and reopen while reserved creation names remain protected. Common record format 15 excludes incompatible format-14 writers; native mapping 8, role-record 2 and routine-record 1 remain unchanged. Actual binaries from `85791313911b574a336fde167ec51098520849a9` verify all nine file configurations, preserving records, identities and receipts and rejecting retained predecessor writers. PostgreSQL 18.4 Docker independently verifies 168 role-dependency schedules, eight no-op permission cases, eight diagnostic/permission cases and seven target-wait schedules. The ignored 14,775-byte reference has SHA-256 `f05918443098088b2ac241397e9abf8f9060fcef4aab5246a5a513b15f736b65`; [ALTER TABLE ownership](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/tablecmds.c), [generic ownership](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/alter.c), [schema ownership](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/schemacmds.c) and [shared dependency publication](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/catalog/pg_shdepend.c) supply the corresponding primary source reference. Broad validation passes the complete execution (428), SQL (539), SQLite (346) and redb (12) unit suites and all 1,364 selected Engine integration cases. The existing ignored execution subprocess helper is exercised by its parent tests. After the final post-wait role-refresh and system-schema restoration corrections, focused validation passes all six Engine ownership cases, eight execution and eight SQL unit cases, and 264 Engine integration cases, including compiled manual fences and executed examples. The owner tests cover 243 owner-before-deletion schedules, 324 deletion-before-owner schedules, 72 target-wait schedules and 27 unchanged-owner object/provider cases, plus routine namespace permissions and system-schema refresh, savepoint undo and reopen. Strict five-crate all-target Clippy, formatting, dependency, Engine ownership, harness, header, line-limit and hygiene checks pass. No Nori allocation or timing measurement is repeated.

## Role membership publication

Membership authority now selects the nearest ADMIN holder through INHERIT edges, requires current-user SUPERUSER authority for superuser targets, uses the bootstrap OID for implicit superuser and creator grants, and enforces explicit grantor authority separately for GRANT and REVOKE. SQL validates all recipients together when checking whether an ADMIN grant depends on the authority it would grant back. Eight focused SQL regressions cover default and explicit grantors, inherited authority versus SET-only membership, superuser targets, independent ADMIN sources, joint recipients and diagnostic precedence. Validation passes all 567 SQL unit cases, 26 execution role cases, 24 Engine role/security integration cases and the manual SQL harness, including compiled fences and executed examples. Strict SQL/execution/Engine all-target Clippy, formatting and repository dependency, ownership, harness, header, line-limit and hygiene checks pass. No allocation or timing measurement is repeated. PostgreSQL 18.4 Docker independently supplies 15 authority observations in a 751-byte ignored reference with SHA-256 `d8a0880d61261d6085fe9050c798e60e1862c41955c3c2a2e5900b6887d58aa7`; [membership execution](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/user.c) and [inherited privilege traversal](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/utils/adt/acl.c) provide the primary source contract.

Independent membership records and target-role coordination now follow the [membership design](../design/concurrent-storage-transactions.md#role-membership-publication). Each tuple retains target/member/grantor incarnations and a separately claimed membership OID. Initial restoration validates and atomically converts the legacy aggregate; refresh overlays only privately changed membership keys. Execution takes target ShareUpdateExclusive, membership-OID reservation and newly referenced grantor AccessShare locks outside registry guards. Authority is captured before the target wait; graph validation follows it. There is no global graph lock or member dependency lock. Common provider record format 17 excludes retained predecessors that cannot preserve this protocol; native mapping 8 and role-record format 2 remain unchanged. Actual format-16 predecessor binaries verify all nine provider/file configurations, preserving records, identities and receipts and rejecting incompatible retained writers.

Eight PostgreSQL lock schedules are retained as an 856-byte ignored reference with SHA-256 `d900939157888b59d659955f6139c54a1d8152506fc20a590b38d21706b36c38`. A separate 589-byte reference (`9ad1ab8778f1391a799e9fa83ca18d0346cc186a9640a6ff6bbf092a0e2015e7`) checks final membership tuples in both GRANT/DROP orders for targets and members, same-name recreation, joint ADMIN back-grants and opposite-direction concurrent grants. Deletion can leave a tuple naming the original target or member OID; recreating the name does not inherit its membership. The opposite grants commit independently. A further revoke-before-grant schedule (`b0186ddd196d86800302f8b0608a231609fe4178053c6465a83dcb2c17eae2c4`) confirms the waiting GRANT succeeds with its previously selected grantor after the ADMIN source is revoked. The additional final-state and authority-wait observations use PostgreSQL 18.4 image ID `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`; they define required UQA schedules and do not establish UQA concurrency acceptance.

Nine additional PostgreSQL 18.4 target-wait schedules distinguish current member attributes from captured identity. A changed INHERIT attribute supplies the default after the target wait; an explicit option overrides it. A dropped or same-name-recreated member causes `XX000` when the default needs its original OID, while explicit INHERIT permits the tuple to retain that original missing endpoint. A renamed member retains its identity and succeeds. A dropped or recreated grantor fails with `42704`. All waits are observed through `pg_locks`; this is functional reference evidence, not timing acceptance. The ignored 406-byte reference has SHA-256 `1f490804fdd36ac271fa1e69324787106520b3256fa5ca273cb2965a938d5eba` and uses the same pinned image above. UQA's provider regressions cover the attribute, deletion and replacement schedules; the role-name unit below adds actual SQL rename during a wait.

Membership commands separate typed named roles from CURRENT_USER/SESSION_USER, capture target and selected grantor authority before waiting, refresh the graph and omitted INHERIT default after waiting, and reserve membership OIDs in execution using shared catalog class 1261. A statement-local delta overlay retains pending membership changes while waits refresh unrelated committed records. CREATE ROLE stages IN ROLE memberships before bootstrap-granted creator administration, then ROLE/ADMIN recipients; role and membership registries publish only after all persistence succeeds. ALTER GROUP uses target-first authorization, and REVOKE plans all recipients against the original graph. Unchanged grants and missing revocations emit their notices without admitting a writer or publishing unchanged registries. A concurrently deleted or updated membership tuple cannot be resurrected or overwritten by the pending overlay.

Verification passes all 581 SQL unit cases, 39 execution role cases, 55 Engine role cases, 24 Engine security/role integration cases and the manual SQL harness, including compiled fences and executed examples. The Engine selection initially passed 53 cases; the remaining two pass after correcting their setup transaction boundaries as described below. New provider schedules cover 72 post-wait endpoint/default cases, 108 ordered target/grantor deletion cases, independent opposite-target publication, captured authority after source revocation, same-target waits, member deletion without dependency waits, CREATE ROLE and ALTER GROUP, commit/full undo/savepoint undo and reopen across native SQLite, SQLite Key/Value and redb. Separate publication cases preserve independent private membership creates, updates and deletes through refresh, and reject forced membership-OID collisions. Execution cases exercise collision retry and losing-lock release, no-op publication, refreshed graph validation and exact-tuple conflicts. Strict SQL/execution/Engine all-target Clippy, formatting and repository dependency, ownership, harness, header, line-limit and hygiene checks pass. The unchanged provider format checks pass three SQLite cases in addition to the nine predecessor-binary upgrade configurations. No allocation or timing workload is rerun; these results do not complete the remaining concurrency acceptance scope.

A further PostgreSQL 18.4 Docker reference checks six ALTER GROUP authority/lookup cases and four duplicate-recipient REVOKE variants, including cascade results and absence of spurious warnings. Its ignored 412-byte report has SHA-256 `259e1846526da9c42106692d272e1a23a347ba35a890c46a0941b0a8f81e802b` and uses the same pinned image above. The new UQA tests mirror these public outcomes rather than depending on the machine report. A Simple Query containing GRANT followed by BEGIN retains the grant and its target lock in that transaction; delivering the setup and BEGIN separately commits the setup first. A 106-byte ignored PostgreSQL reference (`5d476363d027beeb8fc7deb8fd9482d07f7f78381aa10761f464afac0119b9ae`) verifies both tuple visibility and the held ShareUpdateExclusive lock. Lock-isolation fixtures now establish their committed setup in a separate call. The role-name unit below implements the SQL command and validates rename during a target wait through the public SQL path.

Other definition and ACL tuple coordination, complete catalog projections and write/index locks, isolation histories, recovery, reclamation and platform acceptance remain open. Object creation and session identity integration are described below.


## Cursor traversal and session metadata

Default SQL and PL/pgSQL query cursors now use execution’s existing backward-scan support classification after row-lock validation. Explicit `SCROLL` and `NO SCROLL` retain their existing execution contracts. SQL owns the plan representation and execution owns traversal; this correction introduces no Engine production change or dependency. PostgreSQL 18.4 Docker image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636` confirms 12 default/explicit cases against [PostgreSQL backward-scan classification](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/executor/execAmi.c). The ignored 174-byte reference has SHA-256 `0483ff39bf845ed7339efbe768cd98552aba99bc9aaacc0845b8b8f838a3e706`. UQA verification passes all ten execution portal cases and 54 Engine cursor/PL/pgSQL/manual integration cases. Strict execution/Engine all-target Clippy, formatting and dependency/ownership/harness/header/line-cap/hygiene checks pass. No allocation or timing workload was run.

SQL defines `pg_cursors` identity, typed columns, read-only mutation diagnostics and source-preserving PL/pgSQL query nodes. Execution owns live registration, duplicate/name allocation and row projection; Engine retains a registration alongside each operational portal and adapts the existing statement clock. Moving an executor for FETCH or hold materialization leaves its declaration visible without locking the operational map during query execution. Registration drop follows existing close, savepoint, transaction and DISCARD lifecycles. Declared holdability stays distinct from procedural auto-hold. PostgreSQL 18.4 Docker references cover schema/OID, flags, complete message text, statement time, PL/pgSQL/implicit-loop visibility, self-query and cleanup (514 bytes, SHA-256 `8deb26507f69502cc21a8640158cf8c6329606683ef10f19862099c35762ca89`), plus auto-held loop flags, unnamed-name collisions and read-only diagnostics (137 bytes, SHA-256 `0414cb97145823c53a6696475b45c7223ee14f9006f2c125e45159f255b081f5`). Raw references remain temporary. The [PostgreSQL view contract](https://www.postgresql.org/docs/18/view-pg-cursors.html) and [portal implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/utils/mmgr/portalmem.c) supply the primary reference. Verification passes the complete SQL suite (643 cases), the complete execution suite (511 cases, with two native subprocess entry points exercised by parent tests), and 207 distinct Engine cursor/PL/pgSQL/prepared/manual integration cases. The broad integration run passed 206 cases; after correcting the new session-isolation fixture to use a persistent provider, its focused native SQLite self-query/hold-materialization case also passes. Strict SQL/execution/Engine all-target Clippy and formatting, dependency/ownership, harness, header, line-cap and hygiene checks pass. No dependency, Cargo feature, stored format or allocation ceiling changes. New Extended Query protocol execution is outside this concurrent-storage PR; transaction and snapshot lifetimes of the existing SQL and PL/pgSQL portal paths remain in scope.

Owning execution and public Engine regressions reproduced four default-scroll classification failures for ordered window/target-set/UNION ALL queries and a deferred constant CTE. Execution now shares the actual CTE schedule with traversal analysis instead of treating every named source as a stored scan. It preserves reachable/default/explicit/recursive/volatile materialization decisions, follows inherited deferred definitions, and distinguishes outer sorting from post-sort SRFs, reused window ordering and constant keys. Sort expressions resolve through aliases, ordinals and expanded stars. Engine changes only the captured metadata, parameter and scope arguments. The eight-case PostgreSQL 18.4 reference is 145 bytes with SHA-256 `26237eae21fd039d2bc66e4a0ae41385d16b99ba921dbe50e91adc64129f8875`; the additional 21-case reference is 412 bytes with SHA-256 `de3f660356ba5ce64abba6b731ad5bff193b8d2fc9b7b9a8a314edc92a031d2a`. Both use the pinned image above. The [PostgreSQL planner’s sort-input target rules](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/optimizer/plan/planner.c) explain why one sorting SRF keeps all target SRFs below sorting. Raw references remain temporary. Public execution also reproduced SQLSTATE `42883` when a streaming cursor sorted a target set: the sort-key projection incorrectly used scalar evaluation. Execution now keeps all target sets at one expansion level, retains target-list order, postpones unsorted sets until after sorting, and applies OFFSET/LIMIT/WITH TIES to expanded output without sorting it again. Five pinned PostgreSQL cases check unequal zipped sets, slicing, ties and sequence-backed argument evaluation order (56 bytes, SHA-256 `1a7ec7117717a8b6a9ec96efc87b6a17a60bad58778a4a508b9df5a1045d8290`). Verification passes 513 distinct execution unit cases (the 512-case complete suite plus the added inherited-scope regression, with two subprocess entry points exercised by their parent tests) and all 361 selected Engine cursor, PL/pgSQL, prepared, CTE, window, set, ordering, ties and manual integration cases. The manual harness compiles SQL fences and executes marked examples. The new cases compare default metadata and actual forward/backward results, plus exact zipped values, NULL padding, slicing, ties and volatile argument order. Strict execution/Engine all-target Clippy and formatting, dependency/ownership, harness, header, line-cap, hygiene and compatibility-manifest checks pass. No dependency, Cargo feature, storage format or allocation ceiling changes; no allocation or timing workload was run.

## Lossless current-record compaction

SQLite now owns bounded current-record runs, grouped only by exact consecutive eight-byte suffixes, a shared key prefix and revision, and verified NULL, constant or reversible value templates. Every run is limited to 128 members, 1,024-byte keys and 128-byte templates. Indexed scans probe disjoint intervals separately by key length, preserving byte order when differently sized keys interleave. Metadata skips payload materialization; later writes restore the exact predecessor and split its old range atomically. Reclamation still counts only obsolete revisions, and never deletes receipt, identifier or conflict evidence. Native sequence uniqueness checks consume logical record metadata so valued runs remain visible. Common record format 29 adds guarded SQLite run storage and preserves redb’s physical layout; native mapping 8 and catalog version 49 remain unchanged.

The unchanged `pg18_vacuum_full_reclaims_persistent_file_space` now passes its original 2,000-row workload and requirement that the vacuumed file be smaller than half its original size. Verification passes all 371 SQLite unit cases and 330 regular integration cases, with one parent-invoked child helper ignored; all 18 redb unit and 41 integration cases; and 28 Engine VACUUM, commit-resolution and manual SQL integration cases. The six new owning-crate cases cover exact point/metadata/prefix/key output, pagination, interleaved key lengths, four file modes and reopen, NULL/empty/constant/reversible templates, bounded runs, first/middle/last replacement, retained predecessors, conflicts, receipt retries, memory/cancellation, invalid encodings and rollback. Every preserved key retains its original logical bytes and revision.

Actual format-28 binaries from `ebd02ed7ef5863b26a2ac3f7023dd11b2af3f72e` verify all nine provider/file configurations, including retained SQLite readers and writers, closed reopen rejection and unchanged identity, sequence, values and receipts. A native predecessor checks its closed set of physical families before the common format marker and rejects the new guarded run table there; the verifier now recognizes that exact rejection only for native reopen, retaining the stricter record-format checks on live handles and the complete subsequent-state comparison. Strict SQLite/redb all-target Clippy, Emscripten SQLite library compilation, formatting and repository dependency, ownership, harness, header, line-cap and hygiene checks pass. No dependency, feature or memory ceiling changed; no allocation or timing workload was repeated. This closes the SQLite file-space regression, without claiming redb physical compaction or the remaining full concurrency/platform acceptance.

## MVCC snapshot retention and history reclamation

Common storage now owns atomic snapshot capture/registration and the reclamation horizon. SQLite and redb remove only revisions older than each key’s retained predecessor under one physical transaction, leaving all newer revisions, head tombstones, identifier watermarks and durable receipts intact. Retained views share charged leases and registry entries and release liveness without waiting for the collector’s admission gate. SQLite compacts a latest tombstone into its head and restores that deleted revision before any later replacement, eliminating redundant key rows while retaining conflict evidence. SQLite uses a separate provider-owned `.uqa-snapshots` byte-lock file with one descriptor per process/database; native file primitives are shared below execution through `uqa-storage`, without reversing the workspace dependency graph. Browser file opens share process-local admission, in-memory SQLite uses the physical pool and redb uses its exclusive database owner. Native SQLite and SQLite Key/Value run logical reclamation before physical VACUUM; redb returns obsolete pages to its allocator.

The retention unit introduced common record format 28 so predecessors that do not register leases cannot read reclaimed histories. Actual binaries from `32fe426c51ad17974779ad53aeb79776194156ac` verify all nine provider/file configurations, including retained predecessor SQLite readers and writers, unchanged identity, sequence, records and receipts. Head tombstones compact without changing their logical revision or value, and reinsertion restores their history before publishing a new head.

Verification covers 246 common unit tests, 202 common integration tests and one documentation test; 365 SQLite unit cases and all 330 regular integration cases across the full run and corrected-fixture recheck, with one child helper invoked by parent tests; and 18 redb unit cases and all 41 integration cases across the full run and corrected upgrade recheck. The stale redb format-16 fixture now checks format 28 and every recognized predecessor. The SQLite commit-contention fixture observes a PENDING writer from an independent reader, releases the original reader separately and proves that BUSY waits and receipt retries do not repeat staging. The final common/SQLite MVCC recheck passes 12 and 81 cases respectively, including eight separate-process release/kill schedules across four SQLite file modes. Fifty execution row-lock cases pass, with two parent-invoked helper entries ignored. The final read path passes 27 Engine commit-resolution, VACUUM and manual SQL regressions. Strict five-owner all-target Clippy, formatting and dependency/ownership/harness/header/line-cap/hygiene checks pass. Windows GNU common-storage and Emscripten SQLite library compilation pass; these establish compilation, not Windows or browser runtime acceptance. No allocation or timing workload is run, and no allocation ceiling is changed.

At completion of the retention unit, the existing `pg18_vacuum_full_reclaims_persistent_file_space` failure remained open: its unchanged 2,000-row workload leaves 1,601,536 bytes after VACUUM FULL, compared with 2,076,672 before. No obsolete revisions remain. Current heads occupy 503,808 bytes, current versions 311,296 bytes and native occurrence guards 69,632 bytes. Further space recovery must reduce duplicate conflict metadata while preserving every conditional-write precondition, exact record value and retained snapshot; dropping conflict evidence or changing the workload or threshold is not an acceptable fix.

That diagnosis led to the SQLite-owned bounded run encoding described above. A read-only feasibility check reconstructed all 6,028 key/revision/value tuples exactly, grouping 5,980 keys into 49 runs bounded at 128 keys and leaving 48 singletons unchanged. This feasibility check did not establish physical-file or performance acceptance; the unchanged file-size regression now passes in the implemented unit above, and no timing acceptance is claimed. Common storage retains visibility and receipt contracts, and Engine receives no encoding algorithm.

## Role deletion session protection

SQL now captures initial DROP ROLE CREATEROLE authority with the original actor/session identities and resolves one target at a time. Execution waits for each target’s shared-object lock and removes role/member edges before resolving the next name; later permissions and errors therefore observe the preceding waits and private membership changes. Distinct target dependencies and tuple deletion then follow written order, without repeating target authority after a wait. The SQL owner reuses its existing transitive ADMIN traversal for role alteration, rename and deletion, independent of INHERIT and SET edge options. No Engine algorithm, dependency, feature or stored format changes.

Two new execution regressions fail before this correction. A pinned PostgreSQL 18.4 Docker reference passes 22 cases covering later-target errors after observed waits, COMMIT/ROLLBACK/savepoint release, initial CREATEROLE capture, membership removal order, duplicate targets, dependency diagnostic order and transitive role administration. The ignored compact result is 1,860 bytes with SHA-256 `7f4a028c1cec9826918c6f2c8c8ac78b5035bfb228dba4c0bbbb50b5be989942`, using image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`. PostgreSQL’s [role deletion implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/user.c) supplies the target order, and its [role authority implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/utils/adt/acl.c) distinguishes transitive administration from inherited grantor authority. No timing or allocation workload is involved.

The ordered-deletion correction passes 83 unit cases (28 SQL, 30 execution and 25 Engine) and all 25 selected integration cases, including the manual SQL harness. New Engine coverage includes 108 observed wait schedules across three providers, three isolation levels and commit/full-rollback/savepoint outcomes, plus membership undo and reopen, duplicate targets, dependency diagnostic order and transitive administration with every INHERIT/SET combination. Strict SQL/execution/Engine all-target Clippy, formatting, staged dependency, ownership, harness, header, file-size and diff checks pass. No dependency, Cargo feature, stored format or allocation ceiling changes, and no timing or allocation workload is repeated.

DROP ROLE now rejects CURRENT_USER, CURRENT_ROLE, SESSION_USER and PUBLIC, including the exact lowercase quoted name `"public"`, rather than resolving them into session identities. SQL checks CREATEROLE first and validates targets in written order, preserving preceding missing-name errors and IF EXISTS notices. Uppercase quoted keyword names remain literal roles. Invalid multi-target requests preserve the role catalog through statement and savepoint rollback. No parser, Engine algorithm, dependency, Cargo feature or stored format changes are needed.

The special-target correction reproduces two pre-change SQL failures and passes 43 unit cases (seven SQL, 28 execution and eight Engine), including rollback and reopen on all three persistent providers. All 25 selected integration cases pass; the manual harness compiles SQL fences and executes marked examples. Strict SQL/execution/Engine all-target Clippy, formatting, staged dependency, ownership, harness, header, file-size and diff checks pass. A pinned PostgreSQL 18.4 Docker reference passes 31 cases covering CREATEROLE precedence, IF EXISTS, ordinary missing targets, notices and quoted names. Its ignored compact result is 1,793 bytes with SHA-256 `2ca25bd094e940fb02bea4b1e45e24e5982c925c7c8fe349633a1d04e0aead42`, using image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`. PostgreSQL's [DropRole implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/user.c) defines the validation order, and its [role-specifier grammar](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/parser/gram.y) distinguishes PUBLIC from uppercase quoted names. No allocation or timing workload is involved.

Review of PostgreSQL 18's DropRole path found that SQL protected only the effective and session roles, allowing a SECURITY DEFINER function to delete its caller's selected outer role. SQL now uses the existing `RoleReferenceNames::outer_role` identity for the missing check and preserves current/outer-before-session diagnostic ordering. Renaming the original role retains protection; a replacement at its former name is independent. No Engine behavior, crate dependency, feature or stored format changes.

A new owning SQL regression fails before the correction because the outer role is not protected. Final verification passes 38 unit cases (four SQL, 28 execution and six Engine) and all 25 selected integration cases, including routine authority, session restoration and the manual harness that compiles SQL fences and executes marked examples. Strict SQL/execution/Engine all-target Clippy, formatting, staged dependency, ownership, harness, header, file-size and diff checks pass. Four pinned PostgreSQL 18.4 Docker checks confirm effective, outer and session protection and surviving role identities. The compact named-case summary is 254 bytes with SHA-256 `65c4d97f902491c072e585380ea92b9bcb6413b82a2b1d93083d6e352a4a0502`, using image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`; PostgreSQL's [DropRole implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/user.c) supplies the identity and diagnostic order. No allocation or timing workload is involved.

## Role names and publication identity

SQL now implements ALTER ROLE, ALTER USER and ALTER GROUP RENAME with literal identifiers, original authorization and separate session/outer-user protection. Execution retains the original tuple revision and destination-name reservation through waits, refresh and writer admission. Renaming preserves the OID and incarnation, advances the tuple revision and moves the OID claim without rewriting memberships or dependent authority. A replacement at the old name acquires none of the original privileges. Bootstrap restoration and authentication follow OID 10 and its immutable incarnation rather than a fixed name. Common record format 27 excludes writers that assume fixed names; role catalog format 3, native mapping 8 and catalog version 49 remain unchanged.

SQL ACL command bindings retain original recipients, explicit grantor and actor, preserving object-specific error precedence before binding. Execution indexes retained dependency locks by incarnation and projects the original owner's current name after waits. The same protocol covers database/schema/system/table/column/view/foreign/sequence/routine grants, owner changes and creation including implicit serial sequences, CTAS, domains and temporary objects. DROP retains role identities through dependency waits before selecting the definition tuple. Engine adds only the outer-role session adapter and integration regressions; SQL and execution own the behavior. No dependency, Cargo feature or allocation ceiling changes.

Role rename verification reproduces three pre-change SQL failures and passes 220 distinct unit/provider cases: 113 SQL, 73 execution, 23 Engine and 11 provider cases. Seven new Engine cases cover 567 deterministic provider/isolation/finish schedules for tuple and destination-name conflicts, membership endpoints, private refresh, ACL recipients, owner changes and creation, including rollback, savepoint undo and reopen. All 476 selected integration cases pass, including the manual harness that compiles SQL fences and executes marked examples. Final strict six-owner all-target Clippy, formatting, staged dependency, Engine ownership, harness, header, file-size, benchmark-coverage and hygiene checks pass. No allocation or timing workload was run; no dependency, Cargo feature or memory ceiling changed. This unit does not complete general definition/ACL publication or fixed SQL-visible catalog snapshot work.

Pinned PostgreSQL 18.4 Docker references verify 19 name/authority cases, 13 tuple/name-index/membership schedules and four session/definer/bootstrap cases. The ignored compact results are respectively 153 bytes (SHA-256 `4c592ac8a14d8026681e5f3b08e962eab345f0586513af05f5d77ad62c9d3705`), 1,801 bytes (`6d011dbc356b12f5deba6029856ea099c75318799976713ec9c1bd9252a728cc`) and 110 bytes (`d9ba82cc5439c76d483ebe9cf16d0d61e6393a6aebcad315dcb1d73275470b40`). All use image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`; the PostgreSQL [ALTER ROLE contract](https://www.postgresql.org/docs/18/sql-alterrole.html) and [role implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/user.c) define the checked authority and tuple boundaries. These are functional schedules, with no timing or allocation workload.

## Durable owner and ACL role references

Role definition tuple coordination is implemented for attribute changes and deletion. Two pre-change regressions show ALTER ROLE and DROP ROLE finishing instead of waiting for a competing definition change. SQL now retains a nonzero role tuple revision, advances it on every attribute assignment including no-op values, validates publication transitions and distinguishes an updated tuple from a removed incarnation. Execution uses a typed tuple address separate from shared-object dependency locks, retains original authorization, releases role guards before waits and merges the selected replacement into refreshed state. DROP retains dependency exclusion and validates dependencies before its tuple wait. Committed intervening changes return XX000; rollback and savepoint undo allow the original operation. Role catalog format 3 converts older revisions initially and rejects malformed current versions; common record format 26 fences incompatible writers. Role renaming and its publication identity consumers are described in the role-name unit above.

A pinned PostgreSQL 18.4 Docker reference passes 12 role tuple schedules covering attribute and identical-value updates, attribute/deletion ordering, COMMIT, ROLLBACK and savepoint rollback. The ignored 1,680-byte result has SHA-256 `a53878f764a2f808e4bdaef69f2a35908d984df3b944b9ed9e077296bd57f3d6` and uses image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`. The [ALTER ROLE contract](https://www.postgresql.org/docs/18/sql-alterrole.html) and PostgreSQL’s [role mutation implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/user.c) define the authority and tuple-update boundaries. Separate references confirm rename during a membership wait retains the original incarnation and does not wait for its shared dependency; the role-name unit above covers SQL command and destination-name coordination.

Role tuple validation reproduces two failures before the change and passes 48 SQL, 46 execution, 15 Engine and 11 provider cases. The three new Engine tests cover 117 deterministic schedules across native SQLite, SQLite Key/Value and redb, all three isolation levels, committed changes, full and savepoint undo, and membership publication that completes independently of definition mutation. Initial-only conversion preserves role identities and memberships, rejects missing current revisions and rolls back after later restoration failure. All 164 selected integration cases pass, including role/session authority, routine execution and the manual SQL harness. Strict five-owner all-target Clippy, formatting, staged dependency, Engine ownership, harness, header, file-size, benchmark-coverage and hygiene checks pass. No allocation or timing workload was run; no memory ceiling, dependency or Cargo feature changed.

Membership and session bindings, together with the database, schema, system, relation, sequence, domain and routine conversions below, retain role OIDs and incarnations. The owning durable representations project current names without rebinding and retain original references through refresh, undo and publication. SQL role RENAME preserves these identities without rewriting dependent catalogs; its tuple and destination-name coordination and rename-during-membership-wait cases are described above.

Database ownership and ACL conversion retains OID/incarnation values in both memory and persistent registries and their transaction snapshots. SQL binds owner, grantee and grantor references, projects current names for its existing privilege rules and compares dependency identities directly; the shared identity value moves to core with its SQL re-export preserved. Execution serializes the bound record before releasing role guards and converts legacy names only during initial restoration. Engine persists the opaque JSON and supplies the migration mode. Database security format 1 rejects stale name-based readers, and common provider record format 18 excludes retained predecessor writers. Native mapping 8, role records 2 and membership records 1 remain unchanged. Validation passes all 585 SQL unit cases, 52 focused execution cases, four new Engine database cases, both existing role migration cases and 41 Engine integration cases including the manual SQL harness. The provider cases cover 27 isolation/finish schedules, private refresh, commit and savepoint/full undo, atomic migration failure, incarnation corruption and reopen across native SQLite, SQLite Key/Value and redb. Memory snapshots retain the same identities; independent catalog snapshots preserve owner OIDs while displaying the names from their own role views. Actual format-17 predecessor binaries pass all nine provider/file upgrades with old-writer rejection and retained records/receipts; all three SQLite format cases pass. Strict core/SQL/execution/Engine/SQLite/redb all-target Clippy, formatting and dependency, Engine ownership, harness, header, line-limit and hygiene checks pass. These checks complete this database representation unit; subsequent owner/ACL conversion and SQL rename evidence is recorded separately in this section.

Schema registries, persisted rows and transaction/catalog snapshots now retain owner, grantee and grantor OIDs and incarnations. Core supplies shared ACL and schema row data; SQL owns binding, validation, dependency comparison and projection into the existing named privilege rules. Execution restores roles first and validates all schema references before scheduling initial-only conversions within the complete restoration transaction. Engine remains a registry and persistence adapter. Storage distinguishes versioned bound rows from legacy names; native SQLite retains its existing column layout and uses a versioned BLOB owner instead of legacy TEXT. Secondary sessions and malformed current rows cannot trigger name rebinding. The shared bound ACL decoder rejects a missing grantee instead of interpreting it as PUBLIC, for both database and schema rows. Common record format 19 excludes preceding schema writers; native mapping 8, role records 2, membership records 1 and catalog version 49 are unchanged. Validation passes all 588 SQL unit cases, 57 focused execution cases, 19 Engine unit cases and 125 Engine integration cases including the manual SQL harness. After tightening missing-grantee decoding, the owning core case and all six database/schema binding cases also pass. The Engine selection includes four new schema cases with 27 provider/isolation/finish schedules, memory snapshots, all three corrupted role endpoint kinds, atomic conversion failure and reopen; the earlier database and role migration cases remain passing. Common-storage schema tests pass all three cases, SQLite schema/format tests pass all seven cases, and the native schema integration case passes. The native durable codec case also preserves the binary schema owner in all four file modes. Actual format-18 predecessor binaries verify all nine provider/file upgrades after the native owner layout change. Strict seven-crate all-target Clippy, formatting and dependency, ownership, harness, header, line-limit and hygiene checks pass. The subsequent owner/ACL conversions and actual SQL rename command are recorded separately in this section.

Fixed-snapshot namespace authority refresh now overlays exact transaction-private schema records over fresh committed schemas, preserving creation, replacement and deletion. Common Key/Value and native SQLite expose this provenance through `CatalogFacade::schema_has_private_changes`, with savepoint undo and serialized-provider behavior retained. Execution owns the merge and validates its role incarnations; Engine only passes the current and committed snapshots. Direct `has_schema`, `has_namespace` and `list_schemas` calls exercise refresh without starting another SQL command, while the SQL schedule covers ACL/owner changes, private deletion, peer publication and savepoint rollback. The PostgreSQL 18.4 reference uses pinned image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`; its three isolation-level schedules have a 155-byte temporary transcript with SHA-256 `97280decef6b6aa4879dd898474cfb2def45fa192f2970c0903c245e8cd14c8d`. No record-format or dependency changes are needed. Schema ACL/owner tuple replacement and namespace lifetime/name coordination are addressed in the namespace identity work below; its 15-schedule PostgreSQL reference has a 938-byte temporary transcript with SHA-256 `3af87f386b5e9147c0841c52235bce73a1ee8b84b8972a61c1154b8062ed81f7`. Five additional owner/drop and same-name replacement schedules have a 477-byte temporary transcript with SHA-256 `3157b99851b21fba880f31eedd3416c0256b20c238389e3f3805a901d679aee4`: GRANT rebinds a recreated namespace after the lifetime wait, while owner mutation retains its original tuple and reports concurrent deletion. Reference execution alone does not complete those mutation paths.

The direct catalog regression reproduces a lost private schema on native SQLite at REPEATABLE READ with the previous refresh path, then passes with the merge on native SQLite, SQLite Key/Value and redb at both fixed isolation levels. Final validation passes six Engine schema/role cases, 32 public schema authorization, usage, deletion and manual integration cases, eight execution namespace cases, three common private-provenance cases and five SQLite metadata/provenance cases (54 total). Strict four-owner all-target Clippy, formatting, exact-index dependency, Engine ownership, harness, header, file-size, hygiene and PG18 manifest checks pass. Manual fences are compiled and executable examples are run by the integration harness. No allocation or timing workload is executed, and no ceiling is changed.

PostgreSQL 18.4 Docker confirms that owner, grantee and grantor renaming preserve the database catalog tuple and ACL OIDs, that a reused former name acquires no CREATE privilege, that cascade revocation and PUBLIC access survive, and that a role rename commits while a GRANT still holds its AccessShare dependency lock. The ignored 291-byte reference has SHA-256 `56202e3ef57a4fd430e2f3da3e4b2a13fa33bac11fc2e65df2b7381ccabc7af7` and uses image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`. These observations define the storage and scheduling behavior; UQA SQL rename evidence is recorded separately above.

System relation and attribute ACL registries now use the shared SQL-owned `BoundTableSecurity` model, including bound column grant paths and bootstrap owner defaults. Execution versions each independent metadata tuple with system ACL format 1, validates all tuples before scheduling initial-only conversion, and preserves tuple revisions. DROP ROLE dependencies compare incarnations; privilege inquiries and query lineage resolve names from their matching role view. Explicit table locks retain bound ACL state and owner references through view-source waits. The memory rename regression exposed an absent `pg_authid` row projector despite its existing identity/ACL support. SQL now owns its typed virtual schema and execution projects the retained role snapshot; `pg_roles` applies its public password mask to the same source. Engine supplies catalog, registry and role-view adapters. Common record format 20 excludes preceding system ACL writers; native mapping 8, role records 2, membership records 1 and catalog version 49 remain unchanged. Verification passes all 592 SQL unit cases, 14 execution cases, 29 Engine unit cases and 36 Engine integration cases, including the manual harness that compiles SQL fences and executes marked examples. The original role-rename query regression and the new private role catalog projection/authorization case pass. System ACL cases cover 27 provider/isolation/finish schedules, memory rename/name reuse, private refresh, transaction/savepoint undo, reopen, initial-only conversion, retained revisions and corruption of both endpoints in relation/column tuples across all three providers. The SQLite record-format cases pass (3), and actual format-19 predecessor binaries verify format-20 upgrades across all nine provider/file configurations. Strict five-crate all-target Clippy and formatting, dependency, ownership, harness, header, line-limit and hygiene checks pass. No allocation or timing workload is rerun. The later table/column, view, foreign-table, sequence, domain and routine conversions are recorded below; actual SQL role RENAME is recorded above.

A PostgreSQL 18.4 Docker reference confirms that the named role `"PUBLIC"` remains distinct from the PUBLIC grantee, including independent table, column and system-catalog privileges; quoted `"CURRENT_USER"` and `"SESSION_USER"` also name their own roles. The ignored 252-byte report has SHA-256 `1f3840f299c0a84589739f14da640632c3abf76b64fbffa9839d1f667581c367` and uses image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`. Core now retains typed `AclGrantee` values, and SQL retains typed ACL recipients, grantors and object-owner specifications from compilation through policy evaluation, binding and serialization. Execution resolves those specifications and projects catalog ACLs without interpreting role names as keywords. Legacy strings retain their historical meaning, and common record format 21 excludes predecessor writers. Temporary role dependencies now retain the named PUBLIC role too. The ordinary relation identity conversion below consumes this corrected shared model. A second 266-byte reference (SHA-256 `538e690a0723fd72c4147cf145854ec110b95cac7a4d670a29145b5fdd29dfba`) confirms named `"PUBLIC"` grantors, independent CASCADE paths, routine EXECUTE and quoted owner transfer. A third 262-byte reference (SHA-256 `ff8a89e677b4ba1d136c566356f39c6772ddc2ef6afa66a01a4c3c70e983121b`) confirms six information-schema projection observations: displayed PUBLIC names remain visible under PostgreSQL’s name filters, both recipient identities survive row deduplication, and only the real owner receives implicit grantability. The implementation follows the [PostgreSQL 18 view definitions](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/catalog/information_schema.sql#L522) while retaining typed identities for privilege decisions.

Typed ACL and owner validation passes all 108 core and 599 SQL unit cases, 52 focused execution cases, 25 Engine unit cases and 129 Engine integration cases. The manual harness compiles SQL fences and executes marked examples, including the new quoted-PUBLIC grant. Six new Engine cases cover independent named/PUBLIC recipients, grant options, named grantors and alternate CASCADE paths, missing quoted names, temporary dependencies, information-schema projection and quoted owner changes across all three providers, including undo, refresh and reopen. All three SQLite record-format cases pass, and actual format-20 binaries from `8eb5c7ba4543ea4d33746b911bbeb5eeededf79b` pass format-21 upgrades in all nine provider/file configurations while retaining records and receipts and rejecting older writers. Strict seven-crate all-target Clippy, formatting, dependency, Engine ownership, harness, header, line-limit and hygiene checks pass. No allocation or timing workload is rerun.

Ordinary tables, columns, views, materialized views and foreign tables now retain bound owner/grantee/grantor identities in runtime state, snapshots and durable rows. Core owns the shared row data; SQL owns binding, grant-path validation, dependency comparison and name projection. Execution converts legacy names only during complete initial restoration, validates table and foreign candidate sets before persistence and joins view security conversion with the existing definition migration. Engine supplies retained state and publication adapters; foreign restore scheduling moves into execution. Definition-only view JSON cannot construct a runtime view with default authority. Catalog rows use relation security format 1 and versioned SQLite owner BLOBs. Common record format 22 fences prior writers without changing native mapping 8 or catalog version 49. The same unit fixes table refresh fingerprints that omitted column ACLs. Verification passes all 599 SQL unit cases, 74 focused execution cases, 46 Engine unit cases and 129 Engine integration cases, including compiled manual SQL fences and executed marked examples. The five new Engine cases cover memory owner/grantee/grantor rename and name reuse, materialized refresh and CASCADE, 27 provider/isolation/finish schedules, column-only private ACL preservation, atomic legacy conversion failure, and 36 provider/relation/endpoint corruption checks with reopen. Execution tests separately reject legacy view definitions and legacy security before publication. Common-storage catalog tests pass all 30 cases, SQLite catalog/record-format tests pass all 96 cases, and native catalog/table/view/foreign integration tests pass all 24 cases. Actual format-21 predecessor binaries from `e71d12e5efc07f4c3d691623ba69cbce14075288` pass all nine provider/file upgrade configurations with old-writer rejection and retained records/receipts. Strict seven-crate all-target Clippy passes, followed by final execution/Engine Clippy after fixture corrections; formatting, staged dependency, Engine ownership, harness, header, line-limit, benchmark-coverage and hygiene checks pass. No allocation or timing workload is rerun. The sequence, domain and routine conversions are recorded below; SQL role RENAME is recorded above.

Sequence authority and direct SQL catalog projection require separate snapshot checks before the remaining identity conversion is accepted. A pinned PostgreSQL 18.4 Docker schedule starts each isolation level before a peer transfers a sequence to a newly created owner, renames its grantee and recreates the old grantee name. The original session still allocates the next value and retains USAGE; the replacement role has no USAGE. After allocation, a separate frontend message observes the renamed current role. READ COMMITTED projects the new owner and role name through `pg_class` and `pg_roles`, while REPEATABLE READ and SERIALIZABLE retain their earlier catalog rows. The ignored 508-byte reference has SHA-256 `226a0ab257f560cf9c14dbf820c66ca6fdc8de684176ffe935501c755d43c785` and uses image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`. This is reference evidence for required UQA implementation and regression work; it does not establish UQA catalog-snapshot or role-rename acceptance.

Sequence value authorization now retains sequence definitions, ACLs, roles and memberships from one independent provider read transaction, then overlays private records and local temporary entries before checking privileges. The previous split read was reproduced deterministically: a peer created a grantee, granted USAGE and added membership after name resolution, but the value call rejected the new ACL using an older role map. Execution owns the detached read and transaction cleanup; Engine supplies immutable registries and provider sessions. Scalar value functions and sequence introspection consume the same authority snapshot without installing partial live catalogs or advancing ordinary transaction data visibility. OID-based setval preserves a successful autonomous result even when the new sequence is absent from the older live registry. Direct Rust value APIs now prepare their command snapshot before name lookup, preserving private definitions while seeing peer commits, reject aborted transactions and retain an active SQL callback’s outer snapshot. Nested host queries use actual SQL execution depth as well as lock scopes to preserve that snapshot when the outer SELECT has no row-lock clause. PostgreSQL 18.4 Docker confirms committed membership revocation across seven value/introspection functions at all three isolation levels, preservation of unused cached reservations and fixed ordinary rows; the ignored 646-byte reference has SHA-256 `bbe3c8b3a87b3e7cc23697ae019ba4a1dfacf7379955ad24634b0ca739ae5ad3`. Durable sequence owner/ACL identity conversion, coherent definition-registry refresh and SQL-visible catalog snapshots remain required.

Sequence authority verification passes 26 selected Engine unit cases, 17 owning execution cases and 100 sequence/callback/manual integration cases after the final command-boundary correction. Seven new Engine cases cover 42 deterministic provider schedules and aborted memory transactions; execution checks read-transaction cleanup on success, error and unwind, and rejection of borrowed transactions. The manual harness compiles SQL fences and executes marked examples. Strict execution/Engine all-target Clippy, formatting, staged dependency, Engine ownership, harness, header, file-size, benchmark-coverage and repository hygiene checks pass. No dependency, feature, provider format or allocation ceiling changes, and no allocation or timing workload is rerun.

Fixed-transaction catalog refresh now retains private sequence definitions and security with the corresponding private roles. The prior implementation installed committed durable maps and restored only temporary sequences, so an unrelated role commit could make a nested host query report its own transaction's newly created sequence as missing. Two new Engine regressions reproduce the lost records and the public callback error before the fix. Execution now merges complete immutable sequence records using the existing committed/private selection policy, including rename and deletion; Engine installs the prepared snapshot and no longer implements a separate temporary-sequence merge. Independent peer sequence changes remain visible to definition consumers, and no sequence rows are reloaded from a different committed view during this merge. Live sequence-refresh integration is recorded below; durable owner/ACL identity conversion and fixed SQL-visible catalog snapshots remain required work.

Private-definition refresh verification passes 28 selected Engine unit cases, 19 owning execution cases and 117 sequence/callback/isolation/concurrent-write/manual integration cases. The new Engine cases cover 24 native SQLite, SQLite Key/Value and redb schedules across both fixed isolation levels, commit, full rollback, savepoint undo and nested host queries. Two owning execution cases check complete-record selection, retained immutable snapshots, invalid private metadata and failed revision reads. Existing integration checks also cover encrypted and compressed files; the manual harness compiles SQL fences and executes marked examples. Strict execution/Engine all-target Clippy and formatting, staged dependency, Engine ownership, harness, header, file-size, benchmark-coverage and repository hygiene checks pass. No allocation or timing workload is rerun, and provider formats, dependencies, features and allocation ceilings remain unchanged.

Public Rust sequence inspection now consumes detached snapshots instead of reloading sequence rows into the live registries. A peer-created owner could previously appear in the live sequence security map while remaining absent from the live role map after a snapshot, name-list or state read. Engine now retains the session statement gate and supplies SQL's ordered name candidates; execution projects names and values from the detached view. Private roles, private definitions, temporary entries and nontransactional values remain visible, and retained query enumeration keeps its captured catalog. Definition and stored-reference consumers use the coherent live refresh described below; durable sequence owner/ACL identity conversion remains required.

Public inspection verification reproduces the missing live owner before the fix and passes 30 selected Engine unit cases, 20 owning execution cases and 100 sequence/callback/manual integration cases after it. The new cases cover 27 provider/isolation/API combinations, retained query enumeration in memory, immutable projected names and candidate ordering. The manual harness compiles SQL fences and executes marked examples. Strict execution/Engine all-target Clippy, formatting, staged dependency, Engine ownership, harness, header, file-size, benchmark-coverage and repository hygiene checks pass. This correction adds no dependency, feature, format or allocation-ceiling change and reruns no allocation or timing workload; the preceding catalog-refresh unit retains the encrypted/compressed and isolation acceptance evidence above.

Sequence owner and ACL authority now retain role OIDs and incarnations in runtime registries, snapshots and durable rows. Core owns neutral bound/legacy values, SQL owns binding, endpoint and rooted-grant validation and identity-based dependencies, storage owns discriminated codecs, and execution owns complete candidate validation and initial-only migration inside the existing catalog transaction. Creation uses the already retained owner identity; privilege publication and owner transfer bind candidates under matching role guards. Reads resolve names only from the accompanying role view and reject missing authority. SQLite retains its physical columns and encodes bound owners as versioned BLOBs; Key/Value carries `sequence_security_format: 1`. Common record format 23 excludes preceding sequence writers without changing native mapping 8 or catalog version 49. The remaining catalog coordination and snapshot work remain open.

Domain owners now retain role OIDs and incarnations in `StoredDomain`, transaction/catalog snapshots, DROP DOMAIN authority and `pg_type` projections. SQL validates complete domain identity/name/owner candidates and checks DROP ROLE dependencies by incarnation. Execution owns domain catalog format 1, initial-only legacy name binding and registry publication; Engine lends state and installs candidates. Creation reuses the existing original-owner shared-object lock, and the creation/drop role schedules now include domains. Committed refresh uses storage provenance to retain a complete private domain metadata record, including deletion, while untouched sessions take the latest committed definitions; execution validates the selection against the accompanying role map before installation. Common record format 24 excludes prior writers without changing native mapping 8 or catalog version 49. The pre-change regressions reproduce successful deletion of an owning role and transfer of projected ownership to a replacement name. General definition/publication coordination remains required.

Domain ownership verification first reproduces two pre-change authority failures and then a private-domain loss during committed refresh. The correction passes nine distinct SQL owner/dependency cases and 26 execution owner/restoration cases, including the final five domain cases; the extracted dependency checker passes all seven cases after the final refactor. Six Engine domain cases cover name reuse, private creation/deletion, unchanged committed refresh, savepoint/transaction undo, corrupt authority, initial-only conversion and later-failure rollback across native SQLite, SQLite Key/Value and redb. Both expanded creation/deletion tests pass 882 deterministic schedules, including 63 domain schedules, across all three providers and isolation levels. All 11 selected provider format/metadata cases and 250 selected Engine integration cases pass, including domain dependencies, routines, foreign DDL, information schema and the manual SQL harness. Strict five-owner all-target Clippy, formatting, staged dependency, ownership, harness, header, file-size, benchmark-coverage and hygiene checks pass. No allocation or timing workload was run; no memory ceiling or dependency policy changed.

A pinned PostgreSQL 18.4 Docker reference passes ten domain ownership cases: owner OID projection, DROP ROLE rejection while a domain remains, owner-name changes preserving authority, rejection of a replacement name, rollback restoring the dependency and committed domain deletion releasing it. The ignored 153-byte reference has SHA-256 `c5a30ec0149775f1cfacff1bcef61578f65a715696e8ae52d07c5a5d6c28756c` and uses image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`. The memory identity regression isolates retained authority; the role-name unit adds SQL command coverage.

Routine owners and ACL endpoints now retain their original role identities. SQL declarations distinguish unbound owners from stored `RoleIdentity` values; routine ACLs retain bound grantees and grantors, and SQL owns validation, owner-rooted grants, owner replacement and identity-based dependencies. Execution owns routine format 2, complete-candidate initial conversion, private-record refresh selection and SECURITY DEFINER invocation; Engine retains state and its session authorization adapter. Unversioned maps preserve implicit owner EXECUTE, while preceding format 1 preserves explicit owner revocation. Common record format 25 fences older writers. Three pre-change memory regressions reproduce transferred proowner projection, transferred EXECUTE grants and SECURITY DEFINER execution as the replacement role. These isolate retained authority; the role-name unit adds SQL command coverage.

Routine authority verification first reproduces three pre-change failures: owner projection transfers to a reused name, a replacement grantee acquires EXECUTE, and SECURITY DEFINER executes as the replacement owner. The correction passes 71 SQL cases, four execution codec cases, 35 Engine cases, 11 provider format/metadata cases and 355 Engine integration cases, including the manual SQL harness. Engine cases cover owner/grantee/grantor name reuse, PUBLIC and quoted recipients, grant-option CASCADE, owner transfer, session restoration, initial-only conversion, later-failure rollback, corrupt current authority and private definition/ACL/deletion refresh through savepoint undo and reopen on native SQLite, SQLite Key/Value and redb. The two creation/deletion cases pass 882 deterministic schedules, including 126 function/procedure schedules. Codec validation rejects missing current authority fields and invalid incarnations without fallback, while the two legacy formats preserve their distinct owner EXECUTE meanings. Final decoding and restoration checks pass after removing redundant JSON reserialization. Strict five-owner all-target Clippy, formatting, staged dependency, ownership, harness, header, file-size, benchmark-coverage and hygiene checks pass. No allocation or timing workload was run; no memory ceiling, dependency or Cargo feature changed.

A pinned PostgreSQL 18.4 Docker reference passes 22 routine authority cases covering owner/ACL rename and replacement, SECURITY DEFINER, role dependencies, revocable owner EXECUTE, owner-rooted CASCADE, undo and owner transfer. The ignored 153-byte reference has SHA-256 `6fa63015359e88dbdb1ce310f195203d8a85ca1114c6ee692513963bf27a6ea1` and uses image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`. The behavior agrees with the PostgreSQL 18 [GRANT](https://www.postgresql.org/docs/18/sql-grant.html), [ALTER FUNCTION](https://www.postgresql.org/docs/18/sql-alterfunction.html) and [CREATE FUNCTION](https://www.postgresql.org/docs/18/sql-createfunction.html) contracts.

SQLite physical admission now separates pool checkout and write reservation from record evaluation and publication. Typed BUSY can wait before the operation is invoked, at permit preparation and at BEGIN IMMEDIATE; a busy COMMIT keeps the same staged transaction and retries only COMMIT. Scoped timeout changes restore pooled configuration after completion, error or unwind. SQL supplies immutable per-session write cancellation through storage-owned construction; explicit publication, document/identifier reservations and autonomous sequence mutation inherit it without cancelling retained reads or rollback cleanup. Engine retains the provider token and lends it to an independent sequence session. Native SQLite, SQLite Key/Value and redb forward the common capability without new Cargo dependencies or features. Initial schema preparation and abort cleanup retain bounded admission; fairness, recovery, reclamation and complete transaction acceptance remain open.

SQLite writer-admission verification first reproduces four deterministic contention/cancellation failures against the prior implementation, then passes all 82 selected SQLite ownership cases, including forced BUSY admission, a reader-blocked COMMIT with exactly one staging callback, cancellation without consumed IDs or published rows, timeout restoration and pool exhaustion. Common session verification covers 126 distinct cases: 125 pass in the grouped run and the updated provider-contract case passes separately after adding its required cancellation capability. All 404 selected Engine and 16 SQLite integration cases pass, including the formerly failing concurrent nextval case, independent INSERT processes, retained commit outcomes, encrypted/compressed reopen and the manual SQL harness, which compiles SQL fences and executes marked examples. The final Engine cancellation matrix passes for native SQLite, SQLite Key/Value and redb, including TRUNCATE watermarks, rollback and independent siblings; after making physical transaction destruction explicit, all five admission cases pass again. Strict five-owner all-target Clippy, formatting, staged dependency, ownership, harness, header, file-size, benchmark-coverage and hygiene checks pass. This resolves the recorded SQLite BUSY regression without SQL/callback replay, format/feature changes, raised memory ceilings or allocation/timing workload reruns. Fair admission and the remaining full concurrency acceptance are still required.

Main's 0.3.7 generated-identity correction is integrated without replacing autonomous storage-owned document allocation. Versioned providers keep durable object/generation watermarks and do not reuse successful reservations after SQL undo; serialized persistent backends without that capability use execution-owned candidate locks and current committed occupancy checks. Engine only supplies table state, the retained committed reader and lock adapters. Both suites remain registered, including actual separate SQLite processes, VALUES/SELECT, text/composite/no primary keys, fixed snapshots, failed RETURNING, savepoint undo and reopen. The provider matrix also exercises unbound serialized SQLite so the reservation fallback is covered independently of durable allocation. Release metadata and prior 0.3.7 documentation remain intact; package publication is not part of this integration.

Main integration verification passes 53 execution identity/row-lock unit cases (two pre-existing ignored process cases are not claimed) and all 11 generated-identity integration cases. The integration cases include native, compressed and serialized SQLite, redb, independent opens, two actual SQLite processes, VALUES/SELECT, no/text/composite primary keys, fixed snapshots, failed RETURNING, savepoint undo and reopen. Strict execution/Engine all-target Clippy, formatting, dependency, ownership, harness, header, file-size, benchmark coverage and hygiene checks pass. Those INSERT cases did not address the earlier SQLite concurrent nextval BUSY failure; the separate admission correction above supplies that evidence. No allocation or timing workload is rerun.

Sequence authority verification covers 562 distinct owning-crate unit cases across SQL, execution, common storage, SQLite, redb and Engine. The table-owner rollback assertion now compares complete stored sequence rows, including their original ACLs, and its corrected case passes separately. New cases cover owner/grantee/grantor name reuse, replaced incarnations, PUBLIC, invalid grant graphs, initial-only conversion, later restoration rollback and private/savepoint/reopen schedules across all three providers. The integration selection passes 534 cases (395 Engine, 123 common storage and 16 SQLite), including the manual harness, which compiles SQL fences and executes marked examples. That selection also exposed an Engine concurrent nextval failure with SQLite database-is-locked during reservation. Its standalone diagnostic pass did not establish a fix; the later admission correction above supplies deterministic reproduction and integrated verification. Strict all-target Clippy for the seven owning crates, the subsequent Engine test correction, formatting and repository boundary, ownership, harness, header, file-size, coverage and hygiene checks pass. Common record format changes to 23; no dependency, feature or allocation-ceiling change and no allocation or timing workload rerun accompanies this unit.

Live sequence refresh now uses the existing execution-owned snapshot source for definitions, ACLs, current values, roles and memberships, and only installs the complete result after successful capture. The previous Engine path read sequence rows separately and could install a newly committed owner without its role, including during owned-sequence enumeration. Its provider selection and private-row merging are removed from Engine; the adapter only supplies current state and installs prepared maps. Definition and dependency consumers retain latest autonomous values together with private role grants, private sequences, temporary ownership and savepoint undo. Initial hydration remains inside the complete initial catalog transaction. General definition/ACL publication coordination and fixed SQL-visible catalog snapshots remain open.

Live sequence refresh verification reproduces a missing live owner before the correction and passes 36 Engine sequence/role-snapshot unit cases and 345 sequence, information-schema, callback, extended/foreign DDL, routine-identity and manual integration cases. The new adapter regression covers direct refresh and both owned-sequence enumeration APIs in 27 native SQLite, SQLite Key/Value and redb schedules across all three isolation levels, preserving private role grants, temporary ownership, current autonomous values and savepoint undo without advancing fixed ordinary data snapshots. Existing cases exercise creation, owner attachment, rename, ALTER, DROP/CASCADE, stored references, rollback and durable reopen. Snapshot decoding, private-record merging and read-transaction cleanup remain in the existing execution owner verified by the preceding 82-case execution selection. The manual harness compiles SQL fences and executes marked examples. Strict Engine all-target Clippy, formatting, staged dependency, Engine ownership, harness, header, file-size, benchmark-coverage and hygiene checks pass. Dependencies, features, persistent formats and allocation ceilings are unchanged; no allocation or timing workload was rerun.

Sequence privilege inquiry now captures role, membership and ACL authority together instead of independently installing sequence rows during OID lookup. SQL binds explicit role identities before target resolution, preventing a replacement role with the same name or OID from receiving the original subject's authority. Execution retains private and temporary records through the existing detached snapshot source and preserves ordinary transaction data visibility. The inquiry also now follows PostgreSQL's role-before-privilege-before-target error order and applies PUBLIC grants to the `public` pseudo-role and missing role OIDs. Table/column sequence inquiry uses the detached conversion described below. Definition and ACL publication and durable sequence role-reference conversion remain separate incomplete paths.

The inquiry's PostgreSQL 18.4 Docker reference checks 48 argument-precedence, PUBLIC, unknown-role-OID, NULL and inaccessible-schema cases. It uses image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`; the 200-byte report has SHA256 `6898ab76f314a117a779282d24ae18110984e86e4944cde94bec690b7cd20fad`. The reference agrees with the six inquiry entry points and `get_role_oid_or_public` in PostgreSQL's [REL_18_4 ACL implementation](https://raw.githubusercontent.com/postgres/postgres/REL_18_4/src/backend/utils/adt/acl.c). Generated scripts and reports remain outside the repository.

Sequence inquiry verification reproduces three pre-fix failures: membership revocation still returned true, invalid privilege text lost to target lookup, and the public pseudo-role was rejected. The correction passes 4 owning SQL cases, 25 selected execution cases, 29 Engine sequence unit cases and 102 sequence/callback/manual integration cases. New provider coverage exercises all six inquiry overloads across three providers and three isolation levels, plus private and temporary ACL savepoint restoration in another nine provider/isolation schedules. The manual harness compiles SQL fences and executes marked examples. Strict SQL/execution/Engine all-target Clippy, formatting, staged dependency, Engine ownership, harness, header, file-size, benchmark-coverage and hygiene checks pass. No dependency, feature, provider format or allocation ceiling changes, and no allocation or timing workload was rerun.

SQL table, column and sequence privilege checks now distinguish default owner privileges from the owner's implicit grant options. An explicit ACL can revoke ordinary access from the owner and inheriting roles without removing DDL authority or the ability to regrant. The previous unconditional ownership shortcut bypassed these ACLs, and two existing sequence/table integration cases incorrectly expected that bypass. SQL owns the corrected policy and its default/empty ACL, inheritance and column-union regressions. Execution's sequence parameter inspection now requires an actual ordinary sequence privilege instead of reusing the ownership-aware metadata visibility predicate. Engine adds only public API integration cases across memory, native SQLite, SQLite Key/Value and redb.

PostgreSQL 18.4 Docker independently confirms owner ordinary-privilege revocation and retained grant options for sequence, table and schema ACLs, and ten sequence value/metadata cases including reservation preservation. Both references use image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`. The 343-byte ACL reference has SHA256 `06fe69978d1643b5191e4ea0f67fd338d47e743c67d3289b6d54fdf7d3a98e29`; the 379-byte sequence reference has SHA256 `6cc21ee79e41a0fe78245f2290ea8a4de92115ec865fbf4c1b364fea0a5de04a`. Generated reference scripts and reports remain outside the repository.

Owner self-revocation verification reproduces all three owning SQL regressions before the fix and passes 48 SQL security cases, 77 selected execution cases and 213 sequence/information-schema/callback/manual integration cases after it. The new public cases run against memory, native SQLite, SQLite Key/Value and redb, covering owner inheritance, selective column grants, relation/column grant union, view reads, materialized refresh, DDL, regrant and preserved sequence reservations. Two existing integration expectations are corrected to require 42501 after ordinary owner privileges are revoked. The manual harness compiles SQL fences and executes marked examples. Strict SQL/execution/Engine all-target Clippy, formatting, staged dependency, Engine ownership, harness, header, file-size, benchmark-coverage and hygiene checks pass. No dependency, feature, provider format or allocation ceiling changes, and no allocation or timing workload was rerun.

Table and column privilege inquiry bind explicit role arguments to retained incarnations through the same SQL helper as sequence inquiry. Execution captures current roles before target resolution, preserving private roles without loading unrelated sequence definitions. A lookup that refreshes or replaces role names cannot redirect the original subject to a new role. PUBLIC-only subjects also use the normal relation, column and sequence ACL policy, including lowercase `public` and absent role OIDs. Validation follows each overload's PostgreSQL ordering: named relation/column errors precede privilege parsing, while a missing relation OID or invalid attribute number cannot bypass privilege validation before returning NULL. The owning SQL regressions fail before the correction for identity replacement, PUBLIC and early NULL.

Sequence targets in table and column inquiry retain one detached authority snapshot for OID binding and every comma-separated privilege check. Named lookup completes before that snapshot is captured. A later peer commit cannot split sequence ACLs from their roles or memberships, and a removed sequence cannot reappear from the retained statement catalog. The next invocation captures a new view; current inquiry state is never installed into live registries. Private roles, private and temporary sequence ACLs and savepoint undo share the existing detached merge policy, while ordinary transaction data retains its isolation semantics. Execution owns capture and the shared independent read-transaction cleanup; SQL owns all privilege rules, and Engine only lends its session and current registries. Ordinary table/view/foreign/system authority synchronization, definition and ACL publication and durable sequence role-reference conversion remain required.

The PostgreSQL 18.4 Docker reference passes 150 cases: 117 PUBLIC relation/column/sequence checks, 28 error-precedence cases and five NULL cases. Image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636` matches the [REL_18_4 ACL source](https://raw.githubusercontent.com/postgres/postgres/REL_18_4/src/backend/utils/adt/acl.c). Its 190-byte report has SHA256 `ae8d997f89846f1f3d898a8abc3d107808694372e9100610dd274136bc8a2869`. A second reference on that image passes 66 checks in six schedules across READ COMMITTED, REPEATABLE READ and SERIALIZABLE: newly committed explicit roles, named/OID sequence targets, private and temporary ACLs, savepoint undo and ordinary data snapshots. Its 232-byte report has SHA256 `d2687a019229062099ddfcc302256df42cdfc899f7b650c5f88c0bcbc924c0b2`. Generated reference files remain outside the repository.

Table/column sequence authority verification passes 111 Engine catalog role/sequence unit cases, 82 owning execution cases, 96 owning SQL security/role cases and 215 sequence/information-schema/callback/manual integration cases. Two Engine regressions fail before the correction for mixed ACL/membership views and missing newly committed role identities. Three new Engine cases cover 36 provider/isolation schedules across native SQLite, SQLite Key/Value and redb, including both inquiry functions, named/OID subjects and targets, private and temporary ACLs, savepoint undo and unchanged ordinary data isolation. Four owning execution cases verify one sequence authority read per invocation, fresh later calls, retained subjects, removed OIDs and validation before catalog work. The shared read-transaction guard retains its success/error/unwind and caller-transaction tests. The manual harness compiles SQL fences and executes marked examples. Strict SQL/execution/Engine all-target Clippy, formatting, staged dependency, Engine ownership, harness, header, file-size, benchmark-coverage and hygiene checks pass. No dependency, feature, provider format or allocation ceiling changes, and no allocation or timing workload was rerun.

## Session role identity integration

Authorization-interface verification passes 545 SQL unit cases, 435 execution unit cases (the two subprocess entry points are exercised by parent tests), 45 SQL integration cases and all 30 Engine role cases across the persistent providers. The three identity cases also pass after splitting the ACL fixtures. Strict SQL/execution/Engine all-target Clippy, formatting, ownership, dependency, integration-harness, header, line-limit and hygiene checks pass; dependency checks retain 109 runtime workspace edges.

Role incarnation comparison and authorization subjects now belong to `uqa-sql::catalog::roles::identity`; execution reuses the same `RoleBinding` for dependency waits. Table, column, schema, sequence, database, view and system-relation permission checks accept a bound subject, and grantor selection and membership traversal reject a deleted or replaced subject while preserving PUBLIC access. Explicit name arguments still resolve the named current role. The owning SQL tests distinguish changed attributes, rename, name reuse, OID reuse, inheritance, SET authority, ordinary ACLs and grant options. Session integration now retains authenticated, session, selected and effective role bindings in SQL-owned authorization state. Engine stores shared immutable identities, execution owns SET/RESET authorization, and query/CTE, view/rule and DML subjects carry the bound identity through permission checks. CURRENT_USER and SESSION_USER preserve typed missing-identity errors; role and session-authorization LOCAL restoration entries remain independent. Public behavior is documented in the transaction manual.

Stored reference publication keeps the original role-name check in addition to OID and incarnation validation. Authorization lookup may find the same identity under its new name, but dependency publication rejects a changed name until its candidate is rebound safely. A dedicated regression places a renamed original and a privileged replacement of its former name in the same catalog, checking both authorization and publication semantics. Full owning suites then pass 546 SQL and 436 execution cases, and both Engine role-identity cases pass, including create/undo/recreate/reopen across all three providers. Strict SQL/execution/Engine all-target Clippy and repository ownership/dependency, formatting, harness, header, line-limit and hygiene checks also pass.

`SET ROLE` now forwards the optional reset marker from the SQL compiler to execution without converting it into a role name. For explicit text values, only the exact `none` setting resets the role; quoted `NONE`, `DEFAULT` and `default` retain their names. Missing names and empty-string targets return `22023` without changing the selected identity. The manual uses the valid `SET ROLE TO DEFAULT` syntax; bare `SET ROLE DEFAULT` remains a parser error. Explicit role-name decoding remains distinct from the retained session identity. Verification passes all 14 execution role-lifecycle cases and one Engine regression covering all three persistent providers, quoted and string-literal names, missing/empty targets, reset spellings, SET LOCAL, savepoints and full rollback. The manual harness passes both compiled SQL fences and executed examples. Strict execution/Engine all-target Clippy, formatting and repository ownership/dependency, harness, header, line-limit and hygiene checks pass.

PostgreSQL 18.4 Docker supplies six deletion/recreation sessions and 102 post-deletion observations spanning SET ROLE, SET SESSION AUTHORIZATION and authenticated login, with both initially ordinary and initially superuser roles. Old identities do not acquire the replacement's superuser or ACL rights; PUBLIC reads succeed, current-name lookup reports `42704`, and explicit name inquiry sees the new role. RESET succeeds without re-resolving a deleted session role. The reference is a temporary 12,215-byte artifact with SHA-256 `8d855ffc4947eacbb2bbe834f3dc454ca11f97dc9b1a50c745f772157af26c06`, not a committed machine report. PostgreSQL's [SET ROLE](https://www.postgresql.org/docs/18/sql-set-role.html) and [SET SESSION AUTHORIZATION](https://www.postgresql.org/docs/18/sql-set-session-authorization.html) define the distinct authorization sources and reset targets. A further 54 observations verify that SET SESSION AUTHORIZATION checks the authenticated role's current attributes: promotion enables it, demotion disables it, and a deleted authenticated identity does not acquire the replacement's authority (7,782 bytes, SHA-256 `9650a245c4b4ad7eac52a034cee6adb0bb9be724e101451fa8a9481ba9eab862`). No Nori allocation or timing measurement is repeated for this change.

Four new Engine regression cases pass across native SQLite, SQLite Key/Value and redb. They cover all three identity-selection paths with initially ordinary and superuser roles, PUBLIC reads, prepared and invoker-view access, table/column/sequence/routine/database/schema/membership inquiries, live authenticated-role promotion/demotion, SET/RESET/LOCAL, savepoints and DISCARD. A transaction matrix checks deletion/recreation during READ COMMITTED, REPEATABLE READ and SERIALIZABLE and confirms that rollback and LOCAL restoration never bind the replacement. PostgreSQL 18.4 Docker independently confirms both restoration matrices (122-byte setting reference, SHA-256 `6aa957369ceae18c8760cf5a6d8477060f3a0be6b718bb8262124ec67e232076`; 4,604-byte transaction reference, SHA-256 `164bba43842c4f7cd167872d465b0bfe316ac3d39179275a658737a1c6d0214a`). Artifacts remain temporary. Final validation passes all 550 SQL unit cases and 436 execution unit cases, with the two execution subprocess entry points exercised by parent tests. The full Engine unit run passes 302 cases; all four ownership cases then pass after the remaining fixture uses the existing memory-compatible definition transaction scope. All 33 trigger-definition cases pass with the redundant role-name lookup removed from the event expectation, retaining the permission-denial SQLSTATE and error ordering. All 324 selected Engine integration cases pass, including compiled manual SQL fences and executed examples. Strict SQL/execution/Engine all-target Clippy and repository formatting, ownership/dependency, harness, header, line-limit and hygiene checks pass. This does not complete membership coordination or the remaining concurrency plan.

Routine configuration restoration uses SQL-owned parameter scopes for session, LOCAL and saved function assignments. Configured role and session-authorization values restore the caller's retained identity at function exit, including after concurrent deletion/recreation. Ordinary SET and RESET supersede matching restoration across enclosing calls; LOCAL on a configured parameter restores at function exit, while an unconfigured LOCAL lasts through the transaction. Definition validation and error guards retain both values and pending restoration. SECURITY DEFINER restores its effective identity independently of successful generic setting assignments. Engine's direct search-path and session-transaction-characteristic adapters use the same SQL policy. Execution only schedules entry, configuration and exit; Engine retains the typed values and guards.

The initial three regression tests reproduced configuration leaks and lost LOCAL restoration before the fix. PostgreSQL 18.4 Docker independently confirms the basic restoration matrix (166-byte reference, SHA-256 `fa3f6b5bdc4cfce208ce1c95d45b3e4813ba620ebdbd2a1a92642040e024d666`) and nested assignments, unconfigured LOCAL, caught errors and RESET ALL (232-byte reference, SHA-256 `6957eba6360f2e59962c7843ecb597fc4cd6801a4e04d0938a929ec464f2ddc6`). Artifacts remain temporary. The setting-value tests exposed the absent `current_setting` function; its two fixed text/boolean overloads now use SQL argument, strictness, missing-setting and volatility rules, execution-owned PostgreSQL catalog metadata and the existing Engine SHOW state adapter. The [CREATE FUNCTION SET contract](https://www.postgresql.org/docs/18/sql-createfunction.html) and [GUC nesting implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/utils/misc/guc.c) remain the restoration reference. Focused verification passes 15 SQL parameter-scope, setting-expression and fixed-overload cases, all nine new Engine routine/setting regressions and five existing session-identity/role-setting cases. All 273 selected Engine integration cases pass, including manual SQL compilation and executed examples, routine identity/security, PL/pgSQL, prepared statements, search paths and callback transactions. Strict SQL/execution/Engine all-target Clippy and repository formatting, ownership/dependency, harness, header, line-limit and hygiene checks pass. No additional Nori run was performed. Membership coordination and the remaining concurrent-transaction work remain open.

## View definition and source coordination

Execution now shares provisional relation binding and identity-based descendant tracking between explicit table locks, query sources and view definition changes. View creation locks sources before binding, queries retain AccessShare on regular and materialized views, and creation/replacement, view alteration, owner/rename changes and ordinary materialized refresh retain AccessExclusive on the target. Materialized-view fillfactor SET/RESET uses ShareUpdateExclusive and allows concurrent readers. SQL owns that action-specific mode selection. Definitions and owner/maintenance authority are loaded again after a wait. Missing creation targets claim the name before publication and recheck existence after competing creators finish. Engine supplies live catalog/session adapters and defers physical writer admission until execution has acquired its definition locks; direct memory view APIs also receive a transaction-owned lock scope.

PostgreSQL 18.4 Docker (`postgres:18.4`, image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`) confirms 14 cases covering read-versus-definition conflicts, savepoint release, post-replacement SELECT/ALTER results, post-refresh rows, owner changes during ALTER/replacement/refresh waits and fillfactor SET/RESET while another session retains an AccessShare reader. Runtime traces stay outside the repository. General definition/constraint coordination, virtual catalogs, concurrent materialized refresh/indexes, complete isolation histories, reclamation and binding/platform acceptance remain open implementation work.

Verification passes 628 complete Engine/execution unit cases (231 Engine and 397 execution), 286 selected Engine integration cases covering views, row locking/rechecks, concurrent writers and the manual SQL harness, and final focused checks including all 11 new Engine view-lock cases and the SQL action-to-mode policy. The final Engine selection passes 30 cases. Strict SQL/execution/Engine all-target Clippy and formatting, staged dependency, Engine ownership, harness, header, file-size and diff checks pass. These results cover the view coordination change; the full concurrency acceptance rows remain open.

Object creation now participates in the shared owner-dependency protocol for ordinary and temporary tables, implicit serial sequences, CTAS, views, materialized views, foreign tables, sequences, schemas, functions and procedures, including direct table/schema APIs. The owning execution paths capture the selected role before dependency waits, retain AccessShare until transaction completion or savepoint undo and reject a deleted or recreated owner with `42704`. Initially skipped IF NOT EXISTS targets and replacements preserving their original owner do not add a dependency on the invoking role. SQL owns temporary owner/grantee/grantor projection; execution publishes session-lived references before releasing transaction locks, with a rollback guard across provider commit and resolution. Native leases make committed temporary dependencies visible to peer processes and disappear on process exit without treating stale slot bytes as live owners. DISCARD outside a transaction releases session references directly; transactional DISCARD retains them until commit so rollback and savepoint undo preserve the previous dependencies. Shared reference capacity is bounded and exhaustion reports `53200`; corrupt live metadata fails closed, and native admission observes cancellation. Engine retains catalog/session adapters and commit/drop lifecycle guards. Common record format 16 excludes preceding writers; native mapping 8, role-record 2 and routine-record 1 remain unchanged.

The creation reference uses PostgreSQL 18.4 Docker image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`: 273 ordered creation/deletion schedules span thirteen object forms, three isolation levels and commit, rollback, savepoint undo and same-name recreation. The ignored 18,195-byte result has SHA-256 `461659cda6db48e403127a83707d0168c46450e55147abdc05ef5b9369f6687d`. Another 25 temporary owner/ACL/discard lifecycle outcomes use a 261-byte result with SHA-256 `2c7c7955246799b0aa5bc6994098b48d1dac4b6b4e3388b41a3cff5683c5a3d7`. [PostgreSQL shared dependencies](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/catalog/pg_shdepend.c) supplies the owner-reference and role-lock source contract. Actual binaries from `32ed1a517e1c29a39604d7be745d3bb02099e1ad` verify format-15 to format-16 conversion in all nine file configurations, preserving identities, records and receipts while rejecting retained predecessor writers. Creation coordination does not complete membership coordination or remaining catalog/definition locking; the separate session identity implementation is described above.

Creation validation passes all seven Engine cases: 351 creation-before-deletion schedules, 468 deletion-before-creation schedules and 81 temporary lifecycle, no-op/replacement and direct API cases across the three providers. The owning unit suites pass 435 execution, 542 SQL, 346 SQLite and 12 redb cases; both ignored execution subprocess entry points are exercised by their parent tests. Final guard and shared view-analysis checks pass fourteen Engine unit cases and 208 selected Engine integration cases, including compiled manual fences, executed examples and rejected/uncertain commit resolution. Strict five-crate all-target Clippy, formatting, ownership, dependency, harness, header, file-size and hygiene checks pass. That earlier broader Engine integration selection passed 1,373 cases and failed `pg18_vacuum_full_reclaims_persistent_file_space`; the retention and lossless current-record units above subsequently close this failure. No additional Nori allocation/output or timing run is performed.

## Occurrence encoding allocation

Common occurrence encoding exposes its caller-controlled entry point to providers. SQLite physical batch and source-rebuild adapters share one control across clusters; encoded buffers retain their allowance through persistence, and cancellation or quota failure preserves typed errors and existing postings. The encoder reserves the known final position-buffer size and minimum score-stream size before appending bytes. Checked arithmetic, fallible allocation, cancellation and wire output remain unchanged. A fixed-byte fixture covers multiple varint widths and maximum document length with an independent decoder.

Common storage shares evaluated occurrence buffers when the committed boundary has not advanced, preserving three-way merges for competing commits. Staging validates each field revision once per fixed read and moves analyzed occurrence vectors into cluster changes. Evaluated keys/values share charged owners with private history, and direct writes avoid temporary single-record commits. Publication borrows large committed position streams; split Key/Value records retain only compact scores before borrowing positions, preventing provider read-window reentry. Canonical preconditions reach atomic physical admission unchanged, while command refresh checks them before exposing a refreshed view. redb uses visible head revisions for exact lookup and one charged encoding workspace; a failed reservation leaves the durable receipt pending and the sealed batch retryable.

Owner regressions cover quota, cancellation, corruption, atomic conflicts, savepoint undo, retained snapshots and retryable physical publication. A 64 KiB payload fits a 96 KiB allowance through batch application and savepoint undo; final snapshot release returns the allowance to zero. A 30,000-position committed graph validates under an 8 KiB allowance without copying its large stream. Failure injection directly constrains application headroom and preserves prior rows and queued identifiers. The complete three-owner run passed 1,160 unit/integration cases, including predecessor formats 1–15 to current format 16. After the final borrowed-validation correction, all 487 common-storage/redb cases and 29 SQLite occurrence cases pass. Strict three-owner all-target Clippy and repository dependency/ownership, harness, header, line-limit and hygiene checks pass.

Persistent Nori checks use `--allocation-only`: one counted mutation and live/reopen verification for each of the four existing workloads, without warmups, benchmark clock reads or timing fields. They preserve every graph, metadata/count output and all six allocator ceilings; every excess is reported after complete output validation. Entrypoints assert and report their actual transaction model. Completed redb transactions must return private retention to zero before graph verification or close. Limits schema 3 rejects a mismatched or unspecified model as well as an unknown target. All 116 Nori verifier tests and 23 CI-dispatch tests pass; strict provider-benchmark Clippy also passes. Native CI can select this allocation-only job independently of timing and executes both providers even if one fails. The obsolete random pre-MVCC two-table commit-order probe is removed.

At `a91b112ab4267584336564bc0f9b9086a9dc7f0d`, one complete allocation-only observation on each of macOS/aarch64 and Linux/x86_64 verifies all four redb outputs/reopens and zero private retention. Both reports identify the same runtime, benchmark and lockfile hashes. The MVCC budgets are now reviewed separately from the former serialized-provider budgets: exact observed counts, retained bytes and peak bytes, with only a source-derived maximum nine-byte table-order range added to commit totals. Logical rollback receives no such allowance. The macOS 16/2048 commit total cap is 172,690,252 bytes versus the former 179,452,985; its peak cap is 32,454,609 versus 39,746,417. Shared-cache retention explains most of the rollback net bytes, while the independent private allowance must remain zero. All cumulative-byte, peak-byte and peak-count caps decrease on both targets. The same unchanged reports pass every revised cap without reexecution. [Persistent benchmark documentation](../../benchmarks/nori/PERSISTENT.md#mvcc-allocation-budgets) records the derivation, before/after caps and compact report provenance; generated reports stay ignored or in the [CI artifact](https://github.com/cognica-io/uqa-engine/actions/runs/35342064795/artifacts/10545219467). The collection run retains its original old-limit failure and is not described as complete passing CI.

SQLite's numeric ceilings remain unchanged. The new Linux observation passes all four workloads and six caps; earlier native macOS and actual Emscripten observations also passed, with SHA-256 `d247607bb0f6bec2f037da08d96690b8a75c1b7289f8e099175ab1b59068fa3b` and `e2b591712d6af15b34d4b71a41697febf914b6589d7e80515dcb2a7ec1443b65`. These SQLite entrypoints use unbound physical transactions, so bound native MVCC and SQLite Key/Value Nori allocation acceptance remain required. The fixed redb workloads do not complete history reclamation or the full concurrent-storage matrix. No timing acceptance is claimed.

## Implementation units and dependencies

The units below are implementation order and review boundaries, not names to copy into source comments, tests or permanent policy labels. Each unit includes owning tests and a plan update. Provider units can share the same established contract; end-to-end enablement waits for every required dependency.

| Work unit | Owner and intended changes | Dependencies | Exit evidence | Status |
| --- | --- | --- | --- | --- |
| Transaction access inventory and deterministic schedules | Storage/execution owners plus Engine adapter tests: enumerate every durable read/write/open/maintenance path; capture PostgreSQL schedules and current blocking behavior; define the transaction-model capability and typed results | This design | Reviewed access inventory linked to source; reproducible old failure; compact Docker reference traces; no runtime capability advertised | In progress: initial source inventory and three PostgreSQL 18.4 schedules captured; current 0.3.6 SQLite blocking reproduced. Built-in catalog/backend affinity is checked before Engine restoration and sibling attachment; the transaction-model contract is implemented; the complete path audit remains. |
| Versioned record and session contracts | `uqa-storage::mvcc`: stable identities, commit sequences, provider read/commit interface, transaction handle, reference persistence implementation and conformance entry points | Access inventory | Common-crate tests for point/absent/range reads, tombstones, snapshot admission, command visibility and atomic multi-family commits; no provider imports | In progress: common cases cover record histories, private overlays, borrowed merged reads, receipts, logical Key/Value sessions, paired-handle affinity and definition requirements with mergeable data markers. Session commits seal evaluated changes and retain uncertain outcomes; stable object identities and SQL integration remain. |
| Private changes and savepoints | Common storage: ordered overlays, typed mutation log, undo positions, transaction-written state, retention accounting and spill interface | Versioned contracts | Nested and duplicate savepoint cases; read-your-writes; rollback/cancellation/allocation failures; command-boundary tests; no physical writer retained during idle SQL time | In progress: ordered private replacements, retained command views, non-reused private batch identities, bounded changed-key pages, nested/repeated savepoints, allocation-free undo and final preparation implemented. Engine command refresh and savepoint integration are implemented; spill and intermediate journal reclamation remain. |
| SQLite versioned persistence | `uqa-storage-sqlite`: native relational and K/V record adapters, history/head tables, bounded reads, atomic receipts, encrypted/compressed paths, coordinator transport and native-write guards | Versioned contracts and private changes | Both SQLite layouts pass common conformance; SQLCipher and compressed reads/writes remain protected; two independently opened providers coordinate correctly; the private guard function works in native/Emscripten runtime builds | In progress: guarded FULL-synchronous record commits and bounded historical reads serve Key/Value sessions. The explicit native adapter maps 49 catalog/index families including table owners, graph lookups and occurrence accelerators, plus seven scoped standalone graph families, imports schema 48 atomically, and publishes evaluated native rows, trigger-derived cache revisions, history and receipts together. Native document/B-tree, occurrence/accelerator and exact/IVF/HNSW vector APIs and metadata/schema/model/analyzer/view/foreign/table/index/column/statistics/sequence and graph catalog operations, including cache-generation reads and restore-time namespace validation, now share managed logical sessions, compound read snapshots and atomic batches. Tests cover independent prepared and public document writers, generation retirement, cascade omissions, failure rollback and old-writer rejection. Complete native catalog/backend session routing, complete native conformance, admission/lease coordination and Emscripten runtime acceptance remain. |
| redb versioned persistence | `uqa-storage-redb`: logical sessions, head/history/receipt tables, native transactions restricted to durable operations and file-owner coordination | Versioned contracts and private changes | Shared conformance and crash/reopen tests; no retained `WriteTransaction` between SQL commands; preserve typed errors and exclusive file ownership | In progress: existing sessions now use common logical transactions and short Immediate-durability commits. Ten file-backed MVCC/session/migration scenarios and eight native fault/format cases pass, alongside existing provider contracts. Process-crash recovery, reclamation and full SQL integration remain. |
| Shared logical mutations and physical indexes | Existing storage owners plus graph persistence: per-row deltas, evaluated expression keys, posting/occurrence merges, checked counters, canonical vectors and atomic IVF/HNSW generations, object/name mappings | Versioned contracts and private changes; provider tests as adapters land | Concurrent same-cluster/counter/HNSW-node cases preserve both writers; index generations match visible canonical values before and after rollback/reopen | In progress: native per-document B-tree definitions, entries, repair markers, replacement and sparse repair share document sessions. Common typed Key/Value graph effects merge cache invalidations, discover late dependencies, validate builds and preserve reassigned caches with savepoints and receipt-safe preparation retries. Shared deterministic schedules cover memory, redb and all four SQLite file modes including reopen; real provider admission tests force an intervening commit. Bound native graph catalog mutations and path definitions/pairs now use the same resolver and versioned path directory. Complete cache restoration, coordinated allocation, fair preparation admission and general index-definition fencing remain; native SQLite and common Key/Value HNSW merges now share the vector input journal. Common Key/Value occurrence writes now merge evaluated per-document cluster changes and checked field totals with document/structure guards and late-cache invalidation. Native exact/IVF/HNSW and occurrence/accelerator consumer routing is implemented, including retained graph/canonical views, atomic HNSW deltas and source-fenced scorer bounds. Physical HNSW cache identity and atomic candidate preparation are repaired. Common graph cache and algorithm workspace accounting remains part of resource acceptance. Entity/membership lifetime requirements now coordinate raw and GraphStore endpoint writes, orphan cleanup and topology changes in the same evaluated native/KeyValue batches. Shared endpoints and property-only changes retain independent commits; internal guards do not advance user catalog generations. Common record format 9 rejects prior writers and upgrades formats 1–8. Lifetime verification passes the full owning-crate suites: graph 79 unit and 113 integration, common storage 222 unit and 169 integration plus one documentation case, redb 11 unit and 39 integration, and SQLite 332 unit and 323 regular integration cases with its ignored process helper exercised by parent cases. Deterministic schedules cover both commit orders, shared vertices, compatible property updates, edge topology versus new ownership, orphan cleanup, savepoint undo and reopen; SQLite covers native catalog, standalone and Key/Value paths in all four file modes. Thirteen Engine AGE SQL cases and 11 final cache/trigger migration cases pass, as do strict five-owner Clippy and ownership/dependency/harness/header/line-cap/hygiene checks. The Emscripten SQLite library compiles; browser runtime acceptance remains pending. |
| SQL lock and conflict integration | `uqa-execution` and SQL owners: logical reservations, target-version rechecks, predicate observations, deferred constraints and deadlock/wait integration | Common sessions and logical mutation contracts | PostgreSQL schedules for row/key conflicts, `ON CONFLICT`, `MERGE`, FK/exclusion checks, NOWAIT/SKIP LOCKED, partition movement and real deadlocks | In progress: eight-mode explicit table locks, conditional native/local acquisition, ANALYZE coordination, binding rechecks, ordered explicit DROP target locking, direct view deletion, inheritance, stored-view privileges and view/query-source definition coordination are implemented. Materialized fillfactor changes use ShareUpdateExclusive. Virtual catalogs, remaining definition consumers, complete row/key/constraint coordination, predicate observations and timeout/isolation acceptance remain. |
| Serializable dependency tracking | Common storage algorithm with execution observations: overlap graph, commit-order rules, retained dependencies and safe snapshots | Versioned contracts and SQL observations | Forced write-skew/phantom/read-only/pivot schedules, resource bounds and committed-reader retention; safe/unsafe histories compared with PostgreSQL | In progress: common storage now implements a budgeted read/write dependency graph, prepare/commit ordering, retained conflict summaries and safe-snapshot classification. All 50 SSI dependency, observation, admission and publication cases pass within 62 storage MVCC tests. The changed Engine and execution error adapters each pass their focused test; strict three-owner all-target Clippy passes. Earlier wider owner results remain recorded below. SQL predicate capture, provider/process coordination, SQL lifecycle integration and complete serializable histories remain required; Engine SERIALIZABLE is not yet complete. |
| Engine session and publication adapters | Engine transaction/open/cache/notification owners: select model, remove concurrent-session lifetime writer gate, retain snapshots, refresh overlays, resolve durable receipts and publish revisions | Both providers, logical mutations, SQL conflicts and serialization tracking | B commits before A is released on all persistent paths; A commit/rollback/savepoint leaves B intact; catalog/graph/index/cache publication is consistent | In progress: attachment validates catalog/backend affinity, and finish recovery preserves retained state until the provider outcome is known. Common typed outcomes now drive live-session commit resolution without repeating SQL preparation or callbacks; committed and aborted results follow the corresponding session completion path. Model selection, command refresh and removal of lifetime writer ownership are implemented for built-in providers. Fixed-snapshot namespace authority refresh preserves exact private schema records beside fresh committed schemas. Complete publication-failure recovery and durable cross-process publication remain. |
| Allocation, maintenance and extension boundaries | Existing sequence/execution/storage owners; Engine adapters: durable ID/sequence allocation, background statistics, notifications, temporary objects, callbacks, cursor/FDW boundaries | Sessions, provider adapters and Engine integration | No ID collisions; PostgreSQL sequence and event order; maintenance joins the protocol; no callback replay or unrelated rollback | In progress: native statistics and sequence catalog/value operations join logical sessions. Engine document allocation uses durable object/generation namespaces, legacy seeding and TRUNCATE continuation/restart; native and Key/Value document writes consume evaluated ID observations and guarded ownership (`bd4ac347`). Autonomous watermark reads now serve read-only and sealed sessions without physical write admission. The identifier selection (`cargo test -p uqa-storage -p uqa-storage-sqlite -p uqa-storage-redb -- identifiers`) passes 38 cases, followed by final SQLite/redb owner checks, strict four-owner Clippy, Emscripten library compilation and repository policies. Emscripten evidence is compile-only. Persistent graph consumers now reserve entity/label IDs independently, retain graph identities through rename, isolate clear generations and project reservation floors into exports. Graph definition/data guards cover both commit orders. Owner verification passes graph 79 unit/113 integration, common storage 222 unit/162 integration/one documentation case, redb 11 unit/37 integration and SQLite 332 unit/318 integration cases (plus its parent-invoked ignored process helper), with 10 Engine Key/Value regressions. SQLite cache projection/trigger migration excludes internal allocation guards; prior text/binary triggers preserve history on upgrade. Strict five-owner Clippy, Emscripten library compilation and repository policies pass; Emscripten remains compile-only. Raw graph catalog mutation/restore now uses evaluated batches, supplied-ID/imported-counter observations and graph definition guards; Key/Value compound reads retain one view. Format 8 preserves explicit observations separately from completed legacy prefix seeding and upgrades formats 1–7. Raw-consumer verification passes the full graph/common-storage/SQLite/redb owner suites: graph 79 unit and 113 integration cases, common storage 222 unit and 165 integration cases plus one documentation case, redb 11 unit and 38 integration cases, and SQLite 332 unit and 322 regular integration cases with the ignored process helper exercised by parent cases. The final fixed-view maximum-ID correction passes the complete common-storage suite with 166 integration cases, including a competing mutation between identity pages. Ten Engine SQLite Key/Value regressions, strict five-owner all-target Clippy, formatting and ownership/dependency/harness/header/line-cap/hygiene checks pass. The Emscripten SQLite library compiles; this is compile-only evidence. Graph endpoint lifetime follow-up is tracked with shared logical mutations; the complete concurrent SQL boundary remains pending. Autonomous SQL sequences, general definition coordination, background maintenance and remaining extension schedules remain. |
| Version reclamation and failure recovery | Common storage horizon/retention algorithm, provider coordination and secure spill | Providers, publication protocol and serialization tracking | Pinned history survives GC; dead-process cleanup; bounded memory/spill; crash points around durable commit; encrypted temporary data; cancellation and indeterminate outcomes | In progress: common record reclamation now returns unused vector capacity and propagates allocation failures while preserving retained revisions. Provider leases, spill, crash recovery and receipts remain. |
| Existing-file migration and default enablement | Provider migrations, Engine open adapters and upgrade manual | Provider adapters, logical mutations, Engine/SQL integration, allocation and recovery | Atomic/idempotent old-file conversion; actual 0.3.6 writers cannot alter upgraded files; encrypted/compressed backup/reopen; every built-in path advertises capability only after complete conformance | In progress: both Key/Value providers migrate legacy bytes atomically and reject released writers. The explicit native SQLite adapter converts initialized schema 48 files in all four modes and rejects actual released catalog/direct-store writers. Native initialization now joins default Engine restoration atomically; process-crash/backup matrices and full SQL acceptance remain. |
| Binding, platform and final acceptance | Existing Rust, Python, Node.js, WASM/server tests and CI | All runtime units | Complete matrix below at final implementation head, full affected-owner tests, policy checks, public manual/upgrade updates and reviewable PR | Pending |

Introduce and test the memory reference persistence implementation in common storage first, then exercise the exact same conformance contract from both provider crates. Integrate actual Engine SQL as soon as the providers and mutation paths can preserve the required invariants; do not postpone all integration discovery to the last commit. Intermediate commits can expose internal contracts, but incomplete combinations must not claim the concurrent capability.

Common definition requirements and immutable data markers now provide the prerequisite for document-owner adoption without forcing independent document writes to conflict on one owner record. `mvcc::verify_revision_guards` covers independent writers, both definition/data commit orders, requirement-only validation and savepoint undo; owning tests cover absent/recreated definitions, bounded retention, discarded evaluation, combined graph/vector/occurrence preparation and sealed receipt retry. SQLite Key/Value exercises all four file modes and redb exercises closed-file reopen. Common Key/Value document ownership/seeding now consumes these guards; general definition coordination and concurrent Engine SQL remain required.

Common Key/Value IVF and HNSW use the same view-based cache and exactly-once mutation adapter. Metadata, centroids/assignments or graph nodes and canonical tensors share one compound boundary and the original write preconditions. Errors, unwinds and cancellation discard one operation while preserving earlier private work. Exact snapshots retain their selected canonical tensors and memory reservations instead of following the live session; all three snapshot kinds reject mutation after their original handles close. Shared schedules cover memory reference stores, common versioned sessions, SQLite Key/Value in all four native file modes, and redb, including independent commits, rollback/savepoints, definition recreation and reopen. Exact snapshot accounting/cancellation tests also verify shared reservations and failure cleanup. The common IVF owner now prepares immutable metadata from evaluated mutations with reserved candidate/scratch memory, cancellation and retained result allowances; native SQLite and Key/Value callers use the same replacement/delete/training behavior. Other compound consumers, general definition fencing and complete IVF/HNSW cache/search accounting remain required.

Common Key/Value occurrence indexes now evaluate mutations against their original read boundary and use fixed readers for compound and controlled queries, including bulk statistics and scoring inputs. A deterministic interleaving replaces a document between score/length and metadata reads; all projections retain the original complete result. A second schedule commits another writer to the same document after evaluation and verifies a conflict without replay or lost winner data. Additional deterministic schedules exposed cross-field totals mixing 5 instead of 3 occurrences and a paged cursor returning a later frequency of 4 instead of the retained 2; fixed common readers and cursor-owned snapshots preserve the original results. The `OccurrenceStorage` projection contract and typed byte addresses allow native row adapters to reuse these algorithms with already compiled analyzer bindings. Retained occurrence snapshots preserve committed/private generations through savepoints, rollback and later commits; SQLite Key/Value and redb retain MVCC owners without copying the corpus. Shared conformance covers memory, versioned sessions, all four SQLite file modes and redb, with reopen, read-only enforcement, typed quota/cancellation errors and reservation cleanup. General definition fencing and full algorithm workspace accounting remain required.

Bound native SQLite occurrence reads, mutations and retained cursors now reuse common storage algorithms through typed column projections. Mapping format 4 normalizes skip/block-max tables with source-owner validation; document/skip scalars and field counters retain their distinct byte encodings while preserving physical SQLite integer values. Common accelerator builds use the occurrence view, reject mixed scorer identities and publish atomically; source writes and column lifecycle changes invalidate caches and reject late competing builds. Tests cover independent native index writers in all four file modes, committed/private snapshots and cursor pages, rollback/savepoints, exact materialization retry, source rebuilds, format upgrades, retained bounds, invalid scorer rollback, source changes during scoring without replay, and source/build ordering and late-cache invalidation. General definition coordination and complete Engine SQL transaction integration remain open.

Shared occurrence verification covers empty and populated clusters, independent inserts/replacements/deletes, checked field totals, same-document writes to different fields, savepoint undo, retained readers, table/column lifecycle conflicts, late accelerators and combined graph effects. Admission-change and lost-reply fixtures preserve one sealed fingerprint and do not double-apply counters. Common encoding charges temporary/output buffers and releases them on quota or cancellation failure. Provider tests check merged values again after closing and reopening the files.

Native row publication now uses the same `OccurrenceRecordLayout` resolver as Key/Value publication. Deterministic native schedules cover same-cluster document merges, replacements/deletions, retained private snapshots, same-document cross-field conflicts, clear/rebuild and catalog lifecycle ordering, late accelerator invalidation and reopen in all four file modes. Native mapping format 5 adds stable document and structural guard records; predecessor upgrades preserve history and receipts and roll back failed guard installation. Shared counters remain checked against native integer bounds. General definition coordination and the remaining SQL, resource and platform acceptance rows remain open.

Native occurrence merging verification passes 44 native record unit tests and 100 native SQLite integration tests; the final 17 occurrence regressions also cover the first writes to an empty catalog-owned index. Common-storage verification passes 202 unit and 122 integration tests, and redb passes 8 unit and 30 integration tests. Failed native publication can retain its original evaluated batch, admit a separate document commit and then merge both results on retry in all four file modes. Strict three-owner all-target Clippy, the final integration-target lint, Emscripten SQLite library compilation and repository policy checks pass. Emscripten remains compile-only evidence; the implementation does not yet establish complete concurrent Engine SQL or final binding acceptance.

## Access-path audit deliverable

The [initial runtime access inventory](../design/concurrent-storage-transactions.md#runtime-access-paths) identifies native and K/V provider bypasses, initial restore, transaction control, execution locks, shared indexes, graph handles, registries, allocation, notifications, maintenance and retained resources. [`capture_concurrent_writes.py`](../../tests/parity/pg18/capture_concurrent_writes.py) captured three PostgreSQL 18.4 schedules with B's completed commit preceding A's commit/rollback/savepoint ending. Their [compact fixture](../../tests/parity/pg18/concurrent_writes.expected.json) includes SQL steps and observations. A bounded public-0.3.6 SQLite reproduction left B unfinished while A held a write; B committed after A rolled back, preserving rows `(1,0)` and `(2,20)`. This is a correctness baseline, not timing evidence. That release also rejects SQL `lock_timeout`; the PG18 manifest now records these unresolved locking bugs.

The implemented [`mvcc`](../../crates/uqa-storage/src/mvcc/mod.rs) module owns record histories and private changes. `PreparedRecordCommit` owns evaluated bytes before commit admission and validates every original revision, including tombstones. `MemoryVersionStore` publishes complete prepared histories atomically and registers snapshots under the same gate as publication and reclamation. Foreign-budget prepared values are copied into the store allowance before acquiring publication admission; values already owned by that allowance remain shared. `PrivateRecordChanges` stages atomic batches, retains fixed private command views across undo and later writes, restores nested/repeated savepoints without allocating, and prepares final replacements without copying payloads. Intermediate private history stays in its charged journal until the owner and retained views are dropped; spill and intermediate reclamation remain open. The `CommittedRecordSnapshot` interface and `MergedRecordSnapshot` reader combine committed versions and fixed private changes in bounded ordered pages without duplicate identities or implicit snapshot advancement. Record reclamation returns unused header capacity through the core budgeted vector owner; shrinking failure preserves retained revisions and reports a typed resource error. The record and private-view tests include generated savepoint histories and merged pages compared against independent copied-map models; the session and complete owner-suite evidence is recorded below. This remains a storage building block: Engine SQL command/savepoint integration and serializable isolation are not established.

The common [`VersionedPersistence`](../../crates/uqa-storage/src/mvcc/persistence.rs) contract and [`redb record adapter`](../../crates/uqa-storage-redb/src/mvcc/mod.rs) persist non-reused transaction allocations, ordered record versions and commit receipts atomically. Prepared batches carry SHA-256 fingerprints computed before the physical writer; retry returns the existing matching receipt and rejects a different batch under the same allocation. Native commit errors are indeterminate, while failures before native commit are rejected attempts. A redb storage-backend synchronization failure test closes and reopens the actual database format, verifies an atomic old-or-new outcome with a matching receipt and resolves the same prepared commit. This is I/O failure/recovery evidence, not process-kill or power-loss evidence. The adapter retains all record versions and receipts pending reclamation.

[`VersionedKeyValueStore`](../../crates/uqa-storage/src/mvcc/session/mod.rs) now owns logical sessions, borrowed reads, ordered batches, prefix deletion and named savepoints. It seals evaluated changes before persistence and retains a failed attempt, including autocommit, until its outcome is resolved. A known committed receipt cannot be reported as rollback. Session retention is explicitly bounded (64 MiB by default); private history and batch buffers share that allowance, while borrowed encoded values do not consume the caller's query decode budget. Owner tests cover independent writer schedules, atomic conflicts, lost replies, mismatched receipts, cached terminal outcomes, duplicate savepoints/prefix deletion, cancellation and batch-allocation failure. Generated merged-read cases also check borrowed visitors against the independent ordered-map model. Current affected-owner verification is recorded below.

redb's existing `store` and catalog/backend session factory now use the common session. File-backed tests prove B's commit precedes A's commit, rollback or savepoint ending, and verify reopen state. The provider passes eight unit and 21 integration cases, including atomic legacy migration, collision rollback and I/O failure/reopen. [`verify-redb-legacy-writer.py`](../../scripts/verify-redb-legacy-writer.py) uses the actual crates.io 0.3.6 provider (archive SHA-256 `b20b61d2f69be5f39186f220599576ad28afe82d12740592c316c018ee721fc4`) to create a file, migrate/mutate it, reject the released writer and verify a second reopen, with redb pinned to workspace version 4.1.0. The existing 15 Engine redb regression scenarios pass, including catalog/document rollback, savepoints, sibling cache refresh, text/vector queries, index lifecycle and legacy content migration. At this provider-only checkpoint, Engine SQL still retained its writer gate. The later Engine integration section records its removal; these earlier tests do not establish concurrent SQL, shared-index merging or complete PostgreSQL isolation.

The new [`SQLiteRecordStore`](../../crates/uqa-storage-sqlite/src/mvcc/mod.rs) implements the same persistence contract using managed connections. It stores unsigned allocations/sequences as ordered eight-byte BLOBs, validates all original heads before mutation, and publishes histories, heads, the sequence and receipt in one short native transaction. Length probes and payload reservations share one physical read snapshot; logical snapshots retain no physical reader between calls. Connection-local write permission guards all record tables and closes on error or unwind. Key/Value conversion replaces the legacy writable table with a guard view, adds a native-catalog guard and records the mapping in durable metadata. The explicit native adapter also guards existing native tables and rejects old raw/direct handles after conversion. Physical writes explicitly select `synchronous=FULL`; legacy connection defaults are unchanged. The `functions` feature is now declared at runtime for both native SQLCipher and Emscripten SQLite.

SQLite evidence now includes the complete provider suite (262 unit and 177 integration tests); its ignored child-process entry point is invoked by two parent tests across all four file modes. The public Key/Value store and raw record adapter allow a second process to commit before A finishes. Tests also cover connection-clone affinity, one-connection in-memory pools, rollback/savepoints, oversized values with key-only reads, migration collisions and resource failure, active-native-transaction rejection, and missing or altered guards. Native `SQLITE_FULL` preserves pending receipts with no partial records; a busy physical COMMIT is indeterminate and resolves using the identical batch. These checks are not process-kill or power-loss evidence. The common suite passes 191 unit, 84 integration and one documentation test, and redb passes eight unit and 21 integration tests.

The [`native record tests`](../../crates/uqa-storage-sqlite/src/mvcc/native/tests.rs) cover the fixed 43-table source inventory, row/key codecs and the added table-owner and graph-lookup families. They exercise baseline import and rollback on allocation failure; independent native record sessions that commit B before A; atomic materialization/history/cache/receipt failure and retry; TEXT/BLOB B-tree identities and cascade omissions; graph invalidation batches; generation retirement and stale-writer rejection; and table/sequence identity migration with atomic catalog rename. Four file modes exercise baseline/reopen, overlapping sessions and injected history-write failure. Malformed absent tombstones, foreign database keys, direct cache writes, format-version mutation and missing guards are rejected. `cargo test --locked -p uqa-storage-sqlite`, strict provider Clippy, formatting and Emscripten library compilation are the affected-owner gates. These lower adapter cases do not establish default native Engine routing, shared-index merging or concurrent SQL acceptance.

Eight native graph lookup cases cover exact label/source/target/membership entries, embedded-zero text and signed identities, selective reads under 16 KiB while entity properties contain 1 MiB values, two independent writers changing edges under the same adjacency prefix, omitted or unauthorized lookup changes, failure/retry and required guards. Plain, SQLCipher, compressed and encrypted compressed files exercise the baseline, overlapping writers and format 1-to-2 upgrade. Retained revisions cover label/endpoint changes, property-only updates, deletion and reinsertion; source histories, allocation counters, database identity and pending/aborted/committed receipts remain unchanged. Injected failures restore schema and history, and inconsistent source/history states are rejected in both directions. A reproduced missing-head case could silently omit lookup history; conversion now rejects every source revision without a head, including already deleted sources. Graph read routing is covered below; the later native catalog mutation unit below completes this routing while retaining the outstanding SQL/constraint gates. At this unit, SQLite passes 274 unit and 235 integration tests, with the ignored child-process helper invoked by parent tests. Strict SQLite Clippy, formatting, dependency/ownership policy, harness registration and repository checks pass. The Emscripten SQLite library compiles; this does not establish browser runtime acceptance.

Six native graph read cases cover legacy/native parity and reopen in all four file modes; combined graph/label/endpoint filters, counts and maximum IDs; 16 KiB identity reads beside 1 MiB payloads and unrelated keys; private changes, rollback/savepoints and a sibling commit; invalid pages/IDs and corrupt memberships; and 300 leading tombstones across private and committed pages. A deterministic writer commits after the first selected ID while the reader retains the original membership and entity payloads. The test also verifies that the selector releases physical I/O before invoking its consumer. These reads use the shared selector contract in common storage. At this unit, SQLite passes 280 unit and 235 integration tests, common storage passes 191 unit, 84 integration and one documentation test, and redb passes eight unit and 21 integration tests. The ignored SQLite child-process helper is invoked by its parent tests. Strict common-storage/SQLite/redb Clippy, dependency and ownership checks, harness registration, repository policy and formatting pass. The Emscripten SQLite library compiles; browser runtime acceptance remains pending. Native graph catalog mutations and shared path-index invalidation are covered by the later unit below; cache restoration and default Engine SQL integration remain open.

Engine passes 202 unit and 180 storage integration tests, including ten SQLite Key/Value cases, 15 redb cases and seven retained-outcome scenarios each exercised over SQLite's four native file modes and redb records. The latter cover lost commit/abort replies, repeated resolution errors, known committed/aborted results, one-time callbacks/deferred triggers/HOLD materialization/temp-table COMMIT actions, implicit statement cleanup, command tags, notifications and reopen. Common storage caches verified terminal outcomes so later I/O failure cannot erase known commit or abort evidence; Engine retains the corresponding session frame and execution skips automatic cleanup while the outcome is unresolved. The 30 execution statement unit tests, strict Clippy for all five affected owners, formatting and Emscripten SQLite/Engine library compilation pass. Ordinary rejected commits still abort retained provider changes before cache restoration; failed cleanup preserves frames and locks. Native relational routing, failures during postcommit publication, durable publication recovery and concurrent SQL enablement remain unfinished; Emscripten compilation is not browser runtime evidence.

[`verify-sqlite-legacy-writer.py`](../../scripts/verify-sqlite-legacy-writer.py) uses the actual crates.io 0.3.6 provider (archive SHA-256 `3db0f72ba7aaf9b64b00ae2b02c8e36a7273d0c969f0b3da47f95d18beb29f6b`) to create both Key/Value and native document fixtures, migrate/mutate them through their respective adapters, reject old writes, and reopen the results in plain, SQLCipher, compressed and encrypted compressed modes. Native checks exercise the released catalog's schema-version rejection and direct document insert/delete guards, then verify physical tuple metadata and retained record history with the new adapter. Key/Value conversion continues to reject native relational files rather than changing their format.

Native graph catalog mutation coverage verifies independent source writers and reopen in all four SQLite modes; late memberships, reassigned paths and stale builds; complete graph replacement/removal with shared entities, duplicate identities and savepoints; failed-batch atomicity; private path binding followed by invalidation; and a 128 KiB session updating selected dependencies beside unrelated 1 MiB entity/cache payloads. Mapping format 3 upgrades both predecessor formats while preserving source history, existing selectors, allocation metadata and receipts at their original boundaries; quota failures, injected history failures, missing heads and changed sources preserve the predecessor file. Common tests retain canonical preconditions through previews and cover late path-specific invalidation. Graph definition and endpoint lifetime requirements are tracked above; complete cache restoration and Engine SQL acceptance remain open; standalone graph routing is described below.

Native cache-generation routing now projects committed rows and retained private key identities through the same catalog snapshot. Tests cover independent commits/reopen in four file modes, transaction and savepoint restoration, new undo branches, distinct sessions, old/new table names, graph ownership, path-cache exclusions, data/statistics/maintenance scopes, retained command views, unrelated large payloads and aggregate read-budget rejection. A reproduced embedded-zero metadata-name bug is fixed in both legacy and native triggers; the known prior trigger upgrades without changing revision counters/history, while native conversion/reopen rejects missing or altered cache tracking. Complete catalog/backend restoration and Engine cache integration remain pending.

Native document routing uses `ManagedConnection::bind_native_records`, retained common record views and one session gate across mutation evaluation/staging. Eight public document integration cases cover independently opened writers in all four file modes with commit/rollback/savepoint outcomes, reopen, typed fields and tuple metadata, body/BLOB snapshots, selected-field reads with an unrelated corrupt BLOB, same-row conflicts, rejected oversized batches, both B-tree namespaces, empty snapshots and callback reentrancy, format/option/active-transaction rejection, and failed autocommit retry of the sealed batch. Three owning connection cases cover evaluation unwind, read-only logical transactions and a competing commit between autocommit evaluation and staging. Complete catalog, posting/vector/graph APIs and default Engine session routing remain pending; these tests do not establish complete concurrent SQL support.

Native B-tree routing has six public integration cases. The deterministic document-plus-index schedule runs with independently opened providers in every native file mode, retains A's private changes while B commits, and checks commit/rollback/savepoint outcomes after reopen in both column and named-expression namespaces. Additional cases verify retained marker/posting reads, sparse repairs and repair-marker order, replace/clear/drop/delete behavior, tagged values, validation-error atomicity, rejected stale posting writes after index/document deletion, and injected B-tree failure with exact commit retry in all four modes. The row-posting path does not rewrite a shared marker. This does not complete index-definition generation fencing, SQL DDL locks or the remaining shared posting/vector/graph algorithms.

Native catalog registry routing has seven public cases covering independent metadata/schema/model/scoring/analyzer writers in all four modes with commit/rollback/savepoint outcomes and reopen; sorted named/field reads, schema ACLs and nonempty-schema rejection; native format immutability; phase replacement and legacy binding invalidation; oversized-update atomicity; same-record conflicts; and catalog/document materialization failure with exact retry. Already bound `Catalog::open` attaches without legacy physical migration. Six additional view/foreign relation cases cover independent writers in all four modes, commit/rollback/savepoints and reopen, shared legacy/native name-kind validation, atomic rename with name reuse, ACL preservation and security-only changes, nullable migrated view ACLs, retained reads across another session's rename/replacement, conflicting kinds, rejected oversized batches, and physical publication failure followed by exact retry. Table and catalog-index lifecycle now use the same session as described below. Sequence lifecycle now uses the same session as described below. Graph mutations, path-index/cache restoration and SQL definition-generation/locking behavior remain unimplemented.

Both released-provider verifiers also pass under Python 3.10.8. Their shared provenance helper uses locked Cargo metadata to identify the registry package and obtains its archive SHA-256 from Cargo's directory-source checksum or the downloaded archive itself; no Python 3.11-only TOML parser or additional Python package is required. Both checksums match the compact references above.

Native table/catalog-index routing has eleven public cases covering independent table creators with document/B-tree/analyzer/index changes in all four modes and commit/rollback/savepoint outcomes; retained generations through rename, generation replacement, full deletion and recreation; definition-only deletion and standalone adoption; purge with analyzer preservation; duplicate identities, missing backing tables and oversized-batch atomicity; generation retirement against late rows and stale writers; nullable migrated columns and table/column ACL snapshots; and injected materialization failure followed by exact retry. A transport fixture preserves all 23 fixed table-owned families and their old histories through rename/generation replacement and deletion; it does not execute the remaining posting/vector/graph algorithms. Exact standalone names and existing destination rows match legacy behavior in every raw/canonical source/target combination, with collision rejection and atomic name reuse in both lexical orders. Mapping format 4 normalizes recognized dynamic FTS skip/block-max tables into fixed families; conversion continues to reject unknown tables and ambiguous populated source names. Graph/cache restoration and the other remaining consumer paths keep the native routing unit open.

Native column lifecycle and statistics routing has eight public cases. Independent column/document and statistics writers finish while the other transaction remains private in all four file modes, with commit/rollback/savepoint outcomes and reopen. Injected column-statistics publication failure leaves document, B-tree, analyzer and catalog state unchanged until exact retry. Other cases cover malformed index-key JSON, nullable/full-width statistics values, sorted and exact-name reads, duplicate/mismatched/oversized replacement failures, retained statistics views, same-column conflicts, 21 fixed field-family payloads and old history, expression namespaces, existing destinations and B-tree repair ownership. A 256 KiB session renames and deletes a small field while preserving an unrelated 1 MiB BLOB. Both legacy and bound-native paths preserve same-name column renames and remove obsolete repair markers. Legacy B-tree destination preservation is verified with SQLite foreign-key cascades both enabled and disabled. The affected SQLite suite passes 265 unit and 223 integration tests, with the ignored process helper invoked by parent tests. All eight column/statistics cases also pass with Nori-only, Kuromoji-only and both features; the default suite covers neither. Native and Emscripten runtime dependency trees contain exactly the selected analyzer data crates. Strict SQLite Clippy, analyzer-feature isolation, staged dependency/ownership and repository checks pass. Emscripten library compilation passes; compilation does not establish browser execution. These cases verify storage lifecycle and transport; SQL DDL fencing, automatic-maintenance integration and posting/vector algorithms remain required.

Native sequence routing has twelve public cases and one additional prepared-record case. Independent sequence/document writers commit before the other private transaction ends in all four native file modes, with commit/rollback/savepoint outcomes and reopen. A separately selected value session reserves and sets committed sequence values while the parent retains private documents, and parent rollback preserves those durable values. Injected publication failure retains the exact evaluated reservation and paired catalog changes for retry. Other cases cover shared legacy/native decoding, nullable/empty ACLs, both owner-dependency kinds, quoted names, full-width cached bounds and cycles, missing/stale identities, definition-generation changes and retained history, stale writers and competing name claims, invalid/oversized batch atomicity and zero-identity assignment. A 256 KiB session reserves/sets a small sequence while skipping an unrelated 1 MiB ACL. A reproduced race let two independent creators publish different live generations under one incarnation; commit admission now rejects it and also rejects multiple final generations in one prepared batch. Conversion rejects existing aliases atomically. Engine autonomous allocation and definition coordination remain part of the SQL integration unit. The sequence-unit SQLite suite passed 266 unit and 235 integration tests, with the ignored process helper invoked by parent tests. All thirteen new sequence cases pass with neither analyzer feature, Nori-only, Kuromoji-only and both. Strict SQLite Clippy, staged dependency/ownership checks, harness registration and repository checks pass. Actual native/Emscripten dependency trees select exactly the requested analyzer data crates, and the Emscripten SQLite library compiles; browser execution remains unverified.

Common Key/Value IVF now retains ordered evaluated document inputs in the transaction journal and restores the latest committed generation only when re-preparation is needed. Document and structural tombstones preserve conflicts across empty replacements, absent deletions, rebuilds, same-definition recreation and catalog table/column changes. Canonical tensors must match the final journal input, and unjournaled derived changes cannot be overwritten by re-preparation. Shared conformance compares canonical vectors, metadata, centroids and assignments byte-for-byte with serial execution across initial training and retraining; savepoint branches and retained snapshots remain independent. Fault schedules cover admission retries, lost replies before/after publication, budget failure and composition with occurrence/graph changes. SQLite Key/Value and redb reuse the common resolver; native SQLite now supplies its own row codec and lifecycle routing. Mapping format 6 adds IVF guards, atomically upgrades formats 1–5 and preserves source histories and receipts. Development record format 3 upgrades formats 1/2 without rewriting histories or changing IVF row format 1 or existing native IVF row encodings. Verification covers 206 common unit tests and 128 common integration tests, 315 SQLite unit tests and 289 integration tests (plus its parent-invoked process helper), and eight redb unit tests plus 31 integration tests. Final focused checks pass seven common IVF cases, six SQLite Key/Value vector cases and seven redb compound cases after the last validation/refactoring changes. Strict three-owner all-target Clippy and ownership/dependency/harness/header/line-cap/hygiene checks pass; no Engine code or Cargo dependency/feature declaration changes in this unit.

Native SQLite IVF merging now has three deterministic public-provider schedules across plain, SQLCipher, compressed and encrypted compressed files. They compare complete persisted metadata with serial common-owner execution through initial training/retraining, ordered tensor replacement/deletion, retained snapshots and reopen; both commit orders exercise document and catalog definition conflicts. A publication failure followed by an intervening commit retries the sealed changes without losing either writer. Native codec tests cover escaped identities, numeric bounds, malformed payloads, cancellation and allocation rejection. Mapping 5-to-6 tests preserve exact histories, identities, allocation counters and committed/pending receipts, including failed guard installation and idempotent reopen; existing predecessor migration suites cover mappings 1–4. Table and column lifecycle fixtures verify that IVF document guard tombstones retain their original identity and sequence rather than becoming renamed or copied payloads. Verification passes 319 SQLite unit tests and all 292 regular integration cases across the full run and the four-case family fixture recheck; the process helper remains parent-invoked. Common storage passes 206 unit and 129 integration tests, and redb passes eight unit and 31 integration tests. Strict three-owner all-target Clippy, Emscripten SQLite library compilation and ownership/dependency/harness/header/line-cap/format/hygiene checks pass. Emscripten evidence is compilation only. No Engine implementation or Cargo dependency/feature declaration changes in this unit; the full remaining acceptance scope stays open.

Native exact/IVF vector routing has owning public API and read-only transaction cases. All four file modes cover independent exact tensor writers with commit/rollback/savepoint endings, independent IVF fields, retained canonical/derived snapshots, rename/drop, reopen and failed publication followed by exact retry. Other cases cover empty snapshots, first-owner creation, embedded-zero/Unicode names, tensor replacement/removal, invalid inputs, same-document conflicts, read-only snapshots, aggregate collection limits and failed private/autocommit batches. A bounded-session case reads a stored IVF assignment while skipping an unselected 1 MiB canonical vector; the matching exact search and a probe selecting that payload reject the allocation. Native IVF mutations now preserve the common owner's stored centroids and deletion counters, including threshold-driven retraining; deterministic fixtures compare complete centroids and assignments with the common implementation through reopen. Invalid metadata and incomplete assignments reject search/mutation until an explicit rebuild, while retained pre-drop generations remain searchable. Candidate preparation and native codecs share the session allowance. Existing vector and IVF row encodings remain unchanged; mapping format 6 adds the guards required for shared generation merging. Shared mutations to the same native IVF structure now join the common resolver with document/definition guards; this evidence does not complete the shared-index acceptance row or enable concurrent Engine SQL. The owning regressions live in `uqa-storage/src/ivf_index/tests/preparation.rs` and `uqa-storage-sqlite/tests/cases/mvcc/native_vectors.rs` with its nested IVF cases; the shared Key/Value suites also exercise SQLite and redb callers. The Emscripten SQLite library compiles; this is not browser-runtime evidence. Ownership, staged dependency, harness, formatting, header, file-size and repository checks pass. The changes add no Cargo dependency or feature.

Physical HNSW preparation fixes a reproduced cache error: after rollback, another handle could reuse the same numeric revision and searches returned the discarded document. Six owning regressions cover whole/savepoint rollback branches, restoration without another write, independent same-revision recreation and reopen in all four file modes, canonical drift with a warm cache, unchanged-view cache reuse, retained WAL readers and an intervening rebuild during candidate evaluation. The forced intervening commit survives while the obsolete candidate fails after exactly one evaluation. The physical connection and snapshot-identity mechanics remain in the SQLite provider; HNSW algorithms remain in common storage. This physical-path correction is separate from native record routing and shared-index merging.

Physical HNSW verification at `933f5ffb` passes 878 storage-owner tests, including all 16 SQLite HNSW cases, and 41 Engine HNSW, index-lifecycle and session cases. The intervening-commit regression additionally verifies the typed `SQLITE_BUSY_SNAPSHOT` error. Strict three-owner Clippy, the Emscripten SQLite library compile and ownership, dependency, harness, header, line-cap, hygiene and compatibility-manifest checks pass. These results verify the physical cache correction, not concurrent Engine SQL acceptance.

Native HNSW routing now shares one committed/private boundary across header validation, graph reconstruction, canonical consistency, search and immutable snapshots. Evaluated canonical replacements and incremental HNSW headers/nodes/edges publish in one batch. Five new SQLite-owner regressions cover private rollback identities, committed/private canonical drift with a warm cache, catalog parameter/missing-header validation, revision exhaustion without partial writes, and an intervening same-revision rebuild rejected after one evaluation. Three new integration cases cover independent HNSW field commits before another writer ends, failed publication/retry/reopen, and aggregate decoded-graph allowance failure. Shared vector scenarios now include HNSW for lifecycle, private/committed snapshots, invalid inputs, read-only snapshots, budget failures and original-source conflicts. These routing cases precede the same-index merge evidence below; retained graph cache/restoration/query workspace accounting and general definition fencing remain mandatory.

Common Key/Value HNSW now shares IVF’s retained document journal, sealed fingerprints, canonical/input validation, savepoint undo and receipt-safe resolution. Common storage restores the latest validated HNSW graph under its memory allowance and prepares only ordered retained inputs, preserving node allocation and compaction. Provider codecs own the physical node/adjacency representation; SQLite Key/Value and redb share exact serial graph, document/definition conflict, lifecycle and reopen schedules. Mixed IVF/HNSW/occurrence/graph commits use one fresh snapshot. Common record format 5 atomically upgrades formats 1–4 while preserving histories, identities, allocations and receipts. Verification covers 218 common unit and 130 integration tests, 319 SQLite unit and 292 regular integration tests plus its parent-invoked process helper, eight redb unit and 31 integration tests, strict three-owner Clippy and the dependency/ownership/harness/header/line-cap/format/hygiene checks. The SQLite Emscripten library compiles; browser runtime acceptance remains unproven. The native HNSW codec and guard routing are now implemented as described below; complete retained-cache/query workspace accounting remains open; these changes do not enable concurrent Engine SQL.

Native SQLite HNSW now implements the common record layout using its unchanged header, node and separate-edge rows. Ordered evaluated inputs share IVF's journal and resolver, while canonical replacements retain original preconditions. Native vector identity/scalar codecs and preview publication are shared, and IVF/HNSW fences retain the existing family 49 keys and physical `_uqa_mvcc_native_ivf_guards` table. Mapping format 7 excludes old HNSW writers and atomically upgrades formats 1–6. The new native regressions compare every vector/header/node/edge row against serial execution in both commit orders and all four file modes, including ordered replacements/deletions, compaction, retained snapshots, savepoint rollback, publication failure, an intervening commit and reopen. Shared IVF/HNSW conflict schedules exercise empty replacements, absent deletion, rebuild, same/changed-definition recreation and table/column lifecycle in both orders. Codec tests cover shared guard identities, separate adjacency, numeric bounds, malformed rows, cancellation and failed-decode allowance cleanup. Upgrade tests preserve existing tombstone histories and receipts, restore the prior schema after rollback and reject prior-version writer checks. The complete SQLite suite passes 323 unit and 294 regular integration tests, with its process helper invoked by parent cases. Strict three-owner all-target Clippy and the Emscripten SQLite library compile pass; Emscripten evidence is compilation only. Ownership, dependency, harness, formatting, header, file-size and repository checks pass. The change adds no Engine algorithm or Cargo dependency/feature. Full native routing, general definition coordination, SQL isolation, resources and platform acceptance remain required.

Native HNSW verification passes 886 storage-owner tests and 41 Engine HNSW, index-lifecycle and session cases. The final adjacency-buffer accounting change additionally passes all 21 SQLite HNSW owner cases and all 12 native vector/HNSW integration cases. Strict three-owner Clippy, Emscripten SQLite library compilation, formatting and repository ownership/dependency/harness/header/line-cap/hygiene checks pass; the PG18 manifest remains valid with 33 incomplete items. Compilation and manifest verification do not establish browser runtime or concurrent SQL acceptance.

Explicit native Engine restoration now routes catalog namespace validation through the retained committed/private snapshot instead of the rejected raw-connection path. SQLite owns schema membership, relation/definition agreement, duplicate-name and index-parent checks; B-tree validation pages through keys without hydrating large values. Owning regressions reject private corruption while physical rows remain valid, preserve retained views across rollback and sibling changes, and enforce cancellation and bounded reads. Public Engine scenarios cover the initialized in-memory provider and conversion/reopen in plain, encrypted, compressed and compressed-encrypted file modes. They exercise SQL views/sequences, primary keys, occurrence/IVF indexes, graph savepoints, rollback and sibling publication. After integration with the main notification-encryption fix, bound native sessions also verify deferred delivery, rollback and inherited encrypted queue credentials. Verification passes 310 SQLite unit and 280 integration cases (plus the process helper invoked by its parent), 31 Engine session and 22 notification cases, strict SQLite/Engine all-target Clippy, Emscripten SQLite library compilation and ownership/dependency/harness/format/header/line-cap/hygiene checks. Emscripten evidence is compilation only. Default model selection, concurrent Engine writers and the remaining acceptance rows are still required; standalone graph routing is described below.

The common `StorageSessionAffinity` contract identifies one native or logical transaction context without treating a file identity as a session identity. Catalog/backend facades forward their store's identity; connection/store clones share it and independent sessions do not. Engine invokes common validation before restore or sibling attachment. Tests cover valid and mismatched pairs in native SQLite, SQLite Key/Value and redb, malformed provider pairs on initial/sibling opens, omitted identity on one side, and legacy custom pairs that omit both. The 29 Engine open/restore unit cases and the actual Rust storage/transactions example also pass, including reopen, rollback, savepoints and sibling reads. Strict affected-owner Clippy and Emscripten SQLite/Engine library compilation pass; the latter is compile evidence, not browser execution. This establishes handle affinity; durable format/model validation and removal of the Engine writer gate remain separate requirements.

The core buffer owners now reconcile allocator-reported capacity before moving existing elements, including vector growth/shrink, deque growth and string growth. A forced oversized replacement test checks both successful accounting and limit rejection that preserves the original values, capacity and reservation; zero-sized vectors and deques retain a zero-byte allowance. The updated owners pass 104 core unit and 68 core integration tests, the complete common-storage suite and strict core/storage Clippy.

Before changing transaction entry, record concrete source owners and migration/routing decisions for catalog initialization, ordinary SQL DML/DDL, direct document/index APIs, graph/Cypher, B-tree/expression/unique indexes, posting clusters and statistics, IVF/HNSW, models/analyzers/routines, sequences and ID allocation, LISTEN/NOTIFY, prepared statements and cursors, temporary and unlogged relations, background ANALYZE, VACUUM/rebuild, backup/restore, callbacks, FDWs and each Engine/provider open constructor. Include error/unwind and savepoint paths. Update the inventory when a bypass is discovered; do not paper over it with a global transaction lock.

Native SQLite and SQLite K/V require distinct physical adapters to the same common record/visibility contract. Route both completely before claiming default SQLite support. Keep current SQLite tables as atomic current-state materializations with history; do not silently convert `Engine::open` into an unrelated K/V format. Retain the verified redb old-writer guard and exercise corresponding SQLite guards with actual old libraries. These are implementation deliverables, not optional follow-up optimizations.

Audit lower-level native SQLite writers as well as Engine/catalog opens. rusqlite `functions` is now a runtime feature in both SQLite-provider target declarations. Keep checking packaged consumers without development-feature unification while extending record-table guards to native materializations and legacy K/V writes. Reported transaction affinity is now checked for manually paired handles and provider-created Engine sessions. Complete format/model validation before enabling the concurrent capability. These changes belong to the provider/common interface units, with corresponding Rust upgrade notes.

## Standalone graph sessions

`SQLiteGraphStore` now dynamically follows its connection's native logical session, including handles opened before binding. Mapping format 8 imports complete legacy standalone families into scoped records and preserves source JSON/property-format bytes. Standalone-only catalog bootstrap and baseline conversion share one atomic physical transaction. Existing mappings gain empty families, preserving histories, identities and receipts; immutable namespace bindings and retired-name views prevent accidental legacy writes. New namespaces publish matching protective views atomically with their first records; existing handles can open without staging an empty write in a read-only logical session. The provider reuses native catalog selector projection, commit validation and bounded key scans. `uqa-graph` avoids unchanged counter/registry writes when observing existing entities. Persistent graph handles now reserve entity/label IDs and retain graph-definition observations; direct catalog mutation/restore and endpoint lifetime coordination now join evaluated batches, while the full acceptance matrix remains required.

Standalone graph verification covers independent and conflicting existing-entity writers, retained views, namespace case identity, selectors, savepoint/transaction rollback and reopen in all four SQLite file modes. Owning tests preserve legacy JSON and tagged byte values, restore the complete source schema after failed conversion, reject incomplete/retargeted namespace changes and roll back protective views after failed publication. Strict read-only sessions open existing graph handles and permit savepoint control without admitting mutation. Small-memory selectors skip unrelated oversized properties and registry payloads. `scripts/verify-sqlite-legacy-writer.py` executes the actual published 0.3.6 provider for native, Key/Value and standalone graph creation, rejection and reopen, including namespaces created after conversion; all four modes pass. The published SQLite crate checksum remains `3db0f72ba7aaf9b64b00ae2b02c8e36a7273d0c969f0b3da47f95d18beb29f6b`.

Final owner verification passes 218 common-storage unit and 132 integration cases, 328 SQLite unit and 299 regular integration cases, and eight redb unit and 31 integration cases; SQLite process helpers run through parent cases. The preceding graph counter/registry commit passes 79 unit and 113 integration cases. Strict four-owner all-target Clippy, Emscripten SQLite library compilation, formatting and ownership/dependency/harness/header/line-cap/hygiene checks pass. Emscripten evidence remains compilation only; this unit does not enable concurrent Engine SQL or establish complete binding/platform acceptance.

Common storage now defines checked identifier observations and bounded range reservations through `VersionedPersistence::allocate_identifiers`. Native SQLite, SQLite Key/Value and redb persist independent watermarks with short physical writes; logical sessions reject read-only/sealed allocation and preserve successful reservations across undo. Record format 5 adds the watermark table atomically while retaining prior record histories, identities and receipts. The storage-owned `DocumentIdAllocator` now binds Engine document reservations and explicit-ID observations through the backend/byte-store allocator capability. Initial restoration seeds existing maximum IDs and legacy floors, retiring the old per-name value in the catalog transaction; rename and TRUNCATE preserve the appropriate namespace/floor. Native SQLite document replacements and patches now queue observations against their selected table object/generation. Common batches validate and stage records before applying observations directly through persistence, preserving the existing session mutation boundary. SQLite Key/Value and redb forward the same capability. Common Key/Value document-store owner bindings and supplied-ID seeding are connected below; direct graph catalog mutation/restore consumers and autonomous SQL sequences remain required; persistent graph handles now use stable allocation namespaces. The Key/Value backend now maps its read-first transaction hint to the existing upgradeable byte-store entry point, matching native SQLite while preserving strict byte-store read-only sessions. Concurrent SQL and the complete allocation acceptance row remain open.

Evaluated identifier-observation verification passes `cargo test -p uqa-storage -p uqa-storage-sqlite -p uqa-storage-redb`: common storage has 222 unit, 143 integration and one documentation case; redb has 10 unit and 34 integration cases; SQLite has 330 unit and 309 regular integration cases, with its ignored process helper invoked by parent tests. Five common batch cases cover undo, discarded/unwound evaluation, failed allocation, record-publication retry and memory rejection. Five native document cases cover cataloged and standalone owners, explicit IDs and patches, tuple metadata, retained views, independent commits, rename/generation separation, physical failures and closed reopen; the shared byte-store schedule also runs on SQLite Key/Value in all four native file modes and redb. Strict common-storage/SQLite/redb/Engine all-target Clippy and 45 Engine session/SQLite Key/Value regression cases pass, along with formatting and ownership/dependency/harness/header/line-cap/hygiene checks. The Emscripten SQLite library compiles; this is not browser runtime acceptance. Engine SQL concurrency, the remaining allocation consumers remain open.

Document-allocation verification passes `cargo test -p uqa-storage` (222 unit, 138 integration and one documentation case), the SQLite/redb `document_id_backends` cases (native SQLite and Key/Value in all four file modes, plus redb, with closed reopen), and the Engine integration executable's `storage::sessions::` and `storage::sqlite_key_value_backend::` selections (45 cases). These include initial seeding from legacy reservations and existing rows, exhaustion, SQL rollback/savepoints, explicit-ID observation, rename, TRUNCATE continuation/restart and name reuse. A deterministic independent-writer case also verifies that commit-reply fault injection targets the intended foreground transaction; automatic statistics previously could claim that fault and block the test. Strict storage/SQLite/redb/Engine all-target Clippy, the final Engine integration-target Clippy, Emscripten SQLite library compilation, formatting and repository policies pass. Emscripten evidence remains compilation only. No Cargo dependency or feature changed. The preceding reservation-primitive revision `f1650739` records the full provider suites, physical failure/recovery, process, bounds and guard/migration evidence; its published-0.3.6 probes are recorded below.

The actual published 0.3.6 compatibility verifiers also pass after the record-format-5 change: SQLite covers native, Key/Value and standalone graph conversion/reopen in all four file modes, and redb verifies creation, migration, prior-writer rejection and reopened values. The SQLite crate checksum is recorded above; the published redb crate checksum is `b20b61d2f69be5f39186f220599576ad28afe82d12740592c316c018ee721fc4`. Generated probe projects and logs remain outside the repository.

## Acceptance matrix

Use named synchronization events and channels/barriers. Start A, receive proof that A staged a change, start B, receive B's successful commit before allowing A to finish, and verify both session views and reopened state. Timeouts only detect a hung schedule. Where PostgreSQL permits multiple victims, fixtures specify allowed outcomes and serial histories instead of requiring one scheduler-dependent winner.

| Required behavior | Deterministic evidence | Required coverage |
| --- | --- | --- |
| Independent writer progress | A writes row 1 and waits; B writes row 2 and commits; A then commits, rolls back or rolls back a savepoint. Check intermediate visibility and final reopen state. | Default SQLite, SQLite K/V, redb; same table and different tables; separately constructed SQLite providers and native processes |
| Shared physical data | Force two independent documents into one term cluster, common counters, index nodes and catalog containers; both commits survive and query results agree with visible rows. | Full-text scores/occurrences, B-tree/expression keys, IVF/HNSW, graph membership/path data, statistics |
| Snapshot isolation | Interleave reads and commits, including a writer retaining its private changes while a later command sees B; missing keys, empty ranges, paging, reverse scans, cursors, drop/recreate and PostgreSQL TRUNCATE/table-rewrite exceptions. | RC, RU-as-RC, RR; all providers; command, transaction and retained-resource boundaries |
| Conflicting writes and constraints | Same-row update/delete, key collisions, parent removal, deferred checks, cascades, exclusions, partition moves and `MERGE` actions; compare row counts, returned values and SQLSTATE. | Owning execution tests plus real multi-session Engine/Docker PostgreSQL schedules |
| Serializable histories | Write skew, insert phantoms, read-only anomalies, dangerous pivots, overlapping committed transactions and `READ ONLY DEFERRABLE`. | Common algorithm tests, provider SQL schedules and cross-process SQLite |
| Waits and deadlocks | Preserve real row/relation cycles, NOWAIT, SKIP LOCKED, cancellation and timeout semantics; prove the obsolete backend-writer cycle no longer blocks independent writes. | Execution owner, Engine adapters, POSIX/Windows coordination |
| Savepoints and statement effects | Savepoint after writes, nested/duplicate names, failed statements, trigger subtransactions, command snapshots, callback invocation counts and data-modifying CTE visibility. | Common storage and Engine/execution tests; no SQL or callback replay at commit |
| Complete atomic publication | Inject failure at each mutation family and before/after physical commit and in-memory publication; observers see one complete committed version. | Rows, indexes, graphs, catalogs, ACLs, analyzer/model/routine state and notifications |
| IDs, sequences and maintenance | Concurrent allocations across rollback/reopen, sequence definition versus value behavior, automatic statistics and rebuild while other transactions remain open. | Storage/sequence owners and cross-provider Engine schedules |
| Retention and cleanup | Hold an old cursor/snapshot while B commits and GC runs; release it and prove reclamation; cancel/spill/kill a participant. | Memory accounting, version/tombstone/index-root retention, cross-process leases, no plaintext encrypted spill |
| Failure outcome and durability | Kill a writer before persistence, during native commit, and after commit before publication; recover receipts, generations and notifications; distinguish unknown outcome. | Each native provider/open mode; independent process recovery; declared sync guarantees |
| Upgrade and backup | Open representative 0.3.6 files, interrupt conversion, reopen twice, query/mutate, backup/restore and attempt an actual old writer. | Native SQLite, SQLite K/V, SQLCipher, compressed variants and redb |
| Public entry points and platforms | Run overlapping/interleaved sessions through actual binding artifacts and the server; browser close/reopen retains committed state. | Rust/Linux/macOS/Windows where supported, Python, Node.js, Emscripten browser; memory-engine regressions |

redb tests use one supported open database owner and independent logical sessions; concurrent redb file opens by separate processes are not a capability of the current library. SQLite native multiprocess tests are required. Browser tests cover the supported runtime/file ownership model and must not silently skip core session semantics under the label of platform limitations. No entire-provider or isolation-mode exemption is allowed for the requested behavior.

## Test placement and commands

Keep algorithms' tests with their owners. Common MVCC tests and a fault-injectable reference store belong in `uqa-storage`; provider durability/mapping tests belong in the respective provider crates; SQL rechecks and lock algorithms belong in `uqa-execution`; only actual session/cache/publication integration belongs in Engine. Add submodules to each existing test executable. Do not create another top-level `tests/*.rs` file or another test target.

Existing integration entry points to extend include [`uqa-storage/tests/integration.rs`](../../crates/uqa-storage/tests/integration.rs), [`uqa-storage-sqlite/tests/integration.rs`](../../crates/uqa-storage-sqlite/tests/integration.rs), [`redb store_contract.rs`](../../crates/uqa-storage-redb/tests/store_contract.rs), [`Engine transaction_lifecycle.rs`](../../crates/uqa-engine/tests/transaction_lifecycle.rs), [`Engine redb_backend.rs`](../../crates/uqa-engine/tests/redb_backend.rs), [`Engine sqlite_key_value_backend.rs`](../../crates/uqa-engine/tests/sqlite_key_value_backend.rs), [`Engine sessions.rs`](../../crates/uqa-engine/tests/sessions.rs) and [`row-lock rechecks`](../../crates/uqa-engine/tests/queries/sql_row_locks_recheck.rs). Inspect their registration paths before choosing filters. New common/provider tests must be callable through the existing harness, and a filter that runs zero tests is not evidence.

Run focused new tests during development, then the affected crates and existing cross-provider regressions. These are required command families for the completed runtime change, not claims that they were run for this documentation proposal:

```sh
cargo test -p uqa-storage -p uqa-storage-sqlite -p uqa-storage-redb
cargo test -p uqa-execution -p uqa-sql -p uqa-graph
cargo test -p uqa-engine --test integration
cargo clippy -p uqa-storage -p uqa-storage-sqlite -p uqa-storage-redb -p uqa-execution -p uqa-sql -p uqa-graph -p uqa-engine --all-targets -- -D warnings
cargo fmt --all -- --check
python3 scripts/check-workspace-dependencies.py
python3 scripts/check-engine-capabilities.py
python3 scripts/check-integration-test-harnesses.py
python3 scripts/check-analyzer-feature-isolation.py
bash scripts/check-rust-file-headers.sh
bash scripts/check-rust-file-lines.sh
bash scripts/check-public-repository-hygiene.sh
python3 tests/parity/pg18/run_diff.py --validate-manifest
```

Exercise neither/Nori-only/Kuromoji-only/both Cargo configurations for changed analyzer/storage integration and the actual native/Emscripten dependency trees. Use the existing [CI](../../.github/workflows/ci.yml), [Python](../../.github/workflows/python-wheels.yml) and [JavaScript](../../.github/workflows/javascript-bindings.yml) workflows for platform builds and artifact execution. Add focused concurrent-session scenarios to the owning existing suites; do not infer Python/Node/browser success from Rust tests. Run the [manual SQL check](../manual/internals/09-verification.md) when adding implemented public examples.

Extend the existing [PG18 reference tools](../../tests/parity/pg18/README.md) with deterministic multi-session schedules and compact expectations derived from the pinned Docker oracle. A sequence of separate `usql` invocations is not a concurrent-session runner. Use a runner that retains connections and controls named barriers for both PostgreSQL and UQA. Record source/image identity, schedule, expected outcome set and tested UQA commit; raw traces remain ignored artifacts. Keep the PG18 manifest and generated plan ledger synchronized when implementation changes tracked evidence. A successful upstream reference run alone never verifies UQA.

## Review risks that must be closed

- Native SQLite bypasses: a correct K/V adapter cannot compensate for direct native catalog/document/index reads that ignore snapshot visibility.
- Shared physical values: same-term clusters, counters, vector roots and graph adjacency must merge typed logical changes without losing updates or creating unrelated SQL conflicts.
- Publication races: a process can die after physical commit and before publishing its in-memory epoch; surviving processes must recover from durable evidence.
- Read/GC admission: pinning after horizon selection can lose required history; compressed native read transactions can accidentally retain the old writer bottleneck.
- Isolation versus retries: RC target rechecks must preserve command sources and effect counts; RR/serializable errors cannot be replaced by transparent transaction replay.
- Format safety: an unrecognized metadata key does not keep a 0.3.6 redb or SQLite K/V writer out of a new database.
- Security and resources: history, spill and cross-process observations can disclose data or grow without bounds unless their owning budgets and encryption are explicit.

Each risk needs owning tests and review evidence in the implementation PR. An unresolved risk keeps the corresponding unit pending; it is not a reason to waive that portion of the requested feature.

## Commits, evidence and document maintenance

Commit and push completed logical units with their tests and necessary contract changes. Separate mechanical ownership extraction from behavior changes when extraction is needed. Do not create a long chain of experiment, timing-output or corrective bookkeeping commits. Keep one reviewable implementation PR current with the final behavior, validation and remaining work; this design proposal can be reviewed separately before runtime changes begin.

Track status and compact evidence in the table above rather than appending repeated chronological reports. Each accepted unit records the tested commit, commands or CI links, test counts/schedule identifiers and unresolved gates. An implementation change that alters a decision updates the design and plan together; only verified public behavior enters the manual. Review the final diff for dependency direction and ownership before committing, and run `python3 scripts/check-workspace-dependencies.py --staged` on the exact index.

No performance measurements are scheduled by this plan. Deterministic concurrency progress, bounded work/memory and correctness are acceptance gates. Any later throughput study requires a controlled host, independently established noise bound, fixed workload and run limit before measurement. Keep generated reports and diagnostic traces in ignored output directories or CI artifacts; commit only compact fixtures, expected outcomes and provenance.

## Runtime completion checklist

- [ ] All access paths and both SQLite layouts use the intended common transaction contract.
- [ ] SQLite and redb allow B to commit while A remains an independent uncommitted writer, including shared index records.
- [ ] PostgreSQL snapshots, conflicts, constraints, command effects, savepoints and serializable histories pass the required schedules.
- [ ] Engine contains only adapters; storage, execution, SQL and graph behavior and tests remain in their owners.
- [ ] Durable publication, old snapshots, secure bounded spill, GC and process recovery pass fault injection and reopen checks.
- [ ] Every existing persistent open mode migrates safely, rejects unsafe old writers and preserves backup/restore.
- [ ] Actual supported binding/platform artifacts pass, alongside memory regressions and ownership/dependency checks.
- [ ] Final-head implementation CI, public/upgrade documentation and review evidence cover every acceptance row with no requested-provider exemption.

### Key/Value document ownership and table lifecycle

Common Key/Value document writes now use one evaluated view for owner selection, tuple metadata and row mutation. The exact-name owner registry keeps catalog adoption, definition-only drop, full removal, purge, rename and generation replacement consistent with durable reservations; raw standalone names remain distinct from canonical catalog names. Reverse claims reject duplicate identities and inconsistent bindings. Data writers retain definition requirements and merge immutable marker touches, while structural operations fence the corresponding data revision. Rename rejects destination collisions atomically. Common document snapshots and compound reads retain one view, and key-only seeding avoids reading unrelated document payloads.

Queued identifier inheritance preserves deleted or privately rolled-back reservations during Key/Value and native SQLite owner transfers. Changing the storage generation of the same object seeds only visible IDs. Engine retires rows under the old generation before selecting the new namespace, preserving both continued and explicitly restarted TRUNCATE identity semantics. The shared nonzero identity generator now belongs to storage, with the existing execution import retained as a re-export. Record format 6 upgrades formats 1–5 while preserving existing histories and reservations and rejecting older writers. Tests cover real SQLite file modes and redb reopen, exact-name rename combinations, corrupt bindings, metadata races, retained snapshots, legacy seeding and evaluated-batch failures. Concurrent Engine SQL, direct graph catalog mutation/restore coverage, remaining compound consumers, autonomous SQL sequences, general definition coordination, admission, reclamation, spill and runtime platform acceptance remain open.

The full storage-owner run passes 222 common unit, 161 common integration and one documentation case, 10 redb unit and 36 integration cases, and 331 SQLite unit and 312 regular integration cases, with the ignored process helper invoked by its parent cases. After the final generation-order correction, focused verification passes 10 common document cases, 16 SQLite document cases and the Engine rename/TRUNCATE/rollback/reopen scenario; broader affected regressions pass 105 Engine, 87 common MVCC and 11 native SQLite table-lifecycle cases. Strict common-storage/SQLite/redb/Engine all-target Clippy and Emscripten SQLite library compilation pass. Emscripten evidence remains compilation only.

Explicit DROP binding now uses the shared provisional relation-lock protocol for tables, foreign tables, views and materialized views. SQL owns target-kind errors and missing-schema notices, and ALTER SEQUENCE reuses the SQL local-name notice policy; execution checks authority before and after each wait, deduplicates canonical targets without sorting the statement, follows inherited object identities and forwards the selected names to removal. Direct view deletion opens a definition transaction and follows the same identity/authority revalidation. Engine supplies catalog, lock and transaction adapters. The dependent-view traversal described below extends this to stored-query relation edges; other dependency consumers, direct deletion APIs and sequence definition locks remain required.

DROP binding verification passes 17 Engine view/DROP cases, two execution provisional-binding cases and 28 final SQL removal/sequence/resolution cases. The broad DROP, view, relation, sequence, row-lock, manual and concurrent-writer integration selection passed 642 cases and exposed an obsolete sequence NOTICE expectation; the final sequence, rename and manual SQL selection passes all 167 cases after the implementation and expected messages were checked against PostgreSQL. PostgreSQL 18.4 Docker independently confirms 12 concurrent/ordered DROP schedules, 10 missing-target SQLSTATE checks and six NOTICE messages. Its ignored 3,381-byte reference has SHA-256 `bfe56e63f7a7380a2bd6ecfa2d0d80491b7219650b0fda0dac6a8d07c3ed0227`. Strict SQL/execution/Engine all-target Clippy, formatting, staged dependency, Engine ownership, harness, header, file-size and diff checks pass. This unit adds no dependency or Cargo feature and does not rerun the Nori allocation gate.

Relation-dependent view removal now locks each stored-query dependent by object identity and rechecks the dependency after a wait. SQL table/foreign-table/view/materialized-view DROP and direct view deletion perform this execution-owned traversal before writer admission or publication. Both RESTRICT and CASCADE preserve a view whose dependency was removed; renamed dependents and their transitive children follow the original identity without deleting a replacement at the old name. Provisional-lock release preserves older modes, and a removed edge cannot hide a surviving edge from another root. SQL's view dependency rejection now returns 2BP01. Routine, domain, sequence, schema and column dependency selection, other direct deletion APIs and constraint rewrites still require their own coordinated binding before mutation; the full implementation scope remains unchanged.

Dependent-view verification passes 23 Engine view/DROP cases, six execution view-removal cases and five owning SQL removal cases. These cover all three persistent providers, four DROP relation kinds, CASCADE/RESTRICT, transitive dependencies, rename/name reuse, inherited tables, direct view deletion, cancellation and fixed data snapshots under READ COMMITTED, REPEATABLE READ and SERIALIZABLE. PostgreSQL 18.4 Docker independently passes 23 corresponding schedules; the ignored 4,338-byte reference has SHA-256 `516a112d51b98cec7ea53be5dc11939a8bbfe215a4a4ea3d2ab6e123195dfd68`. The broader view/DROP/schema/sequence/routine/function/domain/foreign/inheritance/manual integration selection passes 1,192 cases and exposes an existing nested-query transaction defect in `functions::sql_plpgsql::control_flow::for_over_query_and_record_fields`: the routine's internal SELECT requests a relation lock without an open transaction. That defect remains required implementation work. Strict SQL/execution/Engine all-target Clippy and formatting, dependency/ownership, harness, header, file-size and hygiene checks pass. No dependencies, Cargo features, allocation ceilings or benchmark evidence files change.

The query-FOR failure originates in SQL effect classification: ordinary static query loops open internal portals but were omitted from the procedural-state transaction requirement already used by bound cursors and OPEN/FETCH/CLOSE. The SQL owner now classifies query FOR loops as requiring a statement transaction while deriving engine/database writes separately from the query and body. Execution's existing ordinary, cached and cursor entry points therefore establish the scope before the portal acquires relation locks. This correction adds no Engine implementation or transaction bypass.

Query-FOR verification passes both owning SQL cases, all 29 Engine query cases and 110 PL/pgSQL/cursor/read-only/manual integration cases, including the previously failing `for_over_query_and_record_fields`. The new Engine case covers memory, native SQLite, SQLite Key/Value and redb; repeated SQL and cursor entry; explicit read-only scope; and exception cleanup checked against the actual portal registry. PostgreSQL 18.4 Docker passes five corresponding execution/read-only/cursor/error checks; the ignored 630-byte reference has SHA-256 `01076e0aef0ef190a431c3c81e6205d4c1e34c47fb6f725b0f8759266d31b628`. Strict SQL/execution/Engine all-target Clippy and repository policy checks pass. This resolves the query-FOR failure found by the broader dependent-view run; it does not complete the remaining nested-command or virtual-catalog requirements.

Direct Rust table and foreign-table deletion now uses the same execution-owned binding and removal path as SQL DROP. Engine establishes a deferred definition transaction, including for memory, and adapts the public storage/string error types. Target kind and ownership are rechecked after waits, dependent views are locked and rechecked before mutation, and routine/owned-sequence/other RESTRICT preflight is shared. Missing targets retain the public false result without SQL notices, and ordinary-table storage wrappers preserve typed SQL diagnostics. The separate table entry and foreign-table preflight are removed along with the foreign-only dependency adapter; foreign-table dependency errors now use the common SQL diagnostic. Remaining direct definition APIs, other cascade consumers, nested commands, virtual catalogs and the full concurrency acceptance matrix still require implementation.

Direct deletion verification passes all three new Engine scenario groups in the final focused run: 36 observed relation/view waits across native SQLite, SQLite Key/Value and redb, plus memory/persistent routine-dependency, read-only, wrong-kind and missing-target checks. The broader selection also passes 49 existing Engine query/DROP/foreign-catalog cases, including the owned-sequence regression, and all 354 DROP/view/foreign/catalog-atomicity/dependency integration cases. Strict SQL/execution/Engine all-target Clippy passes, followed by a final Engine all-target check after aligning the new routine-dependency assertion with the common SQL primary message and 2BP01 diagnostic. Formatting, staged dependency, Engine ownership, harness, header, file-size and hygiene checks pass. No dependency, feature, provider format or allocation-gate limit changes.


### Relation and attribute ACL tuple publication

Ordinary table, view, materialized-view and foreign-table GRANT/REVOKE now persist independent relation and attribute ACL replacements over each definition's bound security. SQL selects effective tuple replacements, including unchanged nonempty ACLs; execution retains relation lifetimes and catalog tuple locks, reports `XX000` after conflicting committed replacement, and follows column order across waits without revisiting already skipped attributes. Empty-to-empty attribute changes retain no tuple lock. Disjoint column updates and relation-versus-column updates publish independently. Fresh authority merges only transaction-private ACL records, preserving peer grants under fixed data snapshots. Storage compacts replacement records on full definition writes and maintains them atomically through rename/drop; revision identities remain transient in embedded definitions. Engine supplies state, catalog persistence and guard adapters.

Common provider record format 30 excludes writers that do not understand independent ACL records. SQLite upgrades preserve format-29 record runs and their write guards, while older predecessors create those structures atomically. Record layout, native mapping 8 and native catalog 49 remain unchanged. Provider-serialized catalogs retain their embedded ACL representation. The format-29 predecessor check reproduced and corrected an upgrade path that had incorrectly treated existing run storage as corruption; populated-run and missing-guard regressions cover preservation and failure rollback.

The PostgreSQL 18.4 reference uses pinned Docker image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`. Twelve commit/rollback/savepoint and independent-column schedules across READ COMMITTED, REPEATABLE READ and SERIALIZABLE produce a 512-byte transcript with SHA-256 `216099f758280bf4229a0359f1c12b1ba82f23de528834731a705f2125ebad38`. Five relation/column, empty-REVOKE and unchanged-nonempty-ACL schedules produce a 147-byte transcript with SHA-256 `453fe3fa5dd4818e1e9b552af7b93144952da9013e34255e23dcbef902e47ed2`. A controlled tuple wait followed by a peer grant to an earlier column preserves that grant and revokes the waited column; its 13-byte transcript has SHA-256 `5c8849956b1bf370c6334431ea7ae0cdd02db267adc1ef4b6091800e78c6ae01`. These are functional schedules using observed lock state and completion barriers; raw transcripts remain temporary, and no timing or allocation acceptance is inferred.

Final focused verification passes 32 Engine ACL, retained-authority and role-dependency cases; all 48 selected public table/column/view/foreign privilege, ownership and manual integration cases; 60 SQL security cases; 73 execution security cases; 39 common catalog cases; 97 distinct SQLite catalog/format cases; and both redb metadata cases. The public error-precedence regression verifies missing role `42704` before invalid table privilege `0LP01`, while missing targets retain `42P01`. The skipped/replaced-creation fixture commits its role setup before the tested transaction and probes both role object and tuple locks, avoiding an unrelated uncommitted ALTER ROLE wait. Actual predecessor binaries from `1ee5324dbd7481cd39632f3e3c3c5e5368b6a720` pass all nine provider/file upgrade configurations, including retained-writer rejection and exact record/receipt preservation. Strict seven-owner all-target Clippy, formatting, staged dependency, Engine ownership, harness, header, file-size, hygiene and compatibility-manifest checks pass. No dependency, Cargo feature or allocation ceiling changes, and no timing or allocation workload is rerun. Other catalog/definition coordination and the complete concurrency and platform acceptance matrix remain open.


### Namespace identities, catalog tuples and relation creation

Core now owns namespace OID, incarnation and replacement-revision data; SQL retains it with schema authority. Execution reserves creation names and OIDs, rebinds namespace lifetime lookups after waits and checks original tuple versions before ACL, owner or deletion publication. GRANT/REVOKE preserve target order and replace unchanged ACL tuples. Owner transfer retains the original tuple through deletion/recreation; GRANT can rebind the newly committed namespace. Catalog projection and OID-based inquiry use the retained namespace OID. Engine only adapts existing catalog and transaction lock services. Initial conversion validates complete candidates before writing, preserves predecessor OIDs and rejects malformed or duplicate current identities. Schema security format 2 changes the native schema-specific owner envelope without changing catalog columns; common record format 31 fences preceding writers, while native mapping 8 and catalog version 49 stay unchanged.

PostgreSQL 18.4 functional references use pinned Docker image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636` and observed lock/completion barriers. Fifteen ACL/isolation/finish schedules produce 938 bytes with SHA-256 `3af87f386b5e9147c0841c52235bce73a1ee8b84b8972a61c1154b8062ed81f7`; five owner/deletion/recreation/warned-GRANT schedules produce 477 bytes with SHA-256 `3157b99851b21fba880f31eedd3416c0256b20c238389e3f3805a901d679aee4`; six same-name CREATE/IF NOT EXISTS/finish schedules produce 462 bytes with SHA-256 `19947043bbe734c62555812c2fd820028fc1b3485b3b4b0261c2d4bd31504e08`. A two-case RESTRICT/equal-owner reference verifies completion before the holder releases, producing 201 bytes with SHA-256 `44416a78882542dcde88976da6d2c556af0d9dffabb3b94dd7e014e3972c5cc3`. Raw reports remain temporary. No timing or allocation workload is run and no allocation ceiling changes.

Relation creation now uses the same execution-owned lifetime binder with a lookup callback that rechecks the requested name, search path and authority after each wait. Table, sequence, foreign-table and regular-view publication retain their selected namespace; direct Rust table creation shares the resolver. CTAS and materialized views skip existing targets before acquiring it, preserving their distinct `IF NOT EXISTS` behavior. Namespace deletion observes newly committed relations after waiting, while creator rollback and savepoint undo release the dependency. Routine and domain creation keep their own catalog publication behavior without relation-creation namespace locks.

Eight PostgreSQL creation/deletion schedules produce 645 bytes with SHA-256 `f72fede81238b56b117d0f4ef52fbb39471a7e6657a31f4a07e0f49e2ad176ae`; seven creation-kind, authority and search-path schedules produce 664 bytes with SHA-256 `13585a963c8b7eebe6ed501624570bc78041996e8bf0f7e6b1345da93ee606ab`; two skipped CTAS/materialized-view schedules produce 190 bytes with SHA-256 `d4da35aec00c400364a96fd64553c890201370a0d2c15eb5dbee93bf51832363`. These use the same pinned PostgreSQL 18.4 image and observed lock/completion barriers. The upstream [namespace creation binder](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/catalog/namespace.c) provides the corresponding source reference. Raw outputs remain temporary.

Focused verification passes 34 Engine catalog and role-creation cases, 51 execution cases, 12 SQL security cases, three common schema codec cases, 23 SQLite codec/metadata/format cases, eight native SQLite catalog integration cases and both redb metadata cases. Public integration passes 220 distinct schema, sequence, view, CTAS, foreign-table, search-path, retained-identity and manual cases; executable manual fences run through the actual Engine harness. Three-provider schedules cover all isolation levels, tuple and lifetime waits, committed replacement, deletion/recreation, rollback and savepoint undo. Direct Rust namespace failures retain typed SQL diagnostics. The actual format-30 predecessor from `f4f5b43a488b885191d240af62b1c579c8bb7b96` passes all nine provider/file upgrade configurations with old-writer rejection and exact record/receipt preservation. Strict seven-owner all-target Clippy, formatting, exact-index dependency, Engine ownership, integration harness, header, file-size, hygiene, benchmark-coverage and PG18 manifest checks pass. No Cargo dependency, feature or allocation ceiling changes, and no timing or allocation workload is rerun. Additional namespace-move consumers, other object publication and dependency consumers, fixed SQL catalog snapshots and the full concurrency/platform acceptance matrix remain required.

### Sequence alteration and namespace binding

All sequence alterations now reuse one source binder: SQL chooses ShareRowExclusive for definition options and AccessExclusive for owner, persistence and name changes, including unchanged persistence requests. Acquisition is followed by original-name, object-identity, relation-kind and owner-authority revalidation. SET SCHEMA uses the shared execution namespace context to retain its destination through commit, rollback or savepoint undo, including unchanged destinations. SQL separates destination declaration and owned-sequence rejection from post-lock temporary/TOAST, unchanged-schema and collision validation. Destination replacement rechecks CREATE and collisions against the refreshed namespace, while explicit public targets cannot follow the search path. Temporary aliases allocate only after database TEMP authorization; existing temporary namespace destinations require the current role’s TEMP authority. Engine adds a context adapter and removes duplicated namespace privilege adapters. SQL and direct Rust sequence deletion reuse the shared relation DROP binder, removing the sequence-only lock bypass. Direct deletion keeps the same memory/persistent definition transaction boundary and boolean missing-target result. No persistent encoding, dependency or feature changes are needed.

PostgreSQL 18.4 uses pinned Docker image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`. Five source/destination wait schedules produce 488 bytes with SHA-256 `7a9a7ad61f81fb1ec9aaab5427fd01a1e41f151167f7fa8018891bcb18b6e81b`. Ten additional lock/completion schedules and six validation-order checks produce 1,066 bytes with SHA-256 `d0e4d17f33ad1596e5b59807c88364c237d8d5e8eddf31294e202a8b0e9c40b7`. The upstream [relation namespace move](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/tablecmds.c) and [namespace binder](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/catalog/namespace.c) provide the corresponding source reference. Reports remain temporary. No timing or allocation workload is run.

Five PostgreSQL lock-mode inspections and six definition/read/owner wait schedules additionally produce 540 bytes with SHA-256 `6b63165f8c62d3309841368eb51a158123a433fefcd87f6ed572490dfdc5180c`. The [sequence definition implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/sequence.c) is the corresponding mode and ownership reference.

The regression reproduces a `40001` on the deleting transaction’s COMMIT when an uncommitted DROP SEQUENCE is bypassed by a competing rename. The common DROP binder previously exempted sequences entirely; both SQL and the direct Rust API now acquire and revalidate through the shared binder. Reciprocal deletion waits cover committed definition changes, rename, deletion and recreation, including notice and direct-API absence behavior. Focused verification passes 78 Engine sequence, namespace and role-authority cases, 18 SQL sequence cases, 23 execution namespace/relation-binding cases and 116 public sequence, schema, direct deletion, session, view-dependency and manual integration cases: 235 distinct cases. The manual harness compiles SQL fences and runs executable examples. Three-provider schedules cover observed source/destination waits, replacement, relation kinds, authority, RC/RR/SSI, rollback and savepoint undo. All 32 PostgreSQL reference inspections, schedules and validation checks retain compact provenance above; raw reports stay temporary. Strict SQL/execution/Engine all-target Clippy, formatting, exact-index dependency, Engine ownership, harness, header, file-size and hygiene checks pass. No persistent format, Cargo dependency, feature or allocation ceiling changes, and no timing or allocation workload is run. Remaining ALTER authorization ordering, sequence value-function locks, dependent-object lifecycle consumers, catalog tuple/index coordination, fixed SQL catalog snapshots and the full concurrency/platform acceptance matrix remain required.


### Sequence alteration authority

Sequence ALTER now validates the actual target owner before checking whether its kind matches the command. Execution reads existing table/view/foreign/sequence/system security registries and index-parent ownership without adding an Engine algorithm or opening a sequence-value snapshot. Role identities are checked against the retained authorization guards. SQL owns actual-kind owner diagnostics, pinned-system relation protection and the distinct definition versus relation-lifecycle wrong-kind errors. Rename checks CREATE on the source namespace before sequence-kind validation and repeats every check after relation-lock waits. Temporary namespace CREATE checks current database TEMP authority; definition options and moves retain their distinct source authority. Engine only wires the native context and removes redundant sequence-owner adapters.

Four new Engine regressions fail before the fix: wrong-kind ownership order, missing source CREATE, source CREATE revoked during a relation wait and revoked TEMP on an existing temporary sequence. PostgreSQL 18.4 Docker reference checks use pinned image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`: ten ownership/kind/namespace checks produce 689 bytes with SHA-256 `5023d8b5b6d10fa6bc872ff511a53c03a66129e42ad54eb7b3fdfc13f427216e`; three temporary-sequence checks produce 159 bytes with SHA-256 `3325611f5fbce49e8e2cc8099c8239e5c195458efc5e8aaa91988cc30622bbbb`. The [ALTER callbacks](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/tablecmds.c), [system-catalog classification](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/catalog/catalog.c) and [pinned OID boundary](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/include/access/transam.h) provide the corresponding source reference. Raw outputs remain temporary.

Focused verification passes 24 SQL policy cases, 28 execution cases, 84 Engine sequence, namespace and role-authority cases and 116 public sequence, schema, direct deletion, session, view-dependency and manual integration cases: 252 distinct cases. All six new Engine regressions pass, including three-provider source-CREATE revalidation at RC/RR/SSI, actual view/foreign/index owners and pinned versus unpinned catalog protection. The manual harness compiles SQL fences and runs executable examples. Strict SQL/execution/Engine all-target Clippy, formatting, exact-index dependency, Engine ownership, harness, header, file-size and hygiene checks pass. No persistent format, Cargo dependency, feature or allocation ceiling changes, and no timing or allocation workload is run. Other ALTER entrypoints, sequence value-function locks, dependent-object lifecycle consumers, catalog tuple/index coordination, fixed SQL catalog snapshots and the full concurrency/platform acceptance matrix remain required.


### View and foreign-table alteration authority

View, materialized-view and foreign-table ALTER now retain the actual resolved relation kind until execution checks the current owner, pinned-catalog protection and rename-only source CREATE. The shared native authority helper also serves sequence ALTER and reads existing owner and namespace interfaces; Engine only wires those contexts. Binding repeats the checks after definition waits and follows replacement of the original name without restoring an obsolete view or foreign table. Temporary-view rename checks current database TEMP, while ordinary view options and unchanged owner assignments retain their separate authority rules. Foreign-table ALTER uses a deferred definition transaction and requests write admission after binding; owner transfer keeps its existing role-dependency ordering.

Four regressions reproduce the preceding implementation’s wrong-kind-before-owner error, missing rename CREATE, CREATE revoked during a definition wait and TEMP revoked on an existing temporary view. Additional cases cover committed replacement by a table with a different owner or kind and system-catalog protection. PostgreSQL 18.4 Docker checks 25 corresponding owner/kind/namespace outcomes using pinned image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`; the temporary 2,529-byte reference has SHA-256 `2d8f518f17ebf3b161045234d1b7db881b608bd8907caa07c8f8d1888965a15c`. Raw outputs remain temporary.

Focused verification passes 27 SQL policy cases, 28 execution cases, 39 Engine authority, foreign-table, sequence, view-lock and role-owner cases, and 99 distinct public view, foreign-table, schema-authority, dependency, trigger and manual integration cases: 193 distinct cases. All six new Engine regressions pass, including three-provider source-CREATE revalidation at RC/RR/SSI and replacement of the original name by a differently owned or wrong-kind relation. The manual harness compiles SQL fences and runs executable examples. Strict SQL/execution/Engine all-target Clippy, formatting, exact-index dependency, Engine ownership, harness, header, file-size and hygiene checks pass. No persistent format, Cargo dependency, feature or allocation ceiling changes, and no timing or allocation workload is run. Ordinary-table ALTER binding, other definition consumers and the full concurrency/platform acceptance scope remain required.


### Ordinary table alteration binding

Ordinary ALTER TABLE now retains the original requested reference through shared scoped relation binding. Execution rechecks name, object identity, actual owner, pinned-system protection and rename-only source CREATE after a definition wait, then asks SQL to lower the selected kind. Removal honors IF EXISTS without touching a renamed source; an unqualified source can fall through to another search-path schema; replacement by a view dispatches the view owner path instead of using a stale table handle. Historical ALTER TABLE OWNER TO now lowers both regular and materialized views through their existing native owner-transfer implementation. SQL preserves missing-schema and missing-relation diagnostics for every ordinary table action. Engine supplies existing catalog, owner, namespace and lock adapters and removes its obsolete unrefreshed exclusive-lock wrapper.

Four Engine regressions reproduce the preceding implementation’s missing source CREATE, missing temporary-namespace TEMP, stale renamed table mutation and stale table-kind dispatch after replacement. PostgreSQL 18.4 Docker independently verifies eight authority, missing-target and observed-wait outcomes using pinned image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`; the temporary 567-byte reference has SHA-256 `e81634570ed03b2dbbeb6c407efbb6288b1d94e14ae96a6a16cf786820e84000`. Raw outputs remain temporary. Added regressions cover source CREATE revoked during RC/RR/SSI waits, savepoint release of provisional locks, replacement ownership before kind, search-path fallback and protected system catalogs.

Focused verification passes 12 SQL target and authority cases, 14 execution binding and namespace cases, 20 Engine authority and post-wait cases, and 330 public DDL, inheritance, trigger, view, foreign-table, schema, sequence-lifecycle, ownership and manual cases: 376 distinct cases. All eight Engine regressions and four owning SQL regressions pass. The manual harness compiles SQL fences and executes runnable examples. Strict SQL/execution/Engine all-target Clippy, formatting, exact-index dependency, Engine ownership, harness, header, file-size and hygiene checks pass. Action-specific table lock modes, inherited and secondary relation binding, further relation lifecycle consumers and the full concurrency/platform acceptance scope remain required. No persistent format, Cargo dependency, feature or allocation ceiling changes, and no timing or allocation workload is run.


### Table alteration action and secondary locks

SQL selects the strongest required ALTER action mode: trigger modes and foreign-key addition use ShareRowExclusive, while validation and partition attachment use ShareUpdateExclusive. Execution binds explicit foreign-key and hierarchy references by their original requested names, retains inherited table identities through rename, and prepares the affected relations before each action publishes its definition. Generated constraint names are assigned at the original per-action boundary. Constraint-addition preparation locks every inheritor before CHECK merging or NO INHERIT stops mutation. Column addition follows its own merge boundary, while a newly added column CHECK retains every descendant lock. CHECK validation also retains locks beyond already validated children. Table alteration enters a deferred definition transaction; Engine supplies existing contexts and mode conversion only. INHERIT checks parent ownership and retains ShareUpdateExclusive; NO INHERIT retains AccessShare without requiring the parent owner. CHECK validation no longer upgrades descendants to AccessExclusive. Partition attachment locks the selected subtree and default subtree before their scans and publication. Child enumeration follows each acquired parent lock, including default descendants published during a wait.

Seven Engine lock-conflict cases include six reproduced failures and one strongest-mode control. Setup objects are committed separately before the tested ALTER transaction so their creation locks and visibility cannot contaminate the assertions; this is functional NOWAIT verification, not performance measurement. PostgreSQL 18.4 Docker independently verifies seven corresponding lock inventories using pinned image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`; the temporary 453-byte reference has SHA-256 `00d6b558ad37c3fe588420851f6a4990a76d54a7468c35fe38a66980748d5b5a`. Five additional parent/merge inventories occupy 373 bytes with SHA-256 `991d472eae4d5108b853fecb2b5af0810a7c3a1635bddd505658c6b0b646dd5e`. Three preparation/validation inventories occupy 379 bytes with SHA-256 `59b38f9465c1b2fb79d36abfebca37922561ff5f5075d3d09cb46295832db5d4`. These independently establish that CHECK mutation merging and NO INHERIT do not shorten prepared locks, and already validated children do not shorten validation locks. The default-partition subtree inventory occupies 134 bytes with SHA-256 `7094b7525422c068de65d6acc27c60873dd5b3911d140c1313d94504087f6c21`. The [ALTER TABLE contract](https://www.postgresql.org/docs/18/sql-altertable.html) and [inheritance and CHECK execution](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/tablecmds.c) establish the action, parent and merge boundaries. Raw reports remain temporary.

Focused verification passes 14 SQL lock-mode, target and authority cases, 62 native execution row-lock/namespace cases, 59 Engine alteration and relation-lock cases, and 330 public DDL, inheritance, trigger, view, foreign-table, schema, sequence-lifecycle, ownership and manual cases: 465 distinct cases. All 19 added Engine regressions and both SQL lock-mode cases pass. Two ignored execution entries are subprocess fixtures exercised by their parent cases. The manual harness compiles SQL fences and executes runnable examples. Strict SQL/execution/Engine all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness, header, file-size and hygiene checks pass. Added cases cover referenced-name replacement, REFERENCES revoked during a wait, inherited-child rename with name reuse, savepoint release of primary and secondary locks, inheritance ownership and modes, skipped REFERENCES declarations, prior private changes across a later action wait, generated CHECK merging, independent column-CHECK recursion, noninherited constraint preparation, previously validated descendants and a default descendant published during a parent wait. Recursive removal and other alteration consumers, full concurrent detach, additional definition and catalog consumers and the full concurrency/platform acceptance scope remain required. No persistent format, Cargo dependency, feature or allocation ceiling changes, and no timing or allocation workload is run.


### Sequence value relation locks

All four sequence value functions now acquire RowExclusive before checking privileges or consuming cached values. Execution retains the original object identity through rename and name reuse, rechecks the current definition and authority after acquisition, and promotes only the requested mode to the outer transaction's lock mark. SAVEPOINT rollback therefore preserves the value lock without extending an earlier definition lock. Definition and authorization reads remain independent of ordinary SQL data snapshots. SQL effect analysis requests a statement transaction for every value function while preserving their existing read-only classifications. Engine supplies transaction and lock adapters: workers reuse the parent's active transaction without reentering its thread-owned statement gate, and standalone calls reuse existing scoped transaction completion with typed operation errors. No allocation algorithm, persistent encoding, dependency or Cargo feature changes are needed.

Five initial Engine regressions reproduce missing sequence value locks and definition-wait bypass. In three wait schedules, autonomous allocation against an uncommitted ALTER or DROP causes the definition holder's COMMIT to fail with a record conflict. PostgreSQL 18.4 Docker independently verifies eight value/cache/read-only/savepoint lock inventories using pinned image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`; the temporary 289-byte reference has SHA-256 `c1556fb829cf93b89ff097345d53f31e9134143406b5d6b1eec4e6d9c71f8235`. Six observed definition waits produce 270 bytes with SHA-256 `20284e9b579898d000b4dfca5c096f1911ab3b2510f3d1f1a6690e63855c9745`, covering cached restart, original identity through rename and name reuse, and `XX000` after removal during a value wait. The [PostgreSQL sequence implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/sequence.c) establishes the outer transaction resource owner and lock-before-privilege order. Raw reports remain temporary. No timing or allocation workload is run and no allocation ceiling changes.

The worker regression additionally reproduces statement-gate reentry during memory sequence writer preparation. The existing writer adapter now separates caller-owned statement gating from transaction-frame preparation, and sequence workers reuse that same preparation without reacquiring their parent's gate. Its regression covers cached and uncached values, all four value functions and private definitions in memory and all three persistent providers. Existing fault-injection fixtures now distinguish identity capture from post-lock authority capture, inject peer definition changes before acquiring the conflicting value lock, and inspect OID-based value publication before transaction completion can refresh live registries. These retain coherent authority, changed-bound rejection and autonomous-value guarantees without requiring an impossible same-thread ALTER after a conflicting value lock.

Focused verification passes 25 SQL sequence/effect cases, 88 execution sequence/row-lock/transaction cases, 133 Engine sequence/authority/alteration/relation-lock/transaction cases and 292 public sequence/hierarchy/schema/transaction/callback/manual cases: 538 distinct cases. All nine new Engine regressions and the SQL scope and execution lock-retention regressions pass. Two ignored execution entries are subprocess fixtures exercised by their parent cases. The manual harness compiles SQL fences and executes runnable examples. Three-provider schedules cover cached restart, original identity after rename and name reuse, committed deletion, post-wait authority, savepoint lock lifetime, prior definition-lock release and fixed ordinary data visibility across RC/RR/SSI. Direct Rust and query-worker cases cover implicit completion, explicit read-only access, private definitions and existing callback/error cleanup. Strict SQL/execution/Engine all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness, header, file-size and hygiene checks pass. Further constraint/alteration and dependent-object lifecycle consumers, catalog coordination and the complete concurrency/platform acceptance remain required. No persistent format, Cargo dependency, feature or allocation ceiling changes, and no timing or allocation workload is run.

### Inherited constraint validation and retained dependencies

SQL selects constraint validation requirements, checks enforcement before validated state, rejects ONLY validation of unvalidated inheritable constraints with children, and matches child NOT NULL constraints by column. Execution shares child-before-parent validation for CHECK and NOT NULL, replacing the previous CHECK-only traversal. Parent validation is published only after every child succeeds; statement and savepoint rollback preserve the prior flags. Preparation retains ShareUpdateExclusive across the whole required hierarchy, including descendants behind validated children. Unvalidated foreign keys retain RowShare on the original referenced table identity through a definition wait. Already validated constraints and NO INHERIT constraints preserve their separate secondary-lock requirements. INHERIT retains AccessShare across the existing child subtree before cycle validation. All traversal and binding stay in execution; Engine uses its existing state and transaction adapters without production changes.

Six valid Engine schedules reproduce missing foreign-key and inheritance dependency locks, missing NOT NULL descendant locks and validation, acceptance of child NULLs and ONLY validation, and failure to validate differently named child constraints. PostgreSQL 18.4 Docker independently verifies five lock inventories using pinned image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`; the temporary 412-byte reference has SHA-256 `ab8568af157f38a2ccbe92c64f28b513fb9f09cba627fac50fd6d2be6ec59d02`. Four validation outcomes confirm distinct child names, `23502` for child NULLs, `42P16` for ONLY, and NO INHERIT scope; the temporary 192-byte reference has SHA-256 `0b9110441d3344b2cd4453dcadeff49ab300e2beeb89f537853e6a7481133209`. The [PostgreSQL validation and inheritance implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/tablecmds.c) supplies the corresponding source reference. Raw reports remain temporary. No timing or allocation workload is run and no allocation ceiling changes.

Focused verification passes 10 SQL constraint/table-alteration cases, 63 execution row-lock/constraint/table-alteration/namespace cases, 68 Engine alteration/relation/view/removal-lock cases and 343 public DDL/constraint/hierarchy/trigger/manual cases: 484 distinct cases. All nine new Engine regressions pass across native SQLite, SQLite Key/Value and redb. The four new SQL cases cover inherited column matching, enforcement ordering, validation scope and required diagnostics. Two ignored execution entries are subprocess fixtures exercised by their parent cases. The manual harness compiles SQL fences and executes runnable examples. Strict SQL/execution/Engine all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness, header, file-size and hygiene checks pass. Recursive removal and other alteration/dependency consumers, catalog coordination and final concurrency/platform acceptance remain required. No Engine production code, persistent format, Cargo dependency, feature or allocation ceiling changes, and no timing or allocation workload is run.

### Inherited constraint removal and origin retention

SQL owns inherited constraint matching, origin-based removal decisions and primary-key/identity protection. Execution shares CHECK and NOT NULL removal instead of leaving NOT NULL on a single-table path. It captures direct child identities before waits, locks their current names through the existing relation binder, rechecks metadata and ownership, and removes a child only after its last inherited origin disappears and no local declaration remains. ONLY promotes retained direct children to local definitions when needed. Traversal stops below retained children and revisits shared children when a later branch removes their remaining origin. Engine lends two existing relation-lock adapters; no traversal or policy moves into Engine.

Five initial Engine regressions reproduce direct inherited NOT NULL removal, missing descendant removal and locks, missing ONLY origin promotion, and bypassed child waits. PostgreSQL 18.4 Docker verifies seven removal lock/origin inventories using pinned image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`; the temporary 779-byte reference has SHA-256 `ca93f96b6c7fa18d844e64178d443f11173c118440b2d71a4709fcfc642b717c`. Five further outcomes confirm direct inherited removal errors, distinct named/column identity errors, and differently named inherited constraints after another parent disappears; the temporary 161-byte reference has SHA-256 `82c8acac67476450b3e7c362ea8429522819421f10c71b26551e618d91e8a4a5`. The [PostgreSQL constraint removal implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/tablecmds.c) supplies the corresponding source reference. Raw output remains temporary. No timing or allocation workload is run and no allocation ceiling changes.

Focused verification passes 16 SQL constraint/table-alteration cases, 63 execution row-lock/constraint/table-alteration/namespace cases, 77 Engine alteration/relation/view/removal-lock cases and 343 public DDL/constraint/hierarchy/trigger/manual cases: 499 distinct cases. All nine new Engine regressions pass across native SQLite, SQLite Key/Value and redb, covering recursive and ONLY removal, local/multiple-parent origins, diamond inheritance, different inherited names, original child identity through rename/name reuse, NO INHERIT scope, identity/serial protection and savepoint restoration. Six new SQL cases verify inherited matching, origin decisions, local promotion and syntax-specific protection. Two ignored execution entries are subprocess fixtures exercised by their parent cases. The manual harness compiles SQL fences and executes runnable examples. Strict SQL/execution/Engine all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness, header, file-size and hygiene checks pass. Foreign-key removal dependencies, inherited NOT NULL renaming with durable identity, other alteration/dependency consumers, catalog coordination and final concurrency/platform acceptance remain required. No persistent format, Cargo dependency, feature or allocation ceiling changes, and no timing or allocation workload is run.

### Foreign-key removal and retained dependency identities

SQL resolves materialized foreign keys by their durable identity after metadata refresh, preserving renames, changed vector positions and same-name replacements. Execution captures every selected referrer and partition clone before the first dependent wait, retains AccessExclusive on the original referring and referenced relations, and publishes metadata reread after waits. Key, column and index cascades share that path; direct removals still reject pending events while dependency cascades remove child-side events. Key removal refreshes its own metadata after dependent deletion, preserving unrelated references renamed by another transaction. DROP INDEX refreshes dependencies after its parent lock wait, so intervening foreign-key creation or removal affects both RESTRICT and CASCADE. Engine reuses its existing adapters without production changes.

Five initial Engine failures reproduce missing reference and referrer locks, bypassed definition waits and missing partition locks. PostgreSQL 18.4 Docker verifies five lock inventories using pinned image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`; the temporary 341-byte reference has SHA-256 `6953c0be9e458ddc9f9efa813c6aebd0b8294d37892d0d991fbb7922f223c1ac`. Seven observed-wait outcomes cover reference/referrer renames, multiple simultaneous name reuses, preserved unrelated metadata and a replaced foreign key; one further inventory covers partition clones. This temporary 443-byte reference has SHA-256 `ebabf73e1820e6426f31c4c174aa5cdac396dd53da62f8845602b7e66074f770`. Four index dependency addition/removal outcomes across RESTRICT and CASCADE have a temporary 198-byte reference with SHA-256 `4ad8a7aac505904d9a6b1e993d40c14db3af676e486705b446b1ebc334a059ef`. The [PostgreSQL constraint removal implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/tablecmds.c) supplies the corresponding source reference. Raw reports remain temporary. No timing or allocation workload is run and no allocation ceiling changes.

Focused verification passes 19 SQL constraint/table-alteration cases, 63 execution row-lock/constraint/table-alteration/namespace cases, 87 Engine alteration/relation/view/removal-lock cases and 343 public DDL/constraint/hierarchy/trigger/manual cases: 512 distinct cases. All ten new Engine regressions pass across native SQLite, SQLite Key/Value and redb, covering direct/enforced-state/partition removal, key/column/index cascades, savepoint lock lifetime, original reference and referrer identities, multiple simultaneous name reuses, refreshed metadata and changed index dependencies. Three new SQL cases verify identity lookup after rename and reordering, name reuse and required materialization. Two ignored execution entries are subprocess fixtures exercised by their parent cases. The manual harness compiles SQL fences and executes runnable examples. Strict SQL/execution/Engine all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness, header, file-size and hygiene checks pass. Constraint renaming with durable catalog identities, complete index target binding, remaining alteration/dependency consumers, catalog coordination and final concurrency/platform acceptance remain required. No Engine production code, persistent format, Cargo dependency, feature or allocation ceiling changes, and no timing or allocation workload is run.

## Inherited NOT NULL renaming and catalog identity

NOT NULL constraints now retain their own object incarnation and public OID through column, table and constraint renames. Removal/recreation allocates a new identity, and inherited children receive independent identities. SQL owns identity allocation, validation, predecessor OID preservation and rename diagnostics. Execution shares CHECK/NOT NULL rename traversal, validates descendants before publishing, retains original child relations through waits and name reuse, and preserves transaction/savepoint undo. Both constraint kinds rename by constraint name; a differently named child NOT NULL constraint reports `42704`, while ONLY on an inheritable parent and direct inherited renames report `42P16`. NO INHERIT definitions remain local.

Ordinary constraint restoration and inherited foreign-key identity synchronization move from Engine to execution. The complete ordinary/foreign-table migration candidate is validated before writes. Initial restoration preserves legacy NOT NULL OIDs and writes `sql_not_null_constraint_identity_version=1` within its existing transaction; later errors roll back definitions and the marker. Secondary sessions and table catalog reload require the completed marker and validate the combined ordinary/foreign-table identity set without repairs. Common record format 32 prevents preceding writers from discarding those identities; native mapping 8 and catalog version 49 stay unchanged. Actual format-31 binaries from `a20cccf58135eaf79013dfce33e9c1bdb0728009` pass `scripts/verify-record-format-upgrade.py --old-ref a20cccf58135eaf79013dfce33e9c1bdb0728009 --offline` in all nine native SQLite, SQLite Key/Value and redb file configurations. This physical-format check preserves database identity, records, identifier state and receipts and rejects retained or reopened predecessor access; logical NOT NULL conversion has separate owner and Engine regression coverage.

PostgreSQL 18.4 Docker image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636` confirms NOT NULL identity preservation across column/table/constraint renames, independent recursive identities, differently named child rejection, ONLY rejection and direct inherited rejection. The temporary six-outcome reference also includes a foreign-key rename observation for the remaining work; its 269 bytes have SHA-256 `62bf5cd06d52d347676c7de11bbb02b819d607b842d3dab611631617e7cff96f`. The [PostgreSQL rename implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/tablecmds.c) supplies the source reference. Foreign-key/key constraint and index identity coordination remain required. No allocation or timing workload is run and no allocation ceiling changes.

Focused verification passes the complete 680-case SQL unit suite, 77 selected execution cases, 111 Engine alteration/foreign-table/lock/import cases, five SQLite format cases, nine redb format/recovery cases and 343 public DDL/constraint/hierarchy/trigger/manual cases: 1,225 distinct cases. All 11 new Engine regressions pass across native SQLite, SQLite Key/Value and redb. Five new SQL cases cover identity migration, retirement, validation and rename policy; five execution cases cover ordinary/foreign conversion, complete-candidate rejection, marker validation and load-only duplicate checks. Two ignored execution entries are subprocess fixtures exercised by their parent cases. The legacy partition foreign-key fixture now removes only typed foreign-key identities, preserving unrelated current identities. The manual harness compiles SQL fences and executes runnable examples. Strict five-owner all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness, header, file-size and hygiene checks pass. Remaining foreign-key/key constraint and index identity coordination, other definition/dependency consumers and full concurrency/platform acceptance remain required. No Cargo dependency, feature or allocation ceiling changes, and no timing or allocation workload is run.

## Foreign-key catalog identities and local renaming

Foreign keys now keep an independent catalog row incarnation and OID in addition to their existing partition-family enforcement identity. SQL owns materialization, initial legacy OID preservation, identity validation and local rename policy. Partition creation, attachment and recursive column additions allocate independent row identities; attachment provenance retains the corresponding local identity. Ordinary INHERITS excludes inline foreign keys on inherited columns, including recursive ADD COLUMN. Execution uses the existing bound table lock and schema publication transaction for foreign-key renames without traversing partitions or locking the referenced relation. Column, relation and constraint renames preserve each row's OID; removal/recreation replaces the row identity.

Constraint identity reconciliation moves from Engine to SQL and matches the original relation's renamed row before another partition-family member. Engine retains transaction-owned modes and event queues using this result. This fixes dropped deferred modes and pending checks after a foreign-key rename. Savepoint rollback restores the name, identity and lock lifetime.

Attachment provenance follows the local row identity through renames and validation changes. SQL removes provenance with its live foreign key, and execution publishes the updated hierarchy with the constraint state. Parent constraint removal after a renamed attached child has reopened therefore leaves neither a stale persisted entry nor stale live metadata that breaks the next alteration.

Initial execution restoration validates the combined NOT NULL and foreign-key identity set and attachment provenance before writing converted ordinary or foreign-table definitions. The independent marker `sql_foreign_key_catalog_identity_version=1` prevents missing current identities from being repaired as legacy rows. Secondary restoration and catalog reload require known completed conversion; later restoration failures roll back both rows and the marker. Common record format 33 excludes preceding writers; native mapping 8 and catalog version 49 remain unchanged. Actual format-32 binaries from `b9eb44d22362a4eb0dc4dce7b433650f103d7db0` pass `scripts/verify-record-format-upgrade.py --old-ref b9eb44d22362a4eb0dc4dce7b433650f103d7db0 --offline` in all nine provider/file configurations, preserving database identity, records, identifier state and receipts and rejecting retained or reopened predecessor access.

PostgreSQL 18.4 Docker image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636` verifies seven rename, OID, deferred-event, conflict and lock outcomes; the temporary 342-byte reference has SHA-256 `494f242f0672ec15927a7ce28d9db14501e1c9ee0704208e82a19c4ec626725c`. Three inheritance outcomes separately verify excluded ordinary child references and distinct partition rows; the temporary 185-byte reference has SHA-256 `3c5a217f708fc3b94ae5d8c46242d415d09814cc5ec2fc3ff64d893bdff95865`. An observed definition-wait schedule verifies that an explicitly named ALTER target is resolved again after its name is reused, while retaining both original catalog identities; the temporary 82-byte result has SHA-256 `dbfd58671bf5cf1a62c1122bcf2a5d7e130b4a0d17ff2980eea51ba7d79783e4`. The [PostgreSQL rename implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/commands/tablecmds.c) provides the source reference. Raw reports remain temporary. General constraint/index OID collision reservation, key/index identity and rename coordination and the broader concurrency/platform acceptance remain required. No timing or allocation workload is run and no allocation ceiling changes.

Focused verification passes the complete 687-case SQL unit suite, 80 selected execution cases, 93 Engine alteration/foreign-table/lock cases, five SQLite format cases, five redb format cases and 307 public DDL/constraint/hierarchy/trigger/manual cases: 1,177 distinct cases. All ten new Engine regressions pass across native SQLite, SQLite Key/Value and redb, including migration rollback, load-only corruption checks, local partition renames, deferred checks and renamed attachment provenance followed by another alteration. Seven new SQL cases cover identity migration, validation, reconciliation, rename and removal; three execution cases cover legacy conversion, missing-current identity rejection and combined NOT NULL/foreign-key duplicate rejection before writes. Two ignored execution entries are subprocess fixtures exercised by their parent cases. The manual harness compiles SQL fences and executes runnable examples. No Cargo dependency, feature or allocation ceiling changes.

Strict five-owner all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness, header, file-size and hygiene checks pass. General constraint/index OID reservation, key/index identity coordination and full concurrency/platform acceptance remain required.

## Internal reader maintenance lifetime

Internal fixed snapshots, current-catalog readers and committed retrieval rechecks previously used the public sibling-session path, which registered an automatic-statistics client and could restart background work after application clients released it. Engine now distinguishes internal read construction before maintenance registration. These internal sessions retain independent storage views and cancellation while public sibling sessions retain their normal maintenance client lease. The change stays in session and retained-resource adapters; no analysis or storage algorithm moves into Engine.

A new regression checks the internal-reader and fixed-snapshot client lifetime across native SQLite, SQLite Key/Value and redb, including querying the internal reader and opening a public sibling afterward. All 59 selected open, refresh, transaction and statistics cases pass. The 93 alteration/foreign-table/lock cases also pass, including the strengthened permission-wait rollback diagnostic. Together with the foreign-key unit's SQL, execution, provider-format and public integration suites, this is 1,236 distinct passing cases on the combined tree. Strict five-owner all-target Clippy, formatting and the ownership, dependency, harness, header, file-size and hygiene policies pass. No timing or allocation workload is run, and no dependency, feature, record format or allocation limit changes in this unit.

## Constraint and index catalog coordination

Work remains in progress. Key and CHECK constraints and all stored indexes now retain independent catalog addresses. Constraint-owned and generated partition index rows have durable ownership and parent bindings, verified on `3adbb56ccb020b52033dbe408fd0a8b1a6f5dc87`. Renaming must preserve both the constraint and its index, every foreign-key dependency and the physical access path. The current rename unit separates mutable key names from structural table declarations and retains index incarnation locks; remaining catalog coordination and the complete acceptance matrix must still be verified.

SQL owns constraint/index identity models, legacy decoding, generated names, ownership/dependency matching and diagnostics. Execution owns OID reservation, catalog refresh after waits, complete migration candidates, retained relation bindings, index publication and rename scheduling. Reuse `SharedObjectLockSession` for class-scoped reservations and keep Engine limited to catalog/session/publication adapters. Storage providers keep opaque definition records and their existing physical publication boundary; neither SQL nor execution gains a reverse dependency.

- [x] Store independent key and CHECK constraint identities, preserve existing noncolliding OIDs during initial conversion and reject malformed current metadata before publication.
- [x] Give stored standalone indexes independent catalog addresses and immutable physical namespaces with initial conversion and runtime OID reservation.
- [x] Store durable constraint-owned and partition-derived index rows with explicit ownership and parent bindings.
- [ ] Reserve constraint and relation OIDs against live, private and competing uncommitted objects, including temporary and foreign-table constraints and constraint triggers. Release rejected candidates after a post-wait collision, preserve cancellation/serialization diagnostics and retain accepted reservations through savepoint/transaction lifetime.
- [ ] Bind foreign-key dependencies and physical unique-key coordination to index incarnations, so an index rename neither changes the referenced key nor splits concurrent key admission by display name.
- [ ] Implement key-constraint and index renames with PostgreSQL ownership, schema, conflict and lock behavior. Rebind explicit names after waits, retain identities for dependent traversal, keep partition renames local and preserve rollback/reopen behavior.
- [ ] Project stable `pg_constraint`, `pg_class`, `pg_index`, `pg_inherits` and dependency identities from stored rows, including parent links; validate all creation, attachment, removal, rename, restore and refresh paths before declaring catalog coordination complete.
- [ ] Verify the new storage boundary with actual predecessor binaries, complete owner and provider regressions, strict Clippy and repository policies, then commit and push coherent units and update PR #112. Full concurrent-storage acceptance remains required beyond these catalog changes.

PostgreSQL 18.4 Docker image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636` verifies ten key/index rename outcomes, including preserved constraint/index OIDs, foreign-key dependency identity, owning-constraint rename through ALTER INDEX, local partition names, conflict diagnostics and savepoint restoration. ALTER TABLE retains AccessExclusive on the table and ShareUpdateExclusive on the index; ALTER INDEX retains ShareUpdateExclusive on the index. The temporary 617-byte reference has SHA-256 `29befa86188f1a91196b44ec43fe7e3aed73ee74aa7bbc919f78e505bd72a071`. PostgreSQL's [catalog OID allocator](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/catalog/catalog.c) establishes per-catalog collision checking; UQA must coordinate uncommitted reservations through its existing logical lock lifecycle. No timing or allocation workload is run.

Four additional PostgreSQL 18.4 Docker outcomes from the same pinned image verify independent constraint/index parent edges, preservation of existing standalone and constraint-owned index identities during attachment, and retention of a generated standalone index after detachment. The temporary 244-byte reference has SHA-256 `9c1c1466c69633051fa351ea8688593c7ae473cbcaac1fe6336c25c03468651c`. The [PostgreSQL constraint catalog contract](https://www.postgresql.org/docs/18/catalog-pg-constraint.html) distinguishes the supporting index reference from the parent constraint reference. These observations guide the pending durable registry work; they do not establish its implementation.

The common execution-owned OID reservation primitive now serves roles, role memberships and schemas. It validates the allocatable domain, checks occupancy before and after the existing logical object lock and catalog refresh, releases collided or failed candidates, and retains an accepted address through transaction/savepoint lifetime. Four new reservation regressions and the existing role/namespace regressions pass: 55 cases. Strict execution all-target Clippy passes for this reservation change. Constraint/index creation is still being connected to this primitive; these checks do not establish completion of the pending items above.

Key and CHECK address integration is implemented. The SQL model separates an incarnation from its public OID, validates cross-kind duplicates and attachment provenance, and preserves predecessor key/name and CHECK/object-derived OIDs. Execution connects ordinary, foreign-table and recursive declaration materialization to transaction-owned reservations, validates the complete restoration candidate before writes and adds `sql_constraint_catalog_address_version=1` independently of the existing NOT NULL and foreign-key markers. Current missing identities remain errors; only newly converted legacy addresses may be reassigned on collision. Common record format 34 excludes preceding readers and writers. Actual format-33 binaries from `b854b6c80fb61bd2ab0da033aebf7b760de3ee44` pass `scripts/verify-record-format-upgrade.py --old-ref b854b6c80fb61bd2ab0da033aebf7b760de3ee44 --offline` in all nine provider/file configurations, preserving database identity, records, identifier state and receipts and rejecting retained or reopened predecessor access. Durable index registry conversion and all relation/constraint-trigger allocation consumers remain separate required work.

Review of attachment provenance exposed a pre-existing detach bug: constraints added during attachment were deleted when the partition was detached. PostgreSQL 18.4 Docker verifies that ordinary attachment and partition creation retain the child key and foreign-key rows and OIDs on detach, that the detached foreign key survives removal of its former parent constraint, and that a detached subtree keeps its internal constraint parent links. The temporary four-outcome reference is 268 bytes with SHA-256 `994ba6c5ec2ca4172cfeb5e84c1cca018196eab8aa329ebd5b08e779dc764f90`; the [PostgreSQL ALTER TABLE contract](https://www.postgresql.org/docs/18/sql-altertable.html) describes detached indexes remaining with their table. SQL now supplies enforcement-family splitting and explicit mode preservation without replacing the local catalog identity; execution validates pending events, applies one family replacement throughout the detached subtree and clears only the external attachment provenance. The Engine adapter forwards transaction-owned modes. Seven new Engine regressions cover all three providers, including local and subtree detach, explicit modes, savepoint undo, rename/reopen, current key/CHECK corruption and initial conversion rollback. All 100 selected Engine alteration/foreign-table/lock cases pass; no timing or allocation workloads are involved.

Catalog address review additionally covers identities supplied through Rust declarations: execution verifies the owning relation and both identity components, reserves unpublished supplied addresses, rejects post-wait collisions without replacing the supplied identity, and preserves typed cancellation and serialization errors. The current-catalog contract is explicit for every implementation. Reservation refresh exposed stale retained table handles during schema publication; execution now reacquires the current generation and verifies the original relation incarnation before storing and publishing the complete constraint/provenance candidate. The corrected implementation passes all 696 SQL unit cases, 95 selected execution cases and 100 selected Engine cases. Public integration and provider format verification are recorded below.

The pending index unit must replace name-derived `CatalogIndexRelation::oid` and synthesized constraint/partition index rows with durable ownership and parent bindings. Enforced-key selection now belongs to execution and reference eligibility to SQL, while Engine provides retained table and catalog state; the ownership unit below records that change. The unconnected index model scaffold is excluded from the constraint-address unit.


The constraint-address implementation passes 696 SQL unit cases, 95 execution cases and 100 Engine cases (891 distinct cases), strict Core/SQL/execution/Engine/SQLite/redb all-target Clippy, formatting and the dependency, ownership, harness, header, file-size and hygiene policies. Two existing public detach scenarios incorrectly allowed invalid foreign keys after detachment; their expectations now require `23503` and verify a valid insert, and the local-constraint test name describes preservation. The corrected integration target passes strict Clippy. Public integration confirms 377 distinct cases: 375 passed in the original run, whose only two failures were the obsolete detach expectations, and all three edited detach scenarios pass after correction; the renamed local-constraint scenario is counted once. The manual harness compiles SQL fences and executes runnable examples. Five redb and 16 SQLite format cases pass, for 1,289 distinct passing cases on `eb2cdcd7a25f9fb57a127f5eed17e3ba3fdc7399`. The exact-head PR formatting and review checks also pass; full concurrent-storage acceptance remains required. A single startup diagnostic observed the original integration main thread at `_dyld_start` before its test harness began; the retained process subsequently completed without being restarted. No benchmark/allocation workload or ceiling changes are made, and the diagnostic remains temporary.


## Index enforcement ownership

Execution now selects enforced standalone and partition unique indexes from the supplied retained catalog and caller-selected table constraints. SQL owns foreign-key reference eligibility, excluding partial and expression keys while preserving selected constraint metadata. Engine supplies the same canonical relation, current table constraints, retained catalog and name-resolution state as before; it no longer constructs index key semantics or decides partition applicability. No dependency, feature, storage format or allocation limit changes are needed. Two execution regressions cover declared identity, expression/predicate/null semantics, partition descendants and ordinary inheritance, and one SQL regression covers reference eligibility. This ownership unit passes all three new owner cases and 202 public index, unique-key, foreign-key, conflict, hierarchy and schema/index atomicity cases: 205 distinct cases. Strict SQL/execution/Engine all-target Clippy, formatting and the dependency, ownership, harness, header, file-size and hygiene policies pass. These checks do not rerun the preceding constraint-address unit’s 1,289 cases. Durable index identities, ownership, parent bindings and rename coordination remain required. No timing or allocation workload is run.


## Legacy hierarchy restoration

Review found that initial catalog restoration still deleted key and foreign-key rows from a partition whose parent had disappeared. That algorithm has moved out of Engine into the complete execution-owned constraint candidate. SQL normalizes surviving edges, preserves sequence numbers and constraint addresses, restores identity overrides and supplies NOT NULL/CHECK origin policy. Execution splits each detached root’s enforcement family consistently across its descendants while retaining internal attachment provenance. Complete ordinary/foreign validation precedes writes, and later restoration failures use the existing catalog rollback boundary. No dependency, Cargo feature, durable metadata shape or record format changes are needed.

Two SQL hierarchy regressions cover identity/constraint preservation and retained parent ordering. Two execution regressions cover independent restored subtree families, idempotent reload and rejection before writes when a later definition is malformed. Two Engine regressions cover preserved OIDs, uniqueness, foreign-key enforcement, local constraint changes, subtree removal, reopen and later-failure rollback across native SQLite, SQLite Key/Value and redb. The execution migration test additionally exposed stale attachment provenance when initial foreign-key conversion preserved a predecessor OID. SQL now updates references to the exact prior catalog address during that conversion and continues to reject mismatched current addresses. An additional SQL regression and a third Engine regression cover this legacy conversion both with and without the original partition parent across all three providers. The corrected implementation passes all 700 SQL unit cases, 27 selected execution cases, 94 Engine cases and 170 public hierarchy, DDL, constraint and schema/index atomicity cases: 991 distinct functional cases. All three new Engine regressions cover native SQLite, SQLite Key/Value and redb, including legacy conversion with and without the original parent. The failing execution expectation is unchanged and now passes. Strict SQL/execution/Engine all-target Clippy, formatting, exact-index dependencies, Engine ownership, integration harness, source header, file-size and repository hygiene checks pass. Earlier pre-correction runs are not added to this count. Durable index registry/parent bindings and full concurrent-storage acceptance remain required. No timing or allocation workload is run, and no allocation ceiling changes.

## Stored index addresses and physical namespaces

Stored index definitions now retain their independent catalog address, indexed table incarnation and immutable physical key. Fresh names allocate through execution's relation-class reservations against current unfiltered table, view, foreign-table, sequence, system, graph and index metadata. Rechecks preserve the table and existing index across catalog refresh. Initial-only conversion validates the complete set before writes, preserves noncolliding legacy OIDs and existing physical namespaces, and rejects malformed current metadata through the independent `sql_index_catalog_identity_version=1` marker. No user expressions or postings are rebuilt solely to assign identities. Common record format 35 excludes preceding writers, while native mapping 8 and catalog version 49 remain unchanged.

Execution now owns physical-key selection, parsed definition binding, expression/predicate evaluation and rebuild projection. The generation cache follows copy-on-write registry identity through publication, refresh and rollback; physical lookup uses the opaque key directly. Engine retains the cache and document/store guards and publishes physical replacements. Standalone uniqueness reservations use the index incarnation, and stored-index `pg_class`, `pg_index`, regclass and dependent projections use its persisted OID. Constraint-owned and synthesized partition index rows, durable parent edges, foreign-key dependency conversion and index/key rename scheduling remain required work; this unit does not complete the full catalog or concurrent-storage plan.

PostgreSQL 18.4 Docker image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636` confirms a new OID after same-name recreation, stable OIDs through column/table renames and restoration of the original OID by savepoint undo. The temporary three-outcome reference is 152 bytes with SHA-256 `f6c16026de5c82e0a0e5645a532ff7702799edfc477dbb254e56d167a4fc1299`. Actual format-34 binaries from `19d1f6a1aaf5f2a8a8e7c64309e4066a4c79f508` pass `scripts/verify-record-format-upgrade.py --old-ref 19d1f6a1aaf5f2a8a8e7c64309e4066a4c79f508 --offline` in all nine provider/file configurations, preserving database identity, original records, identifier state and receipts and rejecting retained or reopened predecessor access.

The final stored-index implementation passes 1,140 distinct functional cases: all 701 SQL units, 18 selected execution cases, 12 Engine cases, 31 SQLite MVCC cases, two redb metadata cases and 376 public SQL, index, hierarchy, constraint, catalog atomicity, manual-example and writer-order cases. Four new Engine regressions cover all three persistent providers, including corruption rejection, preservation of legacy physical postings, savepoint/reopen behavior and rollback after a later restoration failure. Strict all-target Clippy for SQL, execution, Engine, SQLite and redb passes, alongside formatting, exact-index dependencies, Engine ownership, integration harness, source header, file-size and repository hygiene checks. Earlier runs before the physical binding cache optimization are not added to this count. No dependency, Cargo feature or allocation ceiling changes; no timing or allocation workload is run. Full index registry and concurrent-storage acceptance remain unfinished.

## Explicit index deletion after definition waits

Review found that `DROP INDEX` retained the row selected before its table-lock wait, allowing later deletion to use a stale table, physical method or owner decision. Execution now uses the shared scoped binding primitive to resolve each original SQL name after refresh, compare its stored index incarnation and indexed table, recheck current authority and release superseded provisional locks. Partitioned targets retain descendant table identities and locks before dependency validation; ordinary inheritance remains local. Constraint-owned indexes also acquire their table locks before the dependency error. Foreign-key dependency discovery continues after the final binding, and physical deletion receives the current row. Engine removes its redundant index-lock adapter. No dependency, Cargo feature or durable-format change is introduced.

The pinned PostgreSQL 18.4 Docker reference confirms seven outcomes for same-name replacement on another table, replacement by another relation kind, required/optional disappearance, replacement by a constraint index, ownership change and indexed-table rename during an observed lock wait. The compact reference is 225 bytes with SHA-256 `d97732094426b905c6db4f4e188049ae9c2f587fe35df4d3c9092da1e93a97c1`. The reference fixture now grants its test role USAGE on the recreated schema; the earlier fixture errors are not acceptance results. PostgreSQL's `RemoveRelations` additionally locks every partition table before deletion, while `RangeVarCallbackForDropRelation` releases a previous index's heap lock when the resolved object changes. A separate two-outcome Docker reference confirms that a constraint-owned index waits for its table before 2BP01 and that partition-table locks remain until savepoint undo; its 104 bytes have SHA-256 `c586fe2f316c4c9fb3dd6cb8e4eee6c10a3ae218bad384b1b546f9db7410b354`.

Final deletion verification passes 229 distinct cases: 11 execution binding/restoration cases, 19 Engine index/dependency cases and 199 public index, uniqueness, foreign-key, hierarchy, catalog atomicity and FTS lifecycle cases. All five new Engine schedules cover native SQLite, SQLite Key/Value and redb. The two initial wait failures came from test setup that placed uncommitted object creation before BEGIN in the same simple-query request; creation now commits before the lock schedule. Savepoint undo retains storage's matching committed/private base, and the next SQL command verifies the current index OID before inspecting physical state. The final SQL assertion preserves the peer's replacement without changing rollback algorithms or relaxing the expected identity. The shared wait fixture now reports early operation errors. Strict execution/Engine all-target Clippy, formatting, exact-index dependencies, ownership, harness, headers, file-size and hygiene checks pass. No dependency, feature, format or allocation-limit change, and no timing/allocation workload is involved. These results do not complete the remaining index registry or concurrent-storage work.

## Document identifier failure diagnostics

PR review identified that Engine converted supplied document-ID observation failures into generic internal errors after staging FTS changes. The adapter now uses execution's shared identifier error conversion. That owner preserves cancellation and memory diagnostics through provider wrappers and maps typed MVCC read/write conflicts to 40001; embedded SQL errors retain their original state. The initial provider regression exposed the missing conflict mapping, which is corrected in the shared helper rather than special-cased in Engine. No dependency, feature, durable format or allocation-limit change is needed.

Final diagnostic verification passes 32 distinct cases: 16 execution mutation/identifier cases and 16 Engine identifier/commit-resolution integration cases. The new owner regression covers direct and provider-wrapped cancellation, memory limits, read/write conflicts, embedded SQL diagnostics and generic failures. The new Engine regression injects foreground supplied-ID observation failures over SQLite logical records in four file modes and redb, verifies 57014/53200/40001, confirms document and FTS posting/count/length rollback in both sessions, then checks same-ID retry and Engine reopening. The first run exposed the missing 40001 mapping; only the corrected final run is counted. Strict execution/Engine all-target Clippy, the final edited integration target, formatting and ownership/dependency/harness/header/file-size/hygiene checks pass. No timing or allocation workload is run.


## Durable owned and partition index registry

PRIMARY KEY, UNIQUE and derived partition indexes now retain real catalog rows with independent index addresses, physical namespaces, owning constraint incarnations and immediate parent index incarnations. SQL owns the declaration/attachment rules and one-to-one inherited-key matching. Execution owns registry preparation, descendant schema changes, OID reservation/rechecks, physical build scheduling, dependency removal and initial conversion. Engine supplies retained state and transaction adapters. Local key enforcement consumes the stored child row; parent conflict arbiters bind through recorded index ancestry. Foreign keys retain their selected index incarnation through constraint renaming. Constraint and index addresses remain separate.

Creation publishes columns and constraints together, avoiding duplicate inline key materialization. Detachment retains local indexes and removes only the external parent edge; owned parents require distinct same-kind owned children, while independent parents can reuse existing primary/unique children. Dropping an independent parent needs CASCADE when a child is owned, and direct catalog removal remains restrictive. GIN/vector partition rows receive physical builds for existing and newly attached children. Multi-index GIN deletion evaluates surviving references once and clears each removed field once. Partition column rename/drop propagates through the locked partition tree, and temporary owned indexes survive durable catalog refresh without entering storage.

Initial conversion preserves compatible predecessor names, noncolliding OIDs and the nearest stored ancestor's existing expression namespace in each physical table. Ownership, ancestry and FK references are validated before writes; current incomplete graphs reject without repair. `sql_index_registry_version=1` records complete conversion alongside the earlier index-address marker. Previously virtual GIN/vector partition indexes are built within the existing initial-restoration transaction. Common record format 36 upgrades formats 1–35 and fences earlier readers/writers; native mapping 8 and catalog version 49 remain unchanged.

The pinned PostgreSQL 18.4 Docker reference confirms the asymmetric owned/independent attachment rule and cascading child-constraint removal (170 bytes, SHA-256 `8ba782ef391b4dbcce045af5c562191d8eb5626cd4012b4eef640d3e1aee3237`). A second reference confirms three independent parent-to-child index links, preservation of the other links when one owned parent is removed, and complete index removal after partition column rename/drop (172 bytes, SHA-256 `781f6a52325eb69c098ed9d8f7b296b7d5799b78d0c8ba6b99d105e7d9d7417f`). The recursive column requirement follows the [PostgreSQL 18 ALTER TABLE contract](https://www.postgresql.org/docs/18/sql-altertable.html).

Actual format-35 binaries from `6241a0c9d5ceba931392c5e7b46c75949de01128` pass all nine provider/file upgrade configurations, preserving original records, identifier allocations, histories and receipts and rejecting predecessor access. An actual predecessor Engine also creates the nested/multiple-index fixture for all three providers; the current Engine preserves index names/OIDs, expression conflict probes and FK targets, builds derived GIN/HNSW indexes, and passes constraint rename and reopen. Its compact reference is 437 bytes with SHA-256 `64956d47e1f7f096dfc97b2239c0af5ef9a71c7967a95e068b9c5a076d3176d1`. Generated reports remain in ignored output or temporary directories.

Final registry verification passes 1,197 distinct functional cases: all 704 SQL units, 13 selected execution cases, 100 Engine catalog/index/hierarchy cases, 358 public SQL/index/constraint/hierarchy/atomicity/manual-example cases, 16 SQLite format cases, five redb format units and one redb integration case covering predecessor formats 1–35. The Engine regressions cover native SQLite, SQLite Key/Value and redb, including separate equivalent parent indexes, constraint rename and undo, local physical search, temporary refresh, legacy conversion and malformed-current rejection. The public failures exposed missing deferred-writer admission before GIN physical changes; both text and vector publication now prepare the existing statement writer while preserving initial restoration's backend transaction. The corrected Engine and public suites pass in full. The manual harness compiles SQL fences and executes examples marked for execution. Strict SQL/execution/Engine/SQLite/redb all-target Clippy, formatting, exact-index dependency, Engine ownership, harness, header, file-size and repository hygiene checks pass. Earlier overlapping or failed runs are not added to this count. No dependency, Cargo feature or allocation ceiling changes, and no timing or allocation workload is run. Explicit index renaming, remaining catalog coordination and full concurrent-storage acceptance remain required.

## Independent index names

The index-rename unit adds `ALTER INDEX ... RENAME TO`, routes the `ALTER TABLE` alias through index binding, and gives indexes incarnation-based relation locks. Owned key names live in index registry format 2; table declarations retain key structure without duplicate mutable names. Execution restores names for both fresh tables and registry-only cache refreshes. Table deletion locks the retained index graph before dependency checks and publication. Engine supplies catalog, retained table, lock and transaction adapters.

The actual predecessor `3adbb56ccb020b52033dbe408fd0a8b1a6f5dc87` record-format probe passes all nine provider/file configurations for format 36→37, preserving records, receipts and identities while rejecting the old writer. The pinned PostgreSQL 18.4 Docker concurrency reference verifies six independent-rename, retained-constraint, replacement-relation and removal-wait outcomes; its temporary 321-byte result has SHA-256 `ee7d4d8593075772fc8c6fef8ae3cbeb3fd3d66ed0c9c43e51c7d1f401874449`. The actual predecessor Engine also upgrades its nested owned/expression/GIN/HNSW/FK catalog on all three providers and passes rename/reopen. SQL passes 705 cases, execution 20 and SQLite MVCC 31; the two targeted redb metadata cases and manual SQL harness also pass. Strict SQL/planner/execution/Engine/SQLite/redb all-target Clippy and dependency, ownership, harness, header, file-size and hygiene checks pass. Engine verification covers 368 distinct catalog cases: the broad run passes 362 before correction, a focused follow-up resolves four runtime failures, and all 24 final rename, registry-restoration, hierarchy and catalog-address cases pass after correcting the legacy fixtures and removing name-only physical hydration. The final cases include ON CONFLICT by a renamed quoted constraint before and after reopen. Eight redb MVCC integration cases pass, including upgrades from formats 1–36. Together with the SQL, execution, SQLite, redb metadata and manual suites, this records 1,135 distinct functional cases for implementation `df3d13ee610cc74607c8162c6e06af49be03b98a`; overlapping runs are counted once. Remaining relation-name reservation, other definition consumers and full concurrent-storage/platform acceptance stay open. No allocation or timing acceptance is inferred from these checks.


## Relation destination reservations

SQL owns relation-kind collision classification and ordinary rename preflight. Execution reserves a schema-qualified destination in the shared catalog lock lifecycle, refreshes committed catalog state, and distinguishes an already-visible duplicate from a competitor that committed after preflight. Table and index creation, key and partition index publication, sequence lifecycle, and regular/materialized view and foreign-table creation and rename share this reservation. Engine supplies the existing catalog and lock adapters for direct table APIs. Existing-view replacement still binds and locks its incarnation; creation no longer locks an absent relation placeholder before the catalog uniqueness check.

PostgreSQL 18.4 Docker image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636` confirms wait/commit/rollback outcomes across relation kinds, conditional creation and fixed snapshots. Compact temporary references are 1,091 bytes (`d47e91f741fcc459c418306f48749ee4cfccd38b63f858a4826799209051e3b2`), 1,771 bytes (`f975b157a63b66ab49e07deaad5c5c0e822548a0fb523871ec1bbf733b84d6a7`) and 900 bytes (`7696bbfafbc01c91e84be8e03df44544d0b0ff006d4ed605e7eab1462b69e497`). Raw outputs remain outside the repository. Verification passes 1,110 distinct functional cases: 705 SQL unit cases, 40 execution cases, 199 Engine catalog cases, 11 Engine view-lock cases and 155 public DDL/view/CTAS/namespace/manual cases. The manual harness compiles all SQL fences and executes the marked examples. New schedules exercise all three providers, commit/rollback/savepoint release, cross-kind and implicit-index collisions, conditional creation, fixed data snapshots, qualified names and separate temporary namespaces. Strict SQL/execution/Engine all-target Clippy, formatting, staged dependencies, Engine ownership, integration harness, file headers, file-size and repository hygiene checks pass. No allocation or timing workload is rerun and no acceptance limit changes.

## Constraint destination reservations

Constraint names are coordinated within the immutable owning relation rather than a schema-wide constraint-name lock. SQL exposes borrowed CHECK, NOT NULL, foreign-key and key catalog identities and excludes constraint-trigger names during automatic name generation. Execution reserves additions and renames through transaction/savepoint completion, includes constraint-trigger creation and rename, and preserves pre-existing duplicate diagnostics. Structural DDL candidates rebind key names after constraint, relation-name and catalog-address waits; index registry revalidation accepts a name-only change on the same untouched index while continuing to reject changed definitions and incarnations. Engine adds only existing-context adapters.

PostgreSQL 18.4 Docker image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636` verifies CHECK, NOT NULL and foreign-key addition/rename against owned-index rename in both orders; constraint-trigger creation/rename; committed and rolled-back competitors; independent index and owner names; automatic column names; and three-session preservation of independently renamed keys. Compact temporary references are 3,830 bytes (`4dc2900a780f05d03e72323e7f657f2c9bb24c00d045ebb78a39c09dc0fc56b7`), 1,743 bytes (`a419c88fa12c449a75a5255adab030acdda592bcbdbf643574637f9e0109162f`) and 893 bytes (`6d18596ac4c585cc6b220650a145f6c7045387354e9931859dcb86bb10885583`). New UNIQUE and PRIMARY KEY declarations reserve their index relation name first; the 489-byte reference has SHA-256 `a2c848937189ce4df24c1440cc59eaa9edb474b968738146d07c97663d586889`. Raw outputs remain outside the repository. Name-coordination verification passes 935 distinct cases: all 710 SQL units, 46 selected execution cases and 179 Engine catalog cases. The new schedules cover all three providers, commit/rollback/savepoint release, trigger collisions, independent key renames, fixed data snapshots and explicit ADD COLUMN key declarations. The public selection passes 401 of 402 cases, including the manual SQL harness; its legacy unqualified foreign-key restoration failure requires a separate initial-conversion correction. Strict SQL/execution/Engine all-target Clippy and repository ownership/dependency/harness/header/size/hygiene checks pass for the name-coordination implementation. Full concurrent-storage acceptance remains open. No allocation or timing workload is rerun and no acceptance limit changes.

Review also found two independent incomplete paths that remain required implementation work: the domain registry still publishes all domains through one metadata record, and implicit inherited NOT NULL names must preserve the parent name, and automatic constraint name selection must inspect the schema-wide constraint namespace. The independent-domain schedule reproduces a shared-metadata commit conflict and must pass after per-domain persistence and private-record merging are implemented. PostgreSQL references confirm parent/child NOT NULL name selection and distinct-column name races; `ChooseConstraintName` checks `conname` and `connamespace` when choosing an automatic name: 423 bytes (`d59ddece7e2f5526fb9bc67f2294907670896cdb899ea5edbef3d2db16ee4e96`). These failures are not accepted behavior and the PR remains unready for merge.

## Legacy foreign-key target conversion

Index restoration now canonicalizes legacy column, table and inherited foreign-key targets before assigning a referenced index incarnation. SQL resolves unqualified names against the complete stored table set independently of search path; execution prepares the full candidate and rejects ambiguous or dangling targets before writes. Converted references remain bound when another schema later receives the same local table name. The regression fixture now distinguishes an actual legacy declaration without an index binding from corrupted current metadata with index-owned key names. Current corruption is rejected without repair. Commit `693a102e` passes two focused SQL cases, three execution restoration cases and seven Engine restoration cases. Final public verification passes all 404 selected cases, including the three corrected legacy/current-format scenarios and the manual SQL harness, which compiles SQL fences and executes the marked examples. Strict SQL/execution/Engine all-target Clippy and repository policies pass.

## Independent domain records

Execution replaces the aggregate domain write with domain catalog format 2: one definition record and one OID claim per domain, plus an immutable format marker. Publication applies only changes from the statement's original registry to the current registry, preserving unrelated definitions committed during waits and rejecting a changed target. Current-catalog refresh overlays exact private replacements and deletions over the latest committed definitions while retaining the ordinary data snapshot. The SQL-aware encoding, validation, migration and merge behavior remain in execution; Engine's existing adapters are unchanged and storage's existing metadata interfaces suffice.

Initial restoration validates all definitions, names, identities, authority and OID claims before converting either legacy owner-name maps or format-1 authority aggregates. Failed later restoration rolls back the complete conversion; load-only paths never migrate. Common record format 38 atomically upgrades formats 1–37 and fences preceding writers that could replace the aggregate. Native mapping 8 and catalog version 49 are unchanged. Actual predecessor `bb2afae1268954be3b27cddcc5127227f8318e32` record binaries pass all nine provider/file upgrade configurations, preserving records, allocations and commit receipts while rejecting retained and reopened old writers. Actual predecessor Engine conversion also passes on native SQLite, SQLite Key/Value and redb, preserving domain identities and OIDs, nested types, defaults, constraints, bound owners, stored data and reopen behavior. Independent creation/deletion, exact private overlays, rollback and fixed-isolation data snapshots pass across all three providers. Domain verification passes 11 execution cases, nine Engine catalog/authority cases, 31 SQLite MVCC cases, 18 redb units and eight redb integration cases. The SQLite timeout-restoration assertion accidentally changed during the format bump was restored to its original 37 ms; its corrected focused run passes alongside the other 30 unchanged passing cases. The public domain oracle passes; the separate schema-cascade correction remains under verification. Strict SQL/execution/Engine/SQLite/redb all-target Clippy and ownership/dependency/harness/header/size/hygiene checks pass for the record-persistence unit. Same-type-name reservation, type and constraint address coordination, dependency lifetimes, automatic/inherited constraint naming and full concurrent-storage acceptance remain required; these results do not make the PR ready for merge. No allocation or timing workload is run and no acceptance limit changes.

SQL domain dependency analysis now distinguishes column-owned index key types from direct expression and predicate dependencies. Dropping a domain column follows the existing column lifecycle to remove its key constraints, owned indexes and referencing foreign keys; independent expression/predicate indexes retain their direct cascade. The three public schema-deletion oracle failures exposed this root classification error. Verification includes the unchanged PostgreSQL oracle plus PRIMARY KEY, UNIQUE, ordinary/expression/partial indexes, generated columns, views, savepoint undo and reopen across all three providers.

The pinned PostgreSQL 18.4 Docker reference verifies domain/schema deletion with owned and independent column indexes, expression/predicate indexes and rollback. The same reference distinguishes schema-wide automatic constraint-name selection from relation-local explicit duplicates, including domain CHECK names and inherited NOT NULL names. Its compact output is 266 bytes with SHA-256 `2702362cc263ba5f3bad513f537486314cc06aad7a5a311c0d10c2ccf9f0d353`; generated output remains temporary.

Final dependency-cascade verification passes all 715 SQL units, 11 execution domain cases, ten Engine domain/authority cases and 11 public domain/schema cases. The four DROP DOMAIN oracle cases also pass after the cascade correction. All three original schema-deletion oracle failures are resolved; the new cascade regression covers native SQLite, SQLite Key/Value and redb, both DROP DOMAIN and DROP SCHEMA, standalone owned-index rejection, surviving unrelated indexes/data, foreign keys, generated views, savepoint undo and reopen. Strict five-owner all-target Clippy and repository policies pass. No dependency, Cargo feature, durable format or allocation-limit changes are added by this cascade correction.

## Automatic constraint name scope

SQL separates explicit relation-local duplicate validation from schema-wide automatic selection. Execution collects ordinary/foreign-table constraints, constraint triggers and domain CHECK/NOT NULL names from the current catalog. Table and key creation, direct and recursive ALTER, partition key publication, domains and foreign tables consume this scope. Parent-selected names propagate through recursive declarations; direct child declarations retain their own local names. Engine adds only existing-context forwarding for domain and key binding. No Cargo dependency, feature, storage format or allocation budget changes are needed.

The pinned PostgreSQL 18.4 Docker reference confirms recursive ADD COLUMN, ADD NOT NULL and SET NOT NULL selection, ordinary and partition inheritance, schema-crossing automatic collisions, explicit duplicate names on separate owners and domain NOT NULL/CHECK selection. Additional compact references are 155 bytes (`1bc49861e37a4b8208be81d9140d1bfff04ee5483bc9dbfd19e890ae542b0084`) and 105 bytes (`b24531536142cf152a248b78459bd28acdba64b0026b29a9abb9e776f7b97a48`). The latter distinguishes pure inheritance from a child explicitly declaring NOT NULL or PRIMARY KEY.

The new schema-name regression exposed column lowering that retained only the final inline CHECK. SQL now preserves all declared checks and their attributes; execution applies each added-column CHECK through its existing independent inheritance lifecycle, including pre-existing child columns. Verification also exposed missing domain rows in `pg_constraint` and missing PostgreSQL byte-length clipping in non-key automatic names. Both are required follow-up fixes; the domain name-scope test directly verifies durable declaration names and reopen independently of catalog projection.

The additional PostgreSQL CHECK-attribute and name-clipping reference is 882 bytes with SHA-256 `c53689c4568d6618f7a5abd06a9e718aa0c8989eb92535baba8aa5f913e09a22`. It confirms independently retained CHECK enforcement/validation/inheritance flags, UTF-8 clipping with collision suffixes, and domain constraint catalog flags. Its raw output remains temporary.

Final declaration verification passes 718 SQL units, 25 execution catalog cases, seven focused Engine cases across native SQLite, SQLite Key/Value and redb, and 71 public DDL/domain/foreign-table/manual cases. The manual harness compiles SQL fences and executes marked examples. The earlier broad catalog run passed 396 cases and exposed the two column-CHECK loss failures plus the separate missing domain projection; the corrected declaration tests now pass, while domain `pg_constraint` rows remain a required next implementation. Strict SQL/execution/Engine all-target Clippy, formatting, dependencies, Engine ownership, harness inventory, headers, file-size and hygiene checks pass. No allocation or timing workload was run and no acceptance budget changed.

## Automatic object name clipping

SQL uses the existing index-name component balancing and UTF-8 clipping for CHECK, NOT NULL, foreign-key, key and domain names. Each collision suffix participates in the 63-byte limit before the varying components are clipped. Unnamed indexes use the same helper and retain quoted source names and existing expression/repeated-key labels without lossy normalization. Runtime behavior remains in SQL; Engine adds only provider and reopen regressions. No dependency, feature, durable format or allocation budget changes are required.

The pinned PostgreSQL 18.4 reference above establishes ASCII and UTF-8 constraint collisions. A second 210-byte reference verifies quoted/default index names and long index collisions (SHA-256 `fd86550f9b2e627aab2d956b652f962e1741dce2cd15daaaebf6ab138bac8ab3`). Raw outputs remain temporary. Final verification passes all 721 SQL units, nine focused Engine cases across all three providers and 55 public index/constraint/domain/foreign-table/manual cases. The manual harness compiles SQL fences and executes marked examples. Strict SQL/execution/Engine all-target Clippy, formatting, dependencies, Engine ownership, harness inventory, headers, file-size and hygiene checks pass. No allocation or timing workload was run and no acceptance budget changed.

## Shared type-name destinations

SQL creation checks distinguish the type namespace from relation names: domains, tables, views, materialized views and foreign tables share type names, while sequences and indexes remain independent. Execution reserves type destinations in catalog class 1247 through the existing transaction/savepoint lock and refresh path; row-bearing relation creation and rename reserve both type and relation destinations. Already visible types report `42710`, while a competitor committed during the destination wait reports `23505`. Engine lends current domain and relation guards so these checks do not reuse a query's retained catalog snapshot. Domain creation preserves its existing namespace-lock behavior. No dependency, feature, durable format or allocation budget changes are introduced.

The pinned PostgreSQL 18.4 Docker image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636` verifies 26 creation schedules, including both orders for every relation kind, and 16 rename schedules. Both references observe catalog waits and commit/rollback outcomes without timing acceptance. Their compact outputs are 1063 bytes (`71cd0c5abb7b9f0953f334742127444848a59eeeafebba721aaaa90cd95cdeba`) and 680 bytes (`0d25c72fab7012c74e6b6e1953fc42a1c0f673a1b524109415b5b34c18ee5e24`). A separate 84-byte reference confirms that domain creation does not retain the relation-creation namespace lock (`82f06c329937fd36b41fcfc649a0ea53c72541b2858ab5be808ca781d457197d`). Generated outputs remain temporary.

Final verification passes 723 SQL units, 35 execution namespace/domain cases, 33 Engine domain/schema/relation cases across all three providers and 62 public domain/view/foreign-table/schema/manual cases. The manual harness compiles SQL fences and executes marked examples. Strict SQL/execution/Engine all-target Clippy, formatting, dependencies, Engine ownership, harness inventory, headers, file-size and hygiene checks pass. No allocation or timing workload was run and no acceptance budget changed. Dynamic array-type names and addresses, domain constraint identities and catalog projection, and remaining type dependency consumers still require implementation; this destination reservation unit does not complete those catalog surfaces.

## Domain constraint catalog identities

SQL now materializes and validates a separate incarnation and public OID for every domain CHECK and NOT NULL constraint. Execution reuses the constraint-address reservation allocator, includes domains in global address claims, loads definitions before type binding and finalizes legacy addresses only after relation, event and index registries are restored. Engine forwards the retained catalog and restoration state. Current metadata rejects missing, invalid and duplicate identities without repair. Domain format 3 upgrades legacy owner maps, format-1 aggregates and format-2 independent records; later catalog restoration failures must roll back the conversion. Common record format 39 fences incompatible predecessors while retaining native mapping 8 and catalog version 49.

Actual predecessor `d64d7b1d374ecd8f4605cddb5d58a2cf0021a988` record binaries pass all nine provider/file upgrade configurations, preserving records, identifier allocations and commit receipts and rejecting retained/reopened prior writers. Engine files created by that predecessor reopen across native SQLite, SQLite Key/Value and redb, preserving domain and owner identities, nested types, defaults, enforcement and data while assigning stable individual constraint identities. Final verification passes 727 SQL units, 25 execution catalog/address cases, 25 Engine domain/address cases, 31 SQLite MVCC cases, 18 redb MVCC cases and 396 public SQL cases. The public manual harness compiles SQL fences and executes marked examples. Strict SQL/execution/Engine/SQLite/redb all-target Clippy, formatting, dependencies, Engine ownership, harness inventory, headers, file-size and hygiene checks pass. No allocation or timing workload was run and no acceptance limit changed. Domain `pg_constraint` projection, CHECK expression catalog representation, dynamic array-type names and type-address/dependency coordination remain required subsequent work.

The pinned PostgreSQL 18.4 reference additionally verifies domain and table CHECK trees, domain NOT NULL rows, nested-domain coercions, quoted identifiers, both deparser modes and information-schema constraint rows. Its 3315-byte output has SHA-256 `44766805a97b215913e439964f20dac3db4b5b230f3a30b35b2cfbb6f7690153` and remains temporary. Issue #119 tracks the remaining typed CHECK and catalog/deparser projection work independently of this PR; its stored constraint identity and publication requirements remain part of concurrent-storage correctness.

## Typed catalog expression representation

SQL owns a bounded PostgreSQL node-tree reader, typed CHECK expression encoder and SQL deparser under `catalog/node_tree`. Column and domain-value nodes retain their types, modifiers and ordinal/OID identities. Explicit and implicit casts use the existing SQL coercion catalog, including function, binary and input/output conversions and length/precision arguments. Existing operand signatures and cast compatibility rules are unchanged: 495 binary signatures and 235 casts gain PostgreSQL operator/function identities and seven supported unary-minus identities share their existing type resolver. Node reconstruction uses borrowed routine and type names without running user functions. Numeric Datums preserve base-10000 short/long headers, scale and special values; temporal Datums translate PostgreSQL's epoch through shared core values. The reader accepts both signed and unsigned C-char byte output, and the writer uses native C-char signedness.

The pinned PostgreSQL 18.4 image establishes 35 retained expected CHECK expressions covering explicit/implicit/modifier casts, nested domains, numeric finite/special values, nested boolean grouping, CASE, COALESCE/GREATEST, IN/ANY/ALL, DISTINCT, arrays, unary minus and temporal values. The compact expected fixture belongs to SQL's existing unit-test harness; raw Docker output stays temporary. Operator, cast and unary identity references contain 14986, 5608 and 106 bytes with SHA-256 `e9fc7688f43271fb245a7c812a2cd28a3756c26fb65a202164fe7c0284a2c15a`, `88e78b36e7cebd84f2e148158c6ea640adce41527000d91a4fc8aba8fe1c575e` and `a0cfd968186dcf8b2fc05a923bb51306b5f0d29d62eb8cd2df26d1c6a1e7eaa0`. Expression reference outputs contain 10268 and 12830 bytes with SHA-256 `59bc70cad6950f05cab5053b625dbd1c56ba8130fe52d1eabec559d149080129` and `113df000d4cbdd5ed602edb5de1cfa51ed16bb1e43031775b5b75bec90cb7e2f`. A 1351-byte multidimensional-array reference has SHA-256 `7d2866fcf0b547fe81fbd863c6b76a1cf46289129e1d33bcb9b9bb2b5af66f8f`.

This codec is a prerequisite for the pending projection work, not completion of domain `pg_constraint`, table/domain `conbin`, `pg_get_expr`, `pg_get_constraintdef` or information-schema constraint rows. Remaining expression forms and Datums, catalog-backed routine resolution, retained snapshot/name adapters and end-to-end provider/reopen tests must be completed before connecting the codec to catalog row construction. No dependency, feature, durable format or allocation acceptance budget changes are introduced.

A further 19-case PostgreSQL reference distinguishes NULLIF, row comparisons, subscripts/slices, symmetric BETWEEN, boolean tests, pattern/concatenation/arithmetic/JSON operators, SQL-syntax functions and array/bytea/UUID constants. Its 13667-byte temporary output has SHA-256 `1324ec3251ed3bea10d722d28603cc0ad51b5eff9d19cfcdc9659e89ad023167`. Inspection of `compiler/tree/expression_operators.rs` identified operators such as `%` losing their identity by becoming ordinary `mod` calls; the numeric operator unit below addresses that arithmetic path. Completing the codec therefore requires preserving those parser-owned operations through SQL AST/IR, binding and durable definitions, with any required provider-format fencing; guessing an operator from a function's display name would misrepresent explicit function calls and overload resolution. The integrated main change preserves `->`, `->>`, `#>` and `#>>` through `FunctionDispatch::JsonExtract`; their catalog codec representation still requires implementation. String, pattern and other JSON operators and the remaining catalog expression forms still require structural identities before projection is enabled.

Verification passes all 738 SQL unit cases, including the 35 PostgreSQL expression fixtures, bound-column qualifier checks, malformed Datum/tree rejection and unknown function-format rejection. Strict SQL all-target Clippy, formatting, workspace dependencies, Engine ownership, harness inventory, headers, file-size and repository hygiene checks pass. All 495 binary operator/function identities and 235 cast contexts/methods/function identities/arities match the pinned reference, while the existing signature ordering and coercion contexts remain unchanged. This unit does not repeat any allocation or timing workload or claim provider-level catalog projection acceptance.

## Numeric operator identity and typed execution

SQL preserves `%`, `^`, unary `+`, `|/`, `||/` and `@` through `FunctionDispatch::NumericOperator` in compiler AST, physical IR and stored definitions. Ordinary `mod`, `power`, `pow`, `sqrt`, `cbrt` and `abs` calls use the existing fixed-signature resolver with visible user routines, preserving search-path order, qualified calls and declared argument widths. Numeric built-in results enforce narrow integer and real return widths carried by those fixed signatures. Numeric syntax uses the shared operator candidate ranking and borrowed declared operand types, independent of function display names or user-function overloads. Generated-column, durable routine and physical-expression binding retain the selected operand signature, including routine and scalar-subquery return widths; preparation infers unknown parameters from operator signatures. Deferred operator errors retain their SQLSTATE, and function-name lookup does not override operator volatility or execution. Explicit `OPERATOR(pg_catalog.…)` syntax uses the same built-in identity. Scalar AST and execution IR invoke SQL's shared typed evaluator and existing arithmetic kernels. Catalog CHECK encoding retains PostgreSQL operator/function identities, and SQL reconstruction uses operator syntax.

The arithmetic kernels preserve PostgreSQL's zero remainder for signed integer minima divided by minus one, enforce int2/int4 absolute-value result bounds, and reject invalid floating power/square-root inputs and finite-input power overflow/underflow with the corresponding SQLSTATE. The pinned PostgreSQL 18.4 Docker reference supplies 20 numeric operator signatures, 23 additional CHECK trees (58 total retained cases) and 25 type/value/error outcomes. The new fixtures contain only expected results. The signature, CHECK and value reference outputs contain 608, 17472 and 2415 bytes with SHA-256 `1aca32e0c65b9962847085f149906cd89730b3675ce1a9af2d4a81e65a9bc74c`, `bac0bec9b715feaf09d97a8a2f517e29c9a4692bee56af6f07eb16bfdd46636d` and `66fb9918ffa5fd421feb065abe38946e99c629fa720150e4ec67bb1aa33a1adb`; raw reports remain temporary. An additional 220-byte reference verifies operator versus function output labels with SHA-256 `dc28e2f8a83c5954a43da6e96c7de787eb7a8a7e5f59aa01efce1c59c275fc74`; ordinary projections, stored-column rebinding and rule output analysis use the same SQL label policy. A 654-byte function-signature and search-path reference has SHA-256 `9ddfc154aa38ec49cfe945ab4d2d9f46322c30c056d151ace093d3b3aaee7941`. Common record format 40 fences both reopened and retained incompatible writers; native mapping 8, catalog version 49 and domain format 3 remain unchanged. Existing legacy expression records are retained, since their missing source distinction cannot be reconstructed by guessing from a function name.

Verification passes 744 SQL units, 14 execution scalar cases, 219 Engine generated-column and PostgreSQL 18 cases, 88 SQLite MVCC cases and 18 redb MVCC cases. The Engine scenarios cover prepared statements, generated columns, views and domains across native SQLite, SQLite Key/Value and redb with reopen. All nine actual format-39 predecessor record-upgrade configurations pass against `720652a57f06fd86c6283801b489cba3034a924d`, preserving records, identities, allocations and commit receipts while rejecting incompatible writers. Strict SQL/execution/Engine/SQLite/redb all-target Clippy, formatting and dependency/ownership/harness/header/size/hygiene checks pass. The same Engine run verifies that nullable routine lookup preserves PostgreSQL's hard qualification errors. No allocation/timing workloads or acceptance ceilings are changed. General catalog projection and nonnumeric expression compatibility are tracked separately in issues #117–#119 and are not merge gates for this PR.

## Catalog function dispatch identity

Execution's shared scoped evaluator preserves the selected routine before offering calls to catalog/session interceptors. Bound user routines and registered untyped callbacks retain their invocation path, structural operators retain their executor identity, and stored resolution errors reach the SQL error boundary. Bound ordinary built-ins dispatch by the binding's canonical name. Both row-lookup and physical-row evaluation use this policy; Engine adds only public integration coverage. No dependency, Cargo feature or durable format changes are required.

The pinned PostgreSQL 18.4 image verifies a user `pg_get_viewdef(oid)` selected before `pg_catalog`, qualified built-in selection, retained view binding after search-path changes, function rename, private rename and rollback. Its 128-byte temporary output has SHA-256 `ca856df46576b00b98060244c08e54c9172cd966cf3e9dbe885b5461d44636db`. New owner tests cover user bindings, callbacks, canonical built-in names, structural operators and `42883` propagation through both evaluation interfaces; public tests cover all three providers with reopen and explicit built-in calls alongside a registered callback.

Verification passes three execution owner tests, four focused public Engine cases and 363 final Engine regressions covering views, registered callbacks, numeric/generated expressions, JSON, PostgreSQL 18 behavior and the manual. The manual harness compiles SQL fences and executes marked examples. The same four public cases fail on the pre-fix binary with NULL instead of the selected routine or callback result. The integrated main tree also passes 744 SQL units, five SQL JSON integration cases and the PR policy workflow. Strict SQL/execution/Engine all-target Clippy, formatting and dependency/ownership/harness/header/size/hygiene checks pass. No allocation or timing workload is run and no acceptance limit changes.

The main integration introduces retained JSON extraction dispatch. Rendering and default labels are tracked separately in issue #117. Prior-writer compatibility remains a storage requirement: the earlier format-39 predecessor proof does not establish compatibility between different definitions written under format 40.

## MVCC writer compatibility after main integration

Common record format 41 fences reopened and retained development format-40 writers that predate the expression dispatch introduced from main. SQLite and redb atomically preserve record histories, database identities, allocations and receipts while updating the boundary. Native mapping 8, catalog version 49 and domain format 3 remain unchanged. No SQL rendering, operand typing or catalog-projection changes are included in this unit.

The unchanged provider implementation passes 88 SQLite MVCC cases, 18 redb MVCC cases and all nine actual predecessor provider/file configurations against `88ab4738f6aaa6694b822f05c662b944e80944de`. The predecessor checks cover native SQLite and SQLite Key/Value in plain, encrypted, compressed and combined modes, plus redb; retained and reopened incompatible callers are rejected while stored state survives. Strict five-owner all-target Clippy also passed before the unrelated SQL/execution changes were separated. Those independent provider and upgrade results remain applicable; they do not claim final-head Engine or full concurrency acceptance. No timing/allocation workload is repeated and no acceptance ceiling changes.

## Serializable dependency ownership

Common storage owns `mvcc::SerializableGraph`, whose participants use non-reused logical allocations within a database/coordinator incarnation and receive logical read/write dependencies. It distinguishes active, prepared, committed, aborted and doomed participants, applies commit-order and read-only snapshot rules, and preserves committed readers plus conflict-out summaries while overlapping transactions remain live. A prepared participant cannot be chosen as a serialization victim, remains retained across uncertain persistence outcomes and reserves ordering space for allocation-free terminal completion. All participant and edge buffers share an explicit retention budget; failed reverse-edge allocation cannot publish a one-sided dependency, and rollback/reclamation remain usable after cancellation. Deferrable readers classify their selected snapshot as pending, safe or unsafe instead of treating an unsafe snapshot as repairable by waiting.

SQL transaction and mutation adapters preserve the typed dependency failure as `40001`; an uncertain durable outcome still takes precedence as `08007`. A failed current observation publishes no dependency and permits statement/savepoint recovery; a peer already selected as a victim remains doomed through savepoint undo. PostgreSQL confirms this distinction: both writes report `40001`, but only the locally rejected write can commit after rollback to its savepoint. The two-case Docker output is 363 bytes with SHA-256 `b6d5be48e4fea7ac26fe5f5001afffc1c95ac6920d4984f3360976229509e70d`. This unit adds no SQL algorithms to Engine and does not enable complete SQL SSI. Execution still needs logical point/range/object observations, including absent results, while providers must coordinate registration, snapshot admission and durable publication across independent sessions and SQLite processes. Savepoint, nested execution, lifecycle cleanup, deferrable waiting and public serializable histories remain acceptance requirements before that integration is complete. Physical posting clusters and shared counters must not become SQL predicate conflicts.

The PostgreSQL 18.4 Docker reference verifies both write-skew commit orders under SERIALIZABLE and REPEATABLE READ, plus actual `SafeSnapshot` waits ending after commit and rollback. The six-case output is 798 bytes, SHA-256 `ec437228af09e7b5beeac8bd6258293433228433e7c5c561054373b6126d44a9`, from image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`. The [SSI algorithm description](https://raw.githubusercontent.com/postgres/postgres/REL_18_STABLE/src/backend/storage/lmgr/README-SSI) and [predicate conflict implementation](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/storage/lmgr/predicate.c) define the ordering and prepared-transaction rules. Generated output remains temporary; no allocation/timing workload or ceiling changes are involved. All 25 SSI owner tests pass, including the two local-error/savepoint cases and an independent transitive-closure check over 384 complete three-transaction graph/commit orders and 4,374 histories with delayed read/write observations. The final storage MVCC selection passes 37 tests; the unchanged Engine and execution adapters pass 11 and five respectively. Strict three-owner all-target Clippy, formatting, dependency and Engine ownership checks, harness registration, Rust headers, file-size limits and diff hygiene pass.

Common storage also retains typed logical object, row and index-key observations. Reads check existing active/prepared/committed write intents; writes check retained reads, including absent point/range results. Object and index incarnations prevent name/OID reuse from aliasing predicates. Range bounds use execution-supplied ordered keys, with inclusive/exclusive endpoints and empty intervals. Registration validates and reserves its entire match set before publishing observations, edges or peer victims. Duplicate observations retain no extra key copies; all predicate keys and match candidates share the graph budget. Statement/savepoint write marks remove subsequent intents while keeping prior reads, dependencies and peer-victim state. Finished readers and writers retain their observations until overlapping snapshots release them. This unit does not yet connect SQL access paths or provider/process admission. Its final owner run passes all 38 SSI cases within 50 storage MVCC tests, including the empty-byte-interval regression. Strict storage all-target Clippy, formatting, staged dependency policy, harness registration, headers, file-size limits and hygiene checks pass. No dependencies, Cargo features, physical formats or allocation limits change.

Serializable admission now allocates a dedicated `SerializableTransactionId` independently of physical commit receipts; read-only/no-op participants need no durable transaction allocation. Reclamation preserves the allocation watermark, and coordinator incarnations reject old participants/savepoints after replacement. A publishing participant seals its separately allocated `StorageTransactionId` and fingerprint exactly once. Pending or unknown receipt status cannot finish the participant; matching confirmed outcomes finish without allocation or cancellation and retain terminal precedence. Admission reconciliation resolves lost commit replies before the next data snapshot, queries the bound physical receipt IDs, and refuses missing authoritative receipts without assuming abort. Providers must retain prepared associations before physical publication and resolved state before releasing shared admission; this common boundary still needs the actual provider/process transport and SQL lifecycle adapters. The final storage MVCC selection passes all 62 tests, including 50 SSI cases. The changed Engine and execution error adapters each pass their focused test, including distinct logical versus physical IDs and `08007` precedence. Strict three-owner all-target Clippy, formatting, dependency/ownership policy, harness registration, headers, file-size limits and hygiene checks pass. No provider format, dependency, feature or allocation limit changes.

The common graph now streams a versioned, checksummed checkpoint containing participant identities, allocation/order watermarks, dependencies, retained summaries, predicates and physical receipt bindings. Decoding charges every participant, edge and predicate key to the supplied allowance, checks identities and references, and returns no partial graph after corruption, cancellation or exhaustion. SQLite's `serializable_admission` holds a short auxiliary `BEGIN IMMEDIATE` across loading and atomically replacing this checkpoint. Independent file pools and native processes share that admission through SQLite; in-memory sessions retain one auxiliary pool with the main owner. The `.uqa-serializable` file inherits SQLCipher credentials, including for compressed encrypted main files, and uses FULL synchronization with DELETE journaling. Existing format/schema mismatches and missing retained rows fail without recreation. Checkpoints stream through incremental BLOB I/O, with no additional encoded-state copy or growing diagnostic journal.

This is the provider's atomic state transport; the participant lifetime and recovery unit below adds its retained-owner protocol. Efficient incremental observation persistence, redb coordination, main-writer format fencing, the common session/SQL lifecycle and access-path integration remain necessary before enabling public SQL SSI. Callers must persist prepared receipt associations before main publication, hold shared admission during publication and reconcile authoritative receipts before capturing another data snapshot. Losing a process or a checkpoint reply never proves that its prepared main transaction aborted. The provider currently loads and replaces the retained graph per admission; its memory use is bounded, but no throughput or complete public serializable-history acceptance is claimed.

The independent-opener schedule exposed a compressed-container initialization defect: two handles opened before the first durable write each chose a private file identity and encryption salt, so the later reader rejected the first publisher with `SQLITE_IOERR_LOCK`. A handle that has never observed or published durable state now authenticates and adopts the first committed container using its original credential under the existing main-file read lock. It then drops that temporary credential. Already loaded headers, including generation-zero empty headers, retain their identity checks; incorrect credentials and encryption-mode changes remain errors without partial adoption. This fixes initial concurrent openers without changing the container format or bypassing authentication.

The transport owner runs pass 71 common storage MVCC tests, including 59 SSI cases; nine SQLite SSI transport tests; and 37 compressed VFS regressions, including three new initialization cases. The SQLite histories cover independent memory/file owners, all four native file modes, concurrent first admission, rolled-back initialization, invalid schemas, cancellation, decode exhaustion, inherited encryption, authoritative main-receipt reconciliation and actual process death after flushing uncommitted auxiliary pages. Formatting, dependency/Engine ownership, harness registration, Rust headers, file-size limits and repository hygiene pass. Generated output remains temporary; no new benchmark report, dependency, Cargo feature, main record format or allocation ceiling is introduced.

## Serializable participant lifetime and recovery

Common storage retains a cloneable participant handle whose final release drops its provider lease without acquiring admission or physical persistence. Nested execution and pinned views must keep a clone of their original participant. Lease-bound admission aborts its newly allocated actor on lease failure; recovery retires only actors whose retained owner is authoritatively gone. A prepared publisher still resolves its exact physical receipt: committed data remains committed, a pending attempt requires provider-confirmed abort, and a missing outcome blocks admission without discarding earlier confirmed resolutions. Checkpoint version 2 records the lease binding; version 1 remains readable with its actors manually owned, so an old checkpoint never invents evidence that a participant died. The local registry supports in-memory, browser and exclusively owned providers and returns abandoned metadata capacity through explicit reclamation.

SQLite's `admit_serializable` and `recover_serializable` perform receipt reconciliation and orphan recovery under auxiliary admission, persisting earlier confirmed outcomes even if a later operation fails. Snapshot capture executes once; failed capture consumes its retained participant allocation and releases the lease for later recovery. Native participant and snapshot leases share one descriptor/byte-lock implementation with separate files and namespace headers. Participant slots contain only allocation tags within the database/coordinator incarnation, so slot reuse cannot revive an older actor. Namespace replacement cannot discard live leases; last-handle release and process death free only the matching ownership. The original snapshot file format is unchanged. The pool retains memory-owner leases, browser file owners share a local registry, and an empty registry does not retain a failed caller's allowance. Generated participant state, rollback journals and lease files are ignored by Git.

Verification passes 181 distinct MVCC owner cases: 77 common storage and 104 SQLite, including all 13 new lifetime/recovery cases, existing snapshot process reclamation and the closed-file upgrade matrix. After the private ownership type and participant codec refactoring, the final SSI selection passes all 65 common and 14 SQLite cases again. The killed-process schedule covers plain, encrypted, compressed and combined files, preserving a live peer and committed data while aborting the dead owner's pending physical write. Strict common storage/SQLite all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness registration, headers, file-size limits and hygiene pass. Both libraries compile for Emscripten; this is compile-only browser evidence. No timing/allocation workload or acceptance ceiling changes are involved. Public SQL lifecycle/access-path integration, redb coordination, main-writer fencing and full platform acceptance remain required.

## Common serializable admission and redb coordination

Common storage exposes an object-safe `SerializableCoordinator` capability through the physical persistence interface. Snapshot admission and abandoned-participant recovery use the same exact-receipt algorithm for both providers, with one invocation and retained outcomes even when a later operation fails. SQLite's existing capture/recovery entry points delegate to it; its raw transactional admission remains available for transport-level work. Common `LocalSerializableState` owns cancellable admission and lease lifetime for exclusive providers. Participant payloads retain both this state and the physical owner, with their complete payload charged before allocation. Every admitted operation also retains the physical owner separately, preventing native final-close work while its gate is held. This preserves redb's ordinary final close without adding a background destructor or replaying transaction work.

redb shares that local state across every record adapter over one exclusive database owner and persists the common checkpoint in `uqa_mvcc_serializable`. A versioned metadata marker and singleton checkpoint publish atomically with immediate durability. Decoding borrows redb's existing value and charges the restored graph to the current caller's allowance; encoding charges redb 4.1's temporary reserved-value allocation before streaming into the mutable page. No physical read or writer spans participant recovery, snapshot capture or the admitted publication operation. Missing rows, missing markers, malformed formats, wrong identities and checksum corruption fail without resetting the coordinator. Clean reopen, process death and uncertain checkpoint completion resolve original physical receipts and retain the logical allocation watermark. Neither a first caller's small allowance nor adapter churn permanently replaces the coordinator.

This completes the shared low-level provider boundary, not public SQL serializable execution. Common session/SQL lifecycle, logical access-path observations, original attribution through nested/pinned views, savepoint/error completion, deferrable waits, main-writer fencing, efficient observation persistence and final recovery/platform acceptance remain required. No Cargo dependency, feature, existing main record format or reviewed allocation ceiling changes.

Verification passes all 93 selected SSI cases: 68 common storage, 11 redb and 14 SQLite. The 14 new common/redb cases include typed cancellation during admission, final-owner lifetime, independent write skew, failed snapshot capture, decode/encoding exhaustion, clean reopen, seven retained-state corruption variants, uncertain checkpoint synchronization after a successful main commit and an actual killed database owner. SQLite retains its file-mode, memory, encryption, independent-process and abandoned-participant coverage through the common interface. Strict three-owner all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness registration, headers, file-size limits and hygiene pass. No timing/allocation workload is run; earlier platform evidence remains separate from this native verification.

## Retained logical completion

Session lifecycle review found that conflict-history reclamation could erase a completed participant while its original lease remained alive. A lost completion response could therefore leave a read-only or no-op transaction unable to inspect its logical result, despite requiring no physical write allocation. Common storage now retains terminal participant metadata through the original handle and every clone, exposes its logical status, and releases that retention only after authoritative lease loss. Completed actors require no physical abort or receipt lookup during that release.

Conflict-history retention remains independent: predicates and both edge indexes obey the existing overlap horizon, including when completed participant metadata remains retained. Ended metadata does not create dependencies or delay later safe snapshots. The existing checkpoint lease flag represents retained outcome ownership; terminal lease release clears that binding without changing confirmed results, database/coordinator identity, allocation watermarks or the encoding version. The common provider recovery helper persists earlier completed cleanup when a later liveness probe fails. Common session/SQL lifecycle integration and full PR acceptance remain required.

Verification passes all 99 selected SSI cases: 72 common storage, 12 redb and 15 SQLite. Six new regressions cover read-only completion without physical allocation, nested handle and checkpoint retention, terminal publication precedence, obsolete conflict-history reclamation, partial liveness failure and independent provider recovery. SQLite covers memory, plain, encrypted, compressed and combined modes. Strict three-owner all-target Clippy, formatting, exact-index dependency policy, Engine ownership, harness registration, headers, file-size limits and hygiene pass. No timing/allocation workload, Cargo dependency/feature, checkpoint/main format or allocation ceiling changes are involved.

## Common session serializable lifecycle

Common `VersionedKeyValueStore` now establishes one original participant and fixed record snapshot before private writes. `SerializableReadContext` retains attribution through nested record views and command refresh. Existing savepoints inherit that first boundary; subsequent savepoints retain logical write marks, and undo removes later write intents without dropping reads or victim state. Session observations accept logical predicates explicitly and do not infer them from physical posting/counter records. Every session observation and completion reconciles exact retained publications before using the graph.

Common session commit freezes the existing evaluated batch, persists the SSI-to-physical binding before any main publication, and reacquires shared admission for physical commit and resolution. Ordinary and serializable sessions share the physical attempt/receipt validator and typed derived-effect retry path. Empty or read-only transactions allocate no physical receipt. Lost logical replies retain the original participant identity; known main receipts remain authoritative after later auxiliary failure. Engine transaction frames, scoped callbacks and statement cleanup accept either completion identity while its algorithms remain in storage. `pending_transaction_completion` covers both; the existing `pending_commit` keeps its physical-only return contract.

This connects the common session and Engine completion boundary without enabling public SQL SSI. First-applicable-statement admission, original fixed-snapshot attachment to internal Engine readers, provider capability forwarding, all logical access-path observations, deferrable waits, efficient observation persistence and main-writer fencing still require integration. No format, Cargo dependency/feature or reviewed allocation ceiling changes are included.

Verification passes 155 selected regressions: 140 common storage session/MVCC cases and 15 Engine completion cases. Twelve new common cases cover original attribution, fixed snapshots, logical write skew, savepoint undo, cancellation, absent physical allocations, ordered preparation, lost completion replies and authoritative physical outcomes; two new Engine cases preserve pending logical completion and scoped callbacks across plain, encrypted, compressed and combined SQLite files plus redb. These Engine cases explicitly attach the common session participant and do not establish automatic SQL SSI. Strict common storage/Engine all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness registration, headers, file-size limits and hygiene pass. No timing/allocation workload is run.

## Exact retained read attachment

Common storage can attach an existing committed/private record view to an independent read-only session. Captured private values and tombstones retain their original status and revision preconditions; they are never relabelled as committed records. Source refresh, savepoint undo, completion and closure cannot advance the reader. It keeps the original SSI read context while its local transaction completion owns no original participant or physical publication. Mutation and autonomous identifier paths reject the read-only owner before evaluating work.

The Key/Value backend and native SQLite forward this capability through paired catalog/data handles; redb uses the common session directly. Engine restores fixed data snapshots and pinned graph readers from that already attached view, outside shared SSI admission. Referential checks that explicitly require current committed data retain their independent read factory. This closes the interval in which an independent reader could otherwise select a later commit than the source transaction. SQL first-statement admission, provider SSI capability forwarding and all logical observations remain required. No dependency, feature, stored format or allocation ceiling changes are included.

Verification passes 216 selected regressions: 144 common storage session/MVCC cases, two Engine internal-reader cases and 70 public Engine isolation, cursor and commit-resolution cases. Four new common cases verify private/tombstone visibility, source and nested-reader lifetime, cancellation, read-only publication rejection, paired affinity and original SSI completion ownership. The new deterministic Engine schedule compares an original fixed reader with a current reader after an intervening commit and preserves the original data/catalog view after source closure across native SQLite, SQLite Key/Value and redb. Existing checks retain maintenance-client suppression, relation lifetimes, catalog refresh, savepoints, cursor indexes and lost completion replies. Strict four-owner all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness registration, headers, file-size limits and hygiene pass. No timing/allocation workload is run; public SQL SSI and remaining platform acceptance are still incomplete.

## Serializable session capabilities

Common storage exposes participant admission, original retained read attribution and logical write intent through `SerializableSession`. Native SQLite and SQLite Key/Value forward through the bound logical connection, preserving cleanup errors and connection gating; redb uses the common implementation. Retained readers cannot admit or publish as the source participant. Observation controls share the original session's allowance and the invoking reader's cancellation. This exposes the existing common lifecycle without automatically admitting public SQL transactions.

All 145 common session/MVCC regressions pass, including the new backend-capability schedule that preserves attribution across retained-reader completion and source reuse, checks the shared allowance, rejects retained writes/admission and preserves independent cleanup after reader cancellation.

## SQL sequential reads and row publication

Execution retains the original SSI participant, immutable relation identity, shared allowance and invoking cancellation in `SerializableRelationRead`. Local and scored sequential sources, including shared projections and borrowed aggregates, observe their relation only when rows are actually requested, including an empty scan. Operator execution observes actual relation enumeration after an index path declines the query. Selected tuple reads and pinned rechecks observe row identities before cached or stored tuple access. Prepared INSERT, DELETE, rewrite, primary-key/partition movement and direct point UPDATE record row intent before storage mutation. Savepoint and statement ownership remain with the existing common lifecycle: undo removes later write intents while retaining reads and dependencies already observed before the undo. Engine only supplies state and retained identities; shared transaction/resource SQLSTATE conversion lives in execution.

The provider SQL schedules explicitly admit participants through the new capability. Automatic first-applicable-statement admission, SQL read-only/deferrable admission, remaining index domains and other logical paths, efficient persistence and main-writer fencing remain required before public SQL SSI acceptance. Temporary relations do not participate in shared observations. No dependency, feature, stored format or reviewed allocation ceiling changes are included.

Verification passes 224 distinct selected regressions: 145 common storage session/MVCC cases, 70 existing Engine isolation/cursor/commit-resolution cases, six new admitted-participant SQL schedules, two retained-reader unit cases and the execution diagnostic case. The six schedules cover INSERT, DELETE, point patch, full rewrite and primary-key movement; empty scans, COUNT/SUM and joined sources; unused LIMIT 0 sources, independent commits and the distinction between removed savepoint write intents and retained prior dependencies. They run across native SQLite, SQLite Key/Value and redb and verify transaction cleanup and resulting values. Strict five-owner all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness registration, headers, file-size limits and hygiene pass. No timing or allocation workload is run.

Value-index predicate eligibility, NULL handling, cardinality and evaluated-key maintenance now live in execution, with their three pure regressions. Engine retains cache generations, hydration/publication lifetimes and provider handles, borrowing stored keys through the native index interface. This establishes the ownership boundary for precise index observations without changing index eligibility, physical storage, dependencies or Cargo features. General heterogeneous numeric comparison is tracked separately in [issue #120](https://github.com/cognica-io/uqa-engine/issues/120); index predicates must be bound to their selected comparison domain rather than assume a universal mixed-value byte order.

Execution supplies ordered logical scalar keys and predicate boundaries for Boolean, integer, float, decimal, text, fixed-character, bytes and JSON-text index domains. Same-type fixed-width boundaries are direct; mixed numeric bounds are projected with the selected index's existing comparator, preserving fractional and large-value boundaries. NULL has its own address, decimal display scale and signed floating zero do not split equal keys, and byte escaping preserves prefixes and embedded zeros. Interval construction retains empty-result predicates for future writes, shares the original allowance, and propagates cancellation and resource SQLSTATEs. The column integration below binds these domains to actual reads and writes; remaining key shapes and logical access paths are still required before automatic admission.

The index ownership change passes all three execution value-index cases and five Engine cache/hydration/repair cases. Strict execution/Engine all-target Clippy, formatting, exact-index dependency and ownership checks, harness coverage, headers, file-size limits and hygiene pass. No timing or allocation workload is run.

The scalar codec passes seven owner tests against the actual value index, covering empty-result future writes, fractional and large mixed numeric bounds, exact decimal order, NULL, signed zero, NaN, string prefixes and embedded zeros, padding, cancellation and shared allocation failure. Randomized integer/float comparisons run as correctness tests without timing thresholds. Strict execution all-target Clippy, formatting, exact-index dependencies, Engine ownership and repository policy checks pass.

Storage exposes a per-document evaluated-key read for cold index caches, with separate unbuilt, absent-row and present-value results. Common Key/Value and native SQLite use existing record identities, preserving named-expression and column namespaces. Legacy SQLite reads the definition and entry in one statement. This avoids full-index materialization and old-expression reevaluation; it does not itself register SQL observations or enable automatic SSI admission.

Four point-read regressions pass: two common storage cases, one legacy SQLite unit case and the augmented native snapshot integration case. They preserve NULL and physical namespaces, reject selected corruption while avoiding unrelated row decoding, and retain old entries across replacement, deletion and index removal. Strict common storage/SQLite all-target Clippy, formatting, exact-index dependencies, ownership and repository policies pass.

Execution now binds selected scalar column predicates to immutable column identities and the original participant, recording ranges before hot, cold, empty or index-only results. Unique-key point UPDATE records equality before lookup, including an absent row. Original and replacement column keys are observed before private row/index publication, reusing evaluated replacement keys and cached/provider old entries. A missing durable column entry follows the same stored-column projection as query repair. Engine retains only metadata, cache guards and provider handles; field eligibility, key binding and observations stay in execution. Statement/savepoint ownership remains in common storage. Automatic SQL admission and remaining key domains/access paths remain incomplete.

This integration passes all eleven selected Engine schedules across native SQLite, SQLite Key/Value and redb, plus the execution callback unit. The five new provider schedules cover matching and nonmatching writes into empty SQL ranges; hot/cold index-only reads followed by deletion, patch, rewrite and primary-key movement; savepoint intent removal; absent primary/unique-key UPDATE; and missing durable postings repaired only in memory. The callback unit verifies observation before both empty and populated results, error propagation and no observation for a declined predicate. General TIME/TIMETZ ordering discovered during the remaining-domain review is tracked independently in [issue #121](https://github.com/cognica-io/uqa-engine/issues/121), without changing Core temporal semantics in this PR. Strict execution/Engine all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness registration, headers, file-size limits and hygiene checks pass. No timing or allocation workload is run.

## Temporal index observations

Core exposes its native temporal comparison key through an allocation-free, fallible sink. Execution binds the declared DATE, TIME, TIMETZ, TIMESTAMP, TIMESTAMPTZ and INTERVAL families, including precision declarations and domain bases, and encodes keys under the original SSI allowance. Text predicates accepted by an empty temporal index retain the declared family's parsed bounds, so later temporal writes conflict with their future matching range even when actual filtering subsequently declines native acceleration. Native parsing workspace is reserved before allocation and checked for cancellation before and after parsing. The original read/write hooks cover NULL predicates and absent unique-key UPDATE without changing index eligibility or general temporal comparison semantics; issue #121 remains separate. Automatic SQL admission and the remaining key families/access paths are still required.

The temporal integration passes 24 selected tests: one Core comparator/sink case, nine execution key cases and fourteen Engine schedules across native SQLite, SQLite Key/Value and redb. The three new provider cases distinguish matching and nonmatching future writes through NULL predicates, empty text ranges and absent unique-key UPDATE for all six temporal families, including equivalent TIMESTAMPTZ and INTERVAL values. These tests explicitly admit SSI participants; they do not establish automatic public SQL admission. Strict Core/execution/Engine all-target Clippy, formatting, Engine ownership, harness registration, headers, file-size limits and hygiene checks pass. The remaining-domain audit identified a separate native JSONB zero/fraction comparison error, recorded from source inspection in [issue #122](https://github.com/cognica-io/uqa-engine/issues/122) without changing that comparator or making its correction a merge gate. No timing or allocation workload is run.

## JSONB index observations

Core supplies an ordered JSONB comparison key using its existing parser, normalized number representation and structural comparator rules. Native object parsing now uses a contiguous buffer sorted by key and source position, preserving last-field-wins semantics and the existing equality/hash encoding while allowing every owned buffer to share the original SSI allowance. Decoding scratch, number digits, object-order references and output are also reserved under that allowance, with cancellation propagated through parsing and encoding. Execution binds JSONB and its domains through the existing immutable column identities, precise predicate bounds and original/replacement mutation hooks; Engine gains only provider integration tests. General JSONB comparison correction remains in issue #122. Automatic SQL admission and remaining container/access-path integration stay open.

Verification passes 143 distinct tests: all 112 Core unit cases, eleven execution index-key cases and twenty Engine cases. The Engine selection contains seventeen admitted-participant schedules across native SQLite, SQLite Key/Value and redb and three existing JSONB comparison, DISTINCT and exact-numeric consumers. The three new provider cases cover matching/nonmatching structural ranges and SQL NULL versus JSON null, hot/cold original keys through deletion and point patch, and absent unique UPDATE with equivalent/different values. Core checks native ordering, unchanged equality keys, duplicate-field resolution, shared parsing/output allowance, cancellation, invalid representations and failure cleanup; execution checks actual index-selected predicates and typed diagnostics. Strict Core/execution/Engine all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness registration, headers, file-size limits and hygiene checks pass. No timing or allocation workload is run, and no dependency, feature, durable format or reviewed allocation ceiling changes.

## Container index observations

Execution binds typed SQL arrays, INT2VECTOR, OIDVECTOR, VECTOR and TENSOR to the existing immutable column identities and read/write observation hooks. Array keys preserve row-major element ordering, NULL-high elements, dimensionality and lower bounds; vector/tensor keys preserve nested list boundaries. Mixed numeric predicate leaves use the selected scalar domain's native comparisons; the first leaf without an equal stored representation defines a cut for every remaining suffix. Equal floating zero ranks are canonicalized before suffixes are encoded. Native temporal/text comparison remains distinct from scalar text-bound parsing. Core supplies one shared array traversal for its comparator and the bounded cursor; its stack, temporary scalar keys and final output share the original allowance and cancellation. Legacy-vector keys preserve both existing native Array and List carriers, including domain inputs; normalization of those SQL carriers is tracked independently in [issue #123](https://github.com/cognica-io/uqa-engine/issues/123). Engine changes are confined to provider tests and shared test helpers. Automatic SQL admission, other logical access paths, deferrable waits and the broader transaction acceptance matrix remain open.

Focused verification passes 154 distinct tests: all 114 Core unit cases, eighteen execution index-key cases and twenty-two Engine cases. Engine coverage contains twenty admitted-participant schedules across native SQLite, SQLite Key/Value and redb, plus two existing array ordering/bounds consumers. The three container provider cases cover matching/nonmatching future writes, SQL NULL versus NULL elements, dimensional lower bounds, both legacy-vector domain input carriers, cold/cached DELETE and point patch, and absent domain-array unique UPDATE. Seven execution cases compare container keys and ranges with actual index selection, including mixed numeric boundaries, signed zero, nested NaN, temporal/text variants, legacy carriers and resource diagnostics. Two Core cases verify shared traversal order, empty dimensions, depth accounting and cancellation. Engine evidence combines nineteen unchanged cases with the three container cases rerun after the final key changes. Strict Core/execution/Engine all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness registration, headers, file-size limits and hygiene checks pass. No timing or allocation workload is run, and no dependency, feature, durable format or reviewed allocation ceiling changes.

## Vector candidate observations

The access-path audit confirms that named expression keys currently serve generic constraint probes; query `IndexScan` uses the leading-column accelerator. This work therefore does not introduce a general expression-index query feature. Actual KNN, calibrated-vector and threshold reads now register the selected canonical candidate field through execution. Observations use immutable relation/declared-column identities, with a separate name encoding for registered dynamic fields, including fields registered alongside declared columns. Full candidate ranges preserve empty-result phantoms independently of physical IVF/HNSW structures. Retained index readers keep the original participant and allowance; metadata probes and unused/zero-K sources do not observe a query. Single-table retrieval planning now returns an unevaluated entries producer; mixed predicates and function leaves use the same retained execution boundary. Single-table and hierarchy sources share a deferred scan that binds its schema and handles up front, executes once when rows are requested, and drops unused or failed work without replay. A zero-row LIMIT does not open blocking children such as a sort. This avoids false candidate observations for unused ordinary, aliased, ordered and mixed retrievals. Global hierarchy ranking, calibration, score-boundary ties and independently current tuple rechecks keep their existing algorithms.

Canonical replacement and deletion record vector document intent before publication. Common and native providers expose point membership at the selected boundary so absent deletions and NULL candidates do not become false writes. Memory, exact Key/Value, IVF, HNSW and retained readers implement the same interface. The shared SSI checkpoint adds vector point/range tags while retaining existing row and ordered-index encodings. Engine supplies only handles, metadata and session state. Automatic SQL admission, text/graph/catalog observation, deferrable waits and the broader transaction acceptance matrix remain open.

Focused verification passes 131 distinct cases: 38 common-storage/provider cases, nine execution cases and 84 Engine cases. The Engine selection includes all 25 admitted-participant schedules across native SQLite, SQLite Key/Value and redb, plus 59 existing tensor, calibration, hierarchy, row-lock recheck, text, fusion, subquery, facet and score-ordering consumers. Five vector schedules cover empty exact/IVF/HNSW candidates, replacement/deletion/NULL handling, unrelated fields, zero-K and ordinary/ordered/aliased/mixed LIMIT 0, dynamic registration on both declared and schema-less tables, and savepoint intent/edge retention. Execution verifies original identity/budget/cancellation, deferred schema/score/recheck preservation, failure without replay, score-boundary ties and zero-limit blocking-child behavior. Common and provider cases cover checkpoint handoff and canonical membership through private writes, rollback, savepoints and retained snapshots. Strict five-owner all-target Clippy, formatting, exact-index dependency policy, Engine ownership, harness registration, headers, file-size limits and hygiene checks pass. No timing or allocation workload is run; dependencies, Cargo features and reviewed allocation ceilings are unchanged. The shared SSI checkpoint adds vector observation tags; canonical row and physical vector formats are unchanged. Public SQL participant admission and the remaining transaction acceptance work are still required.

## Evaluated text mutation changes

Text observations require the analyzed terms actually used by the index, including the persisted terms being removed. Common storage now exposes a provisional visitor over the original and replacement term postings, document/field metadata and semantic field-statistic changes. Memory, common Key/Value and both native and legacy SQLite reuse their original staging and atomic publication paths. Same-length replacements do not report a statistics change from a physical counter rewrite, while zero-token field insertion/deletion still changes field membership. Missing-document deletion reports no logical text intent. Cancellation and resource errors from capture abort publication and retain their typed provider diagnostics.

The visitor cannot reenter the provider's mutation scope. Execution must collect bounded logical keys there, discard them if mutation fails, and register successful intents after the private mutation returns but before the statement completes, under the same statement/savepoint undo. SQL text read/write binding and the remaining automatic serializable lifecycle are still required; this storage interface does not enable them. Dependencies, Cargo features, canonical formats and reviewed allocation ceilings are unchanged.

Fixed snapshots in the unbound legacy SQLite direct-provider API are tracked separately in [issue #124](https://github.com/cognica-io/uqa-engine/issues/124). Its existing live-connection clone is outside this change-capture contract and the bound MVCC provider path. The existing committed/private snapshot conformance and native SQLite snapshot schedules continue to verify the concurrent providers independently.

Focused verification passes 75 cases: 29 common-storage unit cases, 25 SQLite unit cases, 20 native SQLite/SQLite Key/Value integration cases and one redb occurrence schedule. Shared change-capture assertions run on Memory, Memory Key/Value, native SQLite, legacy SQLite, SQLite Key/Value and redb. They verify original/replacement terms, unrelated documents sharing a posting cluster, same-length statistics stability, zero-token membership, absent deletion and atomic rejection with typed cancellation/memory errors when a later document fails capture. Existing occurrence schedules retain committed/private snapshots, savepoints, independent publication, source fencing and reopen, including every SQLite storage mode. Strict three-owner all-target Clippy, formatting, dependency policy, Engine ownership, harness registration, headers, file-size limits and hygiene pass. No timing or allocation workload is run; dependencies, Cargo features, canonical formats and reviewed allocation ceilings are unchanged. The text access-path integration below connects this storage capability to admitted transaction participants.


## Internal read workers and statement ownership

Fixed-transaction catalog refresh can run inside parallel retrieval branches. The parent SQL statement owns its session gate while waiting for those branches, so an internal reader must not reacquire that thread-owned gate. Public sibling creation now owns the gate explicitly and retains its committed-catalog sharing path. Internal readers restore an independent durable or retained catalog without inspecting the parent's transaction stack or temporary namespace; shared runtime extensions, database owners, epochs and maintenance-client behavior retain their existing contracts. An owner regression holds both parent locks while another thread opens and queries latest and retained views, releasing those locks before joining on failure. The SQL text observation schedules also exercise parallel automatic Bayesian calibration with a separately committed catalog change and retain the original corpus dependencies.

## Text access-path observations

Execution now binds actual text reads and evaluated changes to a separate logical text namespace. Immutable relation/column identities and length-delimited dynamic names prevent field aliasing; exact canonical term bytes preserve scalar and raw UTF-16 identity. Posting reads retain full term ranges, including missing matches, while selected frequencies/occurrences and document metadata retain document points. Field normalization statistics have separate addresses, so same-length unrelated-term replacements do not cause a statistics dependency merely because a physical counter was rewritten. Vocabulary and whole-statistics consumers retain their selected logical ranges. Shared field binding preserves the existing vector addresses, and text points/ranges survive SSI checkpoint handoff with distinct tags.

Borrowed live guards and retained index snapshots use an execution-owned adapter that forwards every native read operation, including bulk, cursor and controlled paths. Engine supplies original participants, immutable column metadata, state and index handles. Scoring, phrase, attention, calibration, count and FTS-statistics consumers register data dependencies only when executed. Construction and analyzer metadata do not read query data; unused SQL LIMIT 0 children retain the existing deferred execution behavior. Cancellation, memory limits and serialization conflicts retain their typed SQL diagnostics.

Text mutations collect provisional logical keys from the provider's original reverse entries and already evaluated analysis under the original allowance. The provider releases its non-reentrant mutation gate before execution registers successful intents. SQL statement/savepoint undo covers private data and later intents while preserving previously observed dependencies. No analyzer is replayed and no unrelated posting corpus is materialized. Automatic public SSI admission remains disabled; graph/catalog/definition observations, efficient persistence, deferrable waits and the remaining transaction acceptance matrix still require completion.

## Direct mutation failure boundaries

Direct row, definition, storage, maintenance and mapped-error mutations previously returned failures to an already-open transaction without applying its abort recovery. A caller could ignore an error after private records had changed and later commit an incomplete operation, including text changes whose logical intents had not yet been registered. These boundaries now reuse SQL's transaction/savepoint abort recovery. Successful operations add no nested transaction frame; errors and panics restore the active boundary and leave it failed until rollback or savepoint recovery. Original typed errors and panic payloads survive successful cleanup, cleanup failures retain both causes, and unresolved commit state remains governed by its existing completion path.

The new provider regression injects cancellation immediately after native text mutation and before intent registration, then exercises direct insert/update/delete with whole-frame and savepoint recovery across native SQLite, SQLite Key/Value and redb. It verifies preserved earlier savepoint writes, independently committed peer data and coherent text results after completion. A separate nested-frame regression covers every mutation error family and panic without discarding its parent transaction.

Focused verification passes 283 cases: 74 common-storage SSI cases, 24 execution observation/diagnostic cases, 16 Engine transaction/internal-reader unit cases and 169 Engine integration cases. The integration selection includes all ten new text schedules across native SQLite, SQLite Key/Value and redb, existing row/value-index/vector observations, catalog atomicity, transaction/cursor/isolation lifecycle, lost completion replies and SQL attention fusion. The actual parallel automatic-calibration query completes after an external catalog change while retaining its original corpus dependency. New text coverage verifies absent-term phantoms, native bulk/cursor reads, normalization-statistic precision, zero-token membership, unused contexts, savepoint undo, nested retained snapshots and typed cancellation/resource/serialization errors. Strict storage/execution/Engine all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness registration, headers, file-size limits and hygiene checks pass. No timing or allocation workload is run; dependencies, Cargo features and reviewed allocation ceilings are unchanged. The SSI checkpoint adds text observation tags without changing canonical row, occurrence or physical vector formats. Native macOS executable startup delays are tracked separately in [issue #125](https://github.com/cognica-io/uqa-engine/issues/125); the reported test counts include only completed executions. Automatic public SSI admission and the remaining transaction acceptance work remain open.

## Graph transaction diagnostics

The graph observation audit found that catalog/storage failures were reduced to strings before graph and Cypher consumers could classify their original transaction, cancellation or resource error. Graph now retains the typed storage cause through a shared error owner, preserving its existing clone/equality interfaces without equating unrelated errors by display text. Cypher's catalog and label checks, graph lifecycle, retained snapshots, physical graph operators and graph-aware retrieval retain that source. Execution owns shared SQL diagnostic conversion, including existing graph serialization failures and provider-wrapped resource errors; Engine delegates instead of duplicating that classification. Authoritative completion evidence takes precedence over a nested conflict or cancellation, preserving unresolved completion as `08007` rather than a retryable `40001`.

Engine's mapped implicit transaction boundary now passes the original SQL error through begin, writer preparation and commit instead of converting it to a string. Successful operations and abort/uncertain-completion ownership are unchanged. Graph callback failures preserve their original cause after rollback; direct Cypher reports failed-frame and read-only diagnostics through the same typed boundary. This prerequisite does not implement graph predicates or automatic public SSI admission.

Focused verification passes 274 cases: all 80 graph unit and 113 graph integration cases, four execution diagnostic/Cypher cases, 15 Engine transaction unit cases and 62 Engine integration cases. The new Engine schedules verify callback rollback, failed-frame/read-only diagnostics and a conflict reported only by implicit completion across native SQLite, SQLite Key/Value and redb. Integration coverage includes graph/Cypher/lifecycle/path-cache, commit resolution, migration and DeepFusion. Its evidence combines 61 unchanged passing cases with one corrected assertion rerun: the injected catalog failure now verifies the original typed SQLite constraint error, preserved message, complete graph rollback and distinct Cypher parse error instead of expecting the former string variant. Earlier mutation-failure, savepoint, panic and uncertain-completion regressions remain green. Strict graph/execution/Engine all-target Clippy, the final integration-target Clippy, formatting, exact-index dependencies, Engine ownership, harness registration, headers, file-size limits and hygiene checks pass. Executable startup delays remain tracked separately in issue #125. No timing/allocation workload or dependency, feature, stored-format or allocation-ceiling changes are involved. Full graph observations and transaction acceptance remain open.

## Graph entity observations

Graph owns the selection of payload reads, including absent vertex/edge points and entities returned through scans and Cypher. Common storage owns their logical graph address format, using the original physical scope and clear generation with distinct vertex and edge keys. Every named graph containing a global entity shares that payload identity. Pinned/private overlays select the identity generation from the same source as their entity. Construction, membership validation, ID enumeration and adjacency-only property decoding do not manufacture payload observations. Original participants and allowances follow cloned and retained graph handles; Engine binds those handles and clears query observers before retaining session catalog state.

Common storage batches now capture the original participant and retain bounded logical write inputs alongside evaluated records. Native SQLite catalog/standalone and common Key/Value graph mutations supply payload intents for source replacement and deletion, including raw catalog and restoration paths. Missing deletion adds no intent. Batch application registers those intents without reentering the active storage session; failure and savepoint undo remove the data and new intents together. Cleanup handles an SSI-checkpoint reply lost after publication without reexecuting graph operations or user callbacks. A retained batch cannot move into another participant. Standalone SQLite graph error conversion retains the original typed cancellation and transaction cause.

The graph-entity artifact has 550 completed passing cases: 149 common-storage session, 74 common SSI unit, 80 graph unit, 113 graph integration, 13 SQLite graph/provider unit, four execution diagnostic, 15 Engine transaction unit and 102 Engine integration tests. One standalone cancellation assertion failed because it searched only for an inner unit error below a transparent wrapper; the original `StorageBackendError::Cancelled` variant is preserved. The following selection unit corrects that assertion and its standalone rerun passes. The Engine integration selection includes all five new graph-entity schedules across the three providers. All five changed owners' unit/integration targets compile; strict all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness registration, headers, file-size limits and hygiene checks pass. Pending executions are excluded from the passing count. The four new batch cases cover cycles, evaluation/resource failures, rejected/lost checkpoint replies, savepoint undo, participant reuse and distinct graph scopes/generations/kinds. Native executable startup remains tracked separately in issue #125. No timing or allocation workload is run, and no dependencies, Cargo features or allocation ceilings change. The SSI checkpoint adds distinct graph tags; canonical graph records are unchanged. The selection and stored-path work below add membership, label, adjacency and path-data observations; definitions, automatic public admission and the broader transaction acceptance matrix remain required.

## Graph selection observations

Common storage now encodes graph selector conjunctions as ID intervals and entity memberships as separate points/ranges. Semantic graph consumers register empty results, page suffixes, labels, adjacency, counts and graph algebra through their original participant. Source mutations supply old/new topology, suppress property-only topology writes and reuse evaluated entity fields when an atomic restore adds memberships for rows staged in the same batch. A physical-scope lifetime key connects clear-generation replacement with retained and absent reads. Storage owns keys and atomic intents; graph owns semantic reads; Engine only supplies retained handles, including an unobserved planning-statistics view that does not clone primary in-memory graph data.

Standalone SQLite graph mutations now retain the admitted participant for graph algebra. The cancellation regression assertion is corrected to inspect the preserved `StorageBackendError::Cancelled` variant: its transparent `Error::source` implementation does not expose the inner unit error as another source. The assertion verifies the typed variant used by execution's diagnostic mapping.

New owner schedules exercise selector conjunctions, page bounds, membership identities and scope lifetimes through actual SSI commit histories. New Engine adapter schedules cover empty label/Cypher and adjacency results, unrelated labels/graphs/endpoints, counts, endpoint replacement, raw vertex/edge restoration with new memberships, clear-generation replacement, savepoint undo and planning-only reads across native SQLite, SQLite Key/Value and redb. Standalone SQLite additionally covers graph algebra and clear. Completed runtime verification passes 463 cases: 152 common-storage logical-session cases, including the three new selector/membership/lifetime schedules, all 80 graph unit and 113 graph integration cases, 15 SQLite graph/provider unit cases, 46 SQLite integration cases and 57 Engine integration cases. The SQLite cases include the corrected typed-cancellation assertion and new standalone selection history. All four owners' unit/integration targets compile; strict all-target Clippy, formatting, exact-index dependencies, Engine ownership, harness registration, headers, file-size limits and hygiene checks pass. All seven new selection schedules and five prior entity schedules pass across the three providers. Final lint corrections only replace equivalent closures/default syntax, add semicolons and format documentation; they do not change the tested behavior. No timing or allocation workload is run, and dependencies, Cargo features, canonical graph formats and allocation ceilings are unchanged. Graph catalog/definition observations, automatic public admission and the remaining transaction acceptance matrix stay open; stored path-data observations follow below.

## Stored path observations

Durable path lookups now bind their selected backend's original participant, cancellation and allowance before reading cached pairs or evaluating an invalidated sequence. A valid cache observes the starting-vertex selection and the requested edge labels, including empty results, without rebuilding paths or decoding entity properties. Index construction observes its graph reads through the same owner. Unsupported sequences do not manufacture data reads. Escaped versioned handles own an independent read transaction over their selected retained view instead of replacing an already retained backend with a fresh live session. This preserves fixed data both with and without SSI admission.

Graph-owner regressions use common versioned memory records and real SSI commit histories without provider dependencies. They cover cached and invalidated results, label/graph separation, property-only changes, unused sequences, empty identity paths, builds, retained participants, cancellation, shared observation allowance and cache reads that do not hydrate unused properties. Engine adapter coverage runs public, directly bound and retained handles across the three providers. Both graph and Engine integration targets compile. Twenty focused tests pass: all twelve selected graph-owner cases, including seven new regressions, and eight Engine cases, including both public/direct and retained-handle path schedules across the three providers. The final retained-handle schedule also compiles the final catalog-reference adjustment and passes. Strict graph/Engine all-target Clippy passes. The final lint adjustment only passes the catalog handle by reference instead of making an unnecessary Arc clone; tested query behavior is unchanged. Dependency, ownership, harness registration, headers, file-size limits and hygiene checks pass. No dependency, feature, stored-format or allocation-ceiling changes are involved, and no timing or allocation workload is run. Graph/catalog definition observations, automatic public admission and the remaining transaction acceptance matrix stay open.

## Graph name and path definition observations

Semantic graph-name and path-index-definition lookups now observe the selected name or complete catalog listing, including absent names. Common storage owns bounded logical keys within the original physical scope, independent of entity clear generations. Graph binds the original participant and allowance for direct, retained and cached-definition reads; Engine only selects session handles and attributes its immutable catalog caches. Planner statistics select unobserved handles before catalog lookup. Named graph selections also observe the graph presence on which their result depends. Graph creation/removal observes its existence decision even when the mutation is a no-op.

Common Key/Value and native SQLite mutations stage definition writes with their already evaluated records. Replacing an identical path definition, storing an already present graph name, changing only a standalone graph registry or deleting a missing name does not invent a definition change. Native standalone scopes remain disjoint. Raw graph restoration and removal include deleted path definitions, and savepoint/error cleanup retains the existing data-and-observation boundary. Path definition writes do not initialize graph lookup indexes or hydrate graph data.

Focused graph-owner, Engine and standalone SQLite schedules cover real concurrent commit histories, cached present/absent/listing results, replacement and deletion, unrelated definitions/scopes, identical writes, rollback, retained participants, cancellation and planner exclusion. The final owner and Engine unit/integration targets compile. Completed runtime verification passes 221 cases: 125 graph integration, 54 SQLite graph/provider unit and 42 Engine integration cases. The new Engine name/path-definition and planner-exclusion schedules pass across native SQLite, SQLite Key/Value and redb. Graph evidence combines 124 unchanged passing cases with the corrected savepoint schedule, which distinguishes cancelled intents from dependencies already observed before rollback. The matching corrected Engine savepoint case also passes across all three providers; Engine evidence combines 41 unchanged passing cases with that corrected schedule. Named definition writes use explicit points, while listings use ranges, preserving the storage validation policy. Strict four-owner all-target Clippy, the final common-storage recheck and final graph/Engine integration-target Clippy pass, as do formatting, staged dependencies, ownership, harness registration, headers, file-size limits and repository hygiene. Label-definition observations, automatic public SQL admission, efficient observation persistence and the remaining transaction acceptance matrix remain open. No Cargo dependency, feature, canonical storage format or allocation ceiling changes are involved, and no timing or allocation workload is run.


## Graph label definition observations

Graph label-kind and listing consumers now retain their original participant, including missing names, default-label tombstones, retained handles and Engine catalog caches. No-op label creation/removal observes its decision. Cypher preserves full registry validation but observes only the default relations required by its query, retaining existing diagnostics and legacy numeric-ID projection. Allocation observes its selected label and any required default without treating internal registry restoration as a whole-catalog read; direct registry export retains a separate record observation. Engine only binds the original context to persistent handles and continues to borrow primary in-memory graph data.

Common storage projects name/ID/kind definitions and reserved tombstones from the already evaluated registry records. Borrowed raw fields and bounded digest/scalar vectors preserve the original allowance and typed cancellation/resource failures without materializing a JSON tree or owned names. Field order, escaped name spelling and autonomous counters do not create semantic definition writes. Duplicate keys retain their final value. Opaque legacy replacements conservatively invalidate only that graph's label object. Native SQLite catalog/standalone and common Key/Value paths stage replacement/deletion and raw-restore intents in the same mutation batch and savepoint boundary. No dependency, feature, canonical record format or allocation ceiling changes are required.

Completed runtime verification passes 312 distinct cases: three common-storage projection tests, 80 Graph unit tests, 129 Graph integration tests, 55 SQLite graph/provider unit tests and 45 Engine integration tests. New label definition and savepoint schedules pass across native SQLite, SQLite Key/Value and redb. After narrowing Cypher default-label observations, all four existing Cypher-label unit cases pass again, preserving diagnostics for missing graphs/defaults and nested requirements. Both new Graph/Engine Cypher concurrency tests pass, covering seven Graph schedules and five schedules across all three Engine providers. Strict four-owner all-target Clippy passes; ownership, staged dependencies, harness registration, headers, file-size limits, formatting and repository hygiene pass. New schedules cover selected/absent/default labels, lists, raw registry export, same/other labels and scopes, counter-only changes, raw restoration/deletion, no-op decisions, retained participants, cancellation and both savepoint orderings. SQL system-catalog consumption is covered by the following work unit; binding, restoration and planner views stay unobserved. Public automatic SQL SSI, remaining catalog observations, efficient observation persistence and the broader transaction acceptance matrix remain incomplete.

## SQL graph catalog isolation and recovery

Execution binds the original participant to the retained catalog when constructing an AGE data source, then observes graph names, labels, sequences and entities only when that source produces rows. The existing deferred scan preserves unused children and `LIMIT 0`; binding and planner snapshots remain unobserved. Empty AGE catalog scans retain graph-creation predicates. `ag_graph` does not load unrelated label registries. Graph handles retain original attribution and typed cancellation, resource and serialization diagnostics; Engine supplies only its session context and retained handles.

Engine restores the matching session graph overlay before rehydrating catalogs on transaction/savepoint/statement rollback and failed completion or BEGIN refresh. This prevents cancelled graph names from masking the original serialization or statement error during catalog restoration.

PostgreSQL 18's [`PredicateLockingNeededForRelation`](https://github.com/postgres/postgres/blob/REL_18_STABLE/src/backend/storage/lmgr/predicate.c#L467-L539) excludes pinned system relations. Direct system-catalog reads and namespace scalar/cache inquiries therefore do not create graph predicates. SQL owns classification by exact built-in identity; Execution excludes graph observers from metadata projection and output-cache hydration. Actual AGE extension tables and ordinary tables with catalog-like names remain observed. Effective search-path selection stays in Execution, while the incorrect scalar observation hooks and their conflict expectations are removed. This is a transaction-correctness fix for false serialization failures, not a waiver of catalog publication, definition coordination or real data observations.

The deterministic PostgreSQL 18.4 reference uses Docker image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`. A and B begin SERIALIZABLE; A consumes the selected metadata query, B reads a pivot row, A updates that row, B creates or drops the selected schema and changes an ordinary graph-name table, then A and B commit in that order. All nine metadata forms (`pg_namespace`, `pg_class`, `information_schema.schemata`, `regnamespace` input/output, absent `to_regnamespace`, `current_schema`, `current_schemas` and `has_schema_privilege`) acquire no `SIReadLock` and both transactions commit. Two ordinary-table positive controls acquire data predicates and B fails with `40001`. This plain PostgreSQL image contains no AGE extension; AGE classification follows ordinary extension-table ownership and is covered separately by UQA provider regressions. Reference output is 2,097 bytes, SHA-256 `786b1f91143effd0bac1343d32c3ac2f6d0ef0d59cfb210a3f9521a859663afb`; capture source SHA-256 is `4247bb9dd98bdadb6fa917b0178b24f2b0c5caf8e3f1c542a5a13ffdc2ff1365`. Generated traces remain outside the repository.

Automatic public SQL SSI, remaining real data/definition coverage, efficient observation persistence and the broader transaction acceptance matrix remain open. General `pg_sequence_parameters` resolution, AGE label `regclass` projection and empty-path `current_schema` output remain separate bugs in [#126](https://github.com/cognica-io/uqa-engine/issues/126), [#127](https://github.com/cognica-io/uqa-engine/issues/127) and [#128](https://github.com/cognica-io/uqa-engine/issues/128).

## Common safe-snapshot admission

Common storage now accepts logical read-only/deferrable characteristics independently of a session's physical write permission. It captures a candidate before application execution, waits outside provider admission and caller publication guards, and replaces only an unsafe candidate. Writers admitted later do not extend the original wait. The caller scopes each individual capture with its matching row-change baseline; only the accepted view reaches the session and its existing savepoints. Cancellation and capture/checkpoint failure preserve the previous session view and release the candidate lease for recovery. Proven safe contexts do not retain predicate keys, while retained readers continue to validate the original participant's lifetime. SQLite forwards the common operation; redb already uses the common session. No SQL, callback or evaluated mutation is replayed.

This storage boundary supplies the automatic SQL admission adapter described below. Engine supplies transaction characteristics and scopes each capture with its row-change gate, then retains the accepted data view before refreshing command/catalog state. Efficient observation persistence, writer fencing and the full provider/history acceptance matrix remain required. No dependency, Cargo feature, physical record format or allocation limit changes are included in this admission boundary.

Focused verification covers 43 common-session tests, including eight new admission cases. They exercise the original overlapping cohort, a later writer that does not prolong the wait, unsafe-candidate replacement, cancellation and candidate recovery, all three admission checkpoint-failure boundaries, capture failures/replay, savepoints, retained-reader lifetime and rejection after private writes. Existing completion-receipt, original-attribution, command-refresh and rollback/cancellation cases remain covered. Strict common-storage, SQLite and redb all-target Clippy passes with warnings denied. Formatting, staged dependency policy, Engine ownership, existing harness registration, headers, file-size limits and hygiene checks pass. These common-owner histories and provider compilation checks are supplemented by the SQL integration below; complete provider histories remain required. Generated compiler/test logs remain outside Git, and no timing or allocation acceptance workload is run.

## Automatic SQL serializable admission

Execution now prepares the first snapshot-bearing statement of an implicit transaction or SQL cursor through the same adapter as an explicit transaction, before binding or execution. Engine supplies the top-level transaction characteristics and scopes each common-storage candidate with the matching row-change publication baseline. Direct mutation and maintenance boundaries prepare that snapshot before taking mutation locks or staging records. SQL access-path fixtures now admit their participants through actual SQL instead of the low-level provider capability. BEGIN, SET, SHOW and savepoints alone leave admission pending. The original participant, fixed data view and first-snapshot restrictions survive command refresh and savepoint undo. Snapshot cancellation rolls back an implicit statement before binding or callbacks; typed storage-facing transaction errors retain their original cause.

Common storage keeps logical read-only restrictions separate from physical maintenance publication. A read-only participant still rejects logical user-data writes, while an upgradeable session may publish ANALYZE statistics through ordinary sealed receipts and recovery. SSI checkpoint version 3 permits this state and reads version 1/2 participants under their original validation rules. Physical document/index formats, dependency directions, Cargo features and allocation limits are unchanged.

A safe deferrable candidate may predate catalog commits completed while admission waits. Engine now retains that selected data/graph view before refreshing its current command/catalog view and catalog merge baseline. PostgreSQL 18.4 was checked in Docker image `sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`: an active serializable writer first reads an existing table; a read-only deferrable reader starts `SELECT count(*) FROM created_during_wait`; after observing its `SafeSnapshot` wait, the writer creates that table, inserts one row and commits. The reader resolves the new table but returns zero rows from its original safe snapshot, including on a second statement; after its own commit it sees one row. The three-provider regression reproduces this ordering. Reference script SHA-256 is `786228d28f569c3fe6f53c387c6ed7e524ec7c41d7fd2f258dbebed1eb8d43a2`; compact result SHA-256 is `75368731c401b5d8fad36f46a938b05befff3a9621f161e5da22a65daf1dab0d`. Generated artifacts remain outside Git.

Completed owner verification passes 76 common-storage SSI unit cases, 44 logical-session cases, ten Execution batch-scheduling cases and five Engine internal-reader/direct-mutation failure cases. The final automatic SQL admission artifact passes all 117 focused Engine integration cases, including all eight admission scenarios, read-only temporary DML and ANALYZE, retained access-path observations, SQL cursors, independent provider writes, commit resolution, tuple key-share locking and PostgreSQL isolation behavior. The prelocked row rewrite reuses its typed target permission check and the existing transaction/error lifecycle without reacquiring a stronger tuple lock: temporary rows remain writable, permanent rows remain protected and the logical read-only characteristic is unchanged. Strict Storage/SQLite/redb/Execution/Engine all-target Clippy, the ordinary Engine library check and formatting, dependency, ownership, harness, header, file-size and hygiene checks pass. Native executable startup remains tracked separately in issue #125; no timing or allocation acceptance workload was run.

Complete public direct-read coverage, remaining data/definition coordination, efficient observation persistence, main-writer fencing and the full recovery/upgrade/backup/platform acceptance matrix remain required. General SQL bugs stay in issues #117–#128. No timing or allocation acceptance workload is run.


## Public document read transaction boundaries

`get_document`, `table_doc_ids` and `document_count` now enter the active transaction or own an implicit read transaction using the session defaults. Engine reuses its statement gate, nested execution depth, first-snapshot adapter and scoped completion/error lifecycle. Execution reuses identity-checked AccessShare relation binding. The exact selected table handle supplies both query data and the existing common-storage SSI context, including missing row IDs and empty scans/counts. Fixed data views and the original participant survive command refresh and savepoint undo; nested SQL callbacks retain the outer statement view. Attached physical readers keep their existing snapshot and completion owner.

Internal query/retrieval consumers use unscoped fixed-view adapters, while constraints, referential checks and tuple-lock rechecks use unscoped current-row reads. Worker access does not reenter the coordinator's thread-owned gate or silently create another transaction. Document materialization remains shared, preserving virtual columns and tuple metadata. No planner, scoring, storage or relation-lock algorithm moves into Engine, and dependencies, Cargo features, persisted formats and allocation limits are unchanged.

Direct document insertion, replacement and field updates now call Execution's existing row-write observer before their private mutation. Deletion observes a row write only when its target exists. Common storage owns predicate deduplication and savepoint undo; SQL publication retains its existing observation boundary. No conflict algorithm moves into Engine. Regressions exercise present-row replacement, vector-aware replacement, field update, patch and deletion against point reads in both commit orders, absent update/delete targets and rolled-back write intents across native SQLite, SQLite Key/Value and redb.

Field updates, patches and deletion now bind a precise target-row observation through Execution before reading the retained table's document store, including absence. The original table identity and participant are reused; temporary relations remain outside shared SSI observations. The compiled pre-change Engine reproduces the missing dependency: an absent-target update and a later matching insertion both committed despite their crossed reads and writes. The regression checks matching and unrelated row IDs, savepoint undo and whole-transaction rollback across native SQLite, SQLite Key/Value and redb. Existing no-op write suppression remains in place.

The final document-read/row-mutation artifact passes all 43 focused Engine integration cases, including first direct-read admission, precise absent mutation targets, matching versus unrelated late insertions, both write-skew commit orders, savepoint observation lifetimes, whole-transaction rollback, session-default admission, nested callbacks, independent-backend relation locks, logical completion, SQL mutation paths and PostgreSQL isolation behavior. The pre-change artifact reproduces the absent-target failure; the corrected artifact covers native SQLite, SQLite Key/Value and redb. Prior document-read verification passed six focused Engine unit cases and three SQLite identity owner tests. Strict Engine all-target Clippy with warnings denied, formatting, dependency/ownership, single-harness, header, file-size and hygiene checks pass. Dependencies, features, persisted formats and allocation limits are unchanged. No timing or allocation acceptance workload was run. Further direct catalog coverage, data/definition coordination and the provider acceptance matrix above remain required; general SQL issues stay separate.


## Public retrieval and graph transaction boundaries

Public text, profiled text, vector, calibrated-vector and hybrid search now enter the selected transaction before planning, analyzer/index access and validation. Table-based entry points reuse Execution's identity-checked AccessShare binding and preserve the caller's original calibration signal name separately from the canonical data relation. Calibration parameter/report APIs and `deep_predict` also admit before reading their inputs. Public physical drivers enter the same scope; their internal worker drivers remain below the caller-owned statement gate and snapshot.

Graph handles, graph/label/path-index lookup and listing, and direct Cypher reuse the generic typed-error query boundary. Existing transactions retain their original participant and first snapshot, while persistent implicit queries apply session isolation, read-only and deferrable defaults. Graph statistics still use the existing unobserved statistics view. Cypher parsing and execution errors now reach transaction completion directly, as do physical retrieval errors that previously escaped inside a nested successful result. Potential calibration writes and implicit graph creation retain writable rollback snapshots on memory engines too. The parameter publication adapter enforces the read-only characteristic for physical workers without reacquiring the statement gate, preserving `25006` and preventing memory-state publication as well as provider writes. No planning, scoring, graph algorithm or conflict policy moves into Engine; dependencies, features, formats and allocation limits remain unchanged.

New provider schedules exercise 19 direct API routes as the first query, retaining the original view and participant after a peer commit and savepoint undo. They also cover first-query write skew, validation/execution failure recovery, default deferrable waiting and typed cancellation, calibration signal names and read-only defaults, and rollback of an implicitly created graph after a failing query. Existing Engine unit cases cover optimized and physical calibration rollback on memory/persistent engines, graph/path snapshots and typed graph completion errors; a new worker case verifies that graph catalog adapters retain the established scope without reacquiring the caller's gate. Full data/definition coordination, efficient observation persistence, fencing, retention and recovery/platform acceptance above remain open.

Final verification passes all 61 focused Engine integration cases and eight Engine unit cases. The integration artifact covers all seven new direct-query schedule groups, graph data/definition/path histories, catalog and planner exclusions, Cypher, calibration reports and hybrid parity; the corrected read-only publication check runs on memory, native SQLite, SQLite Key/Value and redb. The unit artifact includes graph catalog workers beneath an owned statement gate, optimized/physical calibration rollback and typed graph completion failures. The preceding broader 135-case artifact passed 134 cases and reproduced the missing read-only publication guard; that failure passes in the final artifact. Strict Engine all-target Clippy, formatting, dependency/ownership, single-harness registration, headers, the 1,000-line ceiling and repository hygiene pass. These focused results do not establish the remaining full-provider acceptance matrix. Generated reports stay outside Git; no timing or allocation acceptance workload was run.

## SSI checkpoint writes after state changes

Common storage now tracks checkpoint changes at the state transitions that own them. Duplicate predicates, status checks, safe-snapshot polling and recovery without state changes keep the validated checkpoint. New coordinator identities, observations, dependencies, victim decisions, savepoint write undo, confirmed receipts, released participant leases and obsolete-history reclamation require persistence. A later operation error or cancellation cannot discard earlier changes, and encoding never acknowledges provider durability. SQLite releases unchanged auxiliary admission without a BLOB update; redb skips its physical writer, reserved encoding page, page writes and synchronization. The checkpoint format, dependency directions, Cargo features and allocation limits are unchanged.

This removes redundant checkpoint publication. Each admission still decodes retained state against the caller's allowance, and new observations still replace the complete checkpoint; efficient incremental observation persistence remains open alongside main-writer fencing and the remaining transaction acceptance matrix. General SQL, native process startup and release work remain outside this unit.

SSI checkpoint verification passes all 112 focused tests: 81 common-storage SSI cases, 17 SQLite transport cases and 14 redb transport cases. New coverage compares tracked mutations with complete encoded state across generated histories, preserves partial receipt recovery after errors, checks duplicate preparation and terminal outcomes, and retains lease retirement. SQLite verifies unchanged BLOB update counts across ordinary, encrypted, compressed, compressed-encrypted and memory modes; redb verifies unchanged page-write/synchronization counts and completion while an independent main writer is held. Existing write-skew, cancellation, memory-exhaustion, malformed-state, checkpoint failure, reopen and process-death schedules also pass. Strict three-owner all-target Clippy with warnings denied, formatting, staged dependency policy, Engine ownership, harness registration, headers, file-size and repository hygiene checks pass. No timing or allocation acceptance workload was run.

## Incremental SSI checkpoint records

Common storage now represents the checkpoint as an ordered header, participant records, dependency records and immutable read/write observations. Stable opaque keys include content fingerprints where values can change. The original key inventory stays within the admitting caller's allowance; a merge against the current graph emits only inserted/deleted records and the changed header. A new read observation with no new dependency or victim publishes two bounded records regardless of retained history; dependency and participant changes produce their own deltas. Savepoint undo, terminal outcomes and history reclamation require no separately allocated deletion journal. Additional future inventory capacity is reserved before record callbacks so successfully published state can be decoded under the same allowance. The existing SSI algorithms and receipt ordering are unchanged.

SQLite auxiliary format 2 and redb marker `UQARED02` store common `UQASREC1` records atomically. Each provider streams only changed values; redb reserves encoding workspace for one changed record. Valid singleton checkpoints migrate with their original identities and observations, while failed or cancelled migrations preserve the previous committed format. Corrupt state cannot become a fresh coordinator. Main MVCC formats, Cargo dependencies/features and allocation limits are unchanged.

This replaces the complete-checkpoint rewrite remaining after the preceding unit. Admissions still decode the complete graph, and no throughput acceptance is inferred from record or byte counts. Main-writer fencing, remaining data/definition coordination, bounded private/retained state and the recovery/upgrade/backup/provider/binding/platform acceptance matrix remain required.

Incremental checkpoint verification passes all 121 focused tests: 86 common-storage SSI cases, 19 SQLite transport cases and 16 redb transport cases. New coverage checks bounded record/byte publication as retained history grows, future inventory allowance before callbacks, savepoint deletions, partial receipt recovery, malformed records and 64 generated dependency/completion histories against complete checkpoints. SQLite verifies migration, rollback and cancellation across ordinary, encrypted, compressed, compressed-encrypted and memory modes; redb verifies migration, injected synchronization failure and reopen. Both reject corrupt singleton state before migration. Existing cancellation diagnostics, independent writers, write skew, process-death recovery and retained participant identities also pass. Strict three-owner all-target Clippy with warnings denied, formatting, staged dependency policy, Engine ownership, single-harness registration, headers, file-size and repository hygiene checks pass. These functional results do not complete the remaining provider acceptance matrix; no timing or allocation acceptance workload was run.
