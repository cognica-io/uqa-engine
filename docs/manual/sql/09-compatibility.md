# PostgreSQL Compatibility and Limits

UQA Engine targets PostgreSQL 18-compatible syntax and behavior while remaining an embedded engine with its own storage, planner, catalog, and extension model. PostgreSQL 18 is the behavioral oracle: every externally observable difference is a compatibility bug, including differences in features not yet implemented.

## Compatibility baseline

- SQL parsing uses PostgreSQL grammar through `libpg_query`.
- Session metadata reports `server_version` as `18.0-uqa`.
- The repository checks all 22 deterministic TPC-H-derived scale-factor `0.001` query results against PostgreSQL 18.4 fixtures.
- The optional wire crate implements PostgreSQL protocol 3.0 through 3.2 codec primitives, minor-version negotiation, reserved startup-option reporting, protocol-specific and middleware-layered cancellation-key validation, context-aware cleartext/MD5/GSS/SSPI/SASL authentication sequencing, shared extended-query and function-call text/binary format resolution, notifications, and COPY format validation. PostgreSQL 18.4 `psql`/libpq tests verify 3.0, 3.2, and `latest` startup, 3.2-to-3.0 downgrade, authentication ordering, SSL rejection and retry, legacy and 256-byte cancellation keys, and extended Parse/Bind/Describe/Execute/Sync flow; the pinned psycopg 3.3.4, pgx 5.10.0, and node-postgres 8.23.0 Docker matrix additionally exercises prepared reuse, binary result selection, binary parameters where the driver supports them, COPY in and out, failed-transaction recovery, and one-connection pool reuse against PostgreSQL 18.4 and the codec fixture server.
- PostgreSQL-shaped `information_schema` and `pg_catalog` virtual relations carry declared PostgreSQL 18 row types even when empty; implemented `pg_type` rows expose exact scalar, array, information-schema domain, pseudo-type, and `information_schema_catalog_name` metadata. Scalar and array `regproc`, `regclass`, `regnamespace`, `regrole`, and `regtype` values use catalog-aware PostgreSQL text output in explicit casts, CLI rendering, and `COPY TO`, including `0` as `-`, unresolved OIDs as decimal text, visible-name or role-name quoting, NULL preservation, and PostgreSQL built-in type aliases. Scalar `regprocedure` additionally resolves and renders an exact routine signature for implemented user functions, procedures, and built-ins. The strict stable `to_regproc(text)`, `to_regprocedure(text)`, `to_regclass(text)`, `to_regnamespace(text)`, `to_regrole(text)`, and `to_regtype(text)` functions resolve the corresponding represented catalog objects with PostgreSQL search-path or global-role visibility, soft-failure, parser, overload, and SQLSTATE behavior.
- Apache AGE-shaped `cypher(...) AS (...)` integrates graph results into SQL, and the AGE catalog surface (`LOAD 'age'`, `ag_catalog.ag_graph`, `ag_catalog.ag_label`, the `agtype` / `graphid` types, and the graph and label management functions) lets AGE drivers bootstrap against the embedded engine.
- Recursive CTE `SEARCH` and `CYCLE` preserve PostgreSQL 18 depth/breadth sequence types, path-sensitive cycle rows, generated-column visibility, recursive wildcard behavior, `UNION` distinctness, iteration-wide limiting, validation order, and stored-plan reopen behavior; `MATERIALIZED` and `NOT MATERIALIZED` select PostgreSQL's eligible folding policy while recursive and volatile definitions remain materialized.

The fixture coverage is evidence for those queries and types, not a claim of complete PostgreSQL 18 compatibility.

<!-- pg18-milestone-snapshot:start -->

Current milestone snapshot: complete — `M1` (Discovered semantic fixes), `M2` (Protocol 3.2); in progress — `M0` (PG18 baseline), `M3` (PG18 DDL and types), `M4` (Core regression parity), `M5` (Client parity); not started — `M6` (Complete compatibility). Each milestone status is derived from its owned evidence items and remains bounded by its exit gate.

<!-- pg18-milestone-snapshot:end -->

## Embedded runtime architecture

UQA Engine does not yet implement PostgreSQL processes, network protocol semantics as its core API, MVCC storage pages, the complete role-administration and non-routine object-privilege models, extensions, background workers, replication, WAL administration, or server configuration. The optional PostgreSQL wire and FDW crates are adapters around the engine. Every externally visible difference caused by this architecture remains an open compatibility bug rather than an accepted alternative behavior.

Some declared SQL types share an internal runtime carrier, but scans and relational plans retain the declared `ColumnType`, integer writes and casts enforce `int2`/`int4`/`int8` ranges, source-sensitive OID/XID/bytea casts retain source width, and result schemas do not infer identity from values. Any remaining PostgreSQL 18 overflow, cast, storage, collation, binary-format, or display difference is an open compatibility bug.

Relational operators keep static row schemas until the final consumer. Spill format version 1 records the declared schema and logical `(alias, column)` identity directly; spilling is not a reason to materialize rows or flatten qualification early, and there is no legacy spill compatibility reader.

## Open PostgreSQL 18 relation-feature bugs

Temporary tables, views, sequences, CTAS, `SELECT INTO`, temporary-relation name resolution through `pg_temp`, automatic temporary views over temporary relations, all three temporary-table `ON COMMIT` actions, `DISCARD TEMP`, unlogged tables and sequences across clean reopen, ordinary materialized views and refresh, validated view/materialized-view reloptions, ordinary inheritance, and declarative list/range/hash partitioning are implemented. Hash partition keys accept typed expressions, hierarchy scans preserve physical `tableoid`, ancestor statistics aggregate descendants, and catalog identity includes `pg_inherits`, partition-aware `pg_class` and `pg_attribute`, `pg_partitioned_table`, `pg_get_expr`, `pg_get_partkeydef`, and partitioned parent and child indexes. `ALTER TABLE ... INHERIT`, `NO INHERIT`, `ATTACH PARTITION`, and `DETACH PARTITION` validate and publish durable hierarchy changes atomically; recursive column, CHECK, and `NOT NULL` additions honor `ONLY`; catalog reopen repairs dangling legacy parent edges; and direct and query COPY paths preserve named-storage, descendant-scan, partition-routing, text and CSV header, generated-value, and rollback behavior.

- Unlogged-relation reset after crash recovery
- Typed tables
- Table storage parameters and tablespaces
- Table `USING` access methods
- Cross-database relation or routine names
- Temporary and unlogged materialized views, concurrent refresh, materialized-view indexes, access methods, and tablespaces
- Complete privilege and optimizer effects for `security_invoker` and `security_barrier` and remaining automatic-updatability shapes involving CTE-backed definitions or other unverified query forms
- PostgreSQL's interruptible `DETACH PARTITION ... CONCURRENTLY` pending state, later `FINALIZE`, and complete multi-session lock-wait behavior
- Remaining upstream inheritance and partition regression schedules, privilege behavior, dump/restore, and unsupported index-method interactions

Unsupported forms fail before catalog mutation. That failure is fail-safe behavior while implementation is incomplete, not a compatibility exemption.

## Open PostgreSQL 18 query-clause bugs

- Collations, domains, user-defined equality operators, and the complete common-type matrix for `JOIN ... USING` columns with different declared types

Each missing clause above must be implemented with PostgreSQL 18 semantics; source-query rewriting is not an accepted compatibility solution.

## Open PostgreSQL 18 DDL bugs

For implemented tables, views, materialized views, sequences, foreign tables, and supported virtual PostgreSQL 18 catalogs, text-to-`regclass` casts resolve the visible relation to the exact OID exposed by PostgreSQL or UQA's relation catalog; a missing cast target reports `42P01`, while `to_regclass(text)` returns `NULL`. Text-to-`regprocedure` casts resolve one exact function or procedure input signature through qualification and `search_path`, including implemented built-ins, report `42883` for a missing overload, and render the visible signature from the OID carrier with `0` displayed as `-`.

`to_regproc(text)` returns the unique visible routine name and returns NULL for a missing or ambiguous name; `to_regprocedure(text)` requires an exact input signature and returns NULL when the signature is omitted or unresolved; `to_regnamespace(text)` resolves an unqualified schema; `to_regrole(text)` resolves an unqualified global role and returns NULL for missing, malformed, or qualified names; and `to_regtype(text)` resolves represented scalar, array, information-schema domain, and pseudo-type identities after PostgreSQL type-name parsing of aliases, qualification, typmods, and array bounds. Object inputs follow PostgreSQL's identifier-string splitting, case-folding, quoted-name, punctuation, and 63-byte clipping rules rather than SQL statement-name grammar. All-digit inputs use PostgreSQL OID syntax, including leading-zero octal notation, `-` denotes OID 0, and neither form requires a live catalog object. The six `to_reg*` functions return their exact `reg*` aliases, propagate NULL, reject cross-database relation, routine, and type names with `0A000`, reject overqualified names and malformed type syntax with `42601`, and report `42883` for unsupported calls. Direct `regnamespace` input is hard and resolves a schema name to the OID carrier used by catalog columns, with exact missing-schema, malformed-OID, overflow, and invalid-name SQLSTATEs. Direct `regrole` and `regrole[]` input remains hard with the corresponding role contract; live table writes resolve role names to durable OIDs, and transaction rollback, catalog reopen, and later role removal preserve those carriers.

Stored expressions follow PostgreSQL's scalar `regrole` dependency rule: a non-NULL scalar `regrole` string constant in a default, `CHECK`, generated expression, partition key, stored query definition, routine declaration or SQL-standard body, trigger condition, or rule reports `0A000` after normal hard-input validation. Runtime conversion through `text` or `oid`, numeric and NULL scalar constants, arrays, and SQL source-string routine bodies remain supported.

- Virtual and stored generated columns implement durable definitions, selective virtual evaluation, exactly-once stored evaluation, DML assignment rules, DDL-time static typing for the implemented expression surface, exact stored SQL routine overload binding and dependencies, supported constraints and indexes, catalogs, ALTER operations, failure atomicity, and reopen behavior. The complete PostgreSQL built-in function and operator overload matrix, privileges, inheritance and partition propagation, `pg_dump`/`pg_restore`, and complete upstream regression coverage remain open compatibility bugs.
- Named CHECK, foreign-key, and `NOT NULL` constraints implement `NOT VALID`, failure-atomic validation, supported `ALTER CONSTRAINT` state changes, actual `INITIALLY DEFERRED` foreign-key checks before outer-commit temporary actions, and per-transaction `SET CONSTRAINTS` with persistent `ALL` state, exact name resolution, operation-sensitive durable constraint-object, row-identity, and firing-relation-bound events, retroactive immediate validation, routine, callback, PostgreSQL simple-query transaction control, and savepoint-aware modes, queued-event survival across deferrability changes, pending-event ALTER, DROP, and TRUNCATE protection with referenced-parent checks and rename continuity, and current- and cross-session replacement, rename, and temporary-namespace lifecycle handling, plus catalog and information-schema flags, dependency-aware drop, multi-action ALTER atomicity, and durable reopen. Inheritance and partition propagation, the complete dependency graph, `pg_dump`/`pg_restore`, and the upstream constraint regression matrix remain open compatibility bugs.
- `WITHOUT OVERLAPS` keys and `PERIOD` foreign keys enforce the implemented range and multirange matrix, aggregate temporal coverage, mutation safety, ALTER validation, catalog flags, and reopen behavior. Physical GiST and exclusion-index planning, temporal actions beyond `NO ACTION`, deferrable temporal enforcement, dump/restore, and the complete upstream regression matrix remain open compatibility bugs.
- Expression indexes are not implemented.
- SQL index access methods are B-tree, GIN, IVF, and HNSW.
- Dependency-sensitive `DROP ... CASCADE` remains incomplete for implemented catalog objects except graph namespaces and the bounded routine graph. `DROP FUNCTION signature CASCADE` removes the exact function, its generated columns, and direct or transitive stored views while retaining unrelated objects; `DROP PROCEDURE signature CASCADE` removes a procedure with no modeled dependents. Every additional routine-dependent object kind, dependent-procedure graph, multi-target CASCADE graph, deletion order, notice, and diagnostic is tracked by `routines.function-procedure-drop-cascade-extended`.
- `ALTER TABLE DROP COLUMN CASCADE` removes inbound foreign-key constraints and the target column's owned constraints; cascade over every other PostgreSQL dependency kind remains incomplete.
- `ALTER COLUMN TYPE USING` is preserved structurally and evaluated once per old row inside the atomic ALTER transaction; the complete PostgreSQL assignment-cast matrix, dependency and collation rewrites, domain checks, and upstream ALTER regression cases remain open.
- The fixed `uqa` database has a durable bootstrap owner and PostgreSQL 18 default and explicit `CONNECT`, `CREATE`, and `TEMPORARY` ACLs, including `TEMP`, `PUBLIC`, membership-aware rooted grant-option chains, dependency-aware revoke, role dependencies, transaction, savepoint, cross-engine, reopen, and `pg_database.datdba` and `datacl` behavior. `CREATE SCHEMA` and every implemented temporary relation or index creation form enforce the corresponding database privilege with inherited grants, immediate revocation, transactional restoration, and PostgreSQL error precedence. All six name/OID `has_database_privilege` overloads preserve strict NULLs, missing objects, error precedence, comma-any checks, grant-option checks, and exact stable `pg_proc` identities. Database lifecycle and owner transfer plus `CONNECT` enforcement at the embedding connection boundary remain open.
- `CREATE SCHEMA AUTHORIZATION` and embedded schema elements are not implemented.
- All six current-user or explicit-role name/OID `has_schema_privilege` overloads implement PostgreSQL 18 `USAGE`, `CREATE`, `WITH GRANT OPTION`, and comma-any checks across durable, system, and current temporary namespaces. They preserve membership-aware ownership and ACL paths, strict NULL behavior, numeric text names, missing-name and missing-OID distinctions, error precedence, and exact stable `pg_proc` identities. Schema `CREATE` is enforced across supported durable relation and routine creation, standalone indexes, and indexed key constraints with exact search-path, source-analysis, definition-error, collision, inheritance, revocation, transaction, savepoint, and inferred-temporary-view behavior. Routine calls and ALTER, DROP, GRANT, and REVOKE target lookup enforce schema `USAGE` with qualified-error precedence, effective-search-path filtering, inherited grants, prepared-plan revocation, security-context-aware dynamic resolution, and exact stored-binding behavior; ordinary relation schema `USAGE` and remaining object paths, remaining object ownership and ACL checks, and default privileges remain open.
- Sequence `AS smallint|integer|bigint`, positive and negative increments, start and restart, explicit and default minimum and maximum bounds, cycle behavior, bound exhaustion, positive cache sizes with bounded durable reservations and session-local consumption, transactional definition ownership, durable reopen, and `pg_sequences` metadata are implemented. `ALTER SEQUENCE ... RENAME TO` and `ALTER SEQUENCE ... SET SCHEMA`, including historical `ALTER TABLE` spellings for sequence targets, preserve stable `regclass` and `pg_class` OIDs, current- and sibling-session cache blocks, `currval`, `lastval`, column-default and stored-view dependencies, serial and identity ownership metadata, transactions, savepoints, and durable reopen, with PostgreSQL errors for temporary schema moves, owned-sequence moves, collisions, missing schemas, relation kinds, and read-only transactions. `ALTER SEQUENCE ... SET LOGGED|UNLOGGED` and its historical spelling implement transactional persistence changes, exact no-op versus changed cache invalidation across sessions, temporary-sequence rejection, ownership-kind independence, durable reopen, and `pg_class.relpersistence`; unlogged crash-recovery reset remains open. `OWNED BY table.column` and `OWNED BY NONE` implement same-schema validation, stable table and column identities across rename and reopen, automatic owner-drop lifecycle with transitive `RESTRICT` and `CASCADE`, `TRUNCATE` restart behavior, `pg_get_serial_sequence`, serial automatic dependencies, identity internal dependencies, and transaction and savepoint rollback. Sequence role ownership follows the creating role; owner or inherited-owner checks protect ALTER and DROP; direct and historical `OWNER TO` require a SET-enabled target-role path, reject serial and identity dependency ownership, update `pg_class.relowner` and `pg_sequences.sequenceowner`, block dependent role removal, and preserve stable identity, definition state, transaction and savepoint rollback, temporary lifecycle, rename, and durable reopen. Sequence `USAGE`, `SELECT`, and `UPDATE` ACLs cover explicit, historical `ON TABLE`, and all-in-schema targets, `PUBLIC`, independent membership-aware grantor paths, grant options, dependency-aware revoke, owner self-revocation and transfer, role dependencies, value-function enforcement, all six name/OID `has_sequence_privilege` overloads, `pg_class.relacl`, read-only and resolution precedence, transaction and savepoint rollback, temporary lifecycle, cross-engine refresh, and durable reopen. Durable schema ownership and `USAGE` and `CREATE` ACLs cover membership-aware owner and grant-option paths, dependent revoke, role dependencies, transactions, cross-engine refresh, reopen, `pg_namespace`, `information_schema.schemata`, and sequence-specific creation, name resolution, owner transfer, schema move, direct scan, value function, grant, inquiry, and information-schema checks with PostgreSQL error precedence. Remaining dependency object kinds, complete concurrent-writer behavior, unlogged crash recovery, and the upstream sequence regression matrix remain open. SQL `DROP SEQUENCE` implements multi-target preflight, `IF EXISTS`, relation-kind errors, `RESTRICT` and `CASCADE` for column defaults and recursive view dependencies, serial-default ownership detachment, transactional rollback, durable reopen, and session-state invalidation.
- Implicit `smallserial`/`serial2`, `serial`/`serial4`, and `bigserial`/`serial8` sequences use `smallint`, `integer`, and `bigint`, respectively, while identity sequences use their column's declared integer type. `information_schema.sequences` exposes exact type, precision, start, bounds, increment, and cycle metadata, omits internal identity sequences, filters rows through inherited ownership or `SELECT`, `UPDATE`, and `USAGE`, and preserves the result across durable reopen; it and `pg_sequences` hide other sessions' temporary sequences.
- `VACUUM` runs only outside transaction blocks, preserves PostgreSQL 18 option and target structure, validates option names, Boolean and parallel values, buffer limits, relations, and columns, and runs targeted statistics collection when `ANALYZE` is requested. `VACUUM FULL` rewrites each named relation and its descendants unless `ONLY` is specified, rebuilds its document, full-text, vector, and B-tree state, and compacts the SQLite database; eager logical backends have no obsolete physical tuple versions for plain `VACUUM` to reclaim. Tuple-freezing metadata, verbose progress output, maintenance catalog counters, and exact PostgreSQL lock granularity remain open compatibility bugs.
- Standalone `ANALYZE` accepts all tables or one table without options or a column list; `VACUUM (ANALYZE)` additionally supports per-relation column lists, multiple targets, and `ONLY` descendant control.
- `EXPLAIN` options are limited to `ANALYZE`, `VERBOSE`, and `FORMAT TEXT` or `FORMAT JSON`.

## PostgreSQL 18 trigger and rule status

The verified DML `RETURNING` row-image boundary covers current, `OLD`, and `NEW` values for ordinary and partitioned `INSERT`, `UPDATE`, `DELETE`, `ON CONFLICT`, and every mutating `MERGE` action. It includes BEFORE-trigger mutation and suppression, immutable original old images, non-retroactive AFTER-trigger writes, stored generated values, same-leaf and cross-leaf updates, and physical source and destination `tableoid` identities; broader trigger and partition lifecycle gaps remain tracked separately below.

The implemented trigger slice provides durable PL/pgSQL `BEFORE` and `AFTER` row and statement execution for `INSERT`, `UPDATE`, `DELETE`, and `TRUNCATE`, including `UPDATE OF`, create-time boolean `WHEN` validation, row-change-time `AFTER WHEN` selection with deferred function execution, PostgreSQL name ordering, recursive referential-action row and statement triggers, ordered explicit, descendant, and cascading `TRUNCATE` targets, zero-row statements, stored and virtual generated-column images, `ON CONFLICT`, `MERGE`, partition-moving `UPDATE`, executable trigger replacement, partition clones, transactional and temporary catalog lifecycle, table and column rewrites, exact trigger-function dependencies, `pg_trigger`, `relhastriggers`, `hastriggers`, and compact and pretty `pg_get_triggerdef`. Table and view `INSERT SELECT`, `UPDATE`, and `DELETE` source and target scans retain the statement-start snapshot across writes performed by `BEFORE` statement triggers. Partition-moving `UPDATE` and `UPDATE FROM` execute source `BEFORE UPDATE` and `BEFORE DELETE`, destination `BEFORE INSERT`, source `AFTER DELETE`, and destination `AFTER INSERT` row triggers in PostgreSQL order without firing `AFTER UPDATE` row triggers; destination suppression, destination-row mutation and leaf-constraint failure, physical deletion without delete referential actions, `RETURNING`, and root update transition rows follow PostgreSQL 18, while partition-moving MERGE update actions retain physical DELETE/INSERT row triggers and empty UPDATE transition sets. `session_replication_role` is a superuser-only enum with transactional `origin`, `local`, and `replica` values; Origin and Always triggers run in origin/local mode, Replica and Always triggers run in replica mode, Disabled triggers never run, and replica mode suppresses foreign-key check and referential-action triggers. PostgreSQL 18 `REFERENCING OLD TABLE` and `REFERENCING NEW TABLE` transition relations provide typed, setwise `AFTER` row and statement execution for single-event `INSERT`, `UPDATE`, and `DELETE`, including empty sets, post-`BEFORE` and stored generated values, `INSERT SELECT`, `ON CONFLICT`, `UPDATE FROM`, `MERGE`, partition and inheritance descendants, cumulative direct multi-foreign-key sets, statement-global ordering across multi-row conflict-update cascade trees, recursive chain and branching cascade waves plus trailing empty sets caused by `AFTER ROW` transition triggers, catalog names and canonical OLD-before-NEW deparsing, nested-routine scope isolation, and persistence guards. Constraint triggers add PostgreSQL 18 `AFTER ROW` execution, optional `FROM` dependencies, immediate and deferred modes, captured row images, retroactive `SET CONSTRAINTS` firing, savepoint and outer-commit lifecycle, queued-event cancellation on trigger or referenced-relation removal, independent trigger and constraint renames and OIDs, trigger-owned `pg_constraint` rows, and partition clone identities. Durable `INSTEAD OF` view triggers provide PostgreSQL 18 row chaining, `NULL` suppression, statement-trigger timing including zero-row mutations, final row-image `RETURNING`, `INSERT SELECT`, `UPDATE FROM`, `DELETE USING`, catalog exposure, rename and drop lifecycle, exact definition and enable-mode SQLSTATEs, and reopen behavior. Automatically updatable single-source projection views rewrite `INSERT`, `INSERT SELECT`, `ON CONFLICT`, `UPDATE`, `UPDATE FROM`, `DELETE`, `DELETE USING`, and `MERGE` through nested views to base DML, preserve defaults, aliases, leading implicit inputs, creation-time star-expanded row types, scalar, `EXISTS`, and `IN` subqueries in projections and predicates with correlated and unqualified references, nested CTE expression bodies, local-alias collision avoidance, statement snapshots, qualifier-correct current, `OLD`, and `NEW` computed row images, check options, `MERGE`, and rewrite-rule images, target-before-source `UPDATE FROM` and `DELETE USING` `RETURNING *` plus source-before-target `MERGE RETURNING *`, computed projections and physical partition `tableoid` row images, source relations named `old` or `new`, source-only hidden-name resolution including unaliased derived sources, and base triggers, enforce the public view row type as the name boundary before shape errors including INSERT `RETURNING` and correlated scalar-subquery references across the complete containing DML namespace, including target and `excluded` rows, `FROM` or `USING` sources, and explicit `OLD` and `NEW` `RETURNING` aliases, reject target-list set-returning projections, computed assignments, ambiguous source references, and INSERT or UPDATE against no-writable-column views with PostgreSQL SQLSTATEs, keep system columns read-only, distinguish materialized-view relation errors, reject user rewrite rules on every automatic `MERGE` target layer, run `ALSO` and `INSTEAD` rules for `INSERT`, `UPDATE`, and `DELETE` on every automatic rewrite layer in PostgreSQL suppression and action order with qualification-before-action lazy row projection, and retain outer rule layers when rewriting stops at a nonautomatic rule-backed inner view, support provider-level `RETURNING` subqueries, pre-routing NULL `tableoid`, base-rule defaults and identities, final-action `INSERT` rule command counts, suppressed `UPDATE` and `DELETE` zero command counts, and actual `RETURNING`-provider selection, reject conditional `INSTEAD`-only paths at every layer, evaluate only sources, inputs, assignments, conditions, and view columns required by surviving and matching rules with short-circuit expression semantics, keep omitted computed columns NULL in INSERT rule images, preserve event-specific rule action cardinality, allow `ON CONFLICT` through view `ALSO` rules, order uniqueness errors before check-option errors, expose rule-backed relation and column updatability, accept computed-column rule inputs, suppress original view triggers under unconditional `INSTEAD` rules, avoid defaults, generated expressions, identity consumption, and partition routing for view-rule-suppressed INSERT rows, combine direct view rules with `INSTEAD OF` triggers in PostgreSQL event order, validate check options on both CREATE VIEW and ALTER VIEW, route defined but replication-suppressed `INSTEAD OF` triggers without base writes, enforce post-`BEFORE` `LOCAL` and `CASCADED` check options atomically from inner to outer including `UPDATE FROM` and `MERGE`, and expose relation, trigger, and column updatability in `information_schema`. The checked-in 609-case stateful oracle uses PostgreSQL 18.4 with Apache AGE and reopens UQA between every case.

Trigger-backed view `MERGE` supports direct and nested automatic-to-trigger targets, all three mutation actions, `DO NOTHING`, action-path validation, PostgreSQL statement-trigger ordering, trigger-returned current, `OLD`, and `NEW` row images, `NULL` suppression, repeated source candidates, statement-start source and target snapshots, replication-suppressed definitions without automatic fallback, user-rule rejection, and final outer check-option validation with failure atomicity.

- Remaining trigger and automatic-view compatibility work includes direct partition-clone lifecycle, trigger privileges, exact `pg_node_tree` serialization, dump and restore, and the complete PostgreSQL trigger regression schedule.
- Durable table rewrite rules support `CREATE RULE`, `CREATE OR REPLACE RULE`, `DROP RULE`, rename and enable modes, INSERT VALUES, INSERT SELECT, UPDATE, and DELETE actions. Actions that reference OLD or NEW execute set-oriented over matching event rows, while row-independent UPDATE and DELETE actions follow the original command's PostgreSQL qualification cardinality, including no-predicate, false-predicate, empty-target, `UPDATE FROM`, and `DELETE USING` cases. The implementation also provides collision-free and correlated LATERAL action sources, PostgreSQL query-scope rejection for CTEs, set-operation members, conditional set-operation actions, the PostgreSQL 18 one-row success and multi-row `0A000` boundary for unconditional `ON INSERT` set-operation actions, and `ON CONFLICT DO UPDATE`, action statement-trigger cardinality, unqualified INSERT and DELETE conditions, alphabetical `ALSO` and conditional or unconditional `INSTEAD` ordering, `NOTHING`, recursion detection, active-rule `ON CONFLICT` and `MERGE` rejection, target-column lifecycle, view `_RETURN` replacement and protection, `pg_rewrite`, `pg_rules`, rule flags, and both `pg_get_ruledef` overloads. Unconditional `INSTEAD` providers implement positional DML `RETURNING` with creation-time row type and type-modifier checks, single-provider restrictions, lazy projection evaluation, provider current/OLD/NEW images, outer aliases, `UPDATE FROM` source-column projection for UPDATE-provider actions, and `DELETE USING` source-column projection for DELETE-provider actions. Origin and Always rules execute for `session_replication_role` values `origin` and `local`, Replica and Always rules execute for `replica`, and Disabled rules never execute. A 194-case PostgreSQL 18.4 with Apache AGE oracle verifies the implemented slice across durable reopen boundaries. Rule condition subqueries, OLD/NEW record expansion, `NOTIFY` actions, complete dependencies, inheritance and partition behavior, privileges, exact node trees and deparsing, dump/restore, and the complete upstream rule schedule remain compatibility bugs.

## Open PostgreSQL 18 type bugs

Supported declarations retain distinct `SMALLINT`, `INTEGER`, `BIGINT`, `OID`, `XID`, `REAL`, `DOUBLE PRECISION`, `TEXT`, `NAME`, `UUID`, `VARCHAR(n)`, `CHAR(n)`, `INTERVAL`, array, and information-schema domain identities. Character lengths, numeric precision/scale, integer ranges, source-sensitive OID/XID/bytea casts, `ALTER TYPE USING`, foreign-table schemas, and supported migration types are enforced by the implemented paths; unknown migration types fail instead of becoming text.

- The complete implicit, assignment, and explicit cast-context matrix is not implemented for every source and target pair.
- User-defined domains, enums, composite declarations, ranges, and multiranges are not implemented completely; the absent enum, range, and multirange value carriers also keep actual `anyenum`, `anyrange`, `anymultirange`, `anycompatiblerange`, and `anycompatiblemultirange` routine substitutions open.
- Large declared `NUMERIC` precision is bounded by the engine decimal carrier in actual values.
- Collation and locale behavior is not a complete PostgreSQL collation implementation.
- Type I/O routine OIDs are present in implemented `pg_type` rows, but the complete corresponding `pg_proc`, typmod, binary I/O, and extension-type catalog surface remains open.
- `VECTOR(n)` and `TENSOR(n)` are UQA Engine retrieval types rather than PostgreSQL core types.

## Open PostgreSQL 18 catalog and administration bugs

The virtual catalogs expose engine metadata needed by supported clients and tests. Implemented PostgreSQL 18 identities include catalog row schemas, information-schema domain OIDs, core/system type layout and I/O routine OIDs, `regnamespace`, `regprocedure` and `_regprocedure` with the four scalar I/O routine rows, `record`, `_record`, `void`, `refcursor`, the `information_schema_catalog_name` view/composite/array with its `pg_class` and `pg_attribute` rows, the trigger surface in `pg_trigger`, `pg_class.relhastriggers`, `pg_tables.hastriggers`, and `pg_get_triggerdef`, and the hierarchy surface in `pg_inherits`, `pg_partitioned_table`, `pg_class.relpartbound`, `pg_attribute.attislocal` and `attinhcount`, `pg_index`, and `pg_indexes`; `pg_get_expr` and `pg_get_partkeydef` expose the corresponding stable deparsers. User SQL and PL/pgSQL routines expose input-only `proargtypes` identity, all-argument metadata, defaults, variadic and return identity, set-returning state, owner, security mode, strictness, leakproofness, parallel-safety, configuration, support-function OID, and per-grantor EXECUTE ACL paths in `pg_proc`; durable logical roles appear in `pg_roles`, login roles in `pg_user`, and per-grantor membership options in `pg_auth_members`. Legacy `int2vector` and `oidvector` values use PostgreSQL's space-separated text representation in casts, tabular and expanded CLI output, and COPY text output. The Apache AGE extension catalog is implemented: `ag_catalog.ag_graph`, `ag_catalog.ag_label`, the `agtype`, `graphid`, `label_id`, and `label_kind` types, one namespace per graph, and the label relations and sequences of every graph appear in `pg_namespace`, `pg_class`, `pg_attribute`, `pg_sequences`, `pg_type`, and the information schema. The complete OID graph, all remaining built-in and extension `pg_proc` rows, ownership and ACLs for other object kinds, server processes, WAL, statistics views, and other extension catalogs remain open.

The `pg_proc` and `information_schema.routines` surfaces expose the PostgreSQL 18 identities and exact `reg*` return aliases for `to_regprocedure(text)` (3479), `to_regtype(text)` (3493), `to_regproc(text)` (3494), `to_regclass(text)` (3495), `to_regnamespace(text)` (4086), and `to_regrole(text)` (4093), including strictness, stable volatility, parallel safety, leakproofness, and internal source names. `pg_type` and `pg_proc` additionally expose the exact PostgreSQL 18 `regrole` (4096), `_regrole` (4097), `regroleout` (4092), `regrolerecv` (4094), `regrolesend` (4095), and `regrolein` (4098) identities and type I/O metadata.

Known mutable settings are `search_path`, `client_encoding`, `datestyle`, `timezone`, and `work_mem`. Unknown or unsupported settings return an error rather than becoming ignored server configuration.

`DISCARD TEMP` removes the current session's temporary tables, views, sequences, and sequence state. It is rejected after an explicit `BEGIN` and permitted inside a multi-statement simple query's implicit transaction segment, as PostgreSQL requires.

`LOAD` accepts the Apache AGE library names (`age`, `age.so`, `$libdir/age`, `$libdir/age.so`) as no-ops because the AGE surface is embedded; every other library fails as a missing `$libdir` file because the engine loads no shared objects.

## Open PostgreSQL 18 routine bugs

SQL and PL/pgSQL routines cover a broad tested subset, including overloads, defaults, set returns, procedures, control flow, dynamic SQL, recursion limits, diagnostics, exception handling, qualified named types, table-backed `%TYPE`, strict assignment/return casts, bound and unbound query and command cursors, `refcursor` parameters and returns, and session portals that survive routine exit until close or transaction end. Top-level SQL `DECLARE`, `FETCH`, `MOVE`, and `CLOSE` and PL/pgSQL static or dynamic `OPEN`, directional `FETCH`, `MOVE`, and `CLOSE` support scrolling and positional movement; PL/pgSQL row-returning commands cover `SHOW`, `EXPLAIN`, output-producing `CALL`, and `INSERT`, `UPDATE`, `DELETE`, or `MERGE` with `RETURNING`, including first-portal-use execution timing, execution-free close, PostgreSQL command scrolling rules, and pre-mutation rejection of commands without result rows. Query-cursor execution evaluates target expressions for `OFFSET` rows, stops at `LIMIT`, advances one row at a time, re-evaluates volatile scan, `VALUES`, filter, projection, sort, and limit pipelines during backwards movement and revisits, traverses backwards-capable `UNION ALL` branches in the requested order across nested boundaries, and materializes supported non-backwards plan or complete-`Append` boundaries with PostgreSQL value stability. Holdable portals, transaction-end cleanup, and savepoint lifecycle apply to the top-level SQL surface. Cursor behavior for PostgreSQL physical node families not implemented by UQA remains open. The routine security slice implements durable logical roles and PostgreSQL 18 role memberships with per-grantor ADMIN, INHERIT, and SET options, transitive role assumption and privilege inheritance, `pg_auth_members`, all six name/OID `pg_has_role` overloads, role-attribute delegation checks, membership-aware routine ownership and EXECUTE ACLs, safe replacement and drop authorization, non-owner grants through direct or inherited grant-option paths, independent per-grantor ACL entries, `GRANTED BY` validation, PostgreSQL warning no-ops without grant option, dependency-aware `RESTRICT` and recursive `CASCADE` with alternate-path retention, ownership-transfer grantor rewriting, `SECURITY DEFINER` and `INVOKER` role context, routine-local configuration, leakproof and parallel metadata, recognized PostgreSQL support-function identities, schema `CREATE` enforcement for functions and procedures, and schema `USAGE` enforcement for routine name resolution and routine DDL while preserving exact stored bindings; passwords, per-role settings, ordinary relation schema `USAGE` and remaining object paths, default privileges, row-level-security interactions, extension languages, and complete planner and server-security consequences remain open.

The bounded original PostgreSQL 18 additions inventory is verified. This closes only `functions.identified-pg18-additions`; the complete PostgreSQL 18 function, operator, type, cast, extension, and catalog matrix remains an open compatibility bug under `functions.full-pg18-matrix`. The implemented-carrier polymorphic, variadic, and pseudo-type routine slice is independently verified under `routines.polymorphic-variadic-pseudotype-overloads`; missing enum carriers, extension languages and security, and extended dependency graphs remain separate items rather than broadening that claim.

The implemented fixed-signature built-ins documented in the function manual share candidate selection with visible SQL routines for qualification, search-path shadowing, exact and implicit matches, preferred and unknown categories, domains, named and default slots, and stable stored bindings where generated expressions are allowed. This shared path includes the six `to_reg*` lookup functions, whose stable volatility excludes generated expressions. Catalog evidence includes the PostgreSQL 18 `pg_proc` identities for unit `random()` (1598), `to_hex(integer|bigint)` (2089 and 2090), `gen_random_uuid()` (3432), `to_regprocedure(text)` (3479), `to_regtype(text)` (3493), `to_regproc(text)` (3494), `to_regclass(text)` (3495), `to_regnamespace(text)` (4086), `to_regrole(text)` (4093), `casefold(text)` (6412), `uuidv4()` (6428), and `uuidv7()` / `uuidv7(interval)` (6429 and 6430). The complete PostgreSQL built-in, operator, cast, and `pg_proc` matrix remains open.

Ordinary scalar SQL and PL/pgSQL, table-returning, and `SETOF` functions, plus procedures and `CALL`, use PostgreSQL-shaped candidate selection for the implemented routine type surface. Function calls preserve declared types from direct casts and scalar-subquery results, while procedures preserve direct casts and concrete PL/pgSQL datum declarations, including typed NULL variables; named and default slots are matched before effective-signature search-path shadowing; typed identities persist across catalog reopen; and exact information-schema domain overloads outrank their base types when the call expression retains or restores the domain through nested casts. Compiler bindings distinguish unqualified `COALESCE`, `GREATEST`, `LEAST`, and `NULLIF` syntax from quoted or schema-qualified ordinary calls, and regular function names continue to obey search-path selection in generated expressions. Select-list projection preserves the selected scalar or set-returning overload through execution, including search-path selection between user routines and built-ins. Table-function source and `ROWS FROM` member bindings remain exact through UPDATE, DELETE, MERGE, correlated LATERAL execution, and stored-view reopen. A stored view records exact user-function dependencies across scalar, table-function, function-group, CTE, subquery, join, and set-operation shapes; `DROP FUNCTION` RESTRICT rejects an exact referenced signature with `2BP01`, unrelated overloads remain droppable, multi-target drops are atomic, and replacing a view replaces its dependency set. `CALL` rejects subquery arguments and reserves required OUT/TABLE placeholders when matching a function of the wrong kind, preserving undefined procedure (`42883`) when the placeholder is omitted and wrong object type (`42809`) when it is supplied.

For scalar and array carriers represented by the engine, user routines resolve the simple `anyelement`/`anyarray`/`anynonarray` family and the common-type `anycompatible`/`anycompatiblearray`/`anycompatiblenonarray` family. Concrete return and parameter substitutions survive SQL and PL/pgSQL scalar calls, `TABLE`, `SETOF`, `RETURN NEXT`, `CALL`, nested overloads, generated columns, stored views, and reopen. Concrete and polymorphic variadic routines implement positional expansion, explicit array pass-through, defaults, named explicit notation, fixed-versus-expanded ranking, and declared-array identity, with PostgreSQL SQLSTATEs for indeterminate, undefined, ambiguous, and invalid-declaration cases.

`ALTER FUNCTION` and kind-neutral `ALTER ROUTINE` change volatility, null-input behavior, security mode, leakproofness, parallel-safety, support-function identity, and routine-local configuration for exact or uniquely visible function identities while preserving the compiled body and durable catalog state; the three ALTER routine kinds also transfer ownership. Explicit empty and omitted signatures remain distinct, `%TYPE`, search-path, ambiguity, missing-object, wrong-kind, membership-aware owner privilege resolution, and schema `USAGE` before object authority follow the tested PostgreSQL 18 boundary, ownership transfer additionally requires a SET-enabled path to the new owner, and applying function-only attributes to a procedure reports `42P13`. Ordinary relation schema `USAGE` and remaining object paths, default privileges, row-level-security interactions, extension languages, and full support-function and planner consequences remain open under `routines.extension-language-privilege-security`.

The bounded routine CASCADE implementation removes generated columns and direct or transitive stored views bound to one exact function, preserves unrelated overloads and objects, supports a no-dependent procedure, remains atomic on RESTRICT and wrong-kind failures, and survives reopen. The remaining dependency-object and ordering matrix is kept partial under `routines.function-procedure-drop-cascade-extended`.

Multi-function `ROWS FROM` follows PostgreSQL 18 row shape for the implemented table-function surface: members resolve independently, columns concatenate in declaration order, rows zip to the longest member with NULL padding, outer aliases rename the combined output, one group-wide `WITH ORDINALITY` column follows all member columns, and correlated groups are implicitly lateral. An unqualified multi-argument `unnest` in a FROM range-function position is PostgreSQL syntax that expands to unary `pg_catalog.unnest` members before ordinary user-overload selection; single-argument unqualified calls and schema-qualified calls remain ordinary functions, while `pg_catalog.unnest` has no ordinary multi-argument signature.

Volatility affects planning and can be changed through the implemented ALTER lifecycle. Parallel-safety, leakproofness, and recognized support-function declarations are persisted and exposed in `pg_proc`, but UQA Engine does not yet reproduce every PostgreSQL optimizer, catalog, security, and privilege consequence of those declarations.

## Graph compatibility

The `cypher` table function follows an Apache AGE-shaped SQL interface and uses `agtype` output. UQA Engine adds concrete SQL output types for direct joins. The Cypher parser is an implemented subset rather than complete AGE or Neo4j Cypher.

Graph and label management follows AGE's `graph_commands.c` and `label_commands.c`: `create_graph`, `drop_graph`, `graph_exists`, `create_vlabel`, `create_elabel`, `drop_label`, and `alter_graph` validate names with AGE's Unicode identifier rules and raise AGE's messages and SQLSTATEs, and `ag_catalog.ag_graph` / `ag_catalog.ag_label` report the same durable rows an AGE database holds. `drop_label` uses dependency-based `DROP TABLE ... RESTRICT`: same-kind inherited labels and stored views block the drop with `2BP01`, while an otherwise independent default or user label is removed even when nonempty; vertex-label removal preserves incident rows in other edge-label relations and can therefore leave dangling endpoints. Direct `DROP TABLE graph.label` is protected by AGE's object-access rule and fails with `2BP01` instead of bypassing `drop_label`. View bindings follow graph renames durably, and a cascading graph drop removes their transitive dependent-view closure. A removed default stays absent across rename and reopen, the graph remains catalog-visible, the surviving kind remains usable, and recreating the whole graph restores both defaults. Apache AGE 1.8.0 can crash a PostgreSQL backend on some operations against a graph missing a default relation; UQA Engine preserves the same observable catalog and dangling-row lifecycle but returns deterministic `42P01` errors for accesses that require the missing kind. Label relations such as `graph._ag_label_vertex` are selectable and participate in stored-view dependencies; default relations include the rows of their surviving same-kind child labels, `cypher(...)` remains the graph-query interface, and `LOAD 'age'` is a no-op rather than a shared-library load.

Regular path query syntax and functions such as `graph_traverse` and `graph_pagerank` are UQA Engine extensions.

## Retrieval compatibility

GIN, IVF, and HNSW names describe UQA Engine physical indexes and do not promise byte-format or parameter parity with PostgreSQL extensions. `_score`, `_doc_id`, `_meta.score`, `_meta.doc_id`, retrieval predicates, Bayesian evidence functions, and model operators are UQA Engine query extensions.

Approximate IVF and HNSW results can differ from exact KNN by design. Text top-K remains exact when the planner selects WAND or Block-Max WAND because unsafe skipping falls back to scoring.

## Porting checklist

1. Inventory statements, types, functions, extensions, catalog reads, and transaction assumptions.
2. Compare them with this manual and reject unsupported shapes early.
3. Load a representative fixture through the target storage backend.
4. Compare column names, row order, NULLs, text bytes, numeric values, and errors.
5. Evaluate retrieval relevance, calibration, and approximate vector recall separately from SQL equality.
6. Test concurrent sessions, savepoints, failures, cancellation, close, reopen, and storage migration.
7. Run the repository compatibility and domain tests relevant to the port.

Never infer compatibility from successful parsing alone. The contract is successful compilation and execution with the required result and failure semantics.
