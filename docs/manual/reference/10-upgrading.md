# Upgrading to UQA Engine 0.3.7

Version 0.3.7 fixes generated document identity collisions between independent sessions and processes, preserving successful INSERT and RETURNING rows in new readers and after reopen. It also encrypts cross-process notification state for encrypted file-backed databases. See the [release history](../../../HISTORY.md#037---2026-09-18) and the notification upgrade requirements below.

Version 0.3.5 adds native Japanese Kuromoji analysis, completion and independent normalization, with shared Nori/Kuromoji mechanisms and both dictionaries in the CLI and official bindings. It also fixes analyzer parameter inference and updates TLS dependencies. Existing 0.3.0 generic/Nori descriptors and database formats remain compatible; Rust callers using analyzer struct literals or explicit simple-lowercase profiles must apply the source changes below. See the [release history](../../../HISTORY.md#035---2026-09-15).

The 0.3.0 release added native Korean Nori analysis, durable analyzer revisions and token graphs, graph-aware phrases and highlighting, PostgreSQL domains and data-modifying CTEs, and prepared-plan improvements. It also moved concrete SQLite APIs into `uqa-storage-sqlite` and changed low-level Rust SQL and retrieval interfaces. The [release history](../../../HISTORY.md#030---2026-09-14) records the changes.

The 0.2 series includes SQL object and privilege lifecycle changes, durable expression and unique indexes, expanded sequences and PL/pgSQL, native cross-process notifications, and a Node.js HTTP client that runs without native addons. These changes were introduced in [0.2.0](../../../HISTORY.md#020---2026-09-05); the [compatibility guide](../sql/09-compatibility.md) defines the verified PostgreSQL 18 surface and the behavior still being implemented.

## Unreleased schema tuple identities

Schema security format 2 preserves namespace OIDs across ACL and owner changes and gives recreated schemas new OIDs and incarnations. Initial open converts legacy and bound-role-only rows inside the complete catalog restoration transaction, preserving the old namespace OIDs and all bound authority. Refresh and secondary sessions require completed conversion; missing, malformed or duplicate current identities are errors. Native SQLite retains the same columns and uses a schema-specific 57-byte owner BLOB; sequence and relation owner encodings are unchanged. Common record format 32 atomically upgrades formats 1–31 and rejects preceding retained readers and writers. Native mapping 8 and catalog version 49 are unchanged; reverting requires a pre-conversion backup. Rust schema row and security models add an optional `SchemaTupleIdentity` for decoding predecessors, while registered runtime schemas require a valid identity. Execution's `schema_object_oid` now takes the captured `CatalogReadView` as its first argument so consumers cannot derive a recreated namespace's identity from its name.

## Unreleased MVCC snapshot retention

Common record format 32 requires every retained logical snapshot to register a liveness lease before obsolete historical revisions may be reclaimed. SQLite upgrades formats 1–28 atomically, preserving records, database identity, allocations and receipts, and revalidates the format on every bounded read. SQLite formats 1–27 gain a guarded `compacted` head flag with default zero during the same transaction; format 28 keeps that flag, and existing history rows remain unchanged. Collection can then retain a latest tombstone solely in its head, restoring that revision into history before a later replacement. Retained predecessor readers and writers reject the new format. redb upgrades after its previous exclusive owner closes. Native mapping 8, catalog version 49 and role catalog format 3 are unchanged. Reverting requires a pre-conversion backup.

Native SQLite’s separate `.uqa-snapshots` coordination file holds only database identity, sequence boundaries and native lock slots. Active leases must remain in place while the database is open; do not replace or remove this file underneath live owners. Process exit releases liveness without requiring a clean shutdown. Collection preserves head tombstones and every durable transaction outcome, including outcomes whose original record versions have been reclaimed.

## Unreleased role names and retained authority

ALTER TABLE now selects action-specific lock modes. Foreign-key addition, inheritance changes, inheritable column and constraint additions, CHECK validation/rename and partition attachment acquire their secondary relation locks before definition publication. Trigger mode changes and foreign-key additions permit concurrent readers, and CHECK validation permits concurrent row writers. INHERIT additionally checks parent ownership; NO INHERIT retains the child-only ownership rule. Referenced-name replacement, inherited-child rename and savepoint rollback preserve their distinct binding and lock lifetimes. See the [ALTER TABLE contract](../sql/02-ddl.md#alter-table) for the resulting lock modes. No persistent format changes are needed.

NOT NULL constraints now retain a nonzero object incarnation and public OID in `ColumnDef.not_null_identity: Option<ConstraintCatalogIdentity>`. Parsed declarations use `None`; materialized definitions require a valid identity. Initial catalog restoration converts ordinary and foreign-table constraints once, preserves their predecessor name-derived OIDs, rejects malformed or duplicate identities before publication, and records `sql_not_null_constraint_identity_version=1` in the same restoration transaction. Later restoration failures roll back both definitions and the marker; refresh and secondary sessions require the completed marker, validate the combined ordinary/foreign-table identity set, and never repair missing identities. Renames retain the stored OID, while removal/recreation allocates a new identity. Common record format 32 upgrades formats 1–31 and rejects old readers and writers that cannot preserve this metadata. Native mapping 8 and catalog version 49 are unchanged; reverting requires a pre-conversion backup.

Inherited NOT NULL validation now checks every child before marking the parent valid and selects child constraints by column even when their names differ. Existing child NULLs cause `23502`, and ONLY validation of an unvalidated inherited constraint reports `42P16`. CHECK and NOT NULL validation retain descendant ShareUpdateExclusive locks, foreign-key validation retains referenced-table RowShare, and INHERIT retains descendant AccessShare for its cycle check. Already validated constraints skip secondary validation locks. No persistent format changes are needed.

Inherited NOT NULL removal now updates descendants and their local/inherited origin metadata, using the same per-level rules as CHECK removal. `ONLY` preserves direct child constraints as local definitions when no other parent supplies them. Recursive removal retains local and multiply inherited constraints and locks only the inspected child levels. Direct inherited removal reports `42P16`; primary keys and identity columns remain protected with the syntax-specific PostgreSQL errors. Child rename and name reuse during a wait preserve the original object. No persistent format changes are needed.

Foreign-key removal now retains AccessExclusive on referenced tables and removed partition clones; key, column, and index cascades retain dependent referrer locks. Relation and constraint identities survive definition waits and name reuse, and deletion publishes refreshed metadata. DROP INDEX rechecks foreign-key dependencies after its parent table lock wait. Direct-removal pending-event errors and CASCADE child-event cleanup remain distinct, and savepoint rollback restores constraints and releases locks. No persistent format changes are needed.

Ordinary `ALTER TABLE` now rebinds the original requested name and current authority after definition-lock waits. A removed source honors `IF EXISTS`, search-path fallback can select a later schema, and historical owner-transfer syntax follows the current table, view, materialized-view, sequence or foreign-table kind. Table rename additionally checks source-schema CREATE or current database TEMP; column changes retain their separate authority. Missing table and explicit schema errors report `42P01` and `3F000`. This correction changes no persistent record format.

SQL role renaming now preserves the original role identity through session authorization, memberships, owner references and ACL paths. Reusing the former name creates a separate role with no inherited authority. Rename, attribute changes and deletion share tuple-version coordination; destination names retain their own transaction and savepoint reservation. See the [SQL role lifecycle contract](../sql/08-transactions-and-routines.md#routine-ownership-roles-and-execute) for syntax, authority and concurrent errors.

Rust statement and command enums add `RenameRole(RenameRoleStmt)`. `RoleReferenceNames` implementers must provide `outer_role()`, the selected role or session user before any SECURITY DEFINER substitution; `current_role()` remains the effective execution role. `SessionAuthorization::outer()` exposes that binding. `RoleBinding::revalidate` follows the original incarnation through a rename while rejecting deletion or replacement. Execution's `prepare_role_owner` callback now receives the current name projected from the retained owner as its third argument. Creation contexts expose bound owner retention instead of the removed `owner_for_publication` name helper. ACL command scheduling uses SQL's `AclCommandRoles` to bind grantees and an explicit grantor once and project their current names after waits, preserving each object's initial validation order.

The bootstrap role is identified by OID 10 and its immutable incarnation independently of its current name. Role publication moves its OID-to-name claim atomically without rewriting memberships or dependent authority. Common provider record format 32 excludes preceding writers that assume fixed role names. Formats 1–31 upgrade atomically with existing records, identities, reservations and receipts preserved; role catalog format 3, native mapping 8 and catalog version 49 are unchanged. Reverting requires a pre-conversion backup.

## Unreleased role definition tuples

`RoleDefinition::revision` now carries a nonzero durable tuple version in addition to the role OID and incarnation. Creation starts at revision 1, and each attribute assignment advances it even when the resulting attributes are unchanged. `advance_revision` rejects missing or exhausted versions. SQL binds the original definition tuple and validates its version; execution serializes definition mutations with the distinct `SharedCatalogLock::Tuple` address. Competing ALTER ROLE and DROP ROLE wait until the holder commits or undoes its change. A committed intervening tuple change fails with SQLSTATE `XX000`; rollback and savepoint undo permit the original operation. Definition mutations retain their original authorization and merge publication into the refreshed role registry without overwriting unrelated role changes.

Role definition tuple locks remain separate from shared-object dependency locks, so a membership grant can retain its role dependency while an independent attribute change commits. Custom `SharedObjectLockSession` implementations must preserve that distinction and the existing transaction/savepoint lifetime. Role deletion keeps its original dependency exclusion and checks dependencies before waiting for the selected definition tuple. Engine supplies state and existing lock adapters; it owns no tuple mutation algorithm.

Role catalog format 3 initializes legacy tuple revisions during complete initial restoration while preserving OIDs, incarnations, attributes and memberships. Missing or zero current revisions are corruption, and secondary sessions never repair them. A later catalog restore failure rolls conversion back. Provider record format 32 excludes preceding writers that cannot retain these versions or coordinate tuple changes; native mapping 8 and catalog version 49 are unchanged.

## Unreleased routine owner and ACL identities

`CreateFunction::owner` is now `Option<RoleIdentity>`: parsed declarations use `None`, registration captures the effective current role, and stored routines require a valid identity. `RoutineAclEntry::role` is an explicit `Option<RoleIdentity>` (null means PUBLIC), and `grantor` is a required `RoleIdentity`. Ownership, EXECUTE and grant-option paths, dependency checks and SECURITY DEFINER execution retain those incarnations. Catalog names are projected from the matching role snapshot; a replacement role cannot inherit stored authority through an old name or reused OID. `RoutinePrivileges::owner` carries an identity, `RoutineInvocationSession::set_current_user` accepts one, and `RoutineExecutionAuthority` uses its identity privilege method.

Routine catalog format 2 requires explicit owner and ACL fields in `sql_functions_json`, including an empty registry marker. Complete initial restoration binds legacy role names once, validates every candidate before installing placeholders and preserves routine object identities. Unversioned legacy maps retain their historical implicit owner EXECUTE; development format 1 already represented a revocable owner EXECUTE and preserves its revocation. Current malformed authority, absent fields and missing owner/ACL incarnations are errors even during initial open; conversion never substitutes a current user or PUBLIC recipient. The empty owner string remains accepted only as the unbound marker in old parsed declarations, which cannot be published as stored authority. Secondary sessions and refresh never perform conversion, and a later restoration failure rolls initial conversion back.

Execution selects the complete private routine metadata record during committed catalog refresh, including definition/ACL changes and deletions, and validates the selected authority against the accompanying role snapshot before Engine installs it. Current provider record format 32 fences preceding routine writers; native mapping 8 and catalog version 49 are unchanged. The remaining definition and publication coordination still require implementation and verification.

## Unreleased domain owner identities

`StoredDomain::owner` now retains a `RoleIdentity` containing the owner OID and incarnation. Runtime domain maps, transaction/catalog snapshots, DROP DOMAIN authority, DROP ROLE dependencies and `pg_type.typowner` use that identity; role-name reuse cannot transfer ownership, and catalog projection never substitutes the current user for a missing owner. `RoleDependencyCatalog` implementers must expose their retained domain map. Domain creation retains its original role dependency through publication, and role deletion rechecks that dependency after waiting. Committed catalog refresh selects the complete private domain metadata record, including deletions, only when storage reports a private change; untouched sessions take the latest committed definitions. Selection validates owner identities against the accompanying role snapshot before registry installation.

Execution owns `sql_domains_json` encoding, complete-candidate validation and publication through `DomainRegistryPublication`; Engine lends registry, catalog and role state. Domain catalog format 1 stores `{"domain_catalog_format":1,"domains":...}`. Only complete initial restoration may bind former owner strings and persist the converted registry, preserving domain identities, OIDs and definitions. Secondary sessions and refresh reject missing conversion or invalid current authority; any later restoration failure rolls the conversion back. Malformed versioned fields never fall back to a legacy role name. Common record format 32 excludes preceding writers; native mapping 8 and catalog version 49 remain unchanged.

## Unreleased storage write cancellation

`PersistentStorageBackend::write_cancellation` and `KeyValueStore::write_cancellation` expose the token for a session's autonomous identifier reservations and logical publication. Versioned backends must expose it, and forwarding wrappers must preserve the underlying token; session validation rejects a versioned backend that omits it. Engine adopts that token for its query runtime. The existing internal retention and cleanup control stays separate, so cancelling a query does not prevent rollback, retained-state inspection or restoration. `Engine::new_session` still creates an independently cancellable SQL session.

`PersistentStorageProvider`, `PersistentStorageBackend` and `KeyValueStore` also expose `open_session_with_cancellation`. Autonomous sequence mutation uses this constructor to inherit the invoking query's cancellation while retaining a separate catalog/backend transaction context and a fresh retention budget. Versioned factories and wrappers must implement or forward the constructor; its default rejects versioned sessions instead of silently discarding the token. Common `VersionedKeyValueStore::new_with_cancellation` and `new_session_with_cancellation` provide this construction, and native SQLite, SQLite Key/Value and redb forward it. The original constructors retain independent tokens. `uqa_storage::read_control` reexports `CancellationToken` for provider implementations that depend only on common storage.

`MutationIdentifiers::advance_next_id` and `persist_next_id` now return `StorageBackendResult<()>` instead of strings. Execution preserves cancellation as SQLSTATE `57014` and memory-limit errors as `53200` through generated/supplied identities, trigger changes, row movement, generated-column rewrites and TRUNCATE watermarks; custom mutation adapters must retain the typed storage error.

SQLite record writes now wait at physical connection checkout, permit preparation and BEGIN IMMEDIATE, checking the supplied cancellation token. Only typed SQLITE_BUSY admission failures are retried. A busy COMMIT retains and retries that same transaction; materialization and staged records are not evaluated again. Cancellation before a successful COMMIT rolls back the physical writes, while other publication errors retain the existing uncertain-receipt contract. The provider temporarily disables the connection's ordinary busy timeout during these controlled writes and restores it before pool reuse, including error and unwind paths. Legacy physical operations, initial schema preparation and rollback cleanup retain their bounded physical admission. This change does not alter persisted formats, provider feature selection or memory ceilings; complete concurrent transaction acceptance remains pending.

## Unreleased command snapshot refresh

`PersistentStorageBackend::refresh_transaction_snapshot` and `KeyValueStore::refresh_transaction_snapshot` take the current command's `CancellationToken` and advance an active logical session's committed boundary while preserving its evaluated private changes. Common versioned sessions, SQLite Key/Value, redb and explicitly bound native SQLite sessions implement the operation; forwarding wrappers must delegate it and its token to the paired session. Preparation retains the session's memory allowance and observes the caller's cancellation token. The default implementation rejects unsupported providers. It never ends or replays the caller's transaction. Engine uses it for providers advertising the versioned transaction model below.

The common owner rebases occurrence totals/clusters, IVF/HNSW generations, graph invalidations and immutable markers before switching views. Canonical write revisions and definition requirements remain checked. A preparation, conflict, quota or cancellation error leaves the previous view intact, and a sealed commit cannot refresh. Retained readers keep their original view. Savepoint rollback restores the private changes together with their matching committed base; the caller selects the next permitted refresh according to its SQL isolation mode. Engine now selects the command catalog/write view separately from retained fixed data snapshots. Complete concurrent transaction acceptance remains pending.

SQLite record format 29 adds guarded bounded current-record runs. Formats 1–28 gain an empty run table atomically; formats 1–27 also gain the compacted-tombstone head flag introduced by format 28. No existing key, value, revision, receipt or allocation is rewritten during this schema upgrade. Subsequent vacuum may coalesce eligible current records while preserving their complete logical representation and retained snapshots. Previous SQLite readers and writers reject the new format before reading or publishing records. redb advances the common development marker to 29 without changing its physical record layout. Returning to a predecessor requires restoring the pre-upgrade backup.

## Unreleased role catalog records

Initial Engine open converts the former `sql_roles_json` map and development role-record formats 1 and 2 into role-record format 3. Each `uqa.sql.role.v1:` record retains a nonzero 128-bit incarnation independently of the recyclable public OID and a nonzero definition `revision`; `uqa.sql.role_oid.v1:` records retain OID uniqueness. Existing OIDs, attributes and memberships are preserved. The aggregate key becomes `{"role_catalog_format":3}`; older role readers reject it. Conversion joins the initial restoration transaction, so a later restoration error rolls back every role conversion. Secondary sessions and catalog refresh require completed conversion and never migrate. Existing nonzero incarnations are preserved; missing or duplicate incarnations in formats 2 and 3, and missing or zero revisions in format 3, are corruption errors. Restoring the pre-conversion backup is required to revert. Common provider record format 32 also excludes retained predecessor writers; native row mappings are unchanged. Rust callers constructing `RoleDefinition` must supply `object_id` and initialize `revision` at 1; `from_create` accepts the OID and incarnation supplied by execution and initializes the revision. Definition mutations must advance the revision before publication.

Initial Engine open also converts `sql_role_memberships_json` into the marker `{"role_membership_catalog_format":1}`, independent `uqa.sql.role_membership.v1:` tuples and `uqa.sql.role_membership_oid.v1:` claims. Every tuple keeps its existing membership OID and binds target, member and grantor names to validated role OIDs and incarnations before any conversion writes. Both role and membership conversions share the restoration transaction; an error restores both original representations. Subsequent reads validate encoded endpoint identities and exact OID ownership without rebinding absent targets or recipients to replacement names. Common record format 32 prevents old aggregate writers from modifying the upgraded database. Rust `RoleMembership` endpoints now carry `RoleBinding` values and its map key carries `RoleIdentity` values. Role declaration and membership AST recipients use `RoleSpecification`, preserving quoted names separately from session keywords; old stored string specifications remain readable. Native mapping format 8 and catalog version 49 are unchanged.

Routine metadata at `sql_functions_json` now uses `{"routine_catalog_format":1,"definitions":...}`. Initial open converts the legacy unversioned routine map and materializes owner EXECUTE in legacy explicit ACLs, preserving their former implicit-owner meaning. NULL/default ACLs remain default. New explicit owner revocations remain revoked across refresh and reopen. Conversion shares the complete initial restoration transaction; later failures roll it back, and secondary sessions refuse legacy metadata without modifying it. Unknown routine format versions are errors. Common record format 32 excludes preceding writers that lack the current creation, temporary-role dependency and independent membership protocols; format-13 and older writers also assume owner EXECUTE is unconditional. Role-record format 3, native mapping format 8 and catalog version 49 are unchanged; reverting requires the pre-conversion backup.

Database security metadata at `sql_database_security_json` now uses `{"database_security_format":1,"security":...}`. Owners, ACL grantees and grantors retain their public role OID and nonzero incarnation instead of a name; an explicit null grantee identifies PUBLIC, while every grantor remains explicit. Initial open validates and converts the legacy name-based object in the full restoration transaction, materializing implicit grantors without changing effective privileges or a default ACL. A later restoration failure restores the original metadata. Secondary sessions and refresh reject legacy data, unknown versions and missing or replaced role incarnations. Current names are projected from the retained identities without rewriting the stored ACL. Common record format 32 excludes predecessors that could replace these references with names. The SQL `RoleIdentity` import remains a re-export of its shared `uqa-core::catalog_role` definition. Database registries, `DatabaseSecurityRead`/`DatabaseSecurityWrite`, dependency readers and `CatalogDefinitionSnapshot.database_security` now retain `BoundDatabaseSecurity`; command and display projections still use the named `DatabaseSecurity` value under the matching role view. `CatalogReadView::database_security` returns the bound value, with `database_security_names` providing fallible name projection. Custom `DatabasePrivilegePublication` adapters now persist execution-serialized JSON, and database restoration receives the explicit initial-migration flag. The corresponding schema, system, relation, sequence, domain and routine conversions are described here.

Schema rows now retain owner, grantee and grantor identities too. `uqa-storage::SchemaRow` distinguishes `Bound(uqa_core::catalog_schema::BoundSchemaRow)` from `Legacy(uqa_core::catalog_schema::SchemaRow)` and exposes the common name through `name()`. Key/Value rows carry `schema_security_format: 1`; SQLite keeps its existing columns and distinguishes legacy TEXT owners from versioned binary identity BLOBs, including in native records. Unknown or malformed current encodings never fall back to legacy names. The shared bound ACL decoder requires the grantee field: only an explicit null represents PUBLIC, so missing data cannot widen access. Initial Engine restoration loads roles before binding legacy schema references and validates every schema before writing conversions inside the complete restoration transaction. Secondary sessions and refresh require bound rows. New low-level `save_schema` calls use the immutable bootstrap identity. SQL and execution schema registry, inquiry, ownership and publication interfaces now carry `BoundSchemaSecurity`; schema registration receives `RoleIdentity`, while `CatalogReadView::schema_security_names` projects the named policy value from the same snapshot. `pg_namespace.nspowner` uses the retained OID, and ACL text and `information_schema.schemata.schema_owner` use the matching role names. Common format 27 excludes name-based predecessor writers; native mapping 8 and catalog version 49 remain unchanged. SQL role renaming uses these retained identities. Versioned catalog implementations and wrappers must also forward `CatalogFacade::schema_has_private_changes` so a fixed-snapshot authority refresh retains private schema creations, replacements and deletions. Serialized catalogs default to no private provenance; a versioned implementation without this capability returns an explicit error. The provenance interface does not change the durable schema encoding or record format.

System relation and attribute ACL tuples under `uqa.system_relation_security.v1:` now use `{"system_acl_format":1,"entry":...}`. Each grantee and grantor retains its role OID and incarnation; PUBLIC uses an explicit null grantee. Initial open binds legacy names only after roles are restored, validates the complete relation/column grant graph before conversion writes, and keeps each tuple revision unchanged. Conversion joins the full restoration transaction; secondary sessions require completed conversion, while missing incarnations and malformed current records never fall back to names. The immutable system owner and default ACLs use the bootstrap role identity. Common record format 32 prevents preceding system ACL writers from modifying the upgraded database. The SQL `SystemAcl` and `SystemRelationSecurityCatalog` interfaces now expose bound values, and table/column inquiry receives the same role view used to project those values. Explicit relation-lock metadata retains `BoundTableSecurity`; view-source authorization retains the selected owner incarnation through later waits. The sequence, domain and routine sections describe their identity conversions.

Object-ACL recipients now use `uqa_core::catalog_acl::AclGrantee`: `Public` is the keyword, while `Role(String)` is a real role, including `"PUBLIC"`. String conversions always construct named roles; callers must use the explicit `Public` variant for grants to everyone. SQL GRANT/REVOKE AST recipients use `AclRoleSpecification`, while grantors and supported OWNER TO fields use `RoleSpecification`; typed session keywords never replace identically spelled quoted names. Current named ACLs and stored statements serialize these variants with explicit tags. Legacy ACL strings retain their former meaning (`"PUBLIC"` means the public recipient), and legacy statement strings retain their former session-keyword meaning. No catalog lookup attempts to infer a different original intent. Common record format 32 fences prior writers; bound ACL identity encodings, role/membership records, native mapping 8 and catalog version 49 are unchanged.

Table, view, materialized-view and foreign-table catalog rows now expose `security: RelationSecurityRow` instead of separate named owner and ACL fields. Its current `Bound` variant carries `uqa_core::catalog_acl::BoundRelationSecurity`; `LegacyRelationSecurity` is only an input to initial restoration. Common Key/Value JSON carries `relation_security_format: 1`, and native/physical SQLite uses the same versioned owner BLOB codec as schemas while keeping its existing columns. Current rows require owner, nullable ACL and column-ACL fields and reject malformed or mixed encodings. Initial restoration validates all role incarnations and table/column grant paths before publishing conversions; a later restoration failure rolls back the complete migration. Secondary sessions cannot bind legacy names. Common record format 32 fences preceding writers; native mapping 8 and catalog version 49 stay unchanged.

Ordinary table, view, materialized-view and foreign-table GRANT/REVOKE now persist independent relation/attribute tuples through `CatalogFacade::save_relation_acl`. Versioned implementations must compose `uqa.relation_acl.v1:` records into loaded security from the same catalog view, and atomically move, remove or compact them with definition lifecycle operations. Serialized implementations keep their embedded ACL format. Execution grant contexts receive a `TableGrantPersistence` adapter for tuple writes; retained tables expose their persistence kind instead of serializing complete definitions for ACL changes. Resolved grant targets retain the selected attribute set through authorization and writer waits. `BoundRelationSecurity::acl_revisions` and SQL `BoundTableSecurity::acl_revisions` carry the identities loaded from those separate records; definition serialization excludes them. Record format 30 rejects earlier writers, including retained format-29 sessions, before they can ignore an ACL replacement. Conversion preserves prior source bytes and commit history; the native mapping remains 8 with catalog version 49.

Sequence catalog rows now expose `security: SequenceSecurityRow` instead of separate named `role_owner` and `acl` fields. `Bound(uqa_core::catalog_sequence::BoundSequenceSecurity)` retains the owner and every ACL endpoint by OID and incarnation; `Legacy(LegacySequenceSecurity)` is an initial-restoration input. Key/Value rows use `sequence_security_format: 1`; SQLite reuses its owner storage-class discriminator (TEXT for legacy names, versioned BLOB for identities) and existing ACL column. Initial Engine open validates all sequence definitions and authority before writing conversions, after restoring roles and inside the complete catalog transaction. Later catalog failures roll conversion back; refresh and secondary sessions reject unconverted or corrupt authority. Current encodings never fall back to a role name. The current [common record format](#unreleased-schema-tuple-identities) also fences earlier sequence and role tuple writers; native mapping 8 and catalog version 49 are unchanged.

Rust view and foreign-table alteration contexts now receive `TablePrivilegeContext` and `RelationCreationContext` for shared native authority checks instead of their old `access` fields. `ViewAlterAccess` remains for view replacement, and `ForeignTableAlterAccess` remains for foreign-table event alteration. SQL view and foreign-table target helpers return `RelationAlterTarget`, which retains the actual relation kind until `require_kind` runs after authority validation. Foreign-table transaction adapters must defer writer admission until definition locking and revalidation have completed.

Rust sequence registry, snapshot, privilege and ownership adapters now retain SQL `BoundSequenceSecurity`. `SequenceCreationPublication` receives the already bound `RoleIdentity`; restoration receives the matching role catalog and an explicit initial-migration flag. `CatalogReadView::sequences` and `sequence_states` return `Result` and project named authority from the same captured roles, so missing or replaced authority produces an error instead of an implicit bootstrap grant. `CatalogSequenceMetadata` names the tuple returned by `sequences`. `SequenceAlterContext` now borrows `TablePrivilegeContext` for actual-relation authority, and the redundant `SequenceRoleAccess` trait and role-context field are removed. SQL `sequence_alter_relation` returns the resolved name and actual kind before `validate_sequence_alter_kind`. `SequenceLifecycleContext` now borrows `RelationCreationContext`; SQL sequence lifecycle analysis separates the declared destination from post-lock validation, and its catalog adapter no longer duplicates namespace existence or CREATE authority methods. Direct sequence deletion now enters `schema::removal::direct::drop_sequence` with the shared relation-removal context; the old unbound `SequenceRemovalContext::drop_sequence` entry is removed.

Runtime relation registries, catalog snapshots and privilege adapters now retain SQL `BoundTableSecurity`. Its `row` and `from_row` methods cross the shared data boundary, while `resolve` interprets names only against the matching role view. `StoredView` retains separate bound `security` and `StoredViewDefinition` fields; definition JSON still excludes authorization, and only the definition type is deserializable. `CatalogFacade::update_foreign_table_security` now accepts `&RelationSecurityRow`, and `CatalogReadView::foreign_table_security` returns a fallibly resolved owned policy value. Definition and query guards retain owner incarnations for view traversal and materialized refresh. Table refresh fingerprints retain owner and definition changes; private ACL tuple provenance independently overlays the latest committed relation and column privileges, preserving both transaction-local changes and fresh peer authority. SQL role renaming projects these retained identities through the current role view.

`CatalogFacade` implementations and wrappers must implement or forward `delete_metadata`, removing the exact key in the caller's transaction and preserving private deletion provenance and the original write precondition. The common Key/Value and native SQLite implementations retain graph definition fences and path invalidation when graph registry metadata is deleted; native SQLite continues to reject changes to its catalog format marker. Role definition and membership publication receive both the original and replacement maps so they can persist only actual changes. CREATE ROLE does not rewrite memberships when it adds no edges. DROP retains the existing membership publication boundary after checking dependencies. Relation, column, database, schema, sequence and routine ACL publication locks newly added grantee and grantor dependencies through typed shared-object addresses; role deletion takes the conflicting object lock and rechecks authority and dependencies after waiting. The execution privilege contexts for database, schema, sequence and routine mutations now receive a `SharedObjectLockSession`; Engine supplies its existing transaction lock and refresh adapter. Table, view, materialized-view, foreign-table, sequence, schema and routine ownership publication now uses the same original-role dependency protocol, with relation/routine target locking and unchanged-owner permission rules following the object kind. Owner-change contexts borrow shared-object locks and captured schema/database security instead of reentering role authorization adapters while holding their guards. Object creation now retains the originally bound owner through dependency waits, including implicit serial sequences, CTAS, schema/routine creation and direct table/schema APIs. Committed temporary table, view and sequence owner/ACL references remain visible to peer sessions and native processes through execution-owned session leases. Failed commits and savepoint undo preserve the preceding committed dependencies; object deletion, DISCARD TEMP and session/process exit release them. Common record format 32 excludes format-26 and older writers that lack role name and retained-authority coordination or role definition tuple versions or bound routine owner/ACL references or bound domain ownership or bound sequence or relation, system ACL, schema or database role references or the complete creation, temporary-reference and membership protocol. The role-table read interface exposes relation persistence, and creation publication adapters receive the captured owner. Remaining target definition and catalog-tuple coordination and session authorization lifetime remain required work tracked in the [concurrent transaction plan](../../plans/0008-concurrent-storage-transactions.md).

## Unreleased Rust sequence value updates

`CatalogFacade::set_sequence_value` and the native SQLite catalog method now require the expected `definition_generation` after `object_id`. They return `SequenceSetValueResult::Set(value)`, `Missing` or `DefinitionChanged`. Implementations and wrappers must check the identity and generation in the same atomic boundary as the value write. A generation mismatch must leave the stored value untouched so execution can resolve the definition again and repeat bounds validation; execution publishes the successful value to its session cache and history only after the update succeeds. Rollback restoration supplies the surviving generation too. This API change does not alter the durable record encoding.

Implementations of `uqa_execution::catalog::sequence::values::context::SequenceValueRuntime` must expose their session cancellation token through `cancellation()`. Execution checks it while resolving changing definitions and retrying known rejected autonomous value mutations. A caller's transaction is never rolled back for an internal value retry, and uncertain outcomes are never replayed. Versioned catalogs must also implement or forward `CatalogFacade::sequence_has_private_changes`. Execution uses it to keep transaction-private sequence definitions in the caller's session while allocating committed sequence values independently, including during unrelated private writes.

`SequenceValueContext` now receives `RelationLockSession` and `SequenceValueTransactions` adapters. The latter reuses the caller's transaction or supplies an implicit one and exposes its outer lock mark. Every nextval, currval, lastval and setval call retains RowExclusive until that outer transaction ends, including cached calls and savepoint rollback. Waiting calls preserve the original sequence identity through rename and name reuse, and refresh sequence authority without advancing ordinary data visibility. A sequence removed during the wait reports `SequenceValueError::MissingOid` with SQLSTATE `XX000`. The public Engine scalar methods retain their signatures and automatically release locks when their implicit call completes; no persistent format changes are required. Further dependent-object lifecycle coordination remains tracked in the [concurrent transaction plan](../../plans/0008-concurrent-storage-transactions.md).

## Unreleased Rust Key/Value compound operations

Versioned catalog and batch wrappers must forward `CatalogFacade::save_statistics_maintenance` and `KeyValueBatch::replace_statistics_maintenance` to preserve evaluated maintenance-counter changes. Their serialized defaults use ordinary metadata replacements. `VersionedPersistence::maintenance_record_layout` defaults to the common Key/Value codec; native providers must supply their metadata-row codec. This API does not change the persisted maintenance JSON fields, metadata key or record format.

Common `KeyValueCatalog` sequence consumers now require `with_read_view` for reads and `with_mutation` for creation, replacement, rename, removal, reservation and set-value operations. Wrappers must forward both methods and use the supplied reader inside the callback; a per-catalog mutex cannot coordinate independent sessions. Durable creation/rename also retain the destination schema through `KeyValueBatch::require_unchanged`. Run `key_value::conformance::verify_sequence_concurrency` with two independent sessions, then `verify_sequence_reopen` after closing and reopening their disposable database. These consumer changes do not alter the record encoding or enable concurrent Engine SQL.

Third-party `KeyValueStore` implementations and wrappers must implement or forward `with_read_view` for common exact/IVF/HNSW vector and occurrence-index reads and `with_mutation` for IVF/HNSW and occurrence-index mutation. The default methods return explicit unsupported errors. Compound reads must retain one committed/private boundary; mutations evaluate once against their original write preconditions, discard errors/unwinds/cancelled batches, and retain unsuccessful publication attempts. A `KeyValueRead` exposes controlled borrowed point/prefix visits and an opaque `KeyValueReadRevision`; retain an unchanged identity and rotate it after writes or undo, using `KeyValueReadRevision::fresh()` if the provider has no equivalent identity. Callback code must use the supplied reader and must not reenter its own session. Built-in SQLite Key/Value, redb and memory reference stores implement this contract. The [provider contract and conformance checks](../../design/kv-storage-backends.md#physical-store-contract) define the required behavior. Retained exact vector snapshots now own a fixed tensor generation and share its memory reservation; IVF/HNSW snapshots retain immutable physical index state. All three reject mutation after the original handle closes. No additional file-format change is introduced by these operations.

Occurrence readers must also implement or forward `KeyValueRead::visit_value_budgeted`, `visit_prefix_after` and `contains_prefix_budgeted` against that same boundary. The defaults reject these missing capabilities. `retain` defaults to a bounded copy of selected prefixes; versioned providers should retain their committed/private visibility owners without loading the corpus. Common SQLite Key/Value and redb snapshots use these retained owners, survive later writes and rollback, share their allowance across nested snapshots and reject mutation. Bounded occurrence cursors now retain their original owner through later cluster pages. Providers with native row layouts can implement `OccurrenceStorage` and use `KeyValueInvertedIndex::from_storage` with existing compiled `AnalyzerBindings`; they must preserve the typed occurrence key projections, controlled reads, atomic coalescing and original mutation preconditions. Legacy namespaces require presence checks and atomic retirement during a source rebuild. Common Key/Value and native SQLite posting/counter merging is implemented; complete concurrent Engine SQL remains in progress.

Occurrence mutation wrappers must also forward `KeyValueBatch::replace_occurrence_record`, `invalidate_occurrence_prefix`, `occurrence_document`, `reset_occurrences` and `fence_record`. Their serialized defaults preserve ordinary replacements; concurrent versioned stores must implement the typed merge and guard contract. Built-in native SQLite, SQLite Key/Value and redb sessions implement it. `VersionedPersistence::occurrence_record_layout` defaults to common Key/Value codecs; native persistence adapters and their wrappers must return the native row layout. `fence_record` retains the original revision even for an absent record, before subsequent changes in the same batch. Document/structural guards use a separate common byte-key namespace so clearing an index cannot reuse an earlier structural boundary. The SQLite and redb development MVCC record version follows the [current schema tuple upgrade](#unreleased-schema-tuple-identities). Development formats 1–31 upgrade atomically without rewriting histories, identities, transaction counters or receipts; formats 1–4 gain empty identifier state, while formats 5–30 retain existing reservations and require the identifier table and guards to be intact. Failed conversion preserves the predecessor format. Previous development writers reject the current format because they lack retained namespace identities, independent relation/attribute ACL publication, role name and retained-authority coordination, role definition tuple versions, bound routine owner/ACL references, bound domain ownership, bound sequence/table/view/foreign owner and ACL references, typed ACL recipients and role specifications, or the complete current role incarnation, bound database/schema owner and system ACL references, independent membership, shared-object dependency and explicit routine owner ACL protocols. SQLite revalidates the format on retained connections; redb revalidates format and database identity before record reads, transaction allocation, receipt lookup, commit, abort, identifier operations and migration. redb retains its exclusive file owner; upgrades occur after the previous owner closes. Released 0.3.6 files still follow their existing atomic conversion paths. Native mapping versions are separate from this record version.

`VersionedPersistence` implementations and wrappers must now implement or forward `allocate_identifiers` and `identifier_watermark`; session-bound `IdentifierAllocator` implementations must also supply the read method. Watermark reads must use physical read transactions, preserve absent namespaces and expose current autonomous reservations to read-only sessions without changing their logical record snapshot. Implementing the read through `Observe(0)` is invalid because it can create state and acquire write admission. Preserve database incarnation checks, exclusive physical admission, request bounds and cancellation/memory controls. A successful reservation must be durable and must survive logical rollback without publishing private records or advancing record visibility. Run `mvcc::verify_identifier_allocations` against two owners of the same disposable database. Backend and byte-store wrappers must also forward `PersistentStorageBackend::identifier_allocator` and `KeyValueStore::identifier_allocator`. The returned `mvcc::IdentifierAllocator` retains its session checks and resource allowance. Engine document reservations now use the storage-owned `DocumentIdAllocator` on SQLite Key/Value, redb and explicitly bound native SQLite sessions. Initial restoration seeds table object/generation namespaces from existing rows and legacy reservation floors before retiring the old per-name metadata in the catalog transaction. Successful reservations and manual-ID observations survive undo and reopen; renames preserve the namespace and TRUNCATE seeds its replacement generation. Serialized custom backends may keep the absent capability and their serialized contract. `KeyValueStorageBackend::begin_read_transaction` follows the backend read-first contract through the byte-store upgradeable entry point, matching native SQLite; `KeyValueStore::begin_read_transaction` remains strictly read-only on versioned stores. Bound native SQLite direct document replacements and patches now observe supplied IDs in the same namespace. Common Key/Value document writes now bind the exact storage name to nonzero object/generation identities and observe supplied IDs in the evaluated batch. Persistent graph allocation and raw graph catalog consumers now consume this capability; autonomous SQL sequences remain pending.

Custom concurrent graph adapters must expose `GraphStorage::identifiers`, forward `guard_definition`, and implement transactional `reset_identifiers`; catalog wrappers must forward `CatalogFacade::guard_graph_definition`. Defaults preserve serialized adapters and reject a missing guard/reset implementation when identifiers are exposed. `GraphLabelRegistry` now includes an `allocation_id` field; legacy JSON defaults it to zero, and persistent handles derive the original incarnation before assigning fresh identities to new graphs. Registry reads and explicit named-graph exports include autonomous reservation floors without publishing a catalog write. Counter increments no longer rewrite graph definitions. Raw catalog mutation/restore now uses evaluated batches, supplied-ID observations and definition guards. Key/Value graph wrappers must forward compound reads and mutations as well as batch requirements and observations. Record format 9 includes entity and membership lifetime requirements as well as separate prefix-seeding state; formats 1–8 upgrade atomically while preserving histories, identities and reserved IDs. Built-in graph mutation and orphan cleanup stage the requirements with their source records, permitting shared endpoints and property-only changes while rejecting incompatible removals. Known prior SQLite metadata cache triggers upgrade atomically to exclude this internal state. This does not enable concurrent Engine SQL.

Batch wrappers over durable stores must forward `KeyValueBatch::observe_identifier`; its default rejects unsupported observations. It buffers an observation until the complete operation has been evaluated and its records staged, then persists it before row publication. Successful observations survive rollback even when later publication fails; sealed record retries do not replay them. Failed evaluation and dropped batches have no allocation effect. Run `mvcc::verify_identifier_batches` through two independent byte-store sessions and verify the returned watermark after close/reopen. No physical schema or format version changes accompany this batch API.

Wrappers supporting versioned definition coordination must forward `KeyValueBatch::require_unchanged` and `touch_marker`; their defaults reject unsupported operations. The first retains the original committed revision without writing the record. The second accepts only an immutable marker payload and merges independent touches at commit. Structural changes must fence the marker and change or fence the associated definition, while data changes require that definition. Providers must validate these requirements under the same exclusive admission as ordinary record preconditions; `PreparedRecordCommit::validate` includes them, and `required_keys` exposes their keys for temporary driver-binding accounting. Run `mvcc::verify_revision_guards` against two independent sessions and check closed-file reopen. These optional primitives change no physical format by themselves. Document-owner and persistent graph consumers now use them; complete SQL lifecycle integration remains pending.

IVF mutation wrappers must forward `KeyValueBatch::ivf_mutation`, `preview_ivf_record`, `preview_ivf_prefix` and `fence_ivf_prefix`. Serialized defaults retain ordinary conditional replacements; concurrent wrappers must preserve ordered evaluated inputs, derived previews and document/definition guards together. `VersionedPersistence::ivf_record_layout` defaults to the common Key/Value codec; providers using another row layout must supply its addressing and codecs before routing typed IVF changes. SQLite Key/Value and redb now merge different documents within one IVF generation, including training changes; same-document and structural changes retain conflicts. Canonical tensors must agree with the sealed inputs, savepoints discard their corresponding input journal, and retries never invoke application evaluation. The common IVF metadata row format remains 1. Native SQLite now implements the same merge contract through its own row codec and mapping format 6; native HNSW uses the same journal and shared document/field guards in mapping format 7. The codec header’s optional mutation counter is present for Key/Value rows and absent for native rows, and `IVFRecordLayout::vector_id` receives the read control for bounded/cancellable key decoding.

HNSW mutation wrappers must likewise forward `KeyValueBatch::hnsw_mutation`, `preview_hnsw_record`, `preview_hnsw_prefix` and `fence_hnsw_prefix`. `VersionedPersistence::hnsw_record_layout` defaults to common Key/Value addressing; native providers must supply their own codec before routing typed HNSW changes. SQLite Key/Value and redb now merge ordered document changes within one HNSW graph, using the same journal, fingerprint and canonical validation as IVF. Their field/document guards share canonical vector identities; savepoint undo and failed operations discard their inputs atomically. HNSW JSON row format 1 is unchanged. The common record-format upgrade to 4 preserves existing histories and receipts while excluding writers that omit these guards.

HNSW callers can use `HNSWIndex::prepare_delta(HNSWMutation, control)` for an immutable evaluated graph change and `prepare_canonical(dimensions, params, vectors, control)` for explicit reconstruction from ordered complete tensors. Both return a `Budgeted<HNSWPersistenceDelta>` whose allowance must remain retained while consuming its buffers. Native SQLite and Key/Value mutation/rebuild adapters use these common owner APIs. Existing graph encodings and record-format markers are unchanged by this preparation API; native SQLite now supplies the codec for same-index merging described below. Complete retained-cache/query accounting remains in progress.

Key/Value document and table-lifecycle consumers now require compound read/mutation support. Read adapters must forward `visit_keys_after`; durable batches must forward `inherit_identifiers` along with definition requirements, marker touches and identifier observations. Identifier inheritance is ordered with observations, completes before record publication, and survives private undo after successful evaluation. Adoption by a different object preserves reservations; a new generation of the same object seeds visible rows. TRUNCATE retires old rows before generation rotation so CONTINUE IDENTITY and RESTART IDENTITY can select distinct floors. Native projected adapters may forward identifier namespaces, but cannot forward arbitrary record keys unchanged between different key layouts. Document snapshots are fixed read-only views; code requiring subsequent live changes must retain the live document handle. The storage-owned identity generator is re-exported from the existing execution path without reversing crate dependencies.

## Unreleased native SQLite record adapter

The development `SQLiteRecordStore::for_native` API atomically initializes or upgrades a native catalog and converts its schema 48 baseline to schema 49 with guarded current tables, table-owner bindings and a complete MVCC baseline. Conversion is atomic, checks the physical layouts and rejects a populated preexisting raw record history or duplicate sequence incarnations. Sequence commits reject concurrent aliases and competing live definition generations. Mapping format 4 imports recognized dynamic FTS skip/block-max accelerator tables into `_occurrence_skips` and `_occurrence_block_max`. Populated names must identify exactly one existing source field; ambiguous owners, malformed schemas and unmapped tables fail conversion atomically. Missing or changed native mapping guards cause reopen to fail. Plain, SQLCipher, compressed and encrypted compressed files use the same conversion. The released 0.3.6 catalog rejects schema 49, and its direct document writes are blocked by persistent guards. Returning to the released format requires restoring a closed-file backup.

Native mapping format 2 adds versioned graph label, adjacency and membership lookup entries. Opening a development format 1 file derives these entries from its complete source history in one atomic transaction, retaining original commit boundaries, database identity, allocations and receipts; failures preserve the previous format. This changes the native mapping marker, not catalog version 49 or the row codec. Earlier format 1 adapters reject the new marker. Prepared graph batches must now include their changed lookup entries together with source rows. Properties are not copied into these lookup entries, and property-only updates do not change their revisions. Bound native catalog graph entity and membership reads, including named-graph hydration, now use the logical session snapshot. Bound native graph catalog mutations and path-index operations now also use this session. Mapping format 3 adds versioned graph-to-path directory entries; upgrading format 2 derives only those missing entries, preserving existing lookup history and all original commit boundaries. Format 1 upgrades both selector sets atomically. Earlier format 1/2 adapters reject the new marker; catalog version 49 and the row codec remain unchanged.

Default `SQLiteStorageProvider` session factories now bind native records before catalog restoration; this includes `Engine::open` and its encrypted/compressed variants. New-file catalog initialization, legacy migration and baseline conversion share one physical transaction. Bound factory clones preserve the configured per-session retention allowance. Unbound legacy handles cannot operate on a converted file; direct callers must use `ManagedConnection::bind_native_records` before `Catalog::open`. Catalog, document/B-tree, occurrence, vector and graph adapters share the logical session, including private cache revisions. Native publication preserves original record preconditions, validates cascade effects and commits current rows, history and receipts together. This is a one-way development format change; keep a closed-file backup for use with the released 0.3.6 binary. General definition coordination, full SQL isolation/publication, recovery and platform acceptance remain tracked in the [implementation plan](../../plans/0008-concurrent-storage-transactions.md).

Native conversion/reopen also validates the catalog cache-invalidation triggers. The known metadata triggers that truncated names at embedded NUL characters are replaced atomically with byte-preserving extraction; catalog open applies the same repair to legacy files. The repair also upgrades the known byte-preserving trigger form to exclude internal graph allocation/guard metadata from SQL registry cache revisions. It preserves data, revision counters and native histories and does not run again after the canonical triggers are installed. Missing or altered native cache tracking is rejected.


Bound native occurrence APIs now join the logical session, including retained snapshots/cursors, persisted block bounds and shared posting/counter merges. Mapping format 5 adds persistent document/structural guards in `_uqa_mvcc_native_occurrence_guards` and rejects older adapters that do not maintain them. A failed upgrade rolls back the new table and leaves the previous format intact. Native mapping formats 1–7 now upgrade atomically to format 8, including the shared vector guards below, while preserving original source history, identities, allocations and receipts; earlier adapters reject the new marker. Catalog version 49 and existing occurrence row codecs remain unchanged. Successful initial conversion retires imported dynamic accelerator tables, and later source or column changes invalidate the corresponding fixed-family caches. Continue using a closed-file backup when migrating a development or released file.

Native mapping format 7 routes evaluated native IVF and HNSW document inputs through the common merge resolver. It retains the `_uqa_mvcc_native_ivf_guards` table introduced in format 6, with the same family 49 document/field keys shared by both algorithms. The Rust family identifier is now `NativeRecordFamily::VectorGuards`. Formats 1–6 upgrade atomically, preserving original vector/IVF/HNSW row bytes, tombstone histories, identity, counters and receipts; a failed upgrade leaves the previous format intact. Earlier mapping adapters reject format 7 because they do not maintain HNSW guards. Canonical tensors and derived generations publish together, including HNSW node allocation, separate edges and compaction. Document and field-definition conflicts survive empty mutations, metadata recreation and catalog lifecycle changes. Savepoints and retained snapshots keep their original generation. The common record version follows the [current schema tuple upgrade](#unreleased-schema-tuple-identities); the catalog remains 49. General SQL definition coordination and the remaining concurrency acceptance matrix remain required.

Native mapping format 8 connects standalone `SQLiteGraphStore` families to bound native sessions. Conversion preserves the original property bytes and legacy format markers, and joins standalone-only catalog bootstrap and baseline capture in one physical transaction. Qualified suffixes use SQLite's ASCII case-insensitive namespace identity; unqualified standalone graphs and catalog graphs occupy different record families. Existing graph handles switch with their connection when it is bound. Retired qualified physical tables become read-only views; the unqualified source catalog/metadata names are also guarded by views, while the overlapping entity names become guarded catalog tables. New namespaces create the same protective views during their first physical commit, so earlier graph handles cannot create overlapping legacy tables. Earlier native mappings gain empty scoped families without rewriting existing records, and earlier adapters reject the new marker. The catalog remains 49; the common record version follows the [current schema tuple upgrade](#unreleased-schema-tuple-identities).

## Unreleased SQLite Key/Value record format

The development SQLite Key/Value provider migrates the legacy `_key_value` table into versioned records on initial open. The copy, committed sequence and replacement guard views publish in one physical transaction; failed conversion preserves the complete previous format. Reopen validates the mapping and guard definitions instead of copying again. The actual crates.io UQA 0.3.6 Key/Value writer and native catalog initializer reject upgraded files, including SQLCipher, compressed and encrypted compressed variants. This one-way upgrade requires restoring a closed-file backup to return to the released provider. Native relational SQLite databases are not converted by this Key/Value path.

`SQLiteKeyValueStore` keeps transaction state in a common logical session shared with its `ManagedConnection` clones. `new_session` creates independent private state. Binding during an active native transaction is rejected. The default retained-memory limit is 64 MiB per session; set it through `SQLiteKeyValueStorage::open_with_options`, `from_connection_with_options` or `SQLiteKeyValueStore::with_options`. Rebinding one connection with a different limit is rejected. Sibling sessions inherit the limit, which remains separate from SQL statement memory. Resource exhaustion is typed, and this implementation does not spill.

Low-level Rust code must perform ordinary reads and writes through the Key/Value store. `ManagedConnection::with` and `with_mut` reject a logical Key/Value session because physical SQLite closures cannot observe its private writes. Use `with_physical` only for explicit diagnostics or physical maintenance outside that session's transaction; it reads committed physical state, leaves internal write guards active and does not provide logical transaction semantics. `change_version` reports the durable record sequence instead of SQLite's connection-relative `data_version`.

Failed commits retain their sealed attempt, as with redb below. Retry `commit_transaction` to resolve the same prepared bytes and receipt; do not replay application operations after an uncertain native outcome. A failed rollback retains unresolved transaction state. Direct Key/Value concurrency does not yet enable concurrent Engine SQL: native relational session routing, shared-index merges, SQL isolation/publication, reclamation and platform acceptance remain in the [implementation plan](../../plans/0008-concurrent-storage-transactions.md).

## Unreleased Rust session affinity

Engine now checks that persistent catalog and data handles report the same transaction context before restoring or attaching a session. This includes `from_persistent_backends`, provider initial sessions and `new_session`. Native SQLite, SQLite Key/Value and redb report `StorageSessionAffinity`; combining handles from separate sessions fails with `StorageBackendError::Backend` whose source is `StorageSessionMismatch`, even when both refer to the same file. Use a provider-created pair or clone handles from one session. Connection clones preserve affinity; `new_session` creates another identity.

Custom `CatalogFacade`, `PersistentStorageBackend` and `KeyValueStore` wrappers must forward both `transaction_affinity` and `transaction_model`. The default model is `StorageTransactionModel::ProviderSerialized`, preserving existing writer admission. `VersionedConcurrent { database }` requires matching database incarnations and a matching nonempty session affinity, together with private writes, savepoints, retained reads and command refresh. Mismatched models fail before restoration. The affinity token is process-local and must not be persisted as a database or transaction ID. This internal transaction selection does not establish full PostgreSQL concurrency conformance.

## Unreleased Engine commit resolution

Engine callers using common logical providers must distinguish an ordinary failed transaction from an unresolved commit. `pending_commit()` exposes the latter and SQL reports `08007` while resolution is pending. Retry COMMIT to resolve the same prepared batch without replaying application operations; whole-transaction ROLLBACK can abort an uncommitted attempt, but if the receipt proves it already committed, Engine completes publication and returns `25000`. A later COMMIT that confirms a recorded abort restores rollback state and returns `25000`. Rust callback transactions and implicit SQL batch cleanup retain unresolved attempts instead of automatically treating them as rolled back. See the [Rust transaction contract](02-rust-engine-api.md#transactions-and-batches).

## Unreleased redb record format

The development redb provider migrates existing Key/Value files when `RedbStorage::open` opens them under redb's exclusive file-owner admission. It copies legacy records into version histories and atomically replaces the old writable table with a typed guard. Migration preserves binary keys, values and the committed change counter; interrupted publication reopens as a complete old or new format, and reopening the new format does not copy data again. The actual crates.io `uqa-storage-redb` 0.3.6 provider rejects the migrated file before writing; the redb dependency remains version 4.1.0. Keep a closed-file backup before this one-way upgrade; restoring that original file is required to return to the released provider. SQLite formats are unchanged by this redb conversion.

`RedbKeyValueStore` now reexports the common `VersionedKeyValueStore`. A failed commit retains a sealed attempt instead of discarding transaction state; `commit_transaction` retries the identical prepared records and resolves their receipt. Further mutations require resolving or rolling back that attempt. `rollback_transaction` reports an error containing the receipt if persistence says it already committed, and leaves the attempt available for commit resolution. Callers must not replay application operations merely because a native commit returned an error.

Private changes, savepoint history, batches and retained view metadata share a default 64 MiB session allowance. Use `RedbStorage::open_with_options(path, VersionedSessionOptions { retained_bytes })` to set it explicitly. This limit is separate from SQL statement memory; exhaustion returns a typed error and no spill files are written. Provider version/receipt reclamation and full concurrent Engine SQL support remain tracked in the [implementation plan](../../plans/0008-concurrent-storage-transactions.md).

## Concurrent inserts and notification encryption

Generated physical document identities are reserved before INSERT rows, triggers or RETURNING results are staged. Reservations skip candidates held by other transactions and recheck the latest committed state when a statement snapshot can be stale. The fix covers tables without an integer primary key, including TEXT and composite primary keys, with VALUES and INSERT ... SELECT. Existing table data needs no format migration. Update every process sharing the database together so all writers participate in the reservation protocol; upgrading does not recover rows overwritten by an earlier release.

Encrypted SQLite and compressed-encrypted databases now encrypt their `.uqa-notification-state` sidecar with the main database credential. An existing plaintext registry or a registry with a different key causes open to fail before any registry mutation; it is never silently converted or replaced. Validate the complete main-file and auxiliary-file set in isolation and update all consumers together. Custom encrypted file providers implement `auxiliary_encryption_key` on both `PersistentStorageProvider` and `PersistentStorageBackend`; the default `None` permits unencrypted auxiliary storage. See [auxiliary storage encryption](../internals/03-storage.md#encryption-and-compression) for the storage and lifecycle contract.

## Vector-threshold optimizer compatibility

`uqa_planner::TreeOptimizerConfig::enable_merge_vector_thresholds` is deprecated and ignored for both `true` and `false`. Remove explicit assignments to avoid deprecation warnings; callers using `TreeOptimizerConfig::default()` need no source change. The optimizer keeps separate threshold operators for identical and nearby query vectors, preserving additive intersection scores, document support and invalid-threshold errors even inside nested operators. No data migration is required when upgrading from 0.3.5.

## Korean analysis and package features

Rust applications using Korean analysis enable `nori` on `uqa` or `uqa-engine`, for example `cargo add uqa@0.3.7 --features nori`. The feature includes the immutable dictionary through `uqa-nori-data`; the official Python, Node.js, and browser WASM packages enable it. No JVM or runtime dictionary download is required. Builds without this feature retain the non-Korean analyzers and reject Korean analysis requests explicitly.

The built-in `nori` analyzer and custom Korean pipelines retain exact dictionary and user-rule identities in durable descriptors. Deploy the same feature configuration and required resources in every process opening the database. Rich analysis retains UTF-16 terms, morphology, token graph edges, and corrected source spans; existing string projections remain available but cannot represent isolated UTF-16 units. See the [analyzer reference](06-text-analyzers.md) and [binding contracts](08-bindings-and-extensions.md) for analysis, normalization, and result APIs.

## Rust graph API and custom catalogs

Version 0.2.3 changes the public Rust graph API to support direct persistent storage reads. `Engine::graph_with` and `Engine::graph_with_mut` callbacks receive `uqa_graph::GraphStoreHandle`, not `MemoryGraphStore`. Import `GraphStore` for its methods, propagate storage errors, and use the owned `Result<Option<Vertex>>` and `Result<Option<Edge>>` point-read results without `.cloned()`. Mutation callbacks return `GraphStoreResult<T>` so an error rolls back the complete storage checkpoint. `PathIndex::lookup` likewise returns an owned, fallible result. Primary in-memory graph storage remains available through `MemoryGraphStore`.

Custom `GraphStore` implementations must support owned, fallible reads, edge memberships, and atomic mutation checkpoints; durable stores must implement bounded identity pages through indexed storage access. Custom `CatalogFacade` implementations must provide selective graph point reads, bounded identity pages, counts, memberships, and durable path-index data methods. Do not implement these by loading complete graph partitions into a resident map. Persistent sessions must bind graph handles to their own physical transaction. See [graph access](07-graphs.md) and [storage contracts](04-storage-and-security.md) for the current signatures and lifecycle requirements.

SQLite catalogs advance to version 46 for durable cache revisions, graph access metadata, path-index data, and invalidation. Initial open also performs the bounded, atomic legacy graph access/label metadata migration where needed. Validate an isolated copy before upgrading all processes; do not reopen a migrated database with an older binary.

## Package versions

Update the UQA packages used by one application together. Rust's `0.1` and `0.2` dependency requirements do not select `0.3.7`; change the requirement explicitly and regenerate the application's lockfile.

| Environment | Versioned installation |
| --- | --- |
| Embedded Rust | `cargo add uqa@0.3.7` |
| Rust HTTP client | `cargo add uqa-client@0.3.7` |
| Python and `usql` | `python -m pip install --upgrade uqa==0.3.7` |
| Embedded Node.js | `npm install @cognica-io/uqa@0.3.7` |
| Node.js HTTP only | `npm install --omit=optional @cognica-io/uqa@0.3.7` |
| Browser WASM | `npm install @cognica-io/uqa-wasm@0.3.7` |

The Rust workspace requires Rust 1.90 or newer. Python requires Python 3.8 or newer, and the Node.js package requires Node.js 16 or newer. The Node.js root package selects an exact-version native optional package for embedded execution; deploy the root and native packages from the same release. Deploy the Browser WASM JavaScript module and `uqa.wasm` from the same package together, including when updating a browser cache.

The [GitHub release](https://github.com/cognica-io/uqa-engine/releases/tag/v0.3.7) contains the Python and npm archives, standalone Node.js addons, and the status of publication to crates.io, PyPI, and npm. Rust applications using Git dependencies should select `tag = "v0.3.7"` consistently for every UQA dependency.

## Automatic statistics and session caches

Persistent query planning uses saved statistics while one database-level background worker collects and publishes replacements. Initial collection, committed-change thresholds, and a maximum dirty age of 60 seconds schedule maintenance automatically. Applications can inspect `Engine::automatic_statistics_status()` and continue to use explicit `ANALYZE` for a full collection. Pending changes survive reopen and follow transaction and savepoint rollback; existing estimates remain available until replacement.

Automatic collection samples at most 4,096 hierarchy rows. Both automatic and explicit collection omit text and binary values above 1,024 bytes from stored value samples, with an 8,192-byte encoded budget for other values; row and NULL counts still include those observations. Older oversized statistics are bounded during reopen and scheduled for one background replacement without another write. See [automatic column statistics](04-storage-and-security.md#automatic-column-statistics) for the complete estimate and payload contract.

SQLite tracks transactional cache revisions so data-only or statistics-only commits reuse unchanged definitions and physical handles. `Engine::new_session()` shares immutable committed catalog and statistics allocations while retaining independent transactions and storage handles. Custom providers may implement `CatalogFacade::cache_revisions()` with the same snapshot and rollback guarantees or return `None` for conservative refresh; an empty revision map must not stand in for unsupported tracking. See [cross-session cache refresh](04-storage-and-security.md#cross-session-cache-refresh).

Version 0.3.0 releases unwritten rollback-journal readers before waiting for writer ownership or snapshot publication. This prevents automatic statistics publication from retaining a SQLite read lock while an application COMMIT waits for readers to finish. The logical transaction, savepoints, and detached repeatable-read snapshot remain intact.

## Cypher expression validation

Deeply nested Cypher and long operator or indexing chains now fail with a parse error instead of exhausting the process stack. The parser limits recursive expression parsing and constructed expression trees to 64 levels; flat lists and independent projection items remain supported. Rust code that exhaustively matches `uqa_graph::cypher::ParseError` must handle `ExpressionTooDeep { limit, position }`. See the [Cypher contract](../sql/07-graph.md#cypher-table-function).

## Prepared statement analysis

Version 0.3.0 analyzes `PREPARE` before execution and infers omitted parameter types in PostgreSQL occurrence order. Statements with missing references or incompatible types can now fail when prepared. Replanning a statement whose result columns, types, or modifiers changed reports `0A000`; deallocate and prepare the updated query to adopt its new result contract. The session metadata view retains the original client SQL string. Register native SQL callbacks before preparing statements that refer to them; later callback registration invalidates cached plans and preserves the original result contract. Two-argument `round` calls require a numeric first argument, so cast floating-point expressions explicitly before supplying a precision. See [prepared statements](../sql/08-transactions-and-routines.md#prepared-statements).

The optimizer returns `OptimizerResult<T>` with either `OptimizerError::Expression(SQLError)` or `OptimizerError::JoinGraph(JoinGraphError)`. Update exhaustive error matches and preserve the SQL error instead of converting every planning failure into an internal error. Immutable constant failures can now occur before execution; unused rule inputs are removed first, and EXECUTE binds arguments before planning the prepared body. Stored routines and views are optimized only when execution needs their definitions.

`SQLError::Diagnostic` carries separate SQLSTATE, primary message, detail, and hint fields. Exhaustive error matches must handle this variant; the PostgreSQL server sends detail and hint in their protocol fields. `TableConstraintSet::columns_declared` records whether an empty relation has a declared SQL schema. Engine-managed SQL declarations set it to `Some(true)`; custom catalogs must preserve this field, while missing legacy metadata infers declaration from existing columns. Native document tables and table functions can retain deferred descriptors without making declared SQL tables accept missing columns.

## SQL AST and CHECK catalog updates

The 0.2 series adds `ColumnDef.check_is_local`, `ColumnDef.check_object_id`, `TableCheck.is_local`, and `TableCheck.object_id`. Rust applications constructing these structs directly initialize local-origin fields to `true` and unassigned CHECK identities to `None`; SQL compilation and engine-owned inheritance fill them automatically. Applications with custom catalogs must also implement the graph storage methods described above.

Initial open assigns and persists missing CHECK identities through the existing transactional catalog-migration boundary. CHECK OIDs then remain stable across constraint and relation renames and reopen. Old serialized definitions lack declaration history and retain their historical local-origin projection; new declarations and hierarchy changes record their actual origin. See [CHECK inheritance and lifecycle](../sql/02-ddl.md#inheritance-and-partitioning) for the SQL changes.

## SQL constraint and ownership updates

Recursive column, CHECK, and NOT NULL additions require ownership of each descendant whose definition changes, including a child whose existing definition is merged. Table privileges alone do not grant this authority; inherited membership in the child's owning role does. Recursion stops after merging an existing child definition and continues through every inheritance edge that still requires a change. Unauthorized operations restore the parent and all previously visited children.

`ALTER TABLE ONLY parent ALTER COLUMN column SET NOT NULL` creates a `NO INHERIT` constraint when an ordinary inheritance parent has children. A later recursive `SET NOT NULL` reports `0A000` instead of silently changing that constraint's inheritance status. An ONLY change on a partition parent with existing partitions reports `42P16`; an ordinary leaf or empty partition parent keeps an inheritable constraint. Existing constraint names remain stable when validation is completed, and constraint state remains consistent through rollback and reopen. See the [compatibility guide](../sql/09-compatibility.md) for the verified boundary.

`pg_constraint.conislocal` now distinguishes an inherited NOT NULL constraint from a local NOT NULL declaration, independently from `coninhcount` and from whether the column was redeclared locally. Recursive changes retain existing child constraint names and give newly inherited constraints their parent's name. Removing the last supplying parent or detaching a partition makes its retained constraint local; attaching a partition makes constraints supplied by its parent inherited. Explicit SET on a previously inherited NOT VALID constraint first makes it local; a subsequent SET validates it.

NOT NULL origin is recorded on new declarations and hierarchy mutations. Older serialized columns lack the original declaration history and keep their previous local catalog projection; opening a database does not infer that missing intent. CHECK identities are assigned by the initial-open catalog migration described above. The shipped providers migrate existing database files through initial open; custom storage implementations must satisfy the graph and catalog contracts above before upgrading.

The public Rust SQL AST adds `ColumnDef.not_null_is_local`. Applications using exhaustive `ColumnDef` struct literals must initialize it to `true` for locally declared columns; inherited NOT NULL definitions use `false`. SQL parsing and engine-managed inheritance initialize the field automatically, and deserialization defaults missing fields to the historical local projection.

## Compatibility verification

Version 0.3.0 adds `bound_columns: Option<Vec<String>>` to `FromClause::Table` and `SourcePlan::Table`. Initialize it to `None` when constructing an ordinary query AST or plan. Engine-owned SQL-standard routine definitions capture their source columns and maintain them across column deletion and renaming, so later additions cannot shift stored positional aliases. Initial open migrates legacy source metadata and explicit string-to-regclass constants in stored schema expressions transactionally; subsequent catalog reloads validate the persisted definitions. See [stored column lifecycle](../sql/02-ddl.md#alter-table) for the SQL behavior.

The release includes all 354 PostgreSQL 18.4 core and isolation tests through the official PostgreSQL drivers, with a pinned source inventory and recorded execution provenance. The PostgreSQL reference run passes this corpus in CI. The UQA run of the complete corpus remains unaudited; release compatibility claims continue to follow the checked differential fixtures and feature manifest. See [verification](../internals/09-verification.md) for commands and evidence boundaries.

## Python CLI update

The release includes the 0.2.1 correction to `usql` interactive startup and `usql script.sql` execution. Upgrade Python installations of 0.2.0 to restore these modes. The entry point uses Python's command-line arguments, so the console launcher and interpreter options are not parsed as SQL input. Python engine APIs and `usql -c` keep their existing behavior.

The following storage guidance applies when upgrading from the 0.1 series.

## Custom Rust storage implementations

The storage traits changed in the 0.2 minor release. Applications implementing `uqa_storage::DocumentStore` must implement `put_stored` and `get_stored` using `StoredDocument`. A record keeps its public field map separate from `DocumentMetadata`, including tuple `xmin`. Preserve metadata through scans, rewrites, snapshots, and persistence; storing it as a user field can collide with application data. The default `put` implementation replaces public fields while preserving existing metadata, while a new tuple version uses `put_stored` with explicit metadata.

The B-tree methods on `uqa_storage::PersistentStorageBackend` now use `ValueIndexKey::Column` and `ValueIndexKey::Index`. Preserve both namespaces even when the enclosed names are equal. Named expression indexes store composite `Value::Row` keys; SQL expression binding and evaluation belong to the engine. Update custom backend signatures and physical key encoding before compiling against 0.3.0. See [Storage internals](../internals/03-storage.md) and the trait definitions in [`document_store.rs`](../../../crates/uqa-storage/src/document_store.rs) and [`backend.rs`](../../../crates/uqa-storage/src/backend.rs).

## Persistent database migration

Opening an older supported database performs the required provider and catalog migrations. The 0.2 minor release adds typed tuple metadata, richer object and column identities, ownership and ACL records, bound routine and rule dependencies, and expression-index metadata. Initial open owns migration writes; later catalog refresh validates the persisted representation. The shipped SQLite and key-value providers handle their storage migrations through the normal engine open path.

1. Stop writers, close every engine using the database, and create a recoverable backup through the [storage backup procedure](04-storage-and-security.md#backups-and-copies).
2. Open a copy with the exact 0.3.7 application and its selected provider, encryption key, and compression configuration.
3. Execute representative reads, writes, role and privilege checks, stored routines and views, and retrieval queries. Verify indexes, transaction rollback, and close-and-reopen behavior with the application's data.
4. Update every process sharing the database before reopening the original file. Register process-local runtime callbacks again when the application starts.
5. If the application must return to an older binary, restore the pre-upgrade backup. Do not rely on an older binary reading a file migrated by 0.3.7.

Keep migration failures visible and resolve them before admitting writes. Retain encryption keys and any external rollback anchor according to the [storage and security contract](04-storage-and-security.md).

## Node.js HTTP clients

`HttpEngine`, SQL parameter helpers, and streaming execute in JavaScript without a native addon. Existing imports from `@cognica-io/uqa` continue to expose these APIs; `@cognica-io/uqa/http` is the explicit HTTP entry point for both CommonJS and ESM. An HTTP-only deployment can omit optional dependencies. Embedded `Engine` use still requires the platform's native package.

Use an explicit URL and token or `HttpEngine.fromEnv()` when deployment configuration already supplies credentials. The asynchronous `local()` and `cloud()` constructors require the installed `uqa` CLI and resolve a project once. Keep using the documented parameter wrappers for vectors and tensors, JavaScript `bigint` for exact signed 64-bit integers, and `Buffer` or `Uint8Array` for binary values. The [HTTP Engine reference](09-http-engine.md) describes errors, response limits, streaming, and cancellation.

## SQLite provider ownership

Version 0.3.0 moves all concrete SQLite persistence into `uqa-storage-sqlite`. This provider extraction is a Rust source migration from 0.2.3 and does not itself change the database format, SQL behavior, encryption options, or compressed-container layout. The analyzer and positional-index changes in this release require the separate migration described below. High-level `Engine` constructors keep their signatures.

| Previous Rust API | Rust API in 0.3.0 |
| --- | --- |
| `uqa_storage::sqlite::{Catalog, ManagedConnection, ...}` | `uqa_storage_sqlite::{Catalog, ManagedConnection, ...}` |
| `uqa_storage::{SQLiteStorageBackend, SQLiteStorageProvider, SQLiteTransaction, SQLiteCompressionOptions, SQLiteError, ...}` | The same concrete types under `uqa_storage_sqlite` |
| `uqa_graph::SQLiteGraphStore` | `uqa_storage_sqlite::SQLiteGraphStore` |
| `uqa_storage::IndexManager::new(connection)` | `uqa_storage::IndexManager::new()` |
| SQLite persistence methods on `BlockMaxIndex` | Import `uqa_storage_sqlite::SQLiteBlockMaxPersistence` to call `save_to_sqlite` and `load_from_sqlite` |

Add `uqa-storage-sqlite` as a direct dependency when importing its types. The common storage and graph crates do not re-export concrete SQLite implementations. `StorageBackendError::SQLite` is replaced by `StorageBackendError::Backend { backend: "SQLite", source }`; use `source.downcast_ref::<uqa_storage_sqlite::SQLiteError>()` for typed diagnostics. `TransactionError::Storage` now carries the provider-independent `StorageBackendError`. `Index::build` and `Index::drop_index` return `StorageBackendResult`, and SQLite value-key encoding belongs to the provider rather than implementing rusqlite traits on the shared `ValueIndexKey`.

## Internal Rust SQL ownership

SQL statement and scalar models, static row schemas, and type resolution now live in `uqa_sql::plan`, `uqa_sql::ir`, `uqa_sql::schema`, and `uqa_sql::type_resolution`. Existing planner and execution exports refer to the same definitions. Low-level callers of `RowSchema::view` or `RowSchema::relayout_physical_row` must import `uqa_execution::RowSchemaExecution`; physical rows and materialization remain execution-owned. Applications using `Engine` require no SQL or query-result migration for this ownership change.

Construct the plan-native `uqa_planner::OptimizerConfig` with `OptimizerConfig::new(uqa_execution::scalar::eval_constant_scalar)` when using the physical scalar runtime. The planner no longer supplies a default execution backend. Import `PlanExecutor`, `OperatorTreeDriver`, `OperatorOutput`, and `ExecutionStats` from `uqa_execution::operator_tree`, and parallel execution helpers from `uqa_execution::parallel`. Engine-level query APIs keep their existing signatures.

The operator-tree `QueryOptimizer` accepts immutable `IndexScanCandidate` values through `with_index_candidates`. Its `index_manager` field and `with_index_manager` constructor are removed; callers discover applicable indexes and their scan costs from their catalog snapshot before invoking the optimizer. The planner has no runtime dependency on `uqa-storage`; optimizer regression tests use it as a development dependency. Its existing retrieval IR still has transitive storage dependencies through `uqa-operators`; removing that coupling requires separating logical descriptors from bound execution objects.

## Durable analyzer descriptors

Version 0.3.0 persists exact named analyzer descriptors and independent index/search bindings. SQLite schema version 47 adds `descriptor_json` to `_analyzers` and `binding_json` to `_table_field_analyzers`. Key/Value catalogs store equivalent descriptor records alongside their compatibility labels. Initial open resolves legacy definitions and any synonym files, validates ownership, and rebuilds affected full-text indexes from original documents in the owning catalog transaction. Subsequent opens restore verified descriptors without reading original synonym files. See [analyzer persistence](../sql/05-analyzers.md#transactions-and-persistence).

Custom `CatalogFacade` implementations must atomically implement `save_analyzer_revision` and `replace_table_field_analyzer_binding`, expose the corresponding `load_analyzer_descriptors` and `load_table_field_analyzer_bindings` reads, and carry the complete records through rename, deletion, and transaction operations. The default write methods reject unsupported persistence. Custom inverted indexes must implement atomic `set_field_analyzer_revisions` for exact restoration. A configuration-only implementation cannot safely substitute for either contract.

Key/Value indexes now store complete token occurrences and original-source metadata under canonical binary term keys. Opening an older positional index requires its original documents and resolvable analyzer descriptors; Engine rebuilds it in the initial catalog transaction, including tokenless fields. Failure preserves the prior positional data. Direct `KeyValueInvertedIndex` users must supply original documents to `try_rebuild_documents` when `source_rebuild_required` returns true. The [occurrence format](../../design/occurrence-posting-format.md#keyvalue-publication-and-migration) describes the persistent keys and migration boundary. SQLite schema 48 stores complete occurrence graphs and source metadata separately from legacy positional tables. Initial open rebuilds legacy full-text indexes from original documents in the same catalog transaction; missing sources or unresolved descriptors fail without committing a partial migration. Standalone SQLite catalog preparation preserves the source-rebuild obligation. See [native SQLite ownership](../../design/occurrence-posting-format.md#native-sqlite-ownership) for the format and publication contract.

## Japanese analysis and distribution features

Version 0.3.5 adds opt-in `kuromoji` to `uqa` and `uqa-engine`, independently of `nori`; both Rust packages retain empty defaults. CLI, Python, Node.js and WASM distribution builds now default to both languages. `--no-default-features` excludes both dictionaries; add `--features nori` or `--features kuromoji` to select one. The WASM build script accepts those same feature arguments.

Deploy a build with the required language features and exact resources to every process opening a database with retained analyzers. The source and binary packages include the pinned Lucene, JDK, MeCab and IPADIC notices and both resource/model identities. The archive verifier checks complete embedded dictionary bytes in wheel, Node and WASM runtimes and complete resources in the source archive.

## Explicit normalization configuration

Version 0.3.5 adds `normalization: Option<NormalizationConfig>` to `uqa_analysis::Analyzer`. Existing Rust struct literals must add `normalization: None` or use `Analyzer::new` or `Analyzer::default`; the existing constructors still omit normalization. Use `.with_normalization(plan)` to select it explicitly. Omitted JSON configurations and existing generic/Nori descriptor identities remain unchanged, so this source change does not rewrite stored revisions or require a storage migration. `CompiledAnalyzer::normalize` and `normalize_budgeted` are now available in all analysis feature configurations; pipelines with no plan return `NormalizationUnavailable`. See [explicit normalization plans](06-text-analyzers.md#explicit-normalization-plans).

## Simple-lowercase profile configuration

`SimpleLowercaseConfig` now belongs to `uqa_analysis` and is still re-exported from `uqa_analysis::nori`. Its `unicode_profile` field changes from `String` to `UnicodeProfileSource`. Convert existing owned strings with `.into()`; existing string-literal `.into()` expressions and the default constructor continue to work. Legacy JSON strings preserve their Nori interpretation, resolved form and descriptor identity. The new explicit `UnicodeProfile` object selects a provider and dictionary independently of the tokenizer. See [profile selection](06-text-analyzers.md#simple-lowercase-profile-selection).

## Lossless Rust query terms

`WANDQuery::terms`, `CursorWANDQuery::terms`, and `ScoreOperator::query_terms` now contain `TokenTermKey` values, and `BlockMaxIndex::entries` yields those canonical keys. Existing `new` constructors still accept string term arrays; use `new_keys` for terms obtained from rich analysis. Explicit struct construction and direct term-array mutation should convert strings with `TokenTermKey::from_text` or `Into`, and preserve non-scalar terms with `TokenTermKey::from_term`. Analyzer string projections still reject unpaired UTF-16; query execution consumes lossless keys directly. See [search internals](../internals/05-search-and-ranking.md) for scoring and cursor semantics.

Native lexical execution is available through `uqa_scoring::score_text_query` and `score_text_terms`; `rebuild_text_block_max` uses the same scorer identity. `TextSearchAlgorithm` and `TextSearchProfile` now live in `uqa-scoring` and remain re-exported from `uqa-engine` at their existing paths.
